---
id: exp-o-kd-20260518-0659
title: Ond. That forced extra global traffic, an extra launch, and synchronization
  thro
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
- register-reuse
kernel_types:
- attention
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: cutlass
---

## Challenge

Optimization focus: Fused the backward pass into one CUTLASS-based kernel and removed the temporary `tmp_sum_i` tensor plus the separate `grad_v` and `grad_qk` kernel launches.
Problem before: The previous backward path split the work across a `grad_v` kernel and a `grad_qk` kernel, with `tmp_sum_i`


## Solution

ond. That forced extra global traffic, an extra launch, and synchronization through memory for a quantity that is only needed inside the same logical backward block.
Applied change: The new file introduces one batched CUTLASS kernel that iterates query/key tiles, computes `Di` inside the kernel, and

## Key Lessons

- Applied change: The new file introduces one batched CUTLASS kernel that iterates query/key tiles, computes `Di` inside the kernel, and immediately feeds that state into `processBlockIJ` to update grad-V, grad-Q, and grad-K without materializing a global intermediate.
