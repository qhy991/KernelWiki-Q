---
id: exp-o-kd-20260518-0264
title: mask_50pct
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- activation
- pipeline-stages
kernel_types:
- activation
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
---

mask_50pct

## Solution

the after source, threads handling masked tokens (even token IDs with expert_id==-1) return immediately after the bounds check and expert_ids comparison, skipping two global float4 loads, the silu computation, and the float4 store. With BLOCK_SIZE=256 and num_vecs=256, each CUDA block maps to exa...

## Key Lessons

- In the before source, all 8192 tokens are processed unconditionally — every thread loads two float4 vectors from global memory (gate and up, 32 bytes total per thread), computes silu(gate)*up for 4 elements (each silu involves expf), and stores one float4 output vector (16 bytes).
