---
id: exp-a-kd-20260518-1206
title: pred_128b_load
experience_type: analysis
source_category: agent-experiment
architectures:
- sm100
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.004x
---

## Challenge

Both before and after versions emit the same PTX ld.global.cg.v4.u32 instruction with identical predicate logic on SM100+, and use identical __ldg fallback loads on SM80. The before version stores via *reinterpret_cast<uint4*>(&out) = uint4{...}, which writes a uint4 temporary through a type-punn...


## Solution

The after version assigns val = uint4{...} directly to the uint4& reference parameter, which the compiler can resolve to a simple struct assignment without the reinterpret_cast indirection. On SM80 with __ldg, both paths produce a single __ldg call followed by one 16-byte write to the output vari...

## Results

- **speedup**: 1.004x (neutral)


## Key Lessons

- On SM80 with __ldg, both paths produce a single __ldg call followed by one 16-byte write to the output variable; the reinterpret_cast overlay and direct reference assignment compile to essentially the same store instruction, yielding no measurable performance difference.
