---
id: exp-a-kd-20260518-1210
title: half_silu_mul
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- activation
kernel_types:
- activation
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: inline_asm
speedup: 0.996x
---

## Challenge

The before kernel uses `if constexpr (std::is_same_v<Type, half>)` with explicit conversion calls (`__half22float2`, `__float22half2_rn`) and a named `fmul2_rn_local` multiply function for float2 element-wise multiplication. The after kernel replaces these with `cast_to_float2` and `cast_to_packed` 


## Solution

on intrinsics via `if constexpr`, plus explicit `make_float2(silu_vec.x * y_f2.x, silu_vec.y * y_f2.y)` scalar multiplication instead of the named `fmul2_rn_local` call. Since `if constexpr` eliminates unused branches at compile time, and float32 multiply is always round-to-nearest (making `fmul2_rn

## Results

- **speedup**: 0.996x (neutral)


## Key Lessons

- The after kernel replaces these with `cast_to_float2` and `cast_to_packed` helpers that internally dispatch the same conversion intrinsics via `if constexpr`, plus explicit `make_float2(silu_vec.x * y_f2.x, silu_vec.y * y_f2.y)` scalar multiplication instead of the named `fmul2_rn_local` call.
