---
id: exp-o-kd-20260518-0748
title: unknown
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

unknown

## Solution

MAX_VPT=32) mismatched the actual data footprint (VPT could be smaller). The scalar loop workaround sacrifices memory bandwidth and adds loop overhead.
Applied change: The static version uses a compile-time Vlen parameter (= num_experts / WARP_SIZE) to define AlignedArray<T, Vlen> (ArrayVal), where 

## Key Lessons

- A comment explicitly notes that a direct vectorized copy was tried but was slower and caused misaligned address issues when VPT < 8, because the AccessType width (MAX_VPT=32) mismatched the actual data footprint (VPT could be smaller).
