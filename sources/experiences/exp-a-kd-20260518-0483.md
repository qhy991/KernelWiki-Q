---
id: exp-a-kd-20260518-0483
title: float_4M
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
- vectorized-loads
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.039x
---

## Challenge

At 4M elements, the after kernel has 4M/16=262144 vector iterations, matching the full grid, so all threads participate — avoiding the under-utilization problem seen at 1M. The uint4 coalesced store writes 16 fp8 bytes as one 128-bit memory transaction, whereas the before kernel writes 4 fp8 bytes p


## Solution

mprovement is only ~4%, falling short of the 5% threshold for 'improved'. This is because the fp8 conversion computation (16 iterations of fmax/fmin/static_cast per vector chunk) dominates execution time, and the after kernel does 4x more conversion work per iteration. The compiler already partially

## Results

- **speedup**: 1.039x (improved)


## Key Lessons

- This is because the fp8 conversion computation (16 iterations of fmax/fmin/static_cast per vector chunk) dominates execution time, and the after kernel does 4x more conversion work per iteration.
