---
id: exp-a-kd-20260518-1036
title: fp16_gs128_131kgrp
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
---

fp16_gs128_131kgrp

## Solution

smax (lines 45-50), followed by a quantization loop over smem_group (shared memory pointer) instead of group_input. This eliminates one global memory read pass. However, on the A800 with its 40MB L2 cache, the 32MB input data largely remains cached after the first pass, so the before kernel's second

## Key Lessons

- The after kernel replaces this with one loop over group_input that copies each element into smem_group while simultaneously computing absmax (lines 45-50), followed by a quantization loop over smem_group (shared memory pointer) instead of group_input.
- The after kernel's __syncthreads() barrier between passes and shared memory bank access patterns add synchronization and access overhead that partially offset the marginal DRAM savings.
