---
id: exp-a-kd-20260518-0109
title: long_k
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- pipeline-stages
kernel_types:
- gemm
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: cutlass
speedup: 1.004x
---

## Challenge

In long_k_before.cu, the same extra __syncthreads() epilogue barrier is present after 32 mainloop iterations (K=512). In long_k_after.cu, it is removed. The measured speedup of 1.004x is directionally consistent with removing one barrier but falls below the 5% threshold. The reason is that with 32 m


## Solution

n __syncthreads(), the one extra barrier at the end contributes only ~1/33 of the total synchronization overhead, making its removal a proportionally small fraction of total kernel time. Additionally, the real CUTLASS optimization removed cp_async_wait<0> alongside __syncthreads — the async drain wa

## Results

- **speedup**: 1.004x (neutral)


## Key Lessons

- Additionally, the real CUTLASS optimization removed cp_async_wait<0> alongside __syncthreads — the async drain wait is more expensive than __syncthreads alone because it stalls until ALL in-flight cp.async transactions complete, adding pipeline drain latency that this synchronous-load microbenchmark
