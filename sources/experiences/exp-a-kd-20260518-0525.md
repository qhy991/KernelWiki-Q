---
id: exp-a-kd-20260518-0525
title: moe_64e_4kt
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
- pipeline-stages
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
---

moe_64e_4kt

## Solution

The after code removes this kernel entirely because align_kernel's serial thread (threadIdx.x == 0) already writes cumsum[0] = 0 and computes cumsum[i] = cumsum[i-1] + padded_count for all i from 1 to num_experts, fully initializing every entry that the subsequent count_and_sort_expert_tokens_ker...

## Key Lessons

- The after code removes this kernel entirely because align_kernel's serial thread (threadIdx.x == 0) already writes cumsum[0] = 0 and computes cumsum[i] = cumsum[i-1] + padded_count for all i from 1 to num_experts, fully initializing every entry that the subsequent count_and_sort_expert_tokens_kernel
