---
id: exp-a-kd-20260518-0136
title: smem_layout_correctness
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- shared-memory-optimization
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 0.932x
---

## Challenge

The before code uses RowMajor layout for TensorRefA, producing wrong memory offsets (1008/1024 elements mismatched) when data is stored in crosswise layout. The after code uses SmemLayoutA (Crosswise) which correctly interprets the permut


## Solution

ed addressing and reads the right data. In this microbenchmark, the after code is slightly slower (0.932x) because the crosswise layout's PitchLinearCoord swap and permuted index calculation add arithmetic overhead for simple sequential r

## Results

- **speedup**: 0.932x (regression)


## Key Lessons

- In this microbenchmark, the after code is slightly slower (0.932x) because the crosswise layout's PitchLinearCoord swap and permuted index calculation add arithmetic overhead for simple sequential reads.
