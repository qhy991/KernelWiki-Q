---
id: exp-o-kd-20260518-0666
title: Nd each conversion introduces rounding error that accumulates across the K-seqle
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- softmax
- register-reuse
kernel_types:
- softmax
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
---

## Challenge

Optimization focus: FP32 accumulator storage for output (kOStorageAccFloat32=1) vs FP16 accumulator storage (kOStorageAccFloat32=0) in online softmax rescaling.
Problem before: With kOStorageAccFloat32=0, the output accumulator R_D stores FP16 values. During online softmax rescaling, each accumulati


## Solution

nd each conversion introduces rounding error that accumulates across the K-seqlen tiling loop.
Applied change: With kOStorageAccFloat32=1, R_D stores FP32 accumulators (4 uint32_t registers per element instead of 2). The online rescaling FMA operates directly on FP32 values without any format conver

## Key Lessons

- Applied change: With kOStorageAccFloat32=1, R_D stores FP32 accumulators (4 uint32_t registers per element instead of 2).
