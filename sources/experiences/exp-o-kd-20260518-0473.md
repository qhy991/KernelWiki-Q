---
id: exp-o-kd-20260518-0473
title: warp_expert_search_64ep
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 4.684x
---

## Challenge

The warp cooperative search achieves 4.68x speedup over the linear scan for 64 experts because it reduces the search from O(E) per thread to O(E/32) cooperative iterations. In the before code, each thread independently reads up to 64 prefix-sum entries in a serial for-loop, comparing each against...


## Solution

the after code, all 32 warp lanes simultaneously read one prefix-sum entry each (lane i reads offset[i]), vote via __ballot_sync whether offset[i] <= token_id, and use __clz on the ballot mask to find the last qualifying entry. For 64 experts, this completes in 2 warp iterations (32 entries per i...

## Results

- **speedup**: 4.684x (improved)


## Key Lessons

- The warp cooperative search achieves 4.68x speedup over the linear scan for 64 experts because it reduces the search from O(E) per thread to O(E/32) cooperative iterations.
