---
id: exp-a-kd-20260518-0630
title: half_1024t_32h
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.002x
---

## Challenge

With 1024 tokens, the before source uses head-major LSE stride of 1024 floats (4096B) between consecutive heads. Despite this large stride creating more scattered DRAM-level accesses in theory, the measured difference is negligible because: (1) the LSE


## Solution

arrays are still only 128KB each (1024*32*4B), fitting entirely in A800's 40MB L2 cache, so scattered reads hit L2 rather than DRAM; (2) each thread still reads only 2 scalar floats from LSE vs 16 packed half values via uint4 loads for the attention a

## Results

- **speedup**: 1.002x (neutral)


## Key Lessons

- Despite this large stride creating more scattered DRAM-level accesses in theory, the measured difference is negligible because: (1) the LSE arrays are still only 128KB each (1024*32*4B), fitting entirely in A800's 40MB L2 cache, so scattered reads hit L2 rather than DRAM; (2) each thread still reads
