---
id: exp-a-kd-20260518-1285
title: azp_h16384
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 0.377x
---

## Challenge

The after code replaces per-element division (val/scale) with multiplication by a precomputed reciprocal (val*inv_s) and wraps the quantization loop in vectorize_with_alignment<16> with only 256 threads. While replacing division wi


## Solution

th multiplication saves ~4x per operation, and vectorized loads reduce memory transactions, the 4x reduction in threads per block (1024->256) dominates the result. With 256 threads the block has only 8 warps vs 32 in the before ver

## Results

- **speedup**: 0.377x (regression)


## Key Lessons

- The after code replaces per-element division (val/scale) with multiplication by a precomputed reciprocal (val*inv_s) and wraps the quantization loop in vectorize_with_alignment<16> with only 256 threads.
