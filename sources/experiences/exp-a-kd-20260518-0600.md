---
id: exp-a-kd-20260518-0600
title: half_hidden_2048
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 1.266x
---

## Challenge

At hidden_dim=2048, each warp reads 4096 bytes of input data, and the total CTA footprint (8 warps * 4096B = 32KB) strains the L1 cache. The before kernel loads the same 4KB per warp from global memory twice: once in Pass-1 for the max-abs computation, and again in Pass-2 for quantization. Since ...


## Solution

The after kernel replaces this redundant global read with a shared memory read via `shared_input[i]` (declared with 32-byte bank-conflict-padded stride). Shared memory on A800 delivers ~5TB/s effective bandwidth versus ~50-80GB/s from HBM, and the __syncwarp() barrier cost is negligible (~10 cycl...

## Results

- **speedup**: 1.266x (improved)


## Key Lessons

- Since L1 cannot fully retain all warps' data between passes with concurrent CTA execution, the second pass frequently misses L1 and re-reads from L2 or HBM.
