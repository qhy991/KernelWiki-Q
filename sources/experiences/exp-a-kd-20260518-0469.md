---
id: exp-a-kd-20260518-0469
title: fill_64K
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
---

fill_64K

## Solution

ld because: (1) the CUDA hardware coalesces scalar 32-bit writes from consecutive threads into full-sector transactions efficiently, so the per-transaction throughput difference between scalar and Vec-wide stores is minimal for this simple fill pattern; (2) kernel launch overhead (~5 microseconds) c

## Key Lessons

- However, this improvement is still below the 5% threshold because: (1) the CUDA hardware coalesces scalar 32-bit writes from consecutive threads into full-sector transactions efficiently, so the per-transaction throughput difference between scalar and Vec-wide stores is minimal for this simple fill 
- However, this improvement is still below the 5% threshold because: (1) the CUDA hardware coalesces scalar 32-bit writes from consecutive threads into full-sector transactions efficiently, so the per-transaction throughput difference between scalar and Vec-wide stores is minimal for this simple fill 
