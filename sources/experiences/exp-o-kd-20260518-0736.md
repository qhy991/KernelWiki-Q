---
id: exp-o-kd-20260518-0736
title: In fp16/bf16 causes precision loss when summing across multiple topk experts
  bec
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Vectorized memory access with float accumulation replacing scalar per-thread access in apply_shuffle_mul_sum_kernel.
Problem before: The original apply_shuffle_mul_sum_kernel used scalar per-thread memory access (one scalar_t element per thread per iteration) and accumulated the 


## Solution

in fp16/bf16 causes precision loss when summing across multiple topk experts because low-precision types have limited mantissa bits and can saturate quickly.
Applied change: The kernel was rewritten to use flashinfer::vec_t vectorized types with cast_load/cast_store for memory access. For fp16/bf16,

## Key Lessons

- This results in poor memory bandwidth utilization — each thread issues a separate load/store for a single 2-byte element, generating many small memory transactions.
- Measured impact should be confirmed via point-level validation.
