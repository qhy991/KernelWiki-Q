---
id: exp-a-kd-20260518-1323
title: int8_alignment
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.999x
---

## Challenge

In the int8_alignment before source, hardcoded alignment=16 for int8_t produces 16*1=16 bytes per vector, which is exactly one 128-bit float4 load (line 48-50 in int8_alignment_before.cu)


## Solution

The after source computes alignment=128/8=16, yielding the identical 16 bytes per vector and the same single float4 load path (lines 48-50 in int8_alignment_after.cu). Since both code paths compile to the same vector width, instruction count, and memory transaction size, there is no measurable pe...

## Results

- **speedup**: 0.999x (neutral)


## Key Lessons

- Since both code paths compile to the same vector width, instruction count, and memory transaction size, there is no measurable performance difference.
