---
id: exp-o-kd-20260518-0409
title: vec_h4096_t32
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.150x
---

## Challenge

In the before vec_h4096_t32_before.cu, each thread processes hidden_size=4096 __half elements in two strided loops using individual __half loads and stores, requiring 4 iterations per loop (4096/1024 threads). Each iteration performs separate __half read, __hadd, float conversion, variance accumu...


## Solution

the after vec_h4096_t32_after.cu, the HalfVec8 reinterpretation processes vec_hidden_size=512 vector elements, so with 1024 threads each active thread handles only one vector element per loop iteration but that single 128-bit transaction covers 8 __half values. The __half2 packed operators (__had...

## Results

- **speedup**: 1.150x (improved)


## Key Lessons

- At 32 tokens, the kernel is memory-latency bound and the reduced memory transaction count yields a stronger 1.15x speedup than at 256 tokens because latency hiding via occupancy is less effective at small batch sizes.
