---
id: exp-a-kd-20260518-0895
title: pipeline_hd128
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
- shared-memory-optimization
- pipeline-stages
- register-reuse
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- pipeline-stages
- register-reuse
impl_family: custom_cuda
speedup: 1.024x
---

## Challenge

The after code introduces cp.async two-group pipelining (group 0 for QKV smem loads, group 1 for cos/sin smem loads) combined with multi-head-per-warp processing (HEADS_PER_WARP=4) and register weight preloading, while the before code uses synchronous global memory reads with one warp per head. On A


## Solution

nefit is marginal because: (1) the before kernel already has high occupancy at 512 tokens * 64 heads = 32768 total warps, saturating the GPU; (2) the after kernel reduces grid size by 4x via multi-head-per-warp, which reduces occupancy counterbalancing the latency hiding from cp.async; (3) the compu

## Results

- **speedup**: 1.024x (improved)


## Key Lessons

- On A800 (SM 8.0), the pipeline benefit is marginal because: (1) the before kernel already has high occupancy at 512 tokens * 64 heads = 32768 total warps, saturating the GPU; (2) the after kernel reduces grid size by 4x via multi-head-per-warp, which reduces occupancy counterbalancing the latency hi
