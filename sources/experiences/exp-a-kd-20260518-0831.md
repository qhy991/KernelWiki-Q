---
id: exp-a-kd-20260518-0831
title: per_tensor_scales
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
impl_family: custom_cuda
speedup: 1.000x
---

## Challenge

The per-tensor scaling path is identical in both before and after code — both route to the same fp8 kernel via a slightly different dispatch mechanism. The before code uses inline checks and a direct kernel call, while


## Solution

the after code uses the templated dispatch_scaled_mm helper that performs the same checks and calls the same fp8_func. Any timing difference is due to dispatch overhead (negligible) or measurement noise, not an algorithmic change.

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- Any timing difference is due to dispatch overhead (negligible) or measurement noise, not an algorithmic change.
