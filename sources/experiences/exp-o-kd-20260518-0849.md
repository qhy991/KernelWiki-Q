---
id: exp-o-kd-20260518-0849
title: 'The optimization removes shared memory allocation from the expand kernel launch '
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Move getMIndices computation before expandInputRowsKernelLauncher and pass pre-computed aligned offsets to the expand kernel.
Problem before: In the original code, the getMIndices kernel that computes aligned expert offsets and m_indices was called AFTER expandInputRowsKernelL...


## Solution

The optimization removes shared memory allocation from the expand kernel launch (reducing SMEM pressure), eliminates one __syncthreads() barrier, removes the serial accumulation bottleneck on lane 0, and eliminates the warp shuffle broadcast. Each thread now independently computes its aligned row...

## Key Lessons

- This eliminates the shared memory usage, the __syncthreads() barrier, the serial lane-0 accumulation loop, and the __shfl_sync broadcast.
- The host-side reordering also ensures the aligned offsets are available before the data-moving kernel runs, avoiding the previous pattern where getMIndices and the expand kernel were serialized with the alignment computation happening after the data permutation.
