---
id: exp-o-kd-20260518-0365
title: bfloat16_256K
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- activation
- register-reuse
- epilogue-fusion
kernel_types:
- activation
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.527x
---

## Challenge

The before code launches two separate kernels (kernel_silu_bf16 then kernel_mul_bf16) that communicate through an intermediate __nv_bfloat16 buffer in global memory. Each element is written to global memory by the first kernel, then read back by the second, adding a full global memory round-trip ...


## Solution

The after code fuses SiLU and Mul into a single kernel (kernel_fused_silu_mul_bf16) that reads gate and up once, computes silu2_device on gate, multiplies by up, and writes the result directly to output—all intermediate values stay in registers. Eliminating the global-memory intermediate buffer r...

## Results

- **speedup**: 1.527x (improved)


## Key Lessons

- The before code launches two separate kernels (kernel_silu_bf16 then kernel_mul_bf16) that communicate through an intermediate __nv_bfloat16 buffer in global memory.
