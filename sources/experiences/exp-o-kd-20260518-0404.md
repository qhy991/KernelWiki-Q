---
id: exp-o-kd-20260518-0404
title: fp8_to_bf16_bulk
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.344x
---

## Challenge

The after code applies the same bit-manipulation principle as the FP16 variant but targets BF16 output. The key difference from the FP16 variant is the bias construction: instead of __float2half2_rn(2^BIAS_OFFSET), it embeds the bias offset directly into a float bit-pattern via constexpr BIAS = (BIA


## Solution

2bfloat162_rn. This exploits the fact that IEEE float and BF16 share the same exponent bit position (bit 23), so the bias can be pre-computed as a float bit-pattern and then truncated to BF16 in one instruction. The before path performs 4 separate FP8→float conversions using std::pow for each expone

## Results

- **speedup**: 1.344x (improved)


## Key Lessons

- The key difference from the FP16 variant is the bias construction: instead of __float2half2_rn(2^BIAS_OFFSET), it embeds the bias offset directly into a float bit-pattern via constexpr BIAS = (BIAS_OFFSET + 127) << 23, then reinterprets those bits as a float and converts to bfloat162 with __float2bf
- The after code applies the same bit-manipulation principle as the FP16 variant but targets BF16 output.
