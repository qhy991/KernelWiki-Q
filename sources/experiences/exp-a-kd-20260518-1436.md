---
id: exp-a-kd-20260518-1436
title: fp8_dynamic_quant_small
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.046x
---

## Challenge

Both before and after kernels perform per-token dynamic FP8 quantization across 8192 tokens with hidden_size=128. The before kernel passes min_scaling_factor as a fifth kernel parameter (float), while the after kernel computes it inline via min_scaling_factor<FP8E4M3FN>::val() using


## Solution

__forceinline__ and constexpr template arguments. Since NVCC constant-folds 1.0f / (448.0f * 512.0f) to the same precomputed value, both versions use the identical constant in the max(token_scale / max_val, min_scale) comparison. The 4.38% speedup falls within the 5% neutral thresh

## Results

- **speedup**: 1.046x (improved)


## Key Lessons

- Since NVCC constant-folds 1.0f / (448.0f * 512.0f) to the same precomputed value, both versions use the identical constant in the max(token_scale / max_val, min_scale) comparison.
