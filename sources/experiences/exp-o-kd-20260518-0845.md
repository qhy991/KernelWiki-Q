---
id: exp-o-kd-20260518-0845
title: In a single kernel launch and has the same template-based BLOCK_SIZE dependency.
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Add block_size==32 dispatch for multi_query_cached_kv_attention with uint16_t (half) dtype.
Problem before: The multi-query cached KV attention kernel only supported block sizes 8 and 16. With block_size=32, the assert(false) would trigger, blocking multi-query workloads from usi


## Solution

in a single kernel launch and has the same template-based BLOCK_SIZE dependency.
Applied change: Added an `else if (block_size == 32)` branch calling `multi_query_cached_kv_attention_launcher<uint16_t, 32>()`. The kernel template's compile-time constants (THREAD_GROUP_SIZE=1, NUM_V_VECS_PER_ROW=2 fo

## Key Lessons

- Expected impact: Enables multi-query attention with larger KV cache blocks.
- This can affect value cache read patterns and warp utilization.
