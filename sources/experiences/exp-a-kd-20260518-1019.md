---
id: exp-a-kd-20260518-1019
title: renorm_topk2_8exp
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
- shared-memory-optimization
- epilogue-fusion
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- epilogue-fusion
impl_family: custom_cuda
speedup: 2.330x
---

## Challenge

The after kernel accumulates selected_sum in a shared variable during the iterative topk argmax loop (one addition per selected weight by thread 0), then divides all k weights by selected_sum in a short serial loop. The before approach writes unnormal


## Solution

ized weights to global memory via the topk kernel, then a separate renormalize kernel reads those weights back from global memory (8192*2*4=64KB read), computes the row-wise sum using atomicAdd into shared memory, and writes the normalized weights bac

## Results

- **speedup**: 2.330x (improved)


## Key Lessons

- The fused approach avoids this extra global memory round-trip and one kernel launch.
