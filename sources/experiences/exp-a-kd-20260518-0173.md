---
id: exp-a-kd-20260518-0173
title: int8_4x4_transpose
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: inline_asm
speedup: 0.981x
---

## Challenge

Both the before (asm volatile prmt.b32) and after (__byte_perm) versions map to the same PTX prmt.b32 hardware instruction. The theoretical advantage of __byte_perm is that NVCC can reorder or combine the 12 intrinsic calls for better scheduling, while volatile forces a strict emission order. Howeve


## Solution

4 ints from global memory (1MB total I/O) but performs only 12 permutation instructions, making compute a negligible fraction of total latency. Under memory-bound conditions, the GPU warp scheduler already hides instruction latency via warp-level interleaving, so any compiler-level instruction reord

## Results

- **speedup**: 0.981x (neutral)


## Key Lessons

- Both the before (asm volatile prmt.b32) and after (__byte_perm) versions map to the same PTX prmt.b32 hardware instruction.
