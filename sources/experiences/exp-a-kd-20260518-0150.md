---
id: exp-a-kd-20260518-0150
title: f64_m8n8k4
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- register-reuse
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: inline_asm
speedup: 0.999x
---

## Challenge

The before code uses uint64_t references/pointers and "l" (64-bit integer register) constraints for all operands (A, B, C, D) in the mma.sync.aligned.m8n8k4 F64 inline asm. The after code changes all to double references/pointers with "d" (64-bit float register) constraints. This is the most extensi


## Solution

it (touching all 6 operands rather than just the 8 C/D elements in the BF16/F16 variants). However, like the other variants, NVCC 12.8 with -O3 generates identical SASS for both versions. The DMMA instruction operates exclusively on 64-bit float ("d") registers for all operands, and the ptxas optimi

## Results

- **speedup**: 0.999x (neutral)


## Key Lessons

- This is the most extensive change in the commit (touching all 6 operands rather than just the 8 C/D elements in the BF16/F16 variants).
- This optimization was critical for CUDA 11 where the compiler would generate mov.b64 instructions to transfer data between integer and double register banks for each operand position, but modern NVCC has eliminated this issue.
