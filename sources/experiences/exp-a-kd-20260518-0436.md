---
id: exp-a-kd-20260518-0436
title: half_32hd_1024t
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- transpose
kernel_types:
- transpose
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.051x
---

## Challenge

The after code accesses LSE via row-major index token_idx * num_heads + head_idx, which yields consecutive 4-byte addresses when threads within a warp share the same token_idx but iterate over different head_idx values. In t


## Solution

he before code, the transposed index head_idx * num_tokens + token_idx creates a stride of num_tokens * 4 = 4096 bytes between adjacent heads for the same token, causing each warp's LSE reads to span multiple disjoint 128-by

## Results

- **speedup**: 1.051x (improved)


## Key Lessons

- The row-major layout condenses all 32 LSE entries for a given token into a single 128-byte region (32*4=128 bytes), enabling one fully-coalesced transaction per warp instead of 32 separate strided transactions.
