---
id: exp-a-kd-20260518-0145
title: sm80_fp16_vs_sm89_fp8_mma_raw
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- pipeline-stages
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- fine-grained-quantization
impl_family: custom_cuda
speedup: 0.487x
---

## Challenge

The before code uses 4 warps each issuing one m16n8k16 FP16 MMA per iteration (4 * 4096 FLOPs/iter). The after code uses 4 warps each issuing the SM89 FP8 MMA path, but on sm_80 hardware the __CUDA_ARCH__ < 890 fallback issues two sequential SM80 FP16 MMA instructions to cover K=32 (4 * 8192


## Solution

FLOPs/iter). Raw latency approximately doubles because 2x the MMA instructions are executed, but the per-FLOP throughput remains consistent (0.09 vs 0.0877 TFLOPs). This confirms the MMA instruction pipeline is the bottleneck: each SM80 FP16 MMA takes roughly the same wall-clock time regard

## Results

- **speedup**: 0.487x (regression)


## Key Lessons

- Raw latency approximately doubles because 2x the MMA instructions are executed, but the per-FLOP throughput remains consistent (0.09 vs 0.0877 TFLOPs).
