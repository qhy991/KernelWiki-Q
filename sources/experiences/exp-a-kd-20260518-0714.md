---
id: exp-a-kd-20260518-0714
title: reduce_uint4
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- pipeline-stages
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: inline_asm
speedup: 1.005x
---

## Challenge

The BEFORE code uses tree reduction (2-step dependency chain: c=add(v.x,v.y), d=add(v.z,v.w), c=add(c,d)) where the first two adds are independent and can execute in parallel. The AFTER code uses sequential reduction (3-step dependency chain: c=add(v.x,v.y), c=add(c,v.z), c=add(c,v.w)) where each ad


## Solution

truction. The measured speedup is 1.005 (neutral), meaning the sequential version is marginally faster despite its longer dependency chain. This is because in this microbenchmark the inner loop with 1024 iterations and heavy memory access dominates the runtime, masking the subtle instruction-level p

## Results

- **speedup**: 1.005x (neutral)


## Key Lessons

- This is because in this microbenchmark the inner loop with 1024 iterations and heavy memory access dominates the runtime, masking the subtle instruction-level parallelism difference between 2-step and 3-step dependency chains.
- The commit chose sequential reduction (#if 1) likely for code simplicity rather than performance gain, since the actual throughput difference is negligible in real workload contexts.
