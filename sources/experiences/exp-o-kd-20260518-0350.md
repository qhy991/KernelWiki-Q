---
id: exp-o-kd-20260518-0350
title: d2h_transfer_overhead
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- elementwise
- pipeline-stages
kernel_types:
- elementwise
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
---

d2h_transfer_overhead

## Solution

the after source (d2h_transfer_overhead_after.cu), the kernel receives d_scale as a device pointer and reads *d_scale directly from GPU global memory inside the kernel body. No device-to-host transfer occurs, so the entire loop of kernel launches can proceed asynchronously without any synchroniza...

## Key Lessons

- In the before source (d2h_transfer_overhead_before.cu), each iteration calls cudaMemcpy(&iter_scale, d_scale, sizeof(float), cudaMemcpyDeviceToHost) which is a synchronous operation that blocks the CUDA stream until all pending GPU work completes, then transfers 4 bytes from device to host, and fina
