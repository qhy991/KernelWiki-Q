---
id: exp-o-kd-20260518-0413
title: strided_half_hs512_stride768
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
- pipeline-stages
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
---

strided_half_hs512_stride768

## Solution

The after version replaces both kernels with a single rms_norm_kernel that takes input_stride=768 as a parameter and reads directly from the strided input buffer using blockIdx.x * input_stride + idx addressing, skipping the padding elements without touching them. By eliminating the copy kernel a...

## Key Lessons

- By eliminating the copy kernel and intermediate buffer, the after version avoids one full global-memory write+read round-trip (128 tokens * 512 elements * 2 bytes = 128KB of redundant traffic) plus one kernel launch, which accounts for the measured 1.4x speedup.
