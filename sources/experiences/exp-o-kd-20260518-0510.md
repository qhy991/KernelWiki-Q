---
id: exp-o-kd-20260518-0510
title: experts_64
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
- shared-memory-optimization
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 2.414x
---

## Challenge

The before code for 64 experts uses dynamic shared memory with a 2D tokens_cnts[(33)*64] = 2,112 int32_t entries plus cumsum[65], fitting in shared memory. It counts tokens via conflict-free per-thread increments, then runs a per-expert prefix-sum across threads requiring an extra __syncthreads a...


## Solution

The after code uses warp-partitioned shared_counts[32][8] with atomicAdd — each thread atomically increments the count for its assigned expert's warp-owned slot. This eliminates the per-expert prefix-sum step entirely (atomicAdd already produces the total), removes one __syncthreads barrier, and ...

## Results

- **speedup**: 2.414x (improved)


## Key Lessons

- This eliminates the per-expert prefix-sum step entirely (atomicAdd already produces the total), removes one __syncthreads barrier, and launches 1024 threads instead of 64, reducing per-thread token-counting work by 16x.
