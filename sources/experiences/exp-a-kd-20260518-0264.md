---
id: exp-a-kd-20260518-0264
title: head64_seq512
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- softmax
- shared-memory-optimization
- pipeline-stages
kernel_types:
- softmax
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- pipeline-stages
impl_family: custom_cuda
speedup: 0.918x
---

## Challenge

The after code reduces Bc from 64 to 32 (kWarpTileSeqLenK 8->4) to satisfy the kCanPrefetchKVg2s condition (Q_tile_size/max(K,V) >= 2), but this smaller Bc doubles the number of K/V tile iterations across the seqlen dimension, increasing total loop overhea


## Solution

d, barrier synchronization, and online-softmax round-trips. The double-buffered async prefetch of V g2s is designed to overlap the V gmem->smem transfer with Q@K^T MMA, but with head_dim=64 and seqlen=512 the per-tile MMA work is too small to hide the full

## Results

- **speedup**: 0.918x (regression)


## Key Lessons

- The after code reduces Bc from 64 to 32 (kWarpTileSeqLenK 8->4) to satisfy the kCanPrefetchKVg2s condition (Q_tile_size/max(K,V) >= 2), but this smaller Bc doubles the number of K/V tile iterations across the seqlen dimension, increasing total loop overhead, barrier synchronization, and online-softm
