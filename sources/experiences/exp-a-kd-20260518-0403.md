---
id: exp-a-kd-20260518-0403
title: row_major_fp16_fp8
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.000x
---

## Challenge

The before code computes scale_output as output_s + (block_group_id + local_group_id), while the after code with IS_COLUMN_MAJOR=false computes scale_output as output_s + global_group_id where global_group_id = block_group_id + l


## Solution

ocal_group_id. These are identical integer expressions producing the same pointer value. The if constexpr branch is resolved at C++ compile time, so no runtime instructions are emitted for the IS_COLUMN_MAJOR=true path when the t

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- The before code computes scale_output as output_s + (block_group_id + local_group_id), while the after code with IS_COLUMN_MAJOR=false computes scale_output as output_s + global_group_id where global_group_id = block_group_id + local_group_id.
