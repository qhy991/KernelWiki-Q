---
id: exp-a-kd-20260518-0610
title: fp16_4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.998x
---

## Challenge

On sm_80 (A800, cc_major=8), the before code queries cc_major at runtime and dispatches via a runtime ternary (if max_vec_size_byte==32 select kernel<32> else kernel<16>). The after code uses compile-time constexpr kMaxVecBytes=16 and directly instantiates kernel<16>. Since the runtime ternary on...


## Solution

The optimization's primary benefit is compile-time: eliminating the dead 32-byte template instantiation reduces JIT compilation time and binary size, not measurable as a kernel latency improvement.

## Results

- **speedup**: 0.998x (neutral)


## Key Lessons

- Since the runtime ternary on cc_major=8 always selects the 16-byte path on this GPU, both before and after execute the identical 16-byte kernel body.
