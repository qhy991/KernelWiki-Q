---
id: exp-a-kd-20260518-0906
title: strided_float
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- elementwise
kernel_types:
- elementwise
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.438x
---

## Challenge

The before code requires a separate copy_strided_to_contiguous_kernel launch (simulating PyTorch's .contiguous()) to relocate strided rows into a contiguous buffer before the norm kernel can process them. This copy kernel reads 128 tokens of 256 floats from stride-512 offsets and writes them cont...


## Solution

The after kernel eliminates this copy by passing input_stride=512 directly, computing blockIdx.x * 512 + idx to read from the original strided buffer in-place. The 1.44x speedup comes from removing the strided-to-contiguous copy overhead: one kernel launch and its associated global memory data mo...

## Results

- **speedup**: 1.438x (improved)


## Key Lessons

- The after kernel eliminates this copy by passing input_stride=512 directly, computing blockIdx.x * 512 + idx to read from the original strided buffer in-place.
