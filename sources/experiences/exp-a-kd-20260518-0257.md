---
id: exp-a-kd-20260518-0257
title: d64_s2048
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- pipeline-stages
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
speedup: 0.955x
---

## Challenge

At seqlen=2048 the workload has more K-tiles (32 for Bc=64, 64 for Bc=32), giving the async pipeline more opportunities to overlap gmem transfers with computation. However, the kStage=2 after variant s


## Solution

till uses the smaller Bc=32 tile (kWarpTileSeqLenK=4), which reduces compute per tile. The double-buffering prefetch of V during Q@K^T and next-K during P@V partially compensates for the smaller tile,

## Results

- **speedup**: 0.955x (regression)


## Key Lessons

- The pipeline benefit is diluted because each Bc=32 tile has only half the MMA work of the Bc=64 tile, so the compute phase that would hide the gmem transfer is shorter.
