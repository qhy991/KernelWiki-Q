---
id: exp-a-kd-20260518-0084
title: conv_interleaved_store
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- convolution
kernel_types:
- convolution
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.008x
---

## Challenge

The before code uses if(guard) wrapping a single *ptr = val store, while the after code uses inline PTX @p st.global.v4.u32 predicated store. In this convolution interleaved store case, each thread computes a complex row_offset using stride_row arithmetic and a mo


## Solution

dulo operation, then conditionally stores. The computed addresses involve scattered writes (non-contiguous across threads), making the kernel memory-latency-bound rather than bandwidth-bound. Under memory-latency-bound conditions, the small difference in instructi

## Results

- **speedup**: 1.008x (neutral)


## Key Lessons

- The computed addresses involve scattered writes (non-contiguous across threads), making the kernel memory-latency-bound rather than bandwidth-bound.
