---
id: exp-a-kd-20260518-0427
title: moe_block_16_valid_scan
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

moe_block_16_valid_scan

## Solution

The after code replaces this with a cooperative shared-memory load (moe_block_size/4 threads each load one int4 concurrently) followed by warp-level parallel counting: the last 32 threads each read a small slice of the now-shared-memory data and count valid tokens locally, then reduce via __shfl_...

## Key Lessons

- The parallel load eliminates the serial global-memory bottleneck, and the warp reduction (5 shuffle stages) replaces the O(moe_block_size) serial comparison loop with O(log32) parallel reduction.
- The before code scans sorted_token_ids serially in global memory: each thread reads an int4 chunk, unpacks it into 4 ints, and checks each int against prob_m_top_k with a break-on-first-invalid pattern.
