---
id: exp-o-kd-20260518-0868
title: unknown
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

unknown

## Solution

e num_heads*head_size region is contiguous in both source and destination memory, so the element-by-element approach wastes memory bandwidth by issuing narrow transactions instead of wide vector loads/stores.
Applied change: For the NHD contiguous-heads layout, the kernel now pre-computes base sourc

## Key Lessons

- For the NHD layout where head_stride == head_size, the entire num_heads*head_size region is contiguous in both source and destination memory, so the element-by-element approach wastes memory bandwidth by issuing narrow transactions instead of wide vector loads/stores.
