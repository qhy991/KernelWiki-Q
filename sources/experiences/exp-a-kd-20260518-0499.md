---
id: exp-a-kd-20260518-0499
title: reduce_32k_256ep
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 1.532x
---

## Challenge

The after kernel replaces atomicAdd-to-global-memory + __syncthreads + global-memory-read with CUB BlockReduce, which performs the sum cooperatively in shared memory using a hierarchical warp-level reduction pattern. In the before kernel, all


## Solution

512 threads in a block atomically write to the same global address (atomic_buffer[expert_id]), causing serialization of the atomic operations. Additionally, the before approach requires a cudaMemset to zero the atomic_buffer before each invo

## Results

- **speedup**: 1.532x (improved)


## Key Lessons

- BlockReduce eliminates all three overhead sources: the serialized atomic contention, the buffer reset, and the extra global read, yielding a 34.7% latency reduction at this workload size.
- The after kernel replaces atomicAdd-to-global-memory + __syncthreads + global-memory-read with CUB BlockReduce, which performs the sum cooperatively in shared memory using a hierarchical warp-level reduction pattern.
