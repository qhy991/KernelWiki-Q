---
id: exp-a-kd-20260518-1400
title: fuse_global_scale
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- pipeline-stages
- epilogue-fusion
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.000x
---

## Challenge

The before code applies topk weight and global_scale as two separate __hmul2 operations per output element (output * tw * gs). The after code fuses them into one __hmul2 by pre-multiplying global_scale into the topk weight at load time (output * (gs * tw)). In this microbenchmark, the total arith...


## Solution

The optimization in the real kernel saves one multiply per output write because the fused weight is reused across multiple K iterations in the matmul loop, but this microbenchmark only has one output write per element, eliminating the reuse benefit. The neutral result reflects that the fusion adv...

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- The optimization in the real kernel saves one multiply per output write because the fused weight is reused across multiple K iterations in the matmul loop, but this microbenchmark only has one output write per element, eliminating the reuse benefit.
- The before code applies topk weight and global_scale as two separate __hmul2 operations per output element (output * tw * gs).
