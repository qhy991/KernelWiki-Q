---
id: exp-a-kd-20260518-0292
title: hgemm_cublas_medium_512
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: cublas
speedup: 1.004x
---

## Challenge

The 0.43% delta at 512x512x512 is within measurement noise. Both before and after paths call cublasGemmEx with identical parameters (CUBLAS_COMPUTE_16F, CUBLAS_GEMM_DEFAULT_TENSOR_OP). The only structural difference is the host-side null-pointer check on g_handle in the before path versus a direc...


## Solution

the after path. Since the branch is always predicted as 'not-null' after warmup, the CPU overhead is effectively zero relative to the multi-millisecond GPU GEMM kernel. No real optimization mechanism operates at the kernel or memory-access level here.

## Results

- **speedup**: 1.004x (neutral)


## Key Lessons

- Since the branch is always predicted as 'not-null' after warmup, the CPU overhead is effectively zero relative to the multi-millisecond GPU GEMM kernel.
