---
id: exp-a-kd-20260518-1084
title: butterfly_reduce
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.001x
---

## Challenge

The before code hardcodes mask=16 and passes width=32 to __shfl_xor_sync, while the after code computes the initial mask as WARP_SIZE/2 (which equals 16 on NVIDIA) and uses VLLM_SHFL_XOR_SYNC which expands to __shfl_xor_sy


## Solution

nc(uint32_t(-1), val, mask) without the explicit width parameter (defaults to warpSize=32). On NVIDIA hardware, both generate identical SASS: the same butterfly reduction over 32 lanes with the same mask sequence. The negl

## Results

- **speedup**: 1.001x (neutral)


## Key Lessons

- The before code hardcodes mask=16 and passes width=32 to __shfl_xor_sync, while the after code computes the initial mask as WARP_SIZE/2 (which equals 16 on NVIDIA) and uses VLLM_SHFL_XOR_SYNC which expands to __shfl_xor_sync(uint32_t(-1), val, mask) without the explicit width parameter (defaults to 
