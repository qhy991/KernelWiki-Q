---
id: exp-a-kd-20260518-1336
title: m64_n4096_k4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- fine-grained-quantization
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
impl_family: cublas
speedup: 0.957x
---

## Challenge

For M=64 with N=4096 and K=4096, the before code already has a reasonably balanced GEMM shape where the standard C=A*B order provides good memory access patterns. The after code's swap-AB approach computes D=B^T*A^T, which does not provide a significant advantage because M=64 is large enough that th


## Solution

with 64 rows and K=4096 leading dim) already achieves good memory utilization. The slight 4.5% slowdown from the swap is likely due to the overhead of the transposed epilogue write (writing C^T=NxM instead of C=MxN) and the different cuBLAS kernel selection for the transposed operation. At M=64, the

## Results

- **speedup**: 0.957x (regression)


## Key Lessons

- The after code's swap-AB approach computes D=B^T*A^T, which does not provide a significant advantage because M=64 is large enough that the first operand (A with 64 rows and K=4096 leading dim) already achieves good memory utilization.
- For M=64 with N=4096 and K=4096, the before code already has a reasonably balanced GEMM shape where the standard C=A*B order provides good memory access patterns.
