---
id: exp-o-kd-20260518-0321
title: broadcast_mul
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- elementwise
- register-reuse
kernel_types:
- elementwise
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 2.272x
---

## Challenge

The BEFORE version (broadcast_mul_before.cu) performs scalar-half x half2-vector broadcast multiplication by: (1) converting scalar half to float via __half2float, (2) decomposing half2 into two separate float lanes via __low2half/__high2half + __half2float, (3) multiplying each lane individually...


## Solution

The AFTER version (broadcast_mul_after.cu) uses the h0_h0 function (PTX mov.b32 {%1,%1} to duplicate a uint16_t scalar into both lanes of a uint32_t half2 in a single instruction) to broadcast the scalar into a half2 vector, then performs mul.f16x2 PTX asm which computes both lane multiplications...

## Results

- **speedup**: 2.272x (improved)


## Key Lessons

- This round-trip through float domain (6 conversion operations per inner iteration: half->float for scalar, 2x half->float for input lanes, 2x float->half for result, 2x half->float for accumulation) is extremely expensive because each conversion requires a separate instruction and the two lanes are 
