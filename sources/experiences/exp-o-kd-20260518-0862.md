---
id: exp-o-kd-20260518-0862
title: unknown
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

unknown

## Solution

ntization scales vary across heads or layers.
Applied change: Changed the kernel parameter type from const float (pass-by-value) to const float* (pass-by-pointer) for both k_scale and v_scale in paged_attention_kernel, paged_attention_v1_kernel, and paged_attention_v2_kernel. The host-side callers n

## Key Lessons

- Expected impact: This enables dynamic, per-head or per-token KV cache scaling factor computation for FP8 quantized attention.
- The dereference pattern (*k_scale, *v_scale) currently reads the first element, but the pointer parameter opens the door for indexing (e.g., k_scale[head_idx]) in future extensions.
