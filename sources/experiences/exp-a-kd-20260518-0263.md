---
id: exp-a-kd-20260518-0263
title: stage2_s512
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
- pipeline-stages
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- pipeline-stages
impl_family: custom_cuda
speedup: 0.912x
---

## Challenge

The after code (kStage=2) regresses at N=512 because the doubled shared memory footprint (kStage=2 doubles both Q_tile_smem and K_tile_smem) causes higher L2 cache and SRAM pressure. At N=512 there are 8 tile_K_seqlen iterations with many concurrent thread blocks competing f


## Solution

or the same L2 cache lines. The kStage=2 kernel requests twice as much smem per block (2*Q_tile_size + 2*K_tile_size for QK), reducing the number of concurrent blocks per SM and hurting occupancy. The async copy overlap benefit is insufficient to compensate for the reduced o

## Results

- **speedup**: 0.912x (regression)


## Key Lessons

- The after code (kStage=2) regresses at N=512 because the doubled shared memory footprint (kStage=2 doubles both Q_tile_smem and K_tile_smem) causes higher L2 cache and SRAM pressure.
