---
id: exp-o-kd-20260518-0856
title: INT8 thread configs to fit within shared memory limits, especially on constraine
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Add is_a_8bit flag to halve shared memory allocation for A-matrix when activation type is INT8 instead of FP16/BF16.
Problem before: The shared memory allocation for the A-matrix tile always assumed 2 bytes per element (FP16/BF16 size), even when the activation type was INT8 whic


## Solution

INT8 thread configs to fit within shared memory limits, especially on constrained GPUs like Turing (SM75) with only 64KB.
Applied change: Compute is_a_8bit = a_type.size_bits() == 8 at runtime and use it to select the correct element size in the A-matrix shared memory calculation: stages * (tb_m * t

## Key Lessons

- Optimization focus: Add is_a_8bit flag to halve shared memory allocation for A-matrix when activation type is INT8 instead of FP16/BF16.
