---
id: exp-o-kd-20260518-0442
title: noncontiguous
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.000x
---

## Challenge

The before kernel computes row_stride = size_k * sizeof(half) / 16 = 32 int4 elements per row, but the actual input memory layout has lda * sizeof(half) / 16 = 64 int4 elements per row. This causes the before kernel to interpret row 1's data starting at int4 offset 32, which actually points into ...


## Solution

The after kernel correctly separates input_row_stride (lda-based) and output_row_stride (size_k-based), so input pointers advance by the actual memory stride while output pointers use the contiguous output stride. The kernel execution time is nearly identical because both perform the same number ...

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- The kernel execution time is nearly identical because both perform the same number of half-element reads and writes per thread — the difference is purely in the addresses computed from the stride values.
