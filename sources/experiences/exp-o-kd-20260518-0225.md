---
id: exp-o-kd-20260518-0225
title: fp16_hd8192
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
impl_family: custom_cuda
speedup: 1.289x
---

## Challenge

With hidden_dim=8192 the before code runs 1024 quantize-loop iterations (8192/8) each issuing 8 scalar byte stores, totaling 8192 individual store transactions. The after code runs only 512 iterations (8192/16) each issuing one 128-bit uint4 store, totaling


## Solution

512 coalesced transactions. The larger hidden_dim amplifies the improvement because the scalar-store overhead grows proportionally with iteration count while the uint4 approach keeps the per-iteration store cost constant at 1 transaction regardless of VEC_

## Results

- **speedup**: 1.289x (improved)


## Key Lessons

- The larger hidden_dim amplifies the improvement because the scalar-store overhead grows proportionally with iteration count while the uint4 approach keeps the per-iteration store cost constant at 1 transaction regardless of VEC_SIZE.
