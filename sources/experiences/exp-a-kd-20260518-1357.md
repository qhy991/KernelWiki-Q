---
id: exp-a-kd-20260518-1357
title: vec4_fp8_to_half
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.599x
---

## Challenge

Same fusion pattern as the scalar case but with vectorized 4-element (uint2) loads/stores. The before code launches fp8x4_to_half_unscaled_kernel to convert fp8x4 vectors to half2x2, writing to an intermediate d_half_tmp buffer, then scale_half2x2_kernel reads that buffer and applies the scale el...


## Solution

The after code uses scaled_vec_conversion_fp8x4_to_half2x2 which performs the fp8x2-to-half2 conversion and inline scale multiply in a single device function call per 4-element vector, avoiding the intermediate global memory write and second kernel launch. The ~1.6x speedup is consistent with the...

## Results

- **speedup**: 1.599x (improved)


## Key Lessons

- Same fusion pattern as the scalar case but with vectorized 4-element (uint2) loads/stores.
