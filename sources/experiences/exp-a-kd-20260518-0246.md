---
id: exp-a-kd-20260518-0246
title: pack_large
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- fine-grained-quantization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
impl_family: custom_cuda
speedup: 1.006x
---

## Challenge

At the large input size (16M half elements), the LDST128BITS consolidation shows only marginal benefit. The before kernel already processes 8 half elements per thread using four half2 loads/stores which the compiler can merge into wider transactions when addresses are contigu


## Solution

ous and aligned. The NVCC optimizer at -O3 likely recognizes that the four consecutive HALF2 loads from x[idx+0], x[idx+2], x[idx+4], x[idx+6] can be coalesced into a single 128-bit load, and similarly for the stores. This means the actual hardware behavior of the before and

## Results

- **speedup**: 1.006x (neutral)


## Key Lessons

- At the large input size (16M half elements), the LDST128BITS consolidation shows only marginal benefit.
