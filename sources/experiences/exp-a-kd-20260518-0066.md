---
id: exp-a-kd-20260518-0066
title: conv3d_dgrad_small
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 2.522x
---

## Challenge

The after code uses the kOptimized iterator algorithm with kUnity stride support, which precomputes a delta table (inc_next[]) for pointer advancement across the T,R,S filter dimensions and bitmask predicates (masks_[s][3]) for validity checks. The before code (kAnalytic/kStrided) recomput


## Solution

es NDHWC coordinates via at() on every tile access and checks validity through coordinate-based arithmetic including stride divisibility tests. The precomputed delta table eliminates per-tile coordinate math in the hot path: advance() applies a single byte offset from inc_next[] instead of

## Results

- **speedup**: 2.522x (improved)


## Key Lessons

- The precomputed delta table eliminates per-tile coordinate math in the hot path: advance() applies a single byte offset from inc_next[] instead of recomputing filter indices and NDHWC coordinates.
