---
id: exp-o-kd-20260518-0304
title: bs256_draft8
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
impl_family: custom_cuda
---

bs256_draft8

## Solution

(8 byte-sized writes per token); the after kernel replaces this with a single byte write (tree_mask[token_tree_idx] = 1 setting bit 0). The before kernel also computes mask offsets via a cumulative verified_seq_len loop running up to O(bid) iterations per thread plus mode bran

## Key Lessons

- The after kernel reduces mask allocation from 16384 bytes (256*8*8 bools) to 2048 bytes (256*8*1 uint8_t), an 8x memory reduction that directly cuts DRAM bandwidth for mask writes.
