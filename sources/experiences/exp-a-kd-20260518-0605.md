---
id: exp-a-kd-20260518-0605
title: bf16_hd512
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- register-reuse
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 1.033x
---

## Challenge

The CTA approach uses 64 threads (2 warps) instead of the warp approach's 32 threads, with each thread handling 4 packed bf16x2 pairs (16 bytes) versus 8 pairs (32 bytes). This doubles the threads performing concurren


## Solution

t memory loads, improving memory-level parallelism. However, the two __syncthreads() barriers in the shared-memory two-stage reduction add synchronization overhead that roughly offsets the parallelism gains. For head_

## Results

- **speedup**: 1.033x (improved)


## Key Lessons

- The CTA approach uses 64 threads (2 warps) instead of the warp approach's 32 threads, with each thread handling 4 packed bf16x2 pairs (16 bytes) versus 8 pairs (32 bytes).
