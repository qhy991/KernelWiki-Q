---
id: exp-a-kd-20260518-0438
title: half_2rank_256k
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.465x
---

## Challenge

The two-shot after code uses a two-phase reduce-then-gather pattern: Phase 1 reduces each rank portion into a shared buffer, Phase 2 gathers from shared buffers to output. On a single-GPU simulation this introduces an extra memory pas


## Solution

s (write to shared buffer then read back) plus additional kernel launch overhead for Phase 1 of both simulated ranks and Phase 2, compared to the one-shot before code which does a single-pass reduce directly to output. The additional

## Results

- **speedup**: 0.465x (regression)


## Key Lessons

- The two-shot after code uses a two-phase reduce-then-gather pattern: Phase 1 reduces each rank portion into a shared buffer, Phase 2 gathers from shared buffers to output.
