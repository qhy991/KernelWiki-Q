---
id: exp-a-kd-20260518-0602
title: mask_50pct_padding
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
impl_family: custom_cuda
---

mask_50pct_padding

## Solution

The after version adds `if (rowIdx_in_expert >= mask[expert_idx]) continue` which creates divergence: within each warp of 32 threads, roughly half hit the continue and skip the computation, while the other half proceeds. Since GPU warps execute in lockstep, the warp still consumes time proportion...

## Key Lessons

- Since GPU warps execute in lockstep, the warp still consumes time proportional to the longest path, and the mask comparison itself adds an extra global memory read (mask[expert_idx]) per iteration.
