---
id: exp-a-kd-20260518-0085
title: land_N128
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.810x
---

## Challenge

In the before code, reduce_array_before<128> executes 128 iterations, each calling array[i] which performs a byte-index + bit-shift extraction (storage[byte_idx] >> bit_idx) & 1, then a LogicalAnd1b functor call. This results in 128 bit-level loads, 128 AND operations, and substantial instruction...


## Solution

the after code, reduce_array_after<128> reinterprets the packed storage as a byte pointer and loops only 16 times (ceil(128/8)), each iteration doing a single byte load and a zero-check (!bits). The 8x reduction in loop iterations eliminates most of the bit-shift and masking instructions, and the...

## Results

- **speedup**: 1.810x (improved)


## Key Lessons

- This results in 128 bit-level loads, 128 AND operations, and substantial instruction overhead from the per-element extraction.
