---
id: exp-o-kd-20260518-0839
title: S launched unconditionally. When CUTLASS group gemm is used instead of DeepGemm,
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: cutlass
---

## Challenge

Optimization focus: Moved align_expert_first_token_offset allocation and getMIndices call inside the align_block_size conditional, avoiding unnecessary GPU memory allocation and kernel launch when alignment is not required.
Problem before: In moe_permute, align_expert_first_token_offset (a GPU tenso


## Solution

s launched unconditionally. When CUTLASS group gemm is used instead of DeepGemm, align_block_size is nullopt, so both the allocation and the kernel launch are wasted work: the allocated tensor is never consumed and getMIndices writes m_indices that are never read.
Applied change: Moved both the alig

## Key Lessons

- When CUTLASS group gemm is used instead of DeepGemm, align_block_size is nullopt, so both the allocation and the kernel launch are wasted work: the allocated tensor is never consumed and getMIndices writes m_indices that are never read.
