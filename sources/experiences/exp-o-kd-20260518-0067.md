---
id: exp-o-kd-20260518-0067
title: float_reduce_1M
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: inline_asm
speedup: 11.883x
---

## Challenge

The after code uses PTX inline asm max.NaN.f32 which compiles to a single FMNMX.NAN SASS instruction, while the before code uses C++ isnan() checks with if-branches that compile to FSETP.GTU + FSETP.GT + FSEL instruction sequences with conditional branches (BR


## Solution

A). In the block reduction, the before code's divergent branches in the shared-memory reduction loop serialize warp execution — threads taking different paths (isnan check vs normal comparison) cannot proceed in lockstep. The after code's branchless FMNMX.NAN

## Results

- **speedup**: 11.883x (improved)


## Key Lessons

- The after code's branchless FMNMX.NAN allows all threads to execute the same instruction, eliminating warp divergence entirely.
