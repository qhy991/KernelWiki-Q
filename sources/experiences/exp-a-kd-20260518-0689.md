---
id: exp-a-kd-20260518-0689
title: float_64epg
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- pipeline-stages
- register-reuse
- epilogue-fusion
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- register-reuse
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.495x
---

## Challenge

The before approach launches two kernels sequentially: compute_scores_with_bias_kernel performs an element-wise sigmoid+bias computation and writes results to a scores_with_bias intermediate buffer (requiring cudaMalloc of TOTAL_ELEMENTS floats and a full global-memory write pass), then topk_with...


## Solution

The after approach uses a single fused kernel where each thread reads raw scores[i] and group_bias[i], applies sigmoid_accurate() inline (0.5f*tanhf(0.5f*x)+0.5f, a single SFU instruction), adds bias inline, and immediately uses the result in the top2 comparison. This eliminates (1) one kernel la...

## Results

- **speedup**: 1.495x (improved)


## Key Lessons

- This eliminates (1) one kernel launch reducing overhead and enabling better pipeline occupancy, (2) the entire scores_with_bias buffer allocation and 2xTOTAL_ELEMENTS global-memory write+read cycle, and (3) keeps intermediate values in registers rather than spilling to global memory.
- The after approach uses a single fused kernel where each thread reads raw scores[i] and group_bias[i], applies sigmoid_accurate() inline (0.5f*tanhf(0.5f*x)+0.5f, a single SFU instruction), adds bias inline, and immediately uses the result in the top2 comparison.
