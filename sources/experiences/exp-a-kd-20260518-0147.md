---
id: exp-a-kd-20260518-0147
title: bf16_m16n8k8
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- register-reuse
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: inline_asm
speedup: 1.000x
---

## Challenge

The before code uses uint32_t* pointers and "r" register constraints for the C (accumulator) and D (output) operands in the mma.sync.aligned.m16n8k8 inline asm, while the after code uses float* pointers and "f" constraints. SASS disassembly confirms both versions produce identical machine code under


## Solution

C 12.8 with -O3: the same number of MOV and HMMA instructions, same register count (12 for the benchmark kernel). The NVCC optimizer at this toolchain version is sophisticated enough to perform correct register bank allocation for the MMA instruction regardless of whether the C++ pointer type and as

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- The before code uses uint32_t* pointers and "r" register constraints for the C (accumulator) and D (output) operands in the mma.sync.aligned.m16n8k8 inline asm, while the after code uses float* pointers and "f" constraints.
