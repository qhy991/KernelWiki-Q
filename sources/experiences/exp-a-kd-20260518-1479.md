---
id: exp-a-kd-20260518-1479
title: nibble_vec_16mb
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- register-reuse
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- vectorized-loads
impl_family: custom_cuda
speedup: 1.120x
---

## Challenge

The after kernel loads 16 bytes per thread via uint4 reinterpret_cast, processes all 16 bytes locally in registers using kNibbleLUT, then writes back as a single uint4 store. The before kernel processes one byte per thread: each thread reads in[i], applies kNibbleLUT, and writes out[i]. At 16 MB, th


## Solution

exceeds L2 cache capacity, so memory bandwidth dominates. The uint4 vectorized path reduces total memory transactions by 16x: each warp of 32 threads doing uint4 loads produces 4 coalesced 128-byte transactions (32 * 16 = 512 bytes), whereas the byte-by-byte warp would issue 32 separate 1-byte reads

## Results

- **speedup**: 1.120x (improved)


## Key Lessons

- The before kernel processes one byte per thread: each thread reads in[i], applies kNibbleLUT, and writes out[i].
