---
id: exp-a-kd-20260518-0606
title: bf16_hd1024
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
- register-reuse
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
---

bf16_hd1024

## Solution

after code stores only 4 pairs per thread (16 bytes each for input and weight), enabling single-pass execution that loads data just once. Second, the CTA approach uses 128 threads (4 warps) versus the warp approach's 32, providing 4x more concurrent memory-level parallelism for global-memory load...

## Key Lessons

- First, the warp before code must use two passes (load input for sum computation, then re-load input and weight for normalization) because storing all 16 packed pairs per thread (32 bytes of input + 32 bytes of weight) would cause severe register spilling; this doubles input memory traffic.
