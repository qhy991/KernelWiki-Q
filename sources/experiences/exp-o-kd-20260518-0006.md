---
id: exp-o-kd-20260518-0006
title: small_q_large_cache
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

small_q_large_cache

## Solution

The after code's CausalMask<true> computes max_blocks_q = ceil_div(1 * 64, 64) = 1, so the single Q-block visits only 1 K-block instead of 128. This 128x reduction in trip count eliminates almost all computation, dropping latency from 250ms to 0.21ms. The extreme asymmetry (N_Q=64 vs N_K=8192) am...

## Key Lessons

- The after code's CausalMask<true> computes max_blocks_q = ceil_div(1 * 64, 64) = 1, so the single Q-block visits only 1 K-block instead of 128.
