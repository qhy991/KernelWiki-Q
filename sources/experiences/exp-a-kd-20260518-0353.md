---
id: exp-a-kd-20260518-0353
title: M256_K16
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.017x
---

## Challenge

The after kernel splits each 32-thread warp into 2 sub-groups of 16 threads (K_WARP_SIZE=16), each computing a different row. In the before kernel, when K=16, only lanes 0-15 contribute to the dot product while lanes 16-31 are idle, wasting 50% of the


## Solution

warp's compute capacity. The after kernel uses lane 0-15 for row m and lane 16-31 for row m+1, with warp_reduce_sum_f32<K_WARP_SIZE=16> performing a sub-warp reduction over only the first 16 shuffle steps. This doubles the rows processed per warp, re

## Results

- **speedup**: 1.017x (neutral)


## Key Lessons

- The after kernel splits each 32-thread warp into 2 sub-groups of 16 threads (K_WARP_SIZE=16), each computing a different row.
