---
id: exp-a-kd-20260518-0317
title: f16x8_unpack_vs_f16x8_pack
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.269x
---

## Challenge

The after kernel replaces 4 separate half2 loads/stores (4 bytes each, 16 bytes total) with single LDST128BITS float4 128-bit transactions for both input and output. It also uses __hadd2 (SIMD half2 addition) in a pragma-un


## Solution

roll loop over 4 half2 pairs, computing 2 half additions in one hardware instruction instead of the scalar __hadd on individual .x/.y components. The 4 separate boundary checks (idx+0, idx+2, idx+4, idx+6 < N) are consolida

## Results

- **speedup**: 1.269x (improved)


## Key Lessons

- It also uses __hadd2 (SIMD half2 addition) in a pragma-unroll loop over 4 half2 pairs, computing 2 half additions in one hardware instruction instead of the scalar __hadd on individual .x/.y components.
