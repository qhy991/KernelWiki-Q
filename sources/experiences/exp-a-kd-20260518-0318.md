---
id: exp-a-kd-20260518-0318
title: relu_f32_vs_f32x4
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.020x
---

## Challenge

The before kernel (relu_f32_kernel) uses 256 threads per block with each thread processing one float element via scalar fmaxf. The after kernel (relu_f32x4_kernel) uses 64 threads per block with each thread processing 4 floats via a single float4 load/store. For FP32 ReLU on ~1M elements (4MB), the 


## Solution

kernel already achieves coalesced memory access since consecutive threadIdx values access consecutive float addresses. The float4 vectorization reduces the total number of memory transactions and thread count by 4x, but at this small problem size the data fits comfortably in L2 cache and the kernel 

## Results

- **speedup**: 1.020x (neutral)


## Key Lessons

- For FP32 ReLU on ~1M elements (4MB), the scalar kernel already achieves coalesced memory access since consecutive threadIdx values access consecutive float addresses.
