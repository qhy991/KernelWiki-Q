---
id: exp-a-kd-20260518-1449
title: ep64_n1536
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
- pipeline-stages
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
speedup: 2.844x
---

## Challenge

The after kernel replaces the before kernel's three-__syncthreads() manual prefix-sum pipeline with CUB BlockScan ExclusiveSum. In the before code, threads 0..63 each perform a serial loop across 128 thread rows to accumulate per-expert counts, then thread 0 alone iterates across all 64 experts t...


## Solution

after code uses 1024 threads for atomicAdd counting (reducing per-thread iteration from ~12 to ~1.5 elements), and BlockScan computes the aligned prefix sum in O(log 64) parallel steps using warp-level shuffle primitives instead of thread-0's 64-step serial loop. The 8x thread increase for counti...

## Results

- **speedup**: 2.844x (improved)


## Key Lessons

- The after kernel replaces the before kernel's three-__syncthreads() manual prefix-sum pipeline with CUB BlockScan ExclusiveSum.
