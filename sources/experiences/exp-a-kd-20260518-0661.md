---
id: exp-a-kd-20260518-0661
title: vecload_exp64
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
- register-reuse
- vectorized-loads
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- vectorized-loads
impl_family: custom_cuda
speedup: 1.137x
---

## Challenge

The before code performs 3 passes over the 64-element row using scalar loads with stride=256: each thread reads 1 element per iteration (64/256 = 1 iteration, but 192 threads are idle). The three-pass pattern (max-finding, sum-computing, output-writing) means each row element is read 3 times from...


## Solution

The after code uses VPT=4 with BYTES_PER_LDG=16 (ELTS_PER_LDG=4) and THREADS_PER_ROW=16, meaning 16 threads each load 4 floats in one 16-byte vectorized transaction, covering the entire 64-element row in one LDG_PER_THREAD=1 load per thread. The interleaved access pattern (`vec_thread_read_ptr[ii...

## Results

- **speedup**: 1.137x (improved)


## Key Lessons

- The three-pass pattern (max-finding, sum-computing, output-writing) means each row element is read 3 times from global memory, producing 3 separate scalar load streams.
