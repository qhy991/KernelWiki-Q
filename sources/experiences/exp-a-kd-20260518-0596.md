---
id: exp-a-kd-20260518-0596
title: bf16_aligned_remainder
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- pipeline-stages
- register-reuse
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- register-reuse
- vectorized-loads
impl_family: custom_cuda
speedup: 1.068x
---

## Challenge

The after code removes the scalar remainder loop (lines handling remaining_start through hidden_dim) that existed in the before code, even though with aligned hidden_dim=4096 that loop executes zero iterations. The 6.7% measured improvement likely stems from reduced kernel code size: removi


## Solution

ng the remainder loop path eliminates roughly 15 instructions from the kernel binary (scalar load, clamp, store, and the loop control logic including the ROCM conditional branch), which reduces pressure on the instruction cache and allows the compiler to generate a tighter loop body for the

## Results

- **speedup**: 1.068x (improved)


## Key Lessons

- The 6.7% measured improvement likely stems from reduced kernel code size: removing the remainder loop path eliminates roughly 15 instructions from the kernel binary (scalar load, clamp, store, and the loop control logic including the ROCM conditional branch), which reduces pressure on the instructio
