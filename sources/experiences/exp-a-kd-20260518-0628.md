---
id: exp-a-kd-20260518-0628
title: count_64exp
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 2.082x
---

## Challenge

The after implementation uses atomicAdd to a single shared_counts[num_experts] array, eliminating the per-thread counting matrix that consumes O(stride * num_experts) shared memory. The before approach with 256 threads and 64 experts requires (256+1)*64*4 = 65


## Solution

.5KB shared memory, plus a serial column-wise reduction that iterates stride times over num_experts columns. atomicAdd avoids both: the shared memory drops to just 64*4=256 bytes, and the serial reduction is eliminated entirely. At 64 experts, the per-thread m

## Results

- **speedup**: 2.082x (improved)


## Key Lessons

- atomicAdd avoids both: the shared memory drops to just 64*4=256 bytes, and the serial reduction is eliminated entirely.
