---
id: exp-o-kd-20260518-0688
title: Ress. This is called per-iteration inside the main k-loop, incurring repeated
  cv
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Changed shared memory addressing from 3D array indexing with __cvta_generic_to_shared to manual byte-offset arithmetic from a pre-computed base pointer for cp.async loads.
Problem before: With static shared memory, cp.async destination pointers are computed via __cvta_generic_to_


## Solution

ress. This is called per-iteration inside the main k-loop, incurring repeated cvta overhead for each async copy.
Applied change: Pre-compute the shared memory base pointers once (smem_a_base_ptr, smem_b_base_ptr) using __cvta_generic_to_shared on the base of s_a and s_b. Then compute byte offsets ar

## Key Lessons

- Expected impact: Eliminates repeated __cvta_generic_to_shared calls inside the hot loop.
