---
id: exp-a-kd-20260518-1090
title: small_m_8exp
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: cutlass
---

small_m_8exp

## Solution

2 writes per expert plus 1 offset write), so the compute_pplx_data kernel latency is nearly identical. The real performance gain from SWAP_AB manifests downstream in the CUTLASS GEMM that consumes these problem sizes, where the swa

## Key Lessons

- In the after code, when SWAP_AB=true (small M scenario where num_local_experts*padded_m <= 64), the kernel writes problem_sizes in (2N, M, K) order instead of (M, 2N, K).
- At this microbenchmark level, the change is neutral because both paths emit the same number of global memory stores with identical access patterns.
