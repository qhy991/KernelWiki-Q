---
id: exp-a-kd-20260518-0533
title: fusion_medium_batch
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- register-reuse
- epilogue-fusion
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- epilogue-fusion
impl_family: custom_cuda
---

fusion_medium_batch

## Solution

the after source stays in registers after the RoPE computation and is written to both k_rope and k_buffer from the same register value (lines 95-100), avoiding the redundant global load. V data is also loaded once in the fused kernel (line 70) and reused directly in the buffer write (line 101), r...

## Key Lessons

- With nnz=32, the absolute kernel launch overhead per iteration is a smaller fraction, but the register reuse benefit is still substantial because k_out in the after source stays in registers after the RoPE computation and is written to both k_rope and k_buffer from the same register value (lines 95-
- The before source (fusion_medium_batch_before.cu) uses the same two-kernel pattern: rope_only_kernel stores rotated K to k_rope in global memory (line 79), and kv_save_kernel re-reads from k_rope (line 108), incurring an extra global memory round-trip per KV head per token.
