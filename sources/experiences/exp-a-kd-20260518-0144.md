---
id: exp-a-kd-20260518-0144
title: sm80_fp16_vs_sm89_fp8_coop_gemm
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- fine-grained-quantization
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
impl_family: custom_cuda
speedup: 0.487x
---

## Challenge

The before code issues a single mma.sync.aligned.m16n8k16.row.col.f32.f16.f16.f32 instruction per iteration (4096 FLOPs), while the after code, running on sm_80 hardware without native FP8 MMA support, falls back to issuing two SM80 FP16 MMA instructions sequentially


## Solution

to cover the same K=32 dimension (2 * 4096 = 8192 FLOPs). This doubles the raw latency from ~1.82ms to ~3.74ms, but the per-FLOP throughput is nearly identical (0.0225 vs 0.0219 TFLOPs). On actual SM89 (Ada Lovelace) hardware, the mma.sync.aligned.m16n8k32.row.col.f

## Results

- **speedup**: 0.487x (regression)


## Key Lessons

- The before code issues a single mma.sync.aligned.m16n8k16.row.col.f32.f16.f16.f32 instruction per iteration (4096 FLOPs), while the after code, running on sm_80 hardware without native FP8 MMA support, falls back to issuing two SM80 FP16 MMA instructions sequentially to cover the same K=32 dimension
