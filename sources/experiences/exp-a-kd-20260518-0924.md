---
id: exp-a-kd-20260518-0924
title: edge_m_64
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: cublas
speedup: 1.021x
---

## Challenge

The before code calls cublasGemmEx with CUBLAS_OP_N on both operands (direct MxK x KxN GEMM), while the after code uses CUBLAS_OP_T on both operands to swap A/B dimensions (NxK x KxM). On SM80 cuBLAS, both paths route to internally optimized kernels that already select tiles appropriate


## Solution

for the problem dimensions. The swapAB transformation changes which dimension cuBLAS sees as the M operand (4096 instead of 64), but cuBLAS on SM80 has its own internal heuristic for handling small-M problems that neutralizes the benefit. The measured ~2% difference is within noise mar

## Results

- **speedup**: 1.021x (improved)


## Key Lessons

- The swapAB transformation changes which dimension cuBLAS sees as the M operand (4096 instead of 64), but cuBLAS on SM80 has its own internal heuristic for handling small-M problems that neutralizes the benefit.
