---
id: exp-a-kd-20260518-0036
title: gelu_erff_float
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- activation
- pipeline-stages
kernel_types:
- activation
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
---

gelu_erff_float

## Solution

_tanh(k0 * z * (1.0f + k1 * z * z))), where fast_tanh uses the single-instruction PTX tanh.approx.f32. However, for float32 with 4M elements on sm_80, the kernel is memory-bandwidth-bound: each thread reads/writes 4 bytes from global memory, and the DRAM throughput saturates before

## Key Lessons

- The before code calls GELU_erff::operator() which computes 0.5f * scalar * (1.0f + erff(scalar / sqrt(2))), where erff is a CUDA math library function implemented as a multi-instruction SFU sequence.
