---
id: exp-a-kd-20260518-0380
title: fuse_rescale_topk8_bs4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
- epilogue-fusion
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
---

fuse_rescale_topk8_bs4096

## Solution

The after code uses a single fused_rescale_kernel that reads each element once, divides by the row sum, multiplies by the scaling factor in the same thread context, and writes the final result once. This cuts global memory traffic by roughly half for this 128KB output array and removes one kernel...

## Key Lessons

- The speedup (1.34x) is slightly lower than the smaller case (1.44x) because the larger workload means the kernel launch overhead is a smaller fraction of total time, but the memory traffic savings remain substantial.
- The after code uses a single fused_rescale_kernel that reads each element once, divides by the row sum, multiplies by the scaling factor in the same thread context, and writes the final result once.
