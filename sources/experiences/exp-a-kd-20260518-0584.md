---
id: exp-a-kd-20260518-0584
title: elem_1024b
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- register-reuse
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: inline_asm
---

elem_1024b

## Solution

tion performs 8 inline asm ld.global.L1::no_allocate loads in a fully unrolled loop with fixed compile-time indices, and the data is immediately consumed by store_vec's matching unrolled inline asm st.global.L1::no_allocate stores. The compiler's optimizer can keep each uint4 element in separate reg

## Key Lessons

- The alignas attribute only affects the hypothetical starting address if the object were placed in local memory, but the optimizer avoids that placement because the data path from load asm to store asm does not require materializing the whole array as a contiguous memory block.
- Even with the strong alignas(128) constraint on a 128-byte array, NVCC 12.8 for sm_80 does not force a measurable local-memory spill in this kernel pattern.
