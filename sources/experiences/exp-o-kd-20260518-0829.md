---
id: exp-o-kd-20260518-0829
title: On or layout transformations, causing incorrect memory addressing and wrong
  resu
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Add input_stride parameter to all kernel functions and propagate to compute_rms, compute_dynamic_per_token_scales, and norm_and_quant for stride-aware memory access.
Problem before: The fused RMS norm + dynamic per-token quantization kernels assumed all input tensors were fully c


## Solution

on or layout transformations, causing incorrect memory addressing and wrong results.
Applied change: Added an input_stride parameter to all kernel functions (rms_norm_dynamic_per_token_quant_vec, rms_norm_dynamic_per_token_quant_kernel, rms_norm_per_block_quant_kernel) and propagated it to the inter

## Key Lessons

- The stride replaces the hardcoded hidden_size in the token offset calculation (blockIdx.x * input_stride), enabling correct addressing for both contiguous and non-contiguous input layouts.
