---
id: exp-a-kd-20260518-0536
title: smem_layout_32_512
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- shared-memory-optimization
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 0.996x
---

## Challenge

With NUM_TOP_K=32 and HOT_BUFFER_SIZE=512, the total shared memory footprint is small (SmemLayout computes BYTES = 132*4 + 576*2 = 1680 bytes). At this scale, the static __shared__ declarations already place all int32_t arrays contiguously before the int16


## Solution

_t arrays because NVCC allocates the full static shared memory block as a single region with per-array alignment. The compiler-generated layout for the static version is nearly identical to the manual SmemLayout pointer offsets, so the dynamic version incu

## Results

- **speedup**: 0.996x (neutral)


## Key Lessons

- At this scale, the static __shared__ declarations already place all int32_t arrays contiguously before the int16_t arrays because NVCC allocates the full static shared memory block as a single region with per-array alignment.
