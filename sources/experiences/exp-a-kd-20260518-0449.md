---
id: exp-a-kd-20260518-0449
title: bf16_m64_topk2_k2048
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- register-reuse
- epilogue-fusion
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- epilogue-fusion
impl_family: custom_cuda
speedup: 2.102x
---

## Challenge

At this small scale (m=64, topk=2, row_stride=2048), the intermediate buffers are relatively tiny (128*2048*4 = ~1MB each), so memory traffic savings are less dominant. The before code launches shuffle_kernel (grid=128 blocks), mul_weight_kernel (grid=128), and sum_reduce_kernel (grid=64) as three s


## Solution

nvocations, each with its own launch overhead and device synchronization gap. The fused after kernel consolidates all three operations into one kernel launch with grid=64 blocks, reading input_tensor directly via the permutation index and accumulating factor*val into a register sum_val across just 2

## Results

- **speedup**: 2.102x (improved)


## Key Lessons

- The 2.10x speedup comes primarily from eliminating the two kernel launch+sync overheads and the two intermediate buffer writes at this small-batch size where launch overhead is a larger fraction of total time, while the memory traffic reduction is proportionally smaller since the intermediates are o
