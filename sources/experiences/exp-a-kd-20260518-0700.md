---
id: exp-a-kd-20260518-0700
title: per_channel_g32_h2048
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 0.131x
---

## Challenge

The after kernel iterates over 64 column groups in a serial outer loop, calling vec_quant per group. With group_n=32 and VEC_SIZE=4, each group has only 8 float4 vectors. Only 8 of 256 threads execute the inner vectorized loop per group iteration (3.1% utilization), while 248 threads idle but still 


## Solution

group loop. The before kernel's scalar strided loop distributes all 2048 elements evenly across 256 threads (8 iterations each) with naturally coalesced warp-level access—consecutive threads access consecutive addresses, satisfying the GPU coalescing requirement. For float (4-byte elements), float4 

## Results

- **speedup**: 0.131x (regression)


## Key Lessons

- This float-based benchmark understates the pattern's real-world benefit: for FP8 (1-byte elements, VEC_SIZE=16), scalar stores are 1 byte each versus 16-byte vectorized stores, a 16x throughput difference that could outweigh thread underutilization for larger group sizes.
