---
id: exp-a-kd-20260518-0586
title: all_layer_4l
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.995x
---

## Challenge

The before kernel iterates over num_layers=4 layers per work item, performing 4 warp-cooperative transfers per layer: load_vec+store_vec for K-cache and load_vec+store_vec for V-cache, plus 4 pointer indirection loads (k_cache_src/dst, v_cache_src/dst) from per-layer pointer tables. This totals 1...


## Solution

The after kernel with kIsMLA=true eliminates the entire V-cache block inside the layer loop via if constexpr (!kIsMLA), removing 8 load_nc/store_nc transfers (2 per layer across 4 layers) and 4 pointer indirection loads (v_cache_src/dst per layer). The remaining K-only path does 8 load_nc/store_n...

## Results

- **speedup**: 1.995x (improved)


## Key Lessons

- The after kernel with kIsMLA=true eliminates the entire V-cache block inside the layer loop via if constexpr (!kIsMLA), removing 8 load_nc/store_nc transfers (2 per layer across 4 layers) and 4 pointer indirection loads (v_cache_src/dst per layer).
