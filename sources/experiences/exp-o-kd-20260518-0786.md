---
id: exp-o-kd-20260518-0786
title: Erpret-cast pointer to compute wrong offsets when reading or writing input
  vecto
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Add vec_input_stride and strided_id for vectorized input access in fused_add_rms_norm_kernel (width > 0).
Problem before: The vectorized fused_add_rms_norm_kernel (width > 0) accessed input through `input_v[id]` where `id = blockIdx.x * vec_hidden_size + idx`, assuming the input'


## Solution

erpret-cast pointer to compute wrong offsets when reading or writing input vectors.
Applied change: A `vec_input_stride = input_stride / width` is computed from the element-level input stride divided by the vector width, and a dedicated `strided_id = blockIdx.x * vec_input_stride + idx` is used for 

## Key Lessons

- For strided input tensors, this assumption breaks because the element-level stride differs from hidden_size, causing the vectorized reinterpret-cast pointer to compute wrong offsets when reading or writing input vectors.
