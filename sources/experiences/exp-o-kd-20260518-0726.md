---
id: exp-o-kd-20260518-0726
title: Y, which is not valid CUDA C++ (shared memory must be at kernel scope), and
  wrot
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Rewrote small token kernel TopK: one-thread-per-expert with direct warp-level reduction, replacing per-warp subset scanning with inner loop.
Problem before: The small token kernel used a per-warp subset scanning approach for TopK: each warp was assigned a partition of ~32 experts


## Solution

y, which is not valid CUDA C++ (shared memory must be at kernel scope), and wrote output to global memory inline during each top-k iteration.
Applied change: The kernel was rewritten to leverage the natural 384-thread / 384-expert mapping: each thread holds exactly one expert value (tid maps to expe

## Key Lessons

- Expected impact: Eliminates the inner scanning loop over expert subsets, reducing shared memory read iterations per warp from ~32 to 1 (each thread already holds its value).
- The approach also declared shared memory arrays inside the for loop body, which is not valid CUDA C++ (shared memory must be at kernel scope), and wrote output to global memory inline during each top-k iteration.
