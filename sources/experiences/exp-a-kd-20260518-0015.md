---
id: exp-a-kd-20260518-0015
title: half_N8
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.000x
---

## Challenge

The before code converts each __half to float one at a time, computes sigmoid via expf, then converts back. The after code uses __half2 intrinsics to load/store two half values at once and processes them in a float2 pair. However, the actual arithmetic (expf) st


## Solution

ill runs on float values individually — there is no native half-precision exp instruction on sm_80, so both paths must convert to float, compute expf per lane, and convert back. The half2 loads reduce the number of load/store instructions (4 half2 loads vs 8 hal

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- The before code converts each __half to float one at a time, computes sigmoid via expf, then converts back.
