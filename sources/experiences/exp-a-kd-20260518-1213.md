---
id: exp-a-kd-20260518-1213
title: fusion_medium_32q
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
- register-reuse
- epilogue-fusion
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.444x
---

## Challenge

In the before path, two kernel launches are issued sequentially: advance_step_base_kernel updates token, seq_len, position, and slot_mapping; then advance_step_extra_kernel reads the modified seq_lens to compute paged_kv_last_page_len and block_table_bound. The second kernel must reload seq_lens_...


## Solution

The after path's single advance_step_flashinfer_kernel reads seq_len once into a register, derives all six outputs from that single value and the block_tables lookup, and writes all results in one pass. The ~1.44x speedup comes from eliminating one kernel launch's driver overhead (~5-10us) and re...

## Results

- **speedup**: 1.444x (improved)


## Key Lessons

- The after path's single advance_step_flashinfer_kernel reads seq_len once into a register, derives all six outputs from that single value and the block_tables lookup, and writes all results in one pass.
