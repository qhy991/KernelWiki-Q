---
id: exp-a-kd-20260518-0072
title: sync16_per_iter
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 1.189x
---

## Challenge

In sync16_per_iter_before.cu, the kernel iterates 16 times with stride 1, issuing two __syncthreads() per iteration (32 barriers total). In sync16_per_iter_after.cu, the outer loop strides by FPP=2 and an inner loop s


## Solution

tores two fragments into separate SMEM partitions before each barrier. This reduces barriers to 2*(16/2)=16. The 19% speedup comes from halving the barrier count: each __syncthreads() forces all 256 threads to converg

## Results

- **speedup**: 1.189x (improved)


## Key Lessons

- In sync16_per_iter_before.cu, the kernel iterates 16 times with stride 1, issuing two __syncthreads() per iteration (32 barriers total).
