---
id: exp-o-kd-20260518-0343
title: small_m
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 1.079x
---

## Challenge

The before kernel uses TILE_M=16 with VALID_ROWS=8, allocating shared memory for 16 rows (sh_a=2*16*128*4=16384 bytes, sh_red=16*136*4=8704 bytes) but only processing 8. The after kernel uses TILE_M=8 with VALID_ROWS=8, allocating exactly what is needed


## Solution

(sh_a=2*8*128*4=8192 bytes, sh_red=8*136*4=4352 bytes). This halves per-block shared memory from 25152 to 12576 bytes. Even though both report 1 block/SM from the occupancy API, the after kernel's tighter tile sizing reduces the sh_a cooperative load loo

## Results

- **speedup**: 1.079x (improved)


## Key Lessons

- The before kernel uses TILE_M=16 with VALID_ROWS=8, allocating shared memory for 16 rows (sh_a=2*16*128*4=16384 bytes, sh_red=16*136*4=8704 bytes) but only processing 8.
