---
id: exp-a-kd-20260518-0425
title: medium_batch_32
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- elementwise
kernel_types:
- elementwise
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 5.278x
---

## Challenge

The multi-backend kernel reads each source value once and broadcasts it to 3 destinations. With bs=32 and max_len=128, the page_table data (32*128=4096 elements) dominates. In the before approach, 3 separate kernel launches each read these 4096 elements from global memory independently, consuming...


## Solution

The after approach reads them once and the 3 writes go to different destination buffers. Additionally, 2 of 3 kernel launches are eliminated, removing ~10us of launch overhead per iteration. The single-read pattern also enables the GPU L2 cache to serve the subsequent destination writes from cach...

## Results

- **speedup**: 5.278x (improved)


## Key Lessons

- The single-read pattern also enables the GPU L2 cache to serve the subsequent destination writes from cached source data.
