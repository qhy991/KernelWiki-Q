---
id: exp-o-kd-20260518-0803
title: Often well below 65535 (e.g., 8 top-k for DeepSeek-V3). The old layout placed
  to
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 10x
---

## Challenge

Optimization focus: Reordered shared memory layout and added token_cnts_t template parameter to enable uint16_t token counts, halving shared memory footprint for the token counts portion.
Problem before: With large num_experts (e.g., 256 for DeepSeek-V3), the shared memory kernel required ((num_thre


## Solution

often well below 65535 (e.g., 8 top-k for DeepSeek-V3). The old layout placed tokens_cnts at the start of shared_mem, which meant cumsum was offset at the end, making it impossible to type-pun tokens_cnts to a smaller type without affecting cumsum alignment.
Applied change: The kernel template was e

## Results

- **speedup**: 10x


## Key Lessons

- This allows the kernel to be instantiated with token_cnts_t=uint16_t when topk_ids.numel() <= 65535, cutting the tokens_cnts shared memory portion from 4 bytes to 2 bytes per element.
