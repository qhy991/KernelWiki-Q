---
id: exp-o-kd-20260518-0679
title: unknown
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

unknown

## Solution

0 reads x[0], thread 1 reads x[2*N], thread 2 reads x[4*N]), producing a non-coalesced pattern that wastes memory bandwidth.
Applied change: The rope_f32_v2_kernel maps one CUDA block to one token position (blockIdx.x = token_pos, threadIdx.x = tid). All threads within a block now access consecutive

## Key Lessons

- Since threads in the same warp read adjacent addresses within the same token row, the GPU can coalesce these 32-bit accesses into fewer 128-bit memory transactions, reducing the effective memory bandwidth cost per access.
- When consecutive threads in a warp belong to different token positions, their global memory accesses are strided across token rows (e.g., thread 0 reads x[0], thread 1 reads x[2*N], thread 2 reads x[4*N]), producing a non-coalesced pattern that wastes memory bandwidth.
