---
id: exp-o-kd-20260518-0262
title: half_64k
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- pipeline-stages
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
speedup: 1.286x
---

## Challenge

At 128KB (64K half elements), the data fits more comfortably in the L2 cache, so the scalar before code gets some benefit from caching. The after code still gains from 8x wider ld.128 memory transactions and re


## Solution

duced instruction count per element via the packed_t upcast/downcast pipeline, but the relative advantage is smaller (1.29x) because at this data size the kernel runtime is short enough that launch overhead and

## Results

- **speedup**: 1.286x (improved)


## Key Lessons

- The after code still gains from 8x wider ld.128 memory transactions and reduced instruction count per element via the packed_t upcast/downcast pipeline, but the relative advantage is smaller (1.29x) because at this data size the kernel runtime is short enough that launch overhead and pipeline startu
