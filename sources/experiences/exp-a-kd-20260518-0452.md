---
id: exp-a-kd-20260518-0452
title: concat_mla_16384tokens
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- rotary-embedding
- pipeline-stages
- register-reuse
- vectorized-loads
kernel_types:
- rotary-embedding
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- register-reuse
- vectorized-loads
impl_family: custom_cuda
speedup: 0.791x
---

## Challenge

The after kernel regresses on A800 (Ampere sm_80) because its PTX-level cache control primitives add net overhead. The ld_na_global_v2 loads use ld.global.nc.L1::no_allocate, which bypasses L1 and forces reads through L2 (~200 cycle latency) instead of potentially hitting L1 (~30 cycle latency). The


## Solution

.L1::no_allocate to prevent L1 allocation, but for streaming writes that won't be re-read this doesn't improve store throughput—it only saves L1 capacity, which is irrelevant in this benchmark since no other data competes for L1. The software pipelining adds instruction overhead: conditional prefetc

## Results

- **speedup**: 0.791x (regression)


## Key Lessons

- The after kernel regresses on A800 (Ampere sm_80) because its PTX-level cache control primitives add net overhead.
- The before kernel's two-phase approach uses simpler control flow—two clean unrolled loops with direct pointer recomputation via head_id * stride—and keeps loaded data in the fastest memory tier (registers via k_nope_buf[16]), which is more efficient on Ampere for this streaming copy pattern.
