---
id: exp-o-kd-20260518-0222
title: rmsnorm_warp_128
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- rotary-embedding
kernel_types:
- rotary-embedding
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

rmsnorm_warp_128

## Solution

The after kernel launches only occupancy-optimal ~6912 blocks and uses a grid-strided loop (`row += gridDim.x`) so each block processes multiple rows sequentially while staying resident on its SM. This eliminates block scheduling overhead, keeps the row data warm in the SM's local L2 cache partit...

## Key Lessons

- This eliminates block scheduling overhead, keeps the row data warm in the SM's local L2 cache partition, and allows all blocks to run concurrently from the start.
