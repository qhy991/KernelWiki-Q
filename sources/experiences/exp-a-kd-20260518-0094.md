---
id: exp-a-kd-20260518-0094
title: smem_transfer
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- shared-memory-optimization
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 1.393x
---

## Challenge

In the before code, each thread applies scale/bias inline inside GEMM1's inner K1 loop (reg_accum[r*K1+kk] * scale[kk] + bias[kk]), resulting in M*N*K1 = 262144 multiply-add operations for scale/bias alone


## Solution

The after code applies scale/bias once during the centralized epilogue write to smem_accum (M*K1 = 4096 operations), then GEMM1 reads the already-transformed A1 values directly. This eliminates redundant scale/bias computation from the K1 inner loop, reducing the arithmetic intensity of GEMM1 fro...

## Results

- **speedup**: 1.393x (improved)


## Key Lessons

- This eliminates redundant scale/bias computation from the K1 inner loop, reducing the arithmetic intensity of GEMM1 from 3 FLOPs per inner iteration (scale*multiply + bias + B1 multiply) to 1 FLOP (smem_accum multiply), which improves instruction throughput despite the added __syncthreads() barrier.
- In the before code, each thread applies scale/bias inline inside GEMM1's inner K1 loop (reg_accum[r*K1+kk] * scale[kk] + bias[kk]), resulting in M*N*K1 = 262144 multiply-add operations for scale/bias alone.
