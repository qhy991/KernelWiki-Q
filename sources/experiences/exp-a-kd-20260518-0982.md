---
id: exp-a-kd-20260518-0982
title: contiguous
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.001x
---

## Challenge

In the contiguous case (lda == size_k), the before kernel computes row_stride = size_k * sizeof(half) / 16 and uses it for both input and output offsets. The after kernel computes input_row_stride = lda * sizeof(h


## Solution

alf) / 16 and output_row_stride = size_k * sizeof(half) / 16, which are numerically identical when lda == size_k. Both kernels perform the same memory access pattern, same number of int4 loads and half stores, and

## Results

- **speedup**: 1.001x (neutral)


## Key Lessons

- Therefore no measurable performance difference is expected.
- Both kernels perform the same memory access pattern, same number of int4 loads and half stores, and same permutation logic.
