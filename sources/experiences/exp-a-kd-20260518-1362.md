---
id: exp-a-kd-20260518-1362
title: small_hidden
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
- epilogue-fusion
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.877x
---

## Challenge

With hidden_size=256 and 128 tokens, each kernel launch has very little per-block work, so the launch overhead and the gap between the two separate kernel dispatches in the before code is proportionally large. The fused after kernel elim


## Solution

inates the second kernel launch entirely and avoids the global memory write+read of the scales array between the two passes. The blockReduceMax result is kept in shared memory (block_absmax_val) and used immediately to compute tmp_scale

## Results

- **speedup**: 1.877x (improved)


## Key Lessons

- The fused after kernel eliminates the second kernel launch entirely and avoids the global memory write+read of the scales array between the two passes.
