---
id: exp-a-kd-20260518-1484
title: contiguous_gemm_256x128x64
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: cutlass
speedup: 0.996x
---

## Challenge

Both the before and after programs compute identical stride values (lda=K=64, ldb=N=128, ldc=N=128) for contiguous row-major matrices with M=256, N=128, K=64. The before code directly assigns stride values from tensor metadata (lda=a.stride(0)=K, ldb=b.stride(1)=N, ldc=out.stride(0)=N) and reuses...


## Solution

The after code computes strides via packed_stride_from_shape(M,K)=K, packed_stride_from_shape(K,N)=N, packed_stride_from_shape(M,N)=N, deriving them from the logical shape dimensions rather than tensor stride fields. Since both produce the same numerical stride values, the GEMM kernel receives id...

## Results

- **speedup**: 0.996x (neutral)


## Key Lessons

- The after code computes strides via packed_stride_from_shape(M,K)=K, packed_stride_from_shape(K,N)=N, packed_stride_from_shape(M,N)=N, deriving them from the logical shape dimensions rather than tensor stride fields.
