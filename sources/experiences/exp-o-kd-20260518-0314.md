---
id: exp-o-kd-20260518-0314
title: float_32epg_cand
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- activation
kernel_types:
- activation
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

float_32epg_cand

## Solution

The after loop uses a structured if-block that reads raw scores[i] and bias[i] separately, checks is_finite on the raw input, applies sigmoid_accurate inline, and adds bias inline. The fusion eliminates the scores_with_bias intermediate buffer (saving TOTAL_ELEMENTS float global-memory write+read...

## Key Lessons

- The fusion eliminates the scores_with_bias intermediate buffer (saving TOTAL_ELEMENTS float global-memory write+read cycle) and one kernel launch.
- The after loop uses a structured if-block that reads raw scores[i] and bias[i] separately, checks is_finite on the raw input, applies sigmoid_accurate inline, and adds bias inline.
