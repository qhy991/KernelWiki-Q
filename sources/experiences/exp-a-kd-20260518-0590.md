---
id: exp-a-kd-20260518-0590
title: stream_hoist_pipeline
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- pipeline-stages
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
speedup: 2.916x
---

## Challenge

The after code removes 3 redundant cudaStreamCreate/Destroy cycles and 3 redundant cudaGetDeviceProperties + cudaSetDevice() calls from the 3-stage pipeline (stage_pre_compute, stage_launch_gemm, stage_distpatch). In the before code, each stage independently performs cudaGetDeviceProperties for is_h


## Solution

equivalent), and then synchronizes and destroys the stream via cudaStreamSynchronize + cudaStreamDestroy. The after code computes stream and is_h20_device once at the pipeline_after() caller level, passing them as parameters to each stage function. This eliminates the per-stage CUDA driver overhead:

## Results

- **speedup**: 2.916x (improved)


## Key Lessons

- This eliminates the per-stage CUDA driver overhead: cudaStreamCreate/Destroy are driver-level operations that involve memory allocation, context setup, and synchronization (~10-15us per create-destroy pair), and cudaGetDeviceProperties copies a struct from the driver (~5-10us per call).
