---
id: exp-o-kd-20260518-0505
title: small_8experts
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- register-reuse
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 1.731x
---

## Challenge

The same register-local accumulation vs per-element atomicAdd tradeoff applies here with 8 experts and topk_length=32768. Each block still processes topk_length/THREADS_PER_EXPERT=64 iterations, but with only 8 experts the per-expert token density is ~


## Solution

8x higher than the 64-expert case, meaning more matches per thread and thus more atomics in the before version. The speedup (1.7x) is smaller than the 64-expert case because the kernel launch overhead and memory access latency for reading topk_ids beco

## Results

- **speedup**: 1.731x (improved)


## Key Lessons

- Each block still processes topk_length/THREADS_PER_EXPERT=64 iterations, but with only 8 experts the per-expert token density is ~8x higher than the 64-expert case, meaning more matches per thread and thus more atomics in the before version.
- The same register-local accumulation vs per-element atomicAdd tradeoff applies here with 8 experts and topk_length=32768.
