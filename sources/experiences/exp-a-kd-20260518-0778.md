---
id: exp-a-kd-20260518-0778
title: float_aligned_4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- elementwise
- vectorized-loads
kernel_types:
- elementwise
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.064x
---

## Challenge

In the before source, each of 1024 threads does 4 iterations reading 4-byte float values (4096 separate 4-byte global loads). In the after source, vectorize_read_with_alignment<8> uses vec_n_t<float, 8> which is a 32-byte (256-bit) aligned struct. With hidden_size=4096 d


## Solution

ivisible by 8 and cudaMalloc-provided 32-byte alignment, the fast path issues 512 vector loads. Each 256-bit load covers 8 float elements, reducing total memory transactions from 4096 individual 4-byte loads to 512 wider loads. The improvement is moderate (6%) rather tha

## Results

- **speedup**: 1.064x (improved)


## Key Lessons

- The improvement is moderate (6%) rather than dramatic because 4-byte float elements already get better warp-level coalescing than 2-byte __half elements — a warp of 32 threads reading consecutive float addresses naturally coalesces into 128-byte transactions, so scalar float reads are already reason
