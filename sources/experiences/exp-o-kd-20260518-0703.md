---
id: exp-o-kd-20260518-0703
title: 584 bytes (576-byte value + 8-byte scale). The generic stride computation produc
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Added DSv4 page-padded KV cache transfer path using kvcacheio.cuh transfer_item, bypassing generic byte-stride warp transfer for miss-copy operations.
Problem before: The original miss-copy path used a generic byte-stride warp transfer (transfer_item_warp) that computes source an


## Solution

584 bytes (576-byte value + 8-byte scale). The generic stride computation produces incorrect addresses for this layout, and the separate V-copy is unnecessary because MLA combines K and V into a single compressed representation.
Applied change: A new DSv4-specific transfer path is introduced via IsD

## Key Lessons

- The generic stride computation produces incorrect addresses for this layout, and the separate V-copy is unnecessary because MLA combines K and V into a single compressed representation.
