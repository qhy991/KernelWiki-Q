---
id: exp-o-kd-20260518-0228
title: concurrent_overlap
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

concurrent_overlap

## Solution

dispatches work in order and only one kernel occupies the SMs at a time. On A800 with 1024x1024 tiled SGEMM, the concurrent overlap allows both kernels to share the 108 SMs simultaneously, reducing total wall-clock time from the sum of two serial launches to approximatel

## Key Lessons

- The before code runs both on the default stream (stream 0), which serializes them because stream 0 dispatches work in order and only one kernel occupies the SMs at a time.
- The explicit stream assignment also includes cudaSetDevice(0) (mirroring at::cuda::CUDAGuard) to ensure the correct device context, which prevents silent errors on multi-GPU systems where the active device might differ from the intended one.
