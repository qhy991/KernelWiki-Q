---
id: exp-o-kd-20260518-0261
title: half_1m
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 4.588x
---

## Challenge

The before code loads one half (2-byte) element per iteration using narrow scalar __half2float conversions and scalar += accumulation, issuing 4 separate 32-bit load instructions per peer per element. The after code uses __align__(16) array_t<half,8> to load 8 half values (1


## Solution

6 bytes) in a single ld.128 transaction via packed_reduce, then upcasts all 8 values to float simultaneously via the upcast template with #pragma unroll, accumulates them in an array_t<float,8> accumulator with packed_assign_add, and downcasts back in one pass. This 8x wider

## Results

- **speedup**: 4.588x (improved)


## Key Lessons

- The before code loads one half (2-byte) element per iteration using narrow scalar __half2float conversions and scalar += accumulation, issuing 4 separate 32-bit load instructions per peer per element.
