---
id: exp-a-kd-20260518-0105
title: reduction_dispatch
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.662x
---

## Challenge

The before code uses only cooperative reduction with two kernel launches (one per peer) and spin-wait barriers inside each kernel. The after code uses the separate reduction path with three kernel launches: two store kernels (one per peer) and one finalize kernel. The extra


## Solution

kernel launch overhead and the additional cudaDeviceSynchronize() between the store and finalize phases explain the measured regression. In a real Stream-K workload, the separate path allows SK blocks to continue computing other tiles while dedicated reduction blocks final

## Results

- **speedup**: 0.662x (regression)


## Key Lessons

- In a real Stream-K workload, the separate path allows SK blocks to continue computing other tiles while dedicated reduction blocks finalize, improving overall SM utilization.
