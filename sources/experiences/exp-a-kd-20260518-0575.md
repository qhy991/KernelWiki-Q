---
id: exp-a-kd-20260518-0575
title: bf16_m256_topk8
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
- register-reuse
- vectorized-loads
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- vectorized-loads
impl_family: custom_cuda
---

bf16_m256_topk8

## Solution

h computation from other warps. Additionally, the after kernel allocates 8 float accumulator values per thread via vec_t sum_vec (32 bytes of registers), plus 8 float values for val_vec, increasing register pressure compared to the before kernel's single bf16 sum_val (2 bytes). The higher register c

## Key Lessons

- The before kernel uses block_dim=256 threads (capped by min(256, row_stride)), where each thread processes one bf16 element without a stride loop since row_stride=256 exactly matches block_dim.
