---
id: exp-a-kd-20260518-0659
title: softmax_exp16
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

softmax_exp16

## Solution

The after code sets VPT=16 so each thread owns all 16 experts of a row, with THREADS_PER_ROW=1. This eliminates all cross-thread reductions: the thread computes max and sum locally from its 16 owned elements, and the butterfly loop with THREADS_PER_ROW=1 has zero iterations (mask starts at 0). Th...

## Key Lessons

- This eliminates all cross-thread reductions: the thread computes max and sum locally from its 16 owned elements, and the butterfly loop with THREADS_PER_ROW=1 has zero iterations (mask starts at 0).
