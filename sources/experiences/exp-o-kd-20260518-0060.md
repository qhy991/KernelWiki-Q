---
id: exp-o-kd-20260518-0060
title: gemm_m128_n128_k2048_split4
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 2.999x
---

## Challenge

The before code runs a single gemm_serial kernel on the default stream with a 4x4=16-block grid iterating over 64 K-tiles (K=2048/TILE=32). The 16-block grid under-utilizes the A800's 108 SMs, leaving many SMs idle. The after code splits K into 4 slices of K_len=512 (16 K-tiles each), launching 4 ge


## Solution

partial kernels on 4 concurrent CUDA streams. Each partial kernel uses the same 16-block grid but processes only 1/4 of the K iterations, making each partial roughly 4x shorter. Because the grid is small, the 4 concurrent kernels can occupy separate SMs simultaneously, achieving near-ideal parallel 

## Results

- **speedup**: 2.999x (improved)


## Key Lessons

- Because the grid is small, the 4 concurrent kernels can occupy separate SMs simultaneously, achieving near-ideal parallel execution.
