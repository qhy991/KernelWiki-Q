---
id: exp-a-kd-20260518-0658
title: softmax_exp8
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

softmax_exp8

## Solution

The after code packs VPT=8 elements per thread with THREADS_PER_ROW=1, so each thread owns an entire row independently. The butterfly shfl_xor reduction operates within a single-warp subgroup (THREADS_PER_ROW=1 means no cross-thread reduction is needed at all - the thread already has all elements...

## Key Lessons

- The before code uses CUB BlockReduce<float, 256> which requires writing all 256 thread values to shared-memory temp storage, performing a multi-level shared-memory reduction across 8 warps, and two __syncthreads() barriers to distribute max and normalizing_factor.
