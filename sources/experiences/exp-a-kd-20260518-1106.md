---
id: exp-a-kd-20260518-1106
title: dispatch_balanced
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.000x
---

## Challenge

In the balanced case (M=512, N=512, K=1024), K=2*N which is less than 3*N, so the after code's heuristic selects Persist


## Solution

entScheduler — the same scheduler the before code always uses. Both launch identical persistent_gemm_kernel with grid_si

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- In the balanced case (M=512, N=512, K=1024), K=2*N which is less than 3*N, so the after code's heuristic selects PersistentScheduler — the same scheduler the before code always uses.
