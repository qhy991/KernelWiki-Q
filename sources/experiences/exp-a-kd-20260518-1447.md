---
id: exp-a-kd-20260518-1447
title: mask_nullptr
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- register-reuse
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
---

mask_nullptr

## Solution

ted every iteration even when the result is always true, adding a predicate instruction; (2) the kernel function signature now includes two extra parameters (topk_num and token_mask pointer) that increase register pressure and parameter-passing cost; (3) even when the branch is consistently taken, t

## Key Lessons

- The after kernel passes `token_mask` as nullptr, so the ternary `token_mask == nullptr ? 1 : token_mask[i / topk_num]` should resolve to 1 at runtime, making the atomicAdd operand identical to the before version.
