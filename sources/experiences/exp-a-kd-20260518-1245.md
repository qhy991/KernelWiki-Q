---
id: exp-a-kd-20260518-1245
title: seq8k
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

seq8k

## Solution

The after code uses convert_to_uint8 which converts FP32 to FP16 via __float2half_rn, then extracts the high byte with a 16-bit sign-flip. The FP16 approach collapses more float values into the same 256 bins (since FP16 has only 10 mantissa bits vs FP32's 23), but the bin boundaries differ slight...

## Key Lessons

- The FP16 approach collapses more float values into the same 256 bins (since FP16 has only 10 mantissa bits vs FP32's 23), but the bin boundaries differ slightly.
