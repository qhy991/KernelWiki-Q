---
id: exp-a-kd-20260518-0068
title: smem_fpi_2
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 0.470x
---

## Challenge

The before code uses a 33*64=2112-float shared memory buffer (one fragment at a time) with 4 __syncthreads() barriers across 2 fragment iterations. The after code allocates a 66*64=4224-float buffer scaled by FragmentsPerIterat


## Solution

ion=2, processing both fragments in one pass with only 2 __syncthreads(). However, the doubled shared memory allocation reduces per-SM occupancy on A800 (164KB shared memory per SM): the before kernel's ~8.5KB per block allows

## Results

- **speedup**: 0.470x (regression)


## Key Lessons

- However, the doubled shared memory allocation reduces per-SM occupancy on A800 (164KB shared memory per SM): the before kernel's ~8.5KB per block allows many concurrent blocks, while the after's ~17KB per block halves that.
