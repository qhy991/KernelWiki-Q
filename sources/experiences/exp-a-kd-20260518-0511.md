---
id: exp-a-kd-20260518-0511
title: medium_experts_16
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 0.944x
---

## Challenge

The before code uses 1024 threads (CEILDIV(8192, 1024) = 8 tokens per thread) while the after code uses 512 threads (CEILDIV(8192, 512) = 16 tokens per thread). The after code doubles per-thread iteration count in the token-counting loop. While shared memory is reduced (64 bytes vs 1024 bytes) and f


## Solution

zations are eliminated, the counting loop dominates kernel execution time. With only 16 experts, both versions use a single warp for expert slots (32 slots in before, 16 slots in after), so the atomicAdd contention pattern is similar. The moderate 5.95% regression reflects the smaller difference in 

## Results

- **speedup**: 0.944x (regression)


## Key Lessons

- With only 16 experts, both versions use a single warp for expert slots (32 slots in before, 16 slots in after), so the atomicAdd contention pattern is similar.
