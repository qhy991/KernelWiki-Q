---
id: exp-o-kd-20260518-0405
title: experts_192
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- shared-memory-optimization
- register-reuse
- epilogue-fusion
- vectorized-loads
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- register-reuse
- epilogue-fusion
- vectorized-loads
impl_family: custom_cuda
speedup: 2.162x
---

## Challenge

The after code is faster because the fused topkGatingSoftmax kernel eliminates the intermediate softmax workspace write and read that the before path requires. In the before path, moeSoftmax writes 768 KB (1024 tokens x 192 experts x 4 bytes) of softmax probabilities to global memory, and moeTopK re


## Solution

ds all of it in a second kernel launch. This global memory round-trip dominates the latency for this workload size. The after path uses 8-byte vector loads (BYTES_PER_LDG=8, giving 2 floats per ldg and THREADS_PER_ROW=96 which is a power of 2), enabling warp-level butterfly shuffle reductions (__shf

## Results

- **speedup**: 2.162x (improved)


## Key Lessons

- The after code is faster because the fused topkGatingSoftmax kernel eliminates the intermediate softmax workspace write and read that the before path requires.
