---
id: exp-o-kd-20260518-0747
title: Erates more DRAM requests than necessary for a contiguous fill pattern.
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Vectorized padding fill using Vec (int4) instead of scalar per-element writes.
Problem before: The small-batch kernel fills the sorted_token_ids padding array one int32 element at a time in a strided loop. Each store is a separate 4-byte write transaction, which underutilizes the


## Solution

erates more DRAM requests than necessary for a contiguous fill pattern.
Applied change: The main kernel packs 4 int32 values into a single Vec (int4) struct and writes the entire 16-byte vector in one store instruction. The loop iterates over total_vecs = ceil(max_num_tokens_padded / 4), reducing th

## Key Lessons

- Optimization focus: Vectorized padding fill using Vec (int4) instead of scalar per-element writes.
- Each store is a separate 4-byte write transaction, which underutilizes the GPU store unit's 128-byte segment bandwidth and generates more DRAM requests than necessary for a contiguous fill pattern.
