---
id: exp-o-kd-20260518-0633
title: 'Fferences across runs and create contention when multiple warps target the
  same '
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- softmax
kernel_types:
- softmax
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Replace atomicAdd-based s_prime update with warp-partitioned addition_storage and deterministic reduction.
Problem before: In the online softmax, after computing the sum of exp values per row (total_row), the code used atomicAdd to accumulate partial sums into s_prime[accum_m] fr


## Solution

fferences across runs and create contention when multiple warps target the same s_prime entry.
Applied change: Instead of atomicAdd, each warp writes its partial sum to a dedicated slot in addition_storage indexed by (accum_m + kQueriesPerBlock * warp_column_index). After __syncthreads, a single lan

## Key Lessons

- Applied change: Instead of atomicAdd, each warp writes its partial sum to a dedicated slot in addition_storage indexed by (accum_m + kQueriesPerBlock * warp_column_index).
