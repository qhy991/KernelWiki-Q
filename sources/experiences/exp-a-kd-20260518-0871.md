---
id: exp-a-kd-20260518-0871
title: float_2048rows
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.137x
---

## Challenge

The before code copies float elements one at a time in a stride loop (dest_row_ptr[elem] = source_row_ptr[elem]), issuing 4-byte load/store instructions per iteration. The after code reinterprets the float pointers as uint4 (128-bit) pointer


## Solution

s, issuing 16-byte load/store instructions that move 4 floats per memory transaction. This reduces the total number of memory instructions by 4x, cutting instruction overhead and allowing the memory controller to coalesce the wider transacti

## Results

- **speedup**: 1.137x (improved)


## Key Lessons

- The before code copies float elements one at a time in a stride loop (dest_row_ptr[elem] = source_row_ptr[elem]), issuing 4-byte load/store instructions per iteration.
