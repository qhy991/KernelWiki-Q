---
id: exp-a-kd-20260518-0011
title: double_elect
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.999x
---

## Challenge

The before code issues two __ballot_sync + __syncwarp pairs per loop iteration (one for A copy, one for B copy), while the after code issues only one __ballot_sync + __syncwarp pair and performs both writes inside the elected lane. However, this benchmark runs on an A800 (sm_80, Ampere) which does n


## Solution

ardware. The simulated workload uses simple global memory stores (dst_a[idx] = src_a[idx] + 1.0f) rather than actual TMA async copies. Since global stores are not bottlenecked by the warp synchronization pattern in the same way TMA descriptor issues are, the elimination of one __ballot_sync per iter

## Results

- **speedup**: 0.999x (neutral)


## Key Lessons

- The simulated workload uses simple global memory stores (dst_a[idx] = src_a[idx] + 1.0f) rather than actual TMA async copies.
- Since global stores are not bottlenecked by the warp synchronization pattern in the same way TMA descriptor issues are, the elimination of one __ballot_sync per iteration is invisible against the memory-bound store throughput.
