---
id: exp-o-kd-20260518-0745
title: Ly, breaking memory coalescing on GPU. It also requires computing CEILDIV(numel,
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Changed loop traversal from contiguous-block iteration to stride-based iteration in moe_align_block_size_kernel.
Problem before: The original moe_align_block_size_kernel used contiguous-block iteration where each thread processed a sequential chunk of the input array (start_idx =


## Solution

ly, breaking memory coalescing on GPU. It also requires computing CEILDIV(numel, blockDim.x) to determine tokens_per_thread and a compound bounds-check (i < numel && i < start_idx + tokens_per_thread) in the loop condition.
Applied change: Replaced contiguous-block iteration with stride-based iterat

## Key Lessons

- Expected impact: Stride-based iteration improves memory access coalescing for the topk_ids read pattern and simplifies the loop structure by removing division overhead and complex bounds checks.
