---
id: exp-o-kd-20260518-0126
title: f16_K256
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.316x
---

## Challenge

The after kernel uses the half2 vector type to process 2 half elements per thread in a single 32-bit memory transaction via HALF2 reinterpret, instead of two separa


## Solution

te 16-bit scalar loads/stores. Thread count halves from 256 to 128 per row, reducing warp scheduling overhead and barrier synchronization cost. Each thread also acc

## Results

- **speedup**: 1.316x (improved)


## Key Lessons

- The after kernel uses the half2 vector type to process 2 half elements per thread in a single 32-bit memory transaction via HALF2 reinterpret, instead of two separate 16-bit scalar loads/stores.
