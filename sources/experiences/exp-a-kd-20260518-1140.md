---
id: exp-a-kd-20260518-1140
title: vcache_fp16_load
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
---

vcache_fp16_load

## Solution

ift/mask/or instructions per element. This per-element conversion overhead is not amortized by the memory bandwidth savings because: (1) the kernel uses atomicAdd into shared memory for accumulation, creating contention that dominates the critical path; (2) the V-cache access pattern reads one eleme

## Key Lessons

- The after code reads FP8 V-cache elements individually (1 byte each via fp8_to_half software conversion) instead of reading fp16 elements (2 bytes each) directly.
- This per-element conversion overhead is not amortized by the memory bandwidth savings because: (1) the kernel uses atomicAdd into shared memory for accumulation, creating contention that dominates the critical path; (2) the V-cache access pattern reads one element per row per iteration (row * block_
