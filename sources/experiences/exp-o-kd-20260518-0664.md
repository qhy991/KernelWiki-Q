---
id: exp-o-kd-20260518-0664
title: Nsactions into a single 128-bit wide transaction, depending on optimization
  leve
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: f32x4 manual unroll -> f32x4_pack using LDST128BITS: replaces four separate scalar float load/stores with a single 128-bit (16-byte) packed load/store via reinterpret_cast<float4*>.
Problem before: The f32x4 kernel unrolls 4 float load/store operations explicitly as separate scal


## Solution

nsactions into a single 128-bit wide transaction, depending on optimization level and alignment guarantees. This results in potentially 4 separate 4-byte memory instructions instead of 1 combined 16-byte instruction.
Applied change: The f32x4_pack kernel uses the LDST128BITS macro (reinterpret_cast<

## Key Lessons

- This results in potentially 4 separate 4-byte memory instructions instead of 1 combined 16-byte instruction.
