---
id: exp-a-kd-20260518-0026
title: two_kernel_dispatch_H128_D512
experience_type: analysis
source_category: agent-experiment
architectures:
- sm100
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.010x
---

## Challenge

The before code uses two sequential kernel launches: write_partial_lse writes all partial outputs to a large workspace in one pass, then host_reduce reads the workspace and accumulates results. Both kernels have simple, streaming memory access patterns with no contention. The after code combines LSE


## Solution

atomicAdd on global memory for output accumulation (simulating TMA_REDUCE_ADD on sm_100). On sm_80 (A800), each of the 128 active threads per CTA performs 512 atomicAdd operations on the global output tensor, and with split_kv=4 CTAs all contending for the same 65536 output locations, the atomic con

## Results

- **speedup**: 0.010x (regression)


## Key Lessons

- Additionally, the spin-wait barriers between phases add ~100us of latency due to busy-waiting on global memory counters.
- Both kernels have simple, streaming memory access patterns with no contention.
