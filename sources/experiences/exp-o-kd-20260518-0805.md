---
id: exp-o-kd-20260518-0805
title: Ycle over the topk weight output.
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Fused renormalize into topkGatingSoftmax kernel after topk selection loop.
Problem before: After topk selection, the selected k weights did not necessarily sum to 1. A separate renormalize step was required (launched as a separate kernel or done in host code) that read each token


## Solution

ycle over the topk weight output.
Applied change: A renormalize boolean parameter was added to the topkGatingSoftmax kernel. When enabled, the kernel accumulates selected_sum during the topk selection loop (adding each winning weight value to the accumulator). After the loop completes, thread_group_

## Key Lessons

- Expected impact: Eliminates one kernel launch and one global memory read+write cycle over the topk weight output.
- Performance impact should be measurable for large token counts where kernel launch overhead and extra memory traffic are significant.
