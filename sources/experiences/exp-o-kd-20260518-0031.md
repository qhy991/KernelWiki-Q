---
id: exp-o-kd-20260518-0031
title: land_N32
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.165x
---

## Challenge

The before code for N=32 performs 32 per-element bit extractions via operator[] (each computing byte_idx=i/8, bit_idx=i%8, then shift-and-mask), followed by 32 LogicalAnd1b calls


## Solution

The after code replaces this with 4 byte-level reads and zero-checks. While the 8x loop iteration reduction is the same ratio as N=128, the absolute reduction in instructions is smaller (28 fewer iterations), and the kernel launch and memory setup overhead becomes proportionally larger at this sc...

## Results

- **speedup**: 1.165x (improved)


## Key Lessons

- The before code for N=32 performs 32 per-element bit extractions via operator[] (each computing byte_idx=i/8, bit_idx=i%8, then shift-and-mask), followed by 32 LogicalAnd1b calls.
