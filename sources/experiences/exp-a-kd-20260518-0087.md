---
id: exp-a-kd-20260518-0087
title: lor_N128
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
- register-reuse
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 16.676x
---

## Challenge

The before code for N=128 runs 128 iterations, each calling operator[] to extract a single bit (byte-index computation, bit-shift, mask) and then invoking the LogicalOr1b functor. For the OR case, the sequential reduction means the compiler cannot easily short-circuit because each iteration depen...


## Solution

The after code replaces all 128 per-element extractions with 16 byte-level reads and a simple non-zero check per byte (item || bits). The dramatically higher speedup here compared to AND likely stems from the before OR path suffering from more register pressure and dependency chain overhead due t...

## Results

- **speedup**: 16.676x (improved)


## Key Lessons

- For the OR case, the sequential reduction means the compiler cannot easily short-circuit because each iteration depends on the previous one through the accumulator variable.
