---
id: exp-a-kd-20260518-0251
title: flash_attn_split_kv_headdim64_seq256
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
- pipeline-stages
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- pipeline-stages
impl_family: custom_cuda
speedup: 1.006x
---

## Challenge

At N=256 the KV loop has only Tc=4 iterations (256/64=4 tiles). The after code (kStage=2) preloads one K tile before the loop and issues async prefetches for current V and next K inside each iteration, but with only 4 iterations the pipeline cannot amortize its setup overhead


## Solution

. The extra shared memory for the double-buffered K slot (46080 vs 36864 bytes) adds smem pressure that may slightly reduce occupancy. Each iteration's compute time is short relative to the total kernel time, so overlapping the async copy with MMA yields minimal latency hidin

## Results

- **speedup**: 1.006x (neutral)


## Key Lessons

- The before code's synchronous load of current K and V per iteration followed by CP_ASYNC_WAIT_GROUP(1)+syncthreads is nearly equivalent in cost because there is little opportunity to overlap the short compute window with the async transfer.
