---
id: exp-a-kd-20260518-0589
title: stream_hoist_multicall
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- pipeline-stages
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
speedup: 3.999x
---

## Challenge

The after code eliminates 4 redundant cudaStreamCreate/Destroy cycles, 4 redundant cudaGetDeviceProperties calls with string comparisons, and 4 redundant cudaSetDevice() calls per pipeline invocation. In the before code, each call_stage() independently creates and destroys a CUDA stream (expensive r


## Solution

with a string comparison, and calls cudaSetDevice(). The after code hoists all three operations to the pipeline_after() caller: a single stream is created and reused across all 4 stages, a single CUDAGuard equivalent (cudaSetDevice) is applied once, and device properties are queried once. Since cuda

## Results

- **speedup**: 3.999x (improved)


## Key Lessons

- The after code eliminates 4 redundant cudaStreamCreate/Destroy cycles, 4 redundant cudaGetDeviceProperties calls with string comparisons, and 4 redundant cudaSetDevice() calls per pipeline invocation.
- In the before code, each call_stage() independently creates and destroys a CUDA stream (expensive runtime operations involving driver context management), queries device properties with a string comparison, and calls cudaSetDevice().
