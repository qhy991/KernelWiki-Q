---
id: exp-o-kd-20260518-0441
title: sparse_mma_dense
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- register-reuse
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 1.507x
---

## Challenge

The after code replaces two sequential mma.sync.m16n8k16 instructions (each covering k=16) with a single mma.sp.sync.aligned.m16n8k32 instruction that covers the full k=32 range in one tensor core dispatch. The FragM metadata register (Vec<uint,1>) provides the 2:4 sparsity selector tha


## Solution

t tells the hardware which pairs of B-matrix elements participate. This reduces MMA instruction count by half per k-tile, eliminates one fragment-load round per tile, and leverages Ampere's dedicated sparse tensor core datapath which doubles the effective K-dimension throughput per mma.

## Results

- **speedup**: 1.507x (improved)


## Key Lessons

- This reduces MMA instruction count by half per k-tile, eliminates one fragment-load round per tile, and leverages Ampere's dedicated sparse tensor core datapath which doubles the effective K-dimension throughput per mma.sp instruction versus dense mma.sync.
