---
id: exp-a-kd-20260518-1023
title: renorm_topk2_8exp_fallback
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
- epilogue-fusion
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
speedup: 2.328x
---

## Challenge

The after moeTopK kernel accumulates selected_sum in a shared variable during the iterative topk argmax loop (one addition per selected weight by thread 0), then divides all k weights by selected_sum in a short serial loop. Th


## Solution

e before approach writes unnormalized weights to global memory via the topk kernel, then a separate renormalize kernel reads those weights back (8192*2*4=64KB), computes row-wise sums, and writes normalized values back (64KB).

## Results

- **speedup**: 2.328x (improved)


## Key Lessons

- The fused approach eliminates this extra global memory round-trip and one kernel launch.
