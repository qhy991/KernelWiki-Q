---
id: exp-a-kd-20260518-1437
title: half_h64_t16
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
impl_family: custom_cuda
speedup: 1.039x
---

## Challenge

At head_size=64, the before kernel launches 512 blocks (one per token-head pair) with 128 threads each, where threads 0-63 iterate over 64 elements via scalar half loads/stores and threads 64-127 do nothing


## Solution

The after kernel launches 32 blocks with 128 threads each (4096 total threads) using uint4 packed loads of 8 half elements per thread. Both kernels execute in roughly 7-8 microseconds, which is dominated by kernel launch and L2 cache latency rather than memory throughput. The 128-bit vectorizatio...

## Results

- **speedup**: 1.039x (improved)


## Key Lessons

- Both kernels execute in roughly 7-8 microseconds, which is dominated by kernel launch and L2 cache latency rather than memory throughput.
