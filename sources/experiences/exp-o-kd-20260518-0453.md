---
id: exp-o-kd-20260518-0453
title: bf16_8experts
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- softmax
- epilogue-fusion
kernel_types:
- softmax
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.078x
---

## Challenge

The after fused kernel reads BF16 input directly and converts each element to float inline using __bfloat162float within the three softmax loops (max-reduction, exp-sum, output-write). The before approach requires two separate kernel launches:


## Solution

first a conversion kernel that writes an entire intermediate float buffer to global memory (8192*8*4=256KB), then a softmax kernel that reads that intermediate buffer back. The fused approach eliminates the intermediate global memory write an

## Results

- **speedup**: 1.078x (improved)


## Key Lessons

- The fused approach eliminates the intermediate global memory write and read, plus saves one kernel launch.
