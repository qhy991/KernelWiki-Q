---
id: exp-a-kd-20260518-0466
title: vec_vs_scalar_hidden4096_topk8
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.188x
---

## Challenge

The after code uses float4 (128-bit) vectorized loads/stores via cast_load/cast_store, processing 4 floats per memory transaction instead of 1. This reduces the total number of memory transactions by 4x and enables fully coalesce


## Solution

d 128-bit wide accesses. The scalar before code generates individual 32-bit load/store instructions per element, which underutilizes the GPU memory subsystem's 128-byte transaction granularity. For bandwidth-bound kernels like th

## Results

- **speedup**: 1.188x (improved)


## Key Lessons

- The after code uses float4 (128-bit) vectorized loads/stores via cast_load/cast_store, processing 4 floats per memory transaction instead of 1.
