---
id: exp-o-kd-20260518-0668
title: Errors can exceed 1e-3, degrading output quality. Additionally, the conversion
  o
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: FP32 accumulator storage for O rescaling (kOStorageAccFloat32=1) maintains full float precision through all online softmax rescale steps, replacing the FP16 storage path (kOStorageAccFloat32=0) that loses precision at each half-to-float-to-half conversion cycle.
Problem before: W


## Solution

errors can exceed 1e-3, degrading output quality. Additionally, the conversion overhead adds extra arithmetic operations per rescale step.
Applied change: When kOStorageAccFloat32=1, R_D is declared with 4 uint32_t registers per element (float *t_fptr_D_0_1 reinterpretation), storing O values in nat

## Key Lessons

- This repeated half-to-float-to-half conversion cycle loses precision at each rescale step because FP16 provides only ~3-4 decimal places of accuracy.
