---
id: exp-a-kd-20260518-0896
title: weight_preload_hd128
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- register-reuse
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 0.998x
---

## Challenge

The after code preloads q_weight and k_weight into register arrays (q_w[4] and k_w[4]) once before the HEADS_PER_WARP=4 head loop, while the before code reads q_weight[dim] or k_weight[dim] from global memory in each loop iteration. In isolation, this produces no measurable speedup because: (1) the 


## Solution

IM=128 floats = 512 bytes per weight vector), and after the first global read they reside in L1 cache for subsequent accesses within the same warp, making re-reading from cache nearly as fast as reading from registers; (2) HEADS_PER_WARP=4 means only 4 redundant reads are eliminated per thread, and 

## Results

- **speedup**: 0.998x (neutral)


## Key Lessons

- In isolation, this produces no measurable speedup because: (1) the weight arrays are tiny (HEAD_DIM=128 floats = 512 bytes per weight vector), and after the first global read they reside in L1 cache for subsequent accesses within the same warp, making re-reading from cache nearly as fast as reading 
