---
id: exp-a-kd-20260518-0186
title: gemm_like
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- register-reuse
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
---

gemm_like

## Solution

alizes the shuffle before the FMA loop. The after variant computes the warp index with a single integer division, which the compiler can fold into a register reuse since the value does not change across iterations. This removes the per-iteration convergence point and the SHFL instruction ove

## Key Lessons

- The after code uses canonical_warp_idx() (a plain threadIdx.x/32 division) instead of __shfl_sync inside the iteration loop of a GEMM-like kernel.
