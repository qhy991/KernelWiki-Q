---
id: exp-a-kd-20260519-1332-2
title: Preload weight values into thread-private registers once before the multi-head
  l
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- rotary-embedding
- register-reuse
kernel_types:
- rotary-embedding
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: experimental
reproducibility: snippet
techniques:
- register-reuse
impl_family: custom_cuda
---

## Challenge

Weight values (q_weight, k_weight) are loaded from global memory on every iteration of the per-head loop, redundantly re-reading the same weight tensor across multiple heads processed by the same warp


## Solution

Preload weight values into thread-private registers once before the multi-head loop, then reuse them across all heads, eliminating redundant global memory accesses for the weight tensors

## Key Lessons

- Preload small, repeatedly-used weight tensors into registers before a multi-iteration loop to eliminate redundant global memory traffic — applies whenever the same weights are reused across loop iterations and fit in available registers (e.g., 4 floats per thread for HEAD_DIM=128)
