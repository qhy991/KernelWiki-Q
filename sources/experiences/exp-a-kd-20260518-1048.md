---
id: exp-a-kd-20260518-1048
title: float_aligned_256
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 0.969x
---

## Challenge

With hidden_size=256 and a single block, the scalar before kernel already has fully coalesced memory access because 256 threads each read one consecutive float element at stride=1


## Solution

The after kernel's vectorize_read_with_alignment<16> loads 128-byte vectors but then unrolls them through DefaultReadVecOp calling the scalar lambda per element, adding template indirection without changing the effective per-thread work. At this tiny workload scale (~5-6 us per launch), kernel la...

## Results

- **speedup**: 0.969x (regression)


## Key Lessons

- With hidden_size=256 and a single block, the scalar before kernel already has fully coalesced memory access because 256 threads each read one consecutive float element at stride=1.
