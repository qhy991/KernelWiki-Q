---
id: exp-a-kd-20260518-1170
title: mqa_8q_1kv
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.001x
---

## Challenge

The before V-cache kernel uses an 8-head MHA layout (block_stride=8192) with all 8 KV slots containing identical V data. The after kernel uses a 1-head MQA layout (block_stride=1024) with head_mapping={0,...,0}. Both read HEAD_SIZE*BLOCK_SIZE=1024 half-values per block for each query


## Solution

head's weighted sum computation. The ~0.12% latency difference is within measurement noise because the dominant cost is the inner loop over 128 blocks * 16 tokens per block accumulating logit*v products, which has identical workload regardless of stride value. The stride arithmetic

## Results

- **speedup**: 1.001x (neutral)


## Key Lessons

- The ~0.12% latency difference is within measurement noise because the dominant cost is the inner loop over 128 blocks * 16 tokens per block accumulating logit*v products, which has identical workload regardless of stride value.
- The stride arithmetic (multiplying by 8192 vs 1024) compiles to the same IMAD instruction pattern on GPU.
