---
id: exp-a-kd-20260518-0372
title: large_batch_bf16
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
---

large_batch_bf16

## Solution

The after code assigns 1 warp (32 threads) per token with stride=kWarpSize=32, requiring each thread to loop 8 times (8 vector iterations). While the total memory traffic is identical (256 vector loads per token), the after code generates at most 32 concurrent wide-load requests per token at any ...

## Key Lessons

- In the before code, 256 threads (8 warps) process one token with stride=block_dim=256, so each thread executes only 1 vector iteration covering 16 bf16 values via a single uint4-pair load.
