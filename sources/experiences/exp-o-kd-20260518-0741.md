---
id: exp-o-kd-20260518-0741
title: Der load instructions (16-byte vector loads) that fetch multiple elements in
  one
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Vectorized the max-finding loop from scalar element-by-element loads to flashinfer::vec_t vectorized loads (vec_size elements per transaction).
Problem before: The max-finding loop in per_token_quant_fp8_kernel used scalar element-by-element loads: each thread loaded one value fr


## Solution

der load instructions (16-byte vector loads) that fetch multiple elements in one transaction.
Applied change: The max-finding loop was rewritten to use flashinfer::vec_t vectorized loads with cast_load, which performs a 16-byte aligned load fetching vec_size elements (4 for fp16/bf16, 2 for fp32) in

## Key Lessons

- This underutilized available memory bandwidth since modern GPUs can service wider load instructions (16-byte vector loads) that fetch multiple elements in one transaction.
