---
id: exp-o-kd-20260518-0842
title: Red two full reduction traversals over shared memory and an explicit barrier
  bet
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: In dynamic_scaled_int8_azp_quant_kernel, merge two sequential BlockReduce<float,1024> calls (one for max, one for min with a syncthreads between) into a single BlockReduce<MinMax,256> that reduces both min and max simultaneously.
Problem before: dynamic_scaled_int8_azp_quant_kern


## Solution

red two full reduction traversals over shared memory and an explicit barrier between them. The sequential structure doubled the reduction latency and wasted shared memory bandwidth.
Applied change: Introduced a MinMax struct that carries both min and max in a single object. The per-thread scan accum

## Key Lessons

- Expected impact: The single-pass MinMax reduction eliminates one full block reduction and one __syncthreads() barrier, reducing synchronization overhead and shared memory traffic.
