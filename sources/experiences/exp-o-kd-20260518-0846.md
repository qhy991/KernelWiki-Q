---
id: exp-o-kd-20260518-0846
title: Ave partially-computed tokens at the start (from a prior incomplete block),
  so t
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Replace uniform kChunkSize-based chunk iteration with variable-length chunk iteration driven by pre-computed cu_chunk_seqlen/last_chunk_indices metadata.
Problem before: The kernel iterated over chunks using a compile-time-constant kChunkSize = kNThreads * kNItems and computed pe


## Solution

ave partially-computed tokens at the start (from a prior incomplete block), so the first chunk can be smaller than block_size, and subsequent chunks must be block_size-aligned to ensure the SSM state can be saved/restored at each block boundary.
Applied change: Replace the uniform chunking scheme wi

## Key Lessons

- The kernel reads the per-chunk token count from cu_chunk_seqlen and uses a tokens_processed accumulator for pointer arithmetic, instead of multiplying chunk index by a constant.
- In prefix caching (APC mode), a prefill sequence may have partially-computed tokens at the start (from a prior incomplete block), so the first chunk can be smaller than block_size, and subsequent chunks must be block_size-aligned to ensure the SSM state can be saved/restored at each block boundary.
