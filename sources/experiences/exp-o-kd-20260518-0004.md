---
id: exp-o-kd-20260518-0004
title: smem_scatter
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- vectorized-loads
impl_family: custom_cuda
speedup: 1.160x
---

## Challenge

In the before code (smem_scatter_before.cu), each thread performs 16 separate byte-level shared memory loads from scattered addresses in the K-major layout, indexed by (row_offset * packed_k + packed_idx) where packed_idx varies non-contiguously across 4 groups. Each load reads only 1 byte contai...


## Solution

the after code (smem_scatter_after.cu), the host-side reorder_data function permutes the INT4 elements so that all 16 values owned by one thread occupy 8 contiguous bytes in shared memory (at offset thread_idx_in_row * 8 within the row). The kernel then loads these 8 bytes from a contiguous smem ...

## Results

- **speedup**: 1.160x (improved)


## Key Lessons

- This contiguous access pattern allows the GPU to coalesce the 8 byte reads into wider shared memory transactions (64-bit), reducing the number of separate smem bank accesses from 16 down to 1 wide load.
