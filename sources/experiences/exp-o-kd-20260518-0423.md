---
id: exp-o-kd-20260518-0423
title: large_contiguous
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 3.577x
---

## Challenge

The before kernel uses a grid-stride scalar loop where each thread processes one bf16 element per iteration, requiring three integer division/modulo operations per element to decompose the flat index into (token, head, dim)


## Solution

The after kernel assigns one warp to each (token, head) pair, computing the base pointer once per warp and then using two 128-bit cache-streaming (ld.global.cs.v4.u32 / st.global.cs.v4.u32) vector load/store iterations to copy the 512-element nope portion. Each of 32 lanes handles one int4 (8 bf1...

## Results

- **speedup**: 3.577x (improved)


## Key Lessons

- The .cs cache-streaming hint bypasses L2 cache allocation for write-once data, reducing cache pollution.
