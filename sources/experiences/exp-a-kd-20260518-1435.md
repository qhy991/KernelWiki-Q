---
id: exp-a-kd-20260518-1435
title: fp8_dynamic_quant_large
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- register-reuse
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 0.982x
---

## Challenge

The before kernel receives min_scaling_factor as a float kernel parameter and uses it in the token_scale max() comparison. The after kernel eliminates this parameter and instead calls min_scaling_factor<FP8E4M3FN>::val(), which expands to 1.0f / (448.0f


## Solution

* 512.0f) via a __forceinline__ device function with constexpr inputs. NVCC constant-folds this to the same precomputed float value (~4.37e-6), producing essentially identical register-level code for the max() operation. With hidden_size=4096, each kerne

## Results

- **speedup**: 0.982x (neutral)


## Key Lessons

- The after kernel eliminates this parameter and instead calls min_scaling_factor<FP8E4M3FN>::val(), which expands to 1.0f / (448.0f * 512.0f) via a __forceinline__ device function with constexpr inputs.
- NVCC constant-folds this to the same precomputed float value (~4.37e-6), producing essentially identical register-level code for the max() operation.
