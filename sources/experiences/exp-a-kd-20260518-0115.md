---
id: exp-a-kd-20260518-0115
title: many_atomic
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.989x
---

## Challenge

With 32 rows and 8 warps, the before code performs up to 32 atomicMaxFloat calls that each include a multiply by scaling. The atomicCAS loop is expensive because contention causes retries. In


## Solution

the after code, the pre-scaling eliminates the multiply from the atomicCAS path, reducing the instruction count inside the critical section. While the absolute time saved per atomicMax is small (one FMUL), the cumulative effect across 32 rows and potential CAS retries becomes measurable. The main...

## Results

- **speedup**: 0.989x (neutral)


## Key Lessons

- The atomicCAS loop is expensive because contention causes retries.
- In the after code, the pre-scaling eliminates the multiply from the atomicCAS path, reducing the instruction count inside the critical section.
