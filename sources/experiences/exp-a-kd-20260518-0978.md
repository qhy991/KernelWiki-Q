---
id: exp-a-kd-20260518-0978
title: sparse_mma
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- pipeline-stages
- register-reuse
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- register-reuse
impl_family: custom_cuda
speedup: 1.017x
---

## Challenge

The before code uses four dense mma.sync.aligned.m16n8k16 instructions (two k-slices x two output columns), while the after code uses four mma.sp.sync.aligned.m16n8k32 instructions (psel=0/1 x two output columns). At this micro-s


## Solution

cale with a single warp and tiny 16x8x32 GEMM, both kernels issue roughly the same number of tensor core instructions, and the sparse variant has additional metadata register pressure (frag_m load). The 2:4 sparsity throughput ad

## Results

- **speedup**: 1.017x (neutral)


## Key Lessons

- The before code uses four dense mma.sync.aligned.m16n8k16 instructions (two k-slices x two output columns), while the after code uses four mma.sp.sync.aligned.m16n8k32 instructions (psel=0/1 x two output columns).
