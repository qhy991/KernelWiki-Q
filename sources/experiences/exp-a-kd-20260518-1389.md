---
id: exp-a-kd-20260518-1389
title: smem_w8a8
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
- fine-grained-quantization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- fine-grained-quantization
impl_family: custom_cuda
speedup: 1.656x
---

## Challenge

The W8A8 config uses is_a_8bit=true, which reduces sh_a_size by half (1 byte per element vs 2 for FP16), but the B matrix buffer (sh_b_size) grows because pack_factor=4 instead of 8 for 4-bit. In the before code with pipe_stages=4, the combined stages-dependent buffers are even larger, totaling 6...


## Solution

The after code with stages=2 reduces sh_a_size, sh_b_size, sh_s_size, and sh_g_idx_size each by 2x, bringing the total down to 39040 bytes. This fits comfortably within Turing's shared memory, enabling kernel launch. The slightly lower speedup ratio (~1.66x vs ~1.78x) is because the non-stages-de...

## Results

- **speedup**: 1.656x (improved)


## Key Lessons

- The W8A8 config uses is_a_8bit=true, which reduces sh_a_size by half (1 byte per element vs 2 for FP16), but the B matrix buffer (sh_b_size) grows because pack_factor=4 instead of 8 for 4-bit.
