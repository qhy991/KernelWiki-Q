---
id: exp-o-kd-20260518-0361
title: fp8_fusion_256
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- epilogue-fusion
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.418x
---

## Challenge

Same fusion pattern as fp8_fusion_128 but with HEAD_SIZE=256, NUM_THREADS=256, and 8 partitions. The before code's intermediate float buffer is larger (~32KB of float data written to and read from global memory), making the memory round-trip cost proportionally greater


## Solution

The after code's fused kernel avoids this intermediate buffer entirely, applying the FP8 out_scale inline (acc *= out_scale where out_scale = 1.0f / *fp8_out_scale_ptr) and writing uint8_t FP8 output directly. The ~1.42x speedup is consistent across both head sizes, confirming that the dominant s...

## Results

- **speedup**: 1.418x (improved)


## Key Lessons

- The after code's fused kernel avoids this intermediate buffer entirely, applying the FP8 out_scale inline (acc *= out_scale where out_scale = 1.0f / *fp8_out_scale_ptr) and writing uint8_t FP8 output directly.
- Same fusion pattern as fp8_fusion_128 but with HEAD_SIZE=256, NUM_THREADS=256, and 8 partitions.
