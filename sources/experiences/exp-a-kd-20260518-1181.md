---
id: exp-a-kd-20260518-1181
title: same_scale_8h128d
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.007x
---

## Challenge

In the same_scale_8h128d case, k_scale equals v_scale equals the original kv_scale. Both the before and after CUDA kernels execute identical arithmetic: roundf(val/scale), clamp, then multiply by scale for dequantization. The only code-level difference is that


## Solution

the after kernel receives two separate float parameters (k_scale, v_scale) instead of one (kv_scale), which does not change the instruction count, memory access pattern, or occupancy. The measured speedup is within noise margin, confirming this is a structurally neutral change when both scales ar...

## Results

- **speedup**: 1.007x (neutral)


## Key Lessons

- The only code-level difference is that the after kernel receives two separate float parameters (k_scale, v_scale) instead of one (kv_scale), which does not change the instruction count, memory access pattern, or occupancy.
