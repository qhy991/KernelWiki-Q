---
id: exp-a-kd-20260518-1146
title: half_output
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.999x
---

## Challenge

The before code uses explicit __float2half() to convert the float dequant result to half before storing to the dst_t* pointer. The after code removes __float2half() and relies on CUDA's im


## Solution

plicit float→half conversion. On SM80+ with nvcc -O3, both paths generate equivalent PTX: the explicit __float2half() and the implicit conversion both produce a F2F.F64.F32 or equivalent h

## Results

- **speedup**: 0.999x (neutral)


## Key Lessons

- Since the computation is dominated by the float arithmetic (multiplication, sign selection, table lookup), the conversion method makes no measurable difference.
