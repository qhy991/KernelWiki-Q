---
id: exp-o-kd-20260518-0303
title: fp16_topk4_r4096
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- vectorized-loads
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
---

fp16_topk4_r4096

## Solution

1.645x) because with topk=4, each element's accumulation requires 4 separate source-row lookups and loads, so the topk inner loop dominates execution time. The vectorized approach still benefits because each topk iteration now uses 4 half2 vector loads (covering 8 elements) instead of 8 individua

## Key Lessons

- The speedup is lower (1.346x vs 1.645x) because with topk=4, each element's accumulation requires 4 separate source-row lookups and loads, so the topk inner loop dominates execution time.
- The after kernel applies the same vectorization mechanism as the topk=2 case: half2 loads reduce memory transactions by 2x per topk iteration, __half22float2 halves conversion instructions, and #pragma unroll on the inner accumulation loop removes branch overhead.
