---
id: exp-a-kd-20260518-0457
title: smem_occupancy
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
- register-reuse
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- register-reuse
impl_family: custom_cuda
speedup: 3.117x
---

## Challenge

The after code reduces the per-block dynamic shared memory allocation from 128KB (kSmem_before = 32*1024*sizeof(uint32_t)) to 32KB (kSmem_after = 8*1024*sizeof(uint32_t)). On an A800 GPU (sm_80, 164KB shared memory per SM), the 128KB configuration allows only 1 block per SM because each block consum


## Solution

t, leaving 108 SMs each running just 1 block of 512 threads — severe under-utilization. The 32KB configuration permits 3-4 blocks per SM (164KB / 32KB = ~5, limited by registers/threads), dramatically increasing occupancy from ~512 to ~1536-2048 concurrent threads per SM. Higher occupancy means more

## Results

- **speedup**: 3.117x (improved)


## Key Lessons

- On an A800 GPU (sm_80, 164KB shared memory per SM), the 128KB configuration allows only 1 block per SM because each block consumes nearly the entire SM shared memory budget, leaving 108 SMs each running just 1 block of 512 threads — severe under-utilization.
