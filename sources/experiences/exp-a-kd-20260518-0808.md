---
id: exp-a-kd-20260518-0808
title: half_hidden_4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.147x
---

## Challenge

The before code loads one 2-byte half value per scalar iteration across 4 loop iterations per thread (4096/1024=4). Each scalar 2-byte load is narrow and wastes cache-line bandwidth because 16 half values fit in a 32-byte segment but the scalar loop accesses them one at a time across separate ite...


## Solution

The after code reinterprets the input as vec4_t<half> (8 bytes, __align__(8)) and loads 4 half values per single vec4_t transaction. With num_vec_elems=1024 and 1024 active threads, every thread processes exactly one vec4_t iteration, achieving full thread utilization. Consecutive threads loading...

## Results

- **speedup**: 1.147x (improved)


## Key Lessons

- Each scalar 2-byte load is narrow and wastes cache-line bandwidth because 16 half values fit in a 32-byte segment but the scalar loop accesses them one at a time across separate iterations.
