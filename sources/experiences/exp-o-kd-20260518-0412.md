---
id: exp-o-kd-20260518-0412
title: strided_half8
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
- fine-grained-quantization
- epilogue-fusion
- vectorized-loads
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
- epilogue-fusion
- vectorized-loads
impl_family: custom_cuda
speedup: 1.423x
---

## Challenge

The before code must launch a separate copy_strided_half_to_contiguous_kernel (simulating PyTorch .contiguous()) before the vectorized fused_add_rms_norm kernel can process the data. This copy kernel reads 128 half-precision rows from stride-512 offsets and writes them contiguously, adding an ent...


## Solution

The after code eliminates this copy by computing vec_input_stride = input_stride/width = 64 and using strided_id = blockIdx.x * 64 + idx to load/store HalfVec8 vectors directly from the strided buffer. The 128-bit vector loads at stride-512 offsets still hit contiguous cache-line segments within ...

## Results

- **speedup**: 1.423x (improved)


## Key Lessons

- The after code eliminates this copy by computing vec_input_stride = input_stride/width = 64 and using strided_id = blockIdx.x * 64 + idx to load/store HalfVec8 vectors directly from the strided buffer.
