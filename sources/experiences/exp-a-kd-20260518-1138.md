---
id: exp-a-kd-20260518-1138
title: kcache_fp16_load
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
impl_family: custom_cuda
speedup: 1.090x
---

## Challenge

The after code reads the K-cache as FP8-E5M2 (1 byte/element) and dequantizes via a software bit-manipulation conversion (extracting sign/exponent/mantissa fields and reconstructing the fp16 bit pattern), while the before code reads fp16 directly (2 bytes/element). The softwa


## Solution

re conversion avoids hardware-intrinsic emulation overhead by performing the conversion through integer arithmetic, which compiles to a compact sequence of shift/mask/or operations. The 2x reduction in memory traffic (reading 1 byte vs 2 bytes per K element across 64 blocks x

## Results

- **speedup**: 1.090x (improved)


## Key Lessons

- The software conversion avoids hardware-intrinsic emulation overhead by performing the conversion through integer arithmetic, which compiles to a compact sequence of shift/mask/or operations.
- The after code reads the K-cache as FP8-E5M2 (1 byte/element) and dequantizes via a software bit-manipulation conversion (extracting sign/exponent/mantissa fields and reconstructing the fp16 bit pattern), while the before code reads fp16 directly (2 bytes/element).
