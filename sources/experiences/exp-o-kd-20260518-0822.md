---
id: exp-o-kd-20260518-0822
title: Non-contiguous tensor or force an expensive host-side torch.contiguous() copy
  th
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Replace hardcoded query offset calculation with stride-based access to handle non-contiguous query tensors.
Problem before: The kernel assumed the query tensor was contiguous in memory, computing the per-sequence offset as seq_idx * num_heads * HEAD_SIZE. In practice, the query i


## Solution

non-contiguous tensor or force an expensive host-side torch.contiguous() copy that moves the entire query tensor to a new memory region before launching the kernel.
Applied change: Added a q_stride kernel parameter set to query.stride(0) from the host launcher. The kernel pointer calculation now use

## Key Lessons

- Expected impact: Eliminates an unnecessary host-side data movement (a full query tensor copy) when the query tensor is non-contiguous.
