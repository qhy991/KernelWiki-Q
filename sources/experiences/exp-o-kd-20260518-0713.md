---
id: exp-o-kd-20260518-0713
title: Between the alignment and padding operations). For MoE models with frequent
  expe
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- rotary-embedding
kernel_types:
- rotary-embedding
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Fused sorted_token_ids padding into moe_align_block_size_kernel and moe_align_block_size_small_batch_expert_kernel via a conditional pad_sorted_token_ids flag.
Problem before: The sorted_token_ids padding (filling unused slots with the sentinel value numel) was performed as a sep


## Solution

between the alignment and padding operations). For MoE models with frequent expert routing, this extra launch adds measurable end-to-end latency.
Applied change: The padding logic is fused directly into both moe_align_block_size_kernel (large-batch variant) and moe_align_block_size_small_batch_exper

## Key Lessons

- In the small-batch kernel, the fill is inserted before the final token sorting loop, with a __syncthreads() barrier to ensure all threads complete the fill before any thread begins sorting — this ordering is critical because the fill overwrites the entire array first, then the sorting loop writes ac
- In the small-batch kernel, the fill is inserted before the final token sorting loop, with a __syncthreads() barrier to ensure all threads complete the fill before any thread begins sorting — this ordering is critical because the fill overwrites the entire array first, then the sorting loop writes ac
