---
id: exp-a-kd-20260518-1493
title: lop3_dequant_int4
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: inline_asm
speedup: 0.901x
---

## Challenge

In this isolated microbenchmark, the LOP3-based after code is slower than the naive AND/OR before code because the standalone kernel allows the compiler to aggressively optimize the simple AND+OR sequence, while the inline PTX asm volatile for lop3.b32 adds template instantiation and volatile-asm ov


## Solution

e compiler from applying its own optimizations around it. The real benefit of lop3 in the Marlin kernel comes from the inner matmul loop context: dequant is invoked per sub-tile alongside tensor core MMA operations, and every saved ALU instruction frees an issue slot that allows better overlap of de

## Results

- **speedup**: 0.901x (regression)


## Key Lessons

- In this isolated microbenchmark, the LOP3-based after code is slower than the naive AND/OR before code because the standalone kernel allows the compiler to aggressively optimize the simple AND+OR sequence, while the inline PTX asm volatile for lop3.b32 adds template instantiation and volatile-asm ov
- The real benefit of lop3 in the Marlin kernel comes from the inner matmul loop context: dequant is invoked per sub-tile alongside tensor core MMA operations, and every saved ALU instruction frees an issue slot that allows better overlap of dequantization with tensor core computation.
