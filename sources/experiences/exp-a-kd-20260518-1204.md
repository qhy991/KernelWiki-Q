---
id: exp-a-kd-20260518-1204
title: ld128_bf16
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- register-reuse
- fine-grained-quantization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- fine-grained-quantization
- vectorized-loads
impl_family: inline_asm
speedup: 1.000x
---

## Challenge

Both implementations generate the same PTX instruction sequence for the 128-bit vectorized path: a ld.global.nc.v4.u32 load, four packed element computations (SiLU conversion followed by __hmul2 multiplication), and a st.global.v4.u32 store. The ld128 templa


## Solution

te wrapper internally performs *reinterpret_cast<int4*>(&val) = __ldg(reinterpret_cast<const int4*>(ptr)), which is semantically identical to the before pattern's direct __ldg on int4* combined with reinterpret_cast<packed_t*> for element access. Since both

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- Since both patterns operate on local variables with no external aliasing, the compiler generates identical register allocation and instruction scheduling.
- The ld128 template wrapper internally performs *reinterpret_cast<int4*>(&val) = __ldg(reinterpret_cast<const int4*>(ptr)), which is semantically identical to the before pattern's direct __ldg on int4* combined with reinterpret_cast<packed_t*> for element access.
