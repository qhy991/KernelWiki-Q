---
id: exp-a-kd-20260518-1405
title: padded_fp32_8h_64dim
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- rotary-embedding
kernel_types:
- rotary-embedding
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

padded_fp32_8h_64dim

## Solution

The after code uses a configurable head_stride parameter and runs a single kernel call directly on the padded data, entirely eliminating the contiguous copy overhead. The copy kernels add 4 extra kernel launches and 2x the memory traffic for query/key data (read + write for each copy), making the...

## Key Lessons

- The before code must perform four extra device-to-device copy kernels to rearrange padded tensors into contiguous layout before running the rotary kernel, then copy results back — two copy_padded_to_contiguous kernels and two copy_contiguous_to_padded kernels, each touching every element of the quer
