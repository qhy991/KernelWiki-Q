---
id: exp-a-kd-20260518-1358
title: fp8_to_float
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- epilogue-fusion
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.702x
---

## Challenge

The before code requires two kernel launches: fp8_to_float_unscaled_kernel converts each fp8 element to float via an intermediate half (fp8->half->float) and writes the unscaled float result to a temporary 4N-byte d_float_tmp buffer; then scale_float_kernel reads that buffer and applies a per-ele...


## Solution

The after code fuses both operations into a single kernel fp8_to_float_scaled_kernel, where scaled_vec_conversion_fp8_to_float performs the fp8->half->float conversion chain and immediately multiplies by scale before writing the final float. This eliminates: (1) one full kernel launch overhead, (...

## Results

- **speedup**: 1.702x (improved)


## Key Lessons

- This eliminates: (1) one full kernel launch overhead, (2) the intermediate global memory write of 4N bytes (float array) and subsequent read, and (3) the allocation of the temporary buffer.
- The before code requires two kernel launches: fp8_to_float_unscaled_kernel converts each fp8 element to float via an intermediate half (fp8->half->float) and writes the unscaled float result to a temporary 4N-byte d_float_tmp buffer; then scale_float_kernel reads that buffer and applies a per-elemen
