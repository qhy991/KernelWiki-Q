---
id: exp-a-kd-20260518-0138
title: shape_16x8x8
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- elementwise
kernel_types:
- elementwise
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

shape_16x8x8

## Solution

the after code computes the same three indices (access_m_idx, inner_idx, inst_m_idx) directly from ldsm_vec_num using integer division and modulo. Since all loop bounds and lane_id values are compile-time constants, nvcc can constant-fold and dead-code-eliminate the before version's loop into the...

## Key Lessons

- Since all loop bounds and lane_id values are compile-time constants, nvcc can constant-fold and dead-code-eliminate the before version's loop into the same direct computation.
- The key structural difference is in the constructor: the before code computes each thread's origin offset via a triple-nested unrolled loop with a conditional branch (if access_idx == ldsm_vec_num), while the after code computes the same three indices (access_m_idx, inner_idx, inst_m_idx) directly f
