---
id: exp-o-kd-20260518-0175
title: pow2_64ep_4topk
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
- register-reuse
- epilogue-fusion
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.353x
---

## Challenge

Same fusion mechanism but with 64 experts and topk=4. The warp layout changes: THREADS_PER_ROW=8, ROWS_PER_WARP=4. The top-K selection loop runs 4 iterations with butterfly shfl_xor argmax reduction across 8 threads per row. The renormalize accumulation (row_sum_for_renormalize += max


## Solution

_val) only happens on thread_group_idx==0 threads (1 per row), and the post-loop division reads 4 output slots per row. The before version's separate renormalize_kernel must launch another grid, read all 2048*4 weight values from global memory, compute the sum per row, and write the n

## Results

- **speedup**: 1.353x (improved)


## Key Lessons

- The fused after kernel avoids this by keeping the sum in a register and doing the division immediately after the top-K loop, re-reading only 4 values per row from its own prior writes.
