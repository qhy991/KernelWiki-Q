---
id: exp-a-kd-20260518-1318
title: dispatch_m8
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- shared-memory-optimization
kernel_types:
- gemm
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: cutlass
speedup: 1.002x
---

## Challenge

For M=8, the before code routes to the M32 tile path with TILE_M=32 (1 block), while the after code routes to the M16 path with TILE_M=16 (1 block). Both kernels share the same scalar K-loop per output element, so the r


## Solution

ow-loop bound difference (32 vs 16 with early exit at row>=8) does not change the arithmetic intensity. The per-element compute cost dominates over the minor difference in how many loop iterations are skipped by the row

## Results

- **speedup**: 1.002x (neutral)


## Key Lessons

- In production Cutlass GEMM, the smaller M16 TileShape reduces shared memory allocation per tile and avoids prefetching rows 9-32 that are never used, which would yield measurable savings for small-batch decode.
