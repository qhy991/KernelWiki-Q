---
id: exp-a-kd-20260518-0789
title: small_kernel_sync
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- elementwise
- pipeline-stages
kernel_types:
- elementwise
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
---

small_kernel_sync

## Solution

The after source (small_kernel_sync_after.cu) eliminates this by keeping the scale on device and passing a device pointer to scale_with_device_ptr, which reads the scalar via *d_scale_ptr inside the kernel (line 20). Both kernels are enqueued back-to-back on the same stream with zero host-side bl...

## Key Lessons

- The after source (small_kernel_sync_after.cu) eliminates this by keeping the scale on device and passing a device pointer to scale_with_device_ptr, which reads the scalar via *d_scale_ptr inside the kernel (line 20).
