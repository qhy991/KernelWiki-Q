---
id: exp-a-kd-20260518-1380
title: pipeline_4stage
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
- pipeline-stages
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- pipeline-stages
impl_family: custom_cuda
speedup: 0.413x
---

## Challenge

The before kernel uses PTX cp.async.cg.shared.global to issue asynchronous global-to-shared memory copies that the hardware executes without blocking the issuing thread. A 4-stage pipeline allows copies for tiles N and N+1 to remain in flight via cp_async_fence (commit_group) while the thread com...


## Solution

The after kernel uses reinterpret_cast<int4> synchronous loads that block each thread until the 16-byte global read completes, forcing a strictly serial copy-then-compute ordering per tile. Each tile iteration requires two __syncthreads() barriers: one after the copy phase to make shared data vis...

## Results

- **speedup**: 0.413x (regression)


## Key Lessons

- A 4-stage pipeline allows copies for tiles N and N+1 to remain in flight via cp_async_fence (commit_group) while the thread computes on tile N-2 data after cp_async_wait.
