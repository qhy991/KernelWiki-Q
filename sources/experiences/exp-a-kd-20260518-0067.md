---
id: exp-a-kd-20260518-0067
title: conv3d_fprop_small
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- register-reuse
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 1.282x
---

## Challenge

The after code replaces IteratorAlgorithm::kAnalytic with IteratorAlgorithm::kOptimized in the DefaultConv3dFprop kernel type. The kAnalytic path computes per-tile filter offset positions using arithmetic expressions evaluated inside the inner GEMM loop, requiring integer division and modulo operati


## Solution

of the three spatial dimensions (D, H, W) on every tile iteration. The kOptimized path precomputes a delta table for all spatial dimensions before the main loop begins, storing the offsets in a lookup table that the tile iterator reads via direct indexed access. This eliminates repeated integer arit

## Results

- **speedup**: 1.282x (improved)


## Key Lessons

- This eliminates repeated integer arithmetic from the hot loop, reduces register pressure from fewer live variables, and enables more predictable memory access patterns.
