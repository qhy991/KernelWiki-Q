---
id: exp-a-kd-20260518-1066
title: fused_large_batch
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.003x
---

## Challenge

The before code uses a runtime conditional (if num_tokens < 256) to select between blockReduceSum<1024> and blockReduceSum<256>. The after code replaces this with a single cub::BlockReduce<float, 1024> call using blockDim.x


## Solution

as the valid-count. Both approaches use warp-shuffle primitives for the core reduction. The conditional branch is evaluated at the same point for all threads in a block, so there is no intra-block warp divergence. The CUB

## Results

- **speedup**: 1.003x (neutral)


## Key Lessons

- The before code uses a runtime conditional (if num_tokens < 256) to select between blockReduceSum<1024> and blockReduceSum<256>.
