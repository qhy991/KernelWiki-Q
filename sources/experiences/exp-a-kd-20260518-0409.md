---
id: exp-a-kd-20260518-0409
title: many_heads_no_pragma
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.000x
---

## Challenge

The before code uses default compiler unrolling for the qo_heads (64 iterations) and kv_heads (8 iterations) loops, while the after code adds #pragma unroll 1 to both. However, the measured performance is essentially identical (~0.03% difference). This is because NVCC with -O3 does not aggressive...


## Solution

The optimization would matter in the original flashinfer code where DISPATCH_HEAD_DIM macro resolves head_dim as a compile-time template parameter, enabling the compiler to see constant loop bounds that it might choose to unroll. In this standalone test with runtime loop bounds, both variants gen...

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- This is because NVCC with -O3 does not aggressively unroll loops whose bounds are runtime parameters (num_qo_heads and num_kv_heads are passed as kernel arguments, not compile-time constants).
- However, the measured performance is essentially identical (~0.03% difference).
