---
id: exp-o-kd-20260518-0447
title: clone_always_large
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

clone_always_large

## Solution

the after source (clone_always_large_after.cu), the cudaMemcpy is eliminated entirely because sortAndScanExpert accepts const int*, allowing d_topk_ids to be passed directly. The CUB radix sort only reads the keys input, so the const qualifier is semantically correct and no data copy is needed. T...

## Key Lessons

- In the after source (clone_always_large_after.cu), the cudaMemcpy is eliminated entirely because sortAndScanExpert accepts const int*, allowing d_topk_ids to be passed directly.
