---
id: exp-a-kd-20260518-1191
title: kcache_deferred_fp8_head128
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- register-reuse
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- fine-grained-quantization
impl_family: custom_cuda
speedup: 1.000x
---

## Challenge

The after code separates K-cache fp8 loading into two phases: Phase 1 loads raw fp8 bytes into a compact uint8_t Klocalb8[16][8] buffer (128 bytes total), while Phase 2 converts all 16 chunks to Half8 Klocal[16] (256 bytes) in a separate unrolled loop. The before code interleaves load and convert in


## Solution

ithin the same loop body. In the deferred pattern, Phase 1 can issue pure memory loads without dependent arithmetic, allowing the GPU scheduler to overlap these loads more efficiently. The raw fp8 buffer (1 byte/element) is also more compact in registers than the converted half buffer (2 bytes/eleme

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- However, at HEAD_SIZE=128 with KHELOOP=16, the total register footprint for both phases combined may exceed the inline approach because both Klocalb8 and Klocal exist simultaneously between Phase 1 and Phase 2.
- In the deferred pattern, Phase 1 can issue pure memory loads without dependent arithmetic, allowing the GPU scheduler to overlap these loads more efficiently.
