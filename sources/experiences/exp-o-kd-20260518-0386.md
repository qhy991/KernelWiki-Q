---
id: exp-o-kd-20260518-0386
title: medium_16kb
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- elementwise
- pipeline-stages
kernel_types:
- elementwise
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
speedup: 140.437x
---

## Challenge

Even at 16 KB, the before code still incurs two synchronous cudaMemcpy calls (DeviceToHost and HostToDevice). While the data volume is small, the synchronous copy overhead and CPU-side processing latency (cache misses, pipeline flush) create a fixed ~2.4 ms floor dominated by kernel launch and me...


## Solution

The after code's GPU kernel processes 16 KB with a single thread block, completing in ~17 microseconds since the entire workload fits in a single warp-group and constant-cache hits eliminate branch divergence. The ~140x speedup shows that eliminating the host-device synchronization boundary is th...

## Results

- **speedup**: 140.437x (improved)


## Key Lessons

- While the data volume is small, the synchronous copy overhead and CPU-side processing latency (cache misses, pipeline flush) create a fixed ~2.4 ms floor dominated by kernel launch and memcpy synchronization overhead rather than raw transfer time.
