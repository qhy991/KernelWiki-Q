---
id: exp-a-kd-20260518-0193
title: gemm_like_warp_idx
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- register-reuse
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
---

gemm_like_warp_idx

## Solution

r k-loop, and the __syncthreads() barriers. The single __shfl_sync at kernel entry is executed once per threadblock invocation and its latency (~20-30 cycles) is completely hidden by the 500-iteration outer loop that contains heavy memory traffic and block-level syncs. The arithmetic division thread

## Key Lessons

- The before code uses __shfl_sync(0xffffffff, threadIdx.x / 32, 0) at kernel entry to compute warp_idx, while the after code replaces it with canonical_warp_idx() which is just threadIdx.x / 32.
