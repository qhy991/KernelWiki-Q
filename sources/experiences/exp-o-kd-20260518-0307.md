---
id: exp-o-kd-20260518-0307
title: dequant_zero15
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- pipeline-stages
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
---

dequant_zero15

## Solution

a __halves2half2. In dequant_zero15_after.cu, z1 is constructed by half_uint16(0xe400 | zero) which directly ORs the runtime zero into the known IEEE 754 bit pattern for -1024.0f (0xe400), bypassing all float arithmetic and conversion hardware. z16 uses __hsub(__int2half_rn(-64), __int2half_rn(zero)

## Key Lessons

- The combined effect eliminates expensive float conversion and reduces instruction count for constant setup, yielding a measurable ~12.6% latency reduction.
- In dequant_zero15_after.cu, z1 is constructed by half_uint16(0xe400 | zero) which directly ORs the runtime zero into the known IEEE 754 bit pattern for -1024.0f (0xe400), bypassing all float arithmetic and conversion hardware.
