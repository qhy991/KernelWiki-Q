---
id: exp-o-kd-20260518-0273
title: moe_dispatch_k256
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: cublas
speedup: 2.657x
---

## Challenge

The before code (moe_dispatch_k256_before.cu) dispatches based on K > 128 alone, selecting the sequential path where 8 separate cublasGemmEx calls are issued per iteration — each expert GEMM is an independent kernel launch


## Solution

The after code (moe_dispatch_k256_after.cu) adds the hardware guard (sm_count == 78 && K > 128), which evaluates to false on the 108-SM A800, routing execution to cublasGemmStridedBatchedEx instead. This single batched kernel processes all 8 expert problems in one launch, eliminating 7 redundant ...

## Results

- **speedup**: 2.657x (improved)


## Key Lessons

- The before code (moe_dispatch_k256_before.cu) dispatches based on K > 128 alone, selecting the sequential path where 8 separate cublasGemmEx calls are issued per iteration — each expert GEMM is an independent kernel launch.
