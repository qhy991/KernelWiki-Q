---
id: exp-o-kd-20260518-0873
title: Lation already applies min__(K * actlN - A_CHUNK, ...) to prevent hardware-level
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Re-enabled min__ clamping on kOffcp in the A[] global-to-LDS load path of wvSplitKrc_.
Problem before: In the wvSplitKrc_ GEMM kernel's A[] global-to-LDS load path, kOffcp was computed as k_str + kOff without clamping. The original code had a min__(K - A_CHUNK, ...) clamping guar


## Solution

lation already applies min__(K * actlN - A_CHUNK, ...) to prevent hardware-level out-of-bounds access, the unclamped kOffcp causes the logical offset to wrap into the next row(s) of the A matrix. This produces incorrect reduction values rather than just safe padding reads.
Applied change: Re-enabled

## Key Lessons

- This produces incorrect reduction values rather than just safe padding reads.
- Although the outer address calculation already applies min__(K * actlN - A_CHUNK, ...) to prevent hardware-level out-of-bounds access, the unclamped kOffcp causes the logical offset to wrap into the next row(s) of the A matrix.
