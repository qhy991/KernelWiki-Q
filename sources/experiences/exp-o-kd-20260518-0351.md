---
id: exp-o-kd-20260518-0351
title: medium_kernel_sync
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: cutlass
speedup: 276.691x
---

## Challenge

In the before source (medium_kernel_sync_before.cu line 60), cudaMemcpy(DeviceToHost) is called between compute_kernel and scale_with_host_scalar. This synchronous memcpy implicitly drains the CUDA stream: the host blocks until compute_kernel finishes, then a 4-byte D2H copy executes, then the ho...


## Solution

The after source (medium_kernel_sync_after.cu) eliminates this by keeping the scale on device and passing a device pointer to scale_with_device_ptr, which reads the scalar via *d_scale_ptr inside the kernel (line 17). Both kernels are enqueued back-to-back with zero host blocking. The measured sp...

## Results

- **speedup**: 276.691x (improved)


## Key Lessons

- The after source (medium_kernel_sync_after.cu) eliminates this by keeping the scale on device and passing a device pointer to scale_with_device_ptr, which reads the scalar via *d_scale_ptr inside the kernel (line 17).
