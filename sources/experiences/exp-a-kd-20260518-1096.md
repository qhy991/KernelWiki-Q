---
id: exp-a-kd-20260518-1096
title: half_mla_128heads_64rotdim_512rank
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
- register-reuse
- epilogue-fusion
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.345x
---

## Challenge

The after code fuses the RoPE computation on k_pe with the KV-cache write into a single kernel. In the before path, the RoPE kernel writes k_pe results back to global memory, then a second kernel reads those same values from global


## Solution

memory and writes them to the paged KV cache — an extra global store/load round-trip and a kernel launch. The fused kernel computes RoPE on k_pe in registers and immediately writes the result to the KV cache offset, eliminating th

## Results

- **speedup**: 1.345x (improved)


## Key Lessons

- With 128 q-heads and 512 kv_lora_rank, the thread block is sized to cover both dimensions, and the fusion avoids the synchronization gap between the two separate launches.
