---
id: exp-a-kd-20260518-0523
title: imbalanced_4groups_8cta
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.114x
---

## Challenge

The before kernel uses `int blk_offset = blockIdx.x` (Groupwise schedule) where every CTA starts at its own blockIdx.x offset for each group. With groups producing 10, 20, 30, 5 tiles respectively (none fully divisible by gridDim.x=8), Groupwise consistently assigns remainder tiles to CTAs 0-3 ac...


## Solution

The after kernel introduces group_total_tiles and head_cta_id accumulators and computes blk_offset = (blockIdx.x + (gridDim.x - head_cta_id)) % gridDim.x, rotating which CTAs start at offset 0 for each new group. This rotation distributes remainder tiles to different CTAs per group (e.g., after g...

## Results

- **speedup**: 1.114x (improved)


## Key Lessons

- The before kernel uses `int blk_offset = blockIdx.x` (Groupwise schedule) where every CTA starts at its own blockIdx.x offset for each group.
