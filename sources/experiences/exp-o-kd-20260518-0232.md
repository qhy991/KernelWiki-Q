---
id: exp-o-kd-20260518-0232
title: moe_64ep_blk8_tok256
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 90.911x
---

## Challenge

In the before source, the benchmark loop (lines 127-136) calls cudaMalloc twice per iteration at lines 130-131 to allocate a 16640-byte tokens_cnts buffer and a 260-byte cumsum buffer, launches the <<<1, 64>>> kernel, then calls cudaFree twice at lines 135-136. Each cudaMalloc/cudaFree is a synch...


## Solution

The after source pre-allocates d_tokens_cnts and d_cumsum once at lines 107-110 before the benchmark loop, then the benchmark loop at lines 127-132 only enqueues the kernel with these pre-allocated pointers and calls cudaStreamSynchronize once after all 200 iterations. The kernel safely reuses th...

## Results

- **speedup**: 90.911x (improved)


## Key Lessons

- The kernel safely reuses the buffers because it zeroes the per-thread rows of tokens_cnts at lines 28-30 and recomputes cumsum at lines 48-52 on every invocation.
