---
id: exp-o-kd-20260518-0613
title: scale_transpose_only
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

scale_transpose_only

## Solution

The after path applies the same two-stage reduction directly to the original scale data, which contains the identical set of values in its original memory arrangement. The 1.37x speedup arises because the extra transpose kernel launch (a separate grid dispatch with its own startup overhead) and t...

## Key Lessons

- The 1.37x speedup arises because the extra transpose kernel launch (a separate grid dispatch with its own startup overhead) and the 16384-element strided copy become a substantial fraction of total execution time when the downstream computation is lightweight (a simple floating-point reduction over 
- The after path applies the same two-stage reduction directly to the original scale data, which contains the identical set of values in its original memory arrangement.
