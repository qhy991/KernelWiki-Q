---
id: exp-o-kd-20260518-0710
title: smem_occupancy
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Reduce kSmem from 128KB to 32KB by changing the constexpr from 32*1024*sizeof(uint32_t) to 8*1024*sizeof(uint32_t), improving SM occupancy for the topk radix kernel.
Problem before: The topk radix kernel allocated 128KB of dynamic shared memory per thread block (kSmem = 32*1024*s


## Solution

hroughput. The 128KB allocation was unnecessarily large because the radix algorithm uses s_input_idx[][SMEM_INPUT_SIZE] as a ping-pong buffer for two rounds, and each round only needs at most ~TopK (2048) candidate entries in the threshold bin, not the full 16K entries the old SMEM_INPUT_SIZE allowe

## Key Lessons

- The 128KB allocation was unnecessarily large because the radix algorithm uses s_input_idx[][SMEM_INPUT_SIZE] as a ping-pong buffer for two rounds, and each round only needs at most ~TopK (2048) candidate entries in the threshold bin, not the full 16K entries the old SMEM_INPUT_SIZE allowed.
