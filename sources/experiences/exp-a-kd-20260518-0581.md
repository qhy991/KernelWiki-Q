---
id: exp-a-kd-20260518-0581
title: float_h128_neox
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
- register-reuse
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
---

float_h128_neox

## Solution

The after code eliminates this round-trip entirely by keeping the normalized+weighted values in the register array e[EPT] after the RMSNorm phase (line 70-71), then applying NeoX RoPE via __shfl_xor directly on those register-held values (lines 77-82) before writing the final result once. The __s...

## Key Lessons

- The after code eliminates this round-trip entirely by keeping the normalized+weighted values in the register array e[EPT] after the RMSNorm phase (line 70-71), then applying NeoX RoPE via __shfl_xor directly on those register-held values (lines 77-82) before writing the final result once.
