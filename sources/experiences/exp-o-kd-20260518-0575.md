---
id: exp-o-kd-20260518-0575
title: medium_hidden
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- epilogue-fusion
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.756x
---

## Challenge

The same structural difference applies: the before code issues two kernel launches (compute_absmax_kernel + static_quant_kernel) requiring two full passes over the input tensor and a global memory round-trip for the scales, while


## Solution

the after code fuses both into one kernel. Additionally, the after quantization loop uses multiplication by tmp_scale (127.0f / block_absmax_val) rather than division by scale, which avoids the more expensive FP division instruction in the hot inner loop. At hidden_size=1024 the kernel work per e...

## Results

- **speedup**: 1.756x (improved)


## Key Lessons

- Additionally, the after quantization loop uses multiplication by tmp_scale (127.0f / block_absmax_val) rather than division by scale, which avoids the more expensive FP division instruction in the hot inner loop.
- The same structural difference applies: the before code issues two kernel launches (compute_absmax_kernel + static_quant_kernel) requiring two full passes over the input tensor and a global memory round-trip for the scales, while the after code fuses both into one kernel.
