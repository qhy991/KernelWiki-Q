---
id: exp-a-kd-20260518-0890
title: aligned_4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 0.991x
---

## Challenge

For aligned 4096-element FP8 quantization, the before code uses scaled_fp8_conversion_vec with a direct #pragma unroll loop over vec_n_t packs, while the after code uses vectorize_with_alignment's fast path with DefaultVecOp wrapping the conversion lambda. Both is


## Solution

sue the same 16-element vector loads and stores. The heavier per-element computation (float division by scale, double clamping via fmaxf/fminf, and uint8_t type cast) dominates the kernel runtime, making the alignment-check and functor overhead nearly negligible a

## Results

- **speedup**: 0.991x (neutral)


## Key Lessons

- For aligned 4096-element FP8 quantization, the before code uses scaled_fp8_conversion_vec with a direct #pragma unroll loop over vec_n_t packs, while the after code uses vectorize_with_alignment's fast path with DefaultVecOp wrapping the conversion lambda.
