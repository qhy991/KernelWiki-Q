---
id: exp-o-kd-20260518-0297
title: float_64exp_k8
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- register-reuse
- epilogue-fusion
- vectorized-loads
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- epilogue-fusion
- vectorized-loads
impl_family: custom_cuda
speedup: 78.634x
---

## Challenge

With 64 experts and k=8, the before code's scalar approach is even more penalized: the topkKernel_blockreduce kernel reads one element per strided iteration with TPB=256, meaning threads 0-63 each read one element while threads 64-255 are idle. The inner prior-k masking loop runs k_idx times per ...


## Solution

The after code loads 4 floats per AlignedArray transaction (LDG_PER_THREAD=2 for VPT=8), so each thread reads 8 elements via two vectorized loads with interleaved addressing. 8 threads per row collaborate via warp-shuffle XOR, eliminating all shared-memory and __syncthreads overhead. The TopK loo...

## Results

- **speedup**: 78.634x (improved)


## Key Lessons

- With 64 experts and k=8, the before code's scalar approach is even more penalized: the topkKernel_blockreduce kernel reads one element per strided iteration with TPB=256, meaning threads 0-63 each read one element while threads 64-255 are idle.
