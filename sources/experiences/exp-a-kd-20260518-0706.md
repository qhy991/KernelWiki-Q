---
id: exp-a-kd-20260518-0706
title: read_attention_h8_hs64_bs16
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
- pipeline-stages
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
speedup: 0.941x
---

## Challenge

Although the after layout makes consecutive positions within a block stride-1 (vs stride=head_size=64 in the before layout), the isolated read microbenchmark shows regression because its simulated kernel has only 64 threads per block processing 4096 bytes per head-block pair — far below the occupan


## Solution

cy and data volume needed for stride-1 coalescing to outperform scattered access. The kernel launch overhead (~5-10 microseconds) and low SM utilization dominate the 0.02ms measured latency, making the 6% memory-access improvement invisible. In the real PagedAttention kernel, hundreds of thread blo

## Results

- **speedup**: 0.941x (regression)


## Key Lessons

- Although the after layout makes consecutive positions within a block stride-1 (vs stride=head_size=64 in the before layout), the isolated read microbenchmark shows regression because its simulated kernel has only 64 threads per block processing 4096 bytes per head-block pair — far below the occupanc
- In the real PagedAttention kernel, hundreds of thread blocks read hundreds of KV blocks simultaneously at high occupancy, and the stride-1 pattern yields measurable bandwidth gains because the L2 cache and DRAM pipeline receive properly coalesced 128-byte transactions from many concurrent warps.
