---
id: exp-o-kd-20260518-0109
title: d64_s512
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- softmax
- register-reuse
kernel_types:
- softmax
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
---

d64_s512

## Solution

f conversion cycle of the before variant (kOStorageAccFloat32=0), which stores R_D in half registers (2 uint32_t per element) and must convert each value to float for rescaling, then back to half via __float2half_rn after each step. FP32 storage maintains full precision through all rescale iteration

## Key Lessons

- This eliminates the repeated half-to-float-to-half conversion cycle of the before variant (kOStorageAccFloat32=0), which stores R_D in half registers (2 uint32_t per element) and must convert each value to float for rescaling, then back to half via __float2half_rn after each step.
