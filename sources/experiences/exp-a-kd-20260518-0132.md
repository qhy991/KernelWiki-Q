---
id: exp-a-kd-20260518-0132
title: softmax_accum_atomic
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
- shared-memory-optimization
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 1.007x
---

## Challenge

In this microbenchmark each query row is processed by exactly one warp (4 warps for 32 rows), so only one atomicAdd ever targets each s_prime entry — there is no contention. The before code uses atomicAdd(&s_prime[q], row_sum) where only lane 0 of the single warp handling row q executes it, makin...


## Solution

The after code replaces this with a write to addition_storage[q + kQueriesPerBlock * wid] followed by a cross-warp reduction loop, but since each row has only one contributing warp the loop just copies the same value. The extra shared memory allocation (addition_storage of 128 floats vs. the orig...

## Results

- **speedup**: 1.007x (neutral)


## Key Lessons

- The after code replaces this with a write to addition_storage[q + kQueriesPerBlock * wid] followed by a cross-warp reduction loop, but since each row has only one contributing warp the loop just copies the same value.
