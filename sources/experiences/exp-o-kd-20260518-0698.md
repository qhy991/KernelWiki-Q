---
id: exp-o-kd-20260518-0698
title: Y_s, 1e-10f)))) requires computing log2, ceiling it, and then computing 2^result
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Bit-manipulation fast_pow2/fast_log2_ceil replacing exp2f/log2f/ceilf for UE8M0 scale computation.
Problem before: The v1 kernel computes UE8M0 quantization scales using three expensive floating-point math functions: exp2f, ceilf, and log2f. These functions are implemented as GPU


## Solution

y_s, 1e-10f)))) requires computing log2, ceiling it, and then computing 2^result, totaling ~3 expensive FP math calls. The UE8M0 extraction also uses log2f.
Applied change: v2 replaces these with fast_pow2 (constructs IEEE 754 float via integer bit-shift: (x+127)<<23 reinterpreted as float) and fast

## Key Lessons

- Applied change: v2 replaces these with fast_pow2 (constructs IEEE 754 float via integer bit-shift: (x+127)<<23 reinterpreted as float) and fast_log2_ceil (extracts exponent field from float bit representation and checks for nonzero mantissa to implement ceiling).
