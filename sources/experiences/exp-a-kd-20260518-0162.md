---
id: exp-a-kd-20260518-0162
title: pdl_launch_at_exit
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: inline_asm
speedup: 1.002x
---

## Challenge

The before code runs 6 GEMM+tail kernel pairs in sequence on the same stream with no device-side launch signal between them. The after code records a CUDA event after the GEMM kernel completes (work-exhaustion point) but before the tail kernel, and the next iteration waits on that event. This simu


## Solution

lates launch_dependent_grids() signaling at work-exhaustion. However, since all kernels are on the same default stream, they already execute serially - the explicit event wait does not change the execution ordering. On SM90a hardware, griddepcontrol.launch_dependents would be a single PTX instruct

## Results

- **speedup**: 1.002x (neutral)


## Key Lessons

- However, since all kernels are on the same default stream, they already execute serially - the explicit event wait does not change the execution ordering.
