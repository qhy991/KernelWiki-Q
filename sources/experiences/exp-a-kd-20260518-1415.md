---
id: exp-a-kd-20260518-1415
title: float_h4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
- epilogue-fusion
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
---

float_h4096

## Solution

The after fused_add_rms_norm_kernel merges both operations into a single kernel: the first loop reads input and residual, accumulates the sum and variance simultaneously, and writes the sum to residual; after the block reduction, the second loop reads residual (which is now a cache-hot L2 hit sin...

## Key Lessons

- The after fused_add_rms_norm_kernel merges both operations into a single kernel: the first loop reads input and residual, accumulates the sum and variance simultaneously, and writes the sum to residual; after the block reduction, the second loop reads residual (which is now a cache-hot L2 hit since 
- The key performance gain comes from eliminating one full kernel launch overhead (two kernel launches become one), and the residual array written in the first loop stays in the L2 cache for the second loop read, reducing global memory round-trips.
