---
id: exp-o-kd-20260518-0474
title: warp_expert_search_256ep
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 9.316x
---

## Challenge

The 9.3x speedup at 256 experts demonstrates the scaling advantage of cooperative search: the linear scan (before) scales as O(E) per thread requiring up to 256 serial comparisons, while the cooperative search (after) scales as O(E/32) requiring only 8 warp iterations. Each warp iteration probes ...


## Solution

the after code has coalesced warp-wide parallel reads from the prefix-sum array.

## Results

- **speedup**: 9.316x (improved)


## Key Lessons

- The speedup ratio (9.3x) is larger than the 64-expert case (4.7x) because the linear scan's serial overhead grows linearly with E while the cooperative approach's overhead grows at E/32.
- Memory access patterns also differ: the before code has each thread doing sequential, potentially divergent reads, while the after code has coalesced warp-wide parallel reads from the prefix-sum array.
