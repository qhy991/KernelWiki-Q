---
id: exp-a-kd-20260518-0484
title: fp16_hd4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
impl_family: custom_cuda
---

fp16_hd4096

## Solution

ia an unrolled scalar store loop (token_output[i*8+j] = output_arr[j] for j=0..7), generating 8 individual byte-width memory transactions. The after code replaces this with a single *(uint4*)(token_output+i*16) = output_storage.vec store that writes all 16 output bytes in one 128-

## Key Lessons

- This consolidation reduces memory-store instruction count and improves DRAM write bandwidth utilization on the output path, which is the dominant cost since the output array (1 byte per element) is much narrower than the input (2 bytes per fp16 element).
- More importantly, the before code writes 8 FP8 output bytes via an unrolled scalar store loop (token_output[i*8+j] = output_arr[j] for j=0..7), generating 8 individual byte-width memory transactions.
