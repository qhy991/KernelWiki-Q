---
id: exp-a-kd-20260518-0034
title: gelu_array_size4
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- activation
kernel_types:
- activation
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.035x
---

## Challenge

The before code uses #pragma unroll with a loop bound of int(rhs.size()), which calls a member function returning N. The after code uses the compile-time constant N directly. For N=4, NVCC a


## Solution

t -O3 fully inlines size() to return the constant 4, making the loop bound identical to the after version at the SASS level. Both paths produce the same unrolled sequence of 4 gelu_scalar ca

## Results

- **speedup**: 1.035x (improved)


## Key Lessons

- The before code uses #pragma unroll with a loop bound of int(rhs.size()), which calls a member function returning N.
