---
id: exp-a-kd-20260518-0966
title: experts_64_topk8
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- pipeline-stages
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
speedup: 271.480x
---

## Challenge

In the before source (experts_64_topk8_before.cu lines 176-189), each benchmark iteration performs: compute_expert_offsets<<<1,1,0,0>>> launch, cudaDeviceSynchronize to force GPU pipeline drain, cudaMemcpy D2H to read back problem_sizes1 (64*3 int32s), a CPU-side compute_blockscale_offsets_host l...


## Solution

the after source (experts_64_topk8_after.cu lines 144-151), the single compute_expert_blockscale_offsets kernel reads problem_sizes1 from GPU global memory, computes both tot_offset (expert_offsets prefix-sum) and tot_offset_round (128-aligned blockscale_offsets prefix-sum) in a single serial loo...

## Results

- **speedup**: 271.480x (improved)


## Key Lessons

- The 64-expert case shows higher speedup than 256-expert because the GPU kernel has fewer loop iterations and no D2H/H2D transfer size increase offsets the fixed sync cost.
