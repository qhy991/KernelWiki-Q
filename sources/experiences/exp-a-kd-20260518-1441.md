---
id: exp-a-kd-20260518-1441
title: loop_deref_scale
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- register-reuse
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 0.989x
---

## Challenge

In the before code, k_scale and v_scale are scalar float kernel parameters used directly at each iteration of the dequantization loop (scaled_convert(k_val, k_scale)). In the after code, they are const float* pointers dereferenced at each call site inside the loop (scaled_convert(k_val, *k_scale)). 


## Solution

noise). The NVCC compiler optimizes the repeated global-memory dereference of a constant address into a single load that is cached in registers or L1 cache across loop iterations, so the per-iteration *k_scale dereference does not actually produce a separate global load each time. The scalar-by-valu

## Results

- **speedup**: 0.989x (neutral)


## Key Lessons

- In the before code, k_scale and v_scale are scalar float kernel parameters used directly at each iteration of the dequantization loop (scaled_convert(k_val, k_scale)).
