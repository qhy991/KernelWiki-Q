---
id: exp-a-kd-20260518-0962
title: gelu_erf_vs_tanh_fp32
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- activation
kernel_types:
- activation
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

gelu_erf_vs_tanh_fp32

## Solution

ds three extra FMA operations (x_cube = f*f*f, f + KAPPA*x_cube, BETA * inner) compared to the single multiply before erf, increasing instruction count by 3-4 FLOPs per element. In a memory-bound kernel reading 2*4096*4096*4B = 128MB of

## Key Lessons

- The before code computes GELU via ::erf(f * ALPHA) while the after code computes the tanh approximation via ::tanhf(BETA * (f + KAPPA * f*f*f)).
