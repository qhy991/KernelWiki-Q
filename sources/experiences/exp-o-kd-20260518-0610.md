---
id: exp-o-kd-20260518-0610
title: eq256_bf16
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
- pipeline-stages
- register-reuse
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- pipeline-stages
- register-reuse
impl_family: custom_cuda
speedup: 5.514x
---

## Challenge

The after expand kernel is faster because it eliminates three major overhead sources present in the before shrink kernel. First, the before code uses cuda::memcpy_async pipeline (producer_acquire/commit, consumer_wait/release) to copy W and X into shared memory before computing; for feat_in==feat...


## Solution

after code loads W and X directly from global memory into registers via float4 reinterpret_cast, bypassing shared memory entirely. Second, the before kernel allocates ~8KB shared memory (2 pipeline stages × 2 arrays × 1024 bf16 entries) and requires multiple block.sync() barriers for cross-warp r...

## Results

- **speedup**: 5.514x (improved)


## Key Lessons

- The after expand kernel is faster because it eliminates three major overhead sources present in the before shrink kernel.
