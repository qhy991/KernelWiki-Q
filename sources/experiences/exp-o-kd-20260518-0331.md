---
id: exp-o-kd-20260518-0331
title: fp8_quant_32k
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 1.788x
---

## Challenge

The before code dispatches 32 grid blocks for segmented_max_reduction, each with 4KB shared memory and a full reduction tree, followed by 32 blocks for the quant kernel over 32768 floats. The reduction kernel reads all 128KB of input data into shared-memory per-block caches, performs per-thread m...


## Solution

The after code skips the reduction entirely and launches only 32 quant-kernel blocks, reading the data once. The ~1.79x speedup reflects both the eliminated 128KB redundant read and the saved shared-memory reduction + atomic overhead.

## Results

- **speedup**: 1.788x (improved)


## Key Lessons

- The before code dispatches 32 grid blocks for segmented_max_reduction, each with 4KB shared memory and a full reduction tree, followed by 32 blocks for the quant kernel over 32768 floats.
