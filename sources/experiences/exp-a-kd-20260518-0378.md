---
id: exp-a-kd-20260518-0378
title: int64_small_256
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.906x
---

## Challenge

The after code replaces three separate kernel launches (compute_neg_indices + gather_from_map + conditional_select) with one single-pass resolve_future_token_ids_kernel. For 256 int64 elements, the before path suffers from triple kernel launch over


## Solution

head (each launch costs ~2-5us of driver overhead on CUDA) and two intermediate buffer allocations (neg_indices, gathered). The single-pass after kernel eliminates these intermediate buffers and reduces to one launch, saving both memory bandwidth f

## Results

- **speedup**: 1.906x (improved)


## Key Lessons

- The single-pass after kernel eliminates these intermediate buffers and reduces to one launch, saving both memory bandwidth for the intermediate writes/reads and driver launch overhead.
