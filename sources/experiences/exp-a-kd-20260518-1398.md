---
id: exp-a-kd-20260518-1398
title: fp8_scales_fp16
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
impl_family: custom_cuda
speedup: 1.004x
---

## Challenge

The after code uses the combined (q & 0xFF00FF00) >> 1 pattern to extract both FP8 values and shift them to FP16 position in two integer operations, while the before code splits the extraction into per-half operations with s


## Solution

eparate masking, shifting, and repacking steps. The NVCC compiler at -O3 optimizes the before code's per-half extraction into nearly the same SASS as the after code's combined pattern, resulting in a 0.38% difference that is

## Results

- **speedup**: 1.004x (neutral)


## Key Lessons

- The after code uses the combined (q & 0xFF00FF00) >> 1 pattern to extract both FP8 values and shift them to FP16 position in two integer operations, while the before code splits the extraction into per-half operations with separate masking, shifting, and repacking steps.
