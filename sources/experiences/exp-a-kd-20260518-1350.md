---
id: exp-a-kd-20260518-1350
title: swap_caller
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- transpose
kernel_types:
- transpose
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: cutlass
speedup: 0.376x
---

## Challenge

The before code passes A and B directly with problem shape (M=8,N=8192,K=4096), giving grid.y=8 thread block rows. The after code swaps the operand pointers and transposes the problem shape to (M=8192,N=8,K=4092), giving grid.y=8192 block rows. However, the naive one-element-per-thread kernel


## Solution

in the after path reads A_swap (which is B^T) in column-major-like strided access, causing poor memory coalescing. On SM80 without TMA, the swap just reorders memory accesses without the hardware-level benefit that CUTLASS 3.x TMA tiles provide on SM90. The resulting cache thrashing from the tran...

## Results

- **speedup**: 0.376x (regression)


## Key Lessons

- The resulting cache thrashing from the transposed access pattern dominates, causing the regression.
