---
id: exp-o-kd-20260518-0792
title: E token had any prefix KV context. For tokens without prefix context (e.g.,
  deco
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Added early-exit fast path in merge_attn_states_kernel: tokens with no prefix context (token_idx >= prefix_num_tokens) copy suffix output directly, bypassing the LSE rescaling, exp, division, and FMA merge computation.
Problem before: In the original kernel, every token thread pe


## Solution

e token had any prefix KV context. For tokens without prefix context (e.g., decode tokens or context-free prefill tokens in a batched MLA run), the prefix LSE was -inf and prefix output was all zeros, so the merge math reduced to copying suffix output — but the kernel still paid the cost of loading 

## Key Lessons

- This bypasses all prefix loads, LSE comparisons, exp, division, and FMA operations for suffix-only tokens.
- Optimization focus: Added early-exit fast path in merge_attn_states_kernel: tokens with no prefix context (token_idx >= prefix_num_tokens) copy suffix output directly, bypassing the LSE rescaling, exp, division, and FMA merge computation.
