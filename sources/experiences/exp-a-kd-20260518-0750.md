---
id: exp-a-kd-20260518-0750
title: spread_32k
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.187x
---

## Challenge

The after code uses 2048 bins per step (vs 512 bins in the before code), which provides finer granularity for the top-k histogram threshold. Although the progressive approach may require multiple passes over the data (steps 0-3), the l


## Solution

arger bin count at each step more precisely isolates the threshold bin. For the spread_32k case with 32768 elements and topK=50, the first step alone typically narrows the candidates enough that only one scan round (512 bins out of 204

## Results

- **speedup**: 1.187x (improved)


## Key Lessons

- The after code uses 2048 bins per step (vs 512 bins in the before code), which provides finer granularity for the top-k histogram threshold.
