---
id: exp-a-kd-20260518-0250
title: embedding_f16x8_vs_f16x8_pack
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 2.604x
---

## Challenge

The after kernel uses LDST128BITS (reinterpret_cast<float4*>) to load and store 8 half values (16 bytes) in a single 128-bit packed memory transaction, replacing 8 separate 2-byte scala


## Solution

r half load/store instructions in the before kernel. This reduces total memory instructions from 16 (8 loads + 8 stores) to 2 (1 packed load + 1 packed store), cutting instruction dispa

## Results

- **speedup**: 2.604x (improved)


## Key Lessons

- This reduces total memory instructions from 16 (8 loads + 8 stores) to 2 (1 packed load + 1 packed store), cutting instruction dispatch overhead and ensuring the hardware issues wide 128-bit memory transactions rather than potentially narrower ones that the compiler might generate for individual hal
