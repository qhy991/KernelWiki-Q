---
id: exp-a-kd-20260518-1371
title: float_dtype_hidden_4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- elementwise
kernel_types:
- elementwise
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.995x
---

## Challenge

Same mechanism as the 1024-hidden case: the after version reads scale from a device pointer in global memory versus the before version receiving it as a kernel argument in constant/parameter memory. With a larger hidden_size


## Solution

(4096), each thread processes 4 elements in the strided loop, making the kernel runtime dominated by the input/output memory accesses rather than the single scale read. The one-instruction difference in how scale is materia

## Results

- **speedup**: 0.995x (neutral)


## Key Lessons

- With a larger hidden_size (4096), each thread processes 4 elements in the strided loop, making the kernel runtime dominated by the input/output memory accesses rather than the single scale read.
- At the kernel level, the performance delta should be negligible; the real optimization benefit is the eliminated .item() host synchronization at the caller site.
