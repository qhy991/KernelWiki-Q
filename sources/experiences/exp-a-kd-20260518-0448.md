---
id: exp-a-kd-20260518-0448
title: bf16_m512_topk8_k4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- pipeline-stages
- register-reuse
- epilogue-fusion
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- register-reuse
- epilogue-fusion
impl_family: custom_cuda
speedup: 10.881x
---

## Challenge

The before code launches three kernels: shuffle_kernel with grid=4096 blocks (m*topk=512*8), mul_weight_kernel with the same grid, and sum_reduce_kernel with grid=512 blocks. Each kernel has its own launch overhead, and the two intermediate buffers (d_shuffled and d_multiplied, each 4096*4096*4 byte


## Solution

additionally serializes over topk=8 iterations per column element, re-reading scattered rows from d_multiplied. In contrast, the fused after kernel uses just 512 blocks (one per output row) with 256 threads striding across columns. Each thread reads input_tensor directly via permutation[src_row] and

## Results

- **speedup**: 10.881x (improved)


## Key Lessons

- This eliminates two intermediate writes (~128MB total), reduces global memory reads from 3 passes to 1, and consolidates 3 kernel launches into 1.
