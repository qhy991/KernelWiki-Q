---
id: exp-a-kd-20260518-1260
title: absmax_16384
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- register-reuse
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- vectorized-loads
impl_family: custom_cuda
speedup: 0.939x
---

## Challenge

The after kernel uses vec4_t reinterpretation to load 4 floats per transaction, but both kernels run with gridDim.x > 1 blocks each processing a portion of the 16384 elements. At 64KB total input size, data fits in the GPU L2 c


## Solution

ache, so the memory bandwidth benefit of wider loads is minimal. The vectorized loop does fewer iterations but the same total fabs+max operations per element, plus the #pragma unroll 4 and struct-field access pattern may introd

## Results

- **speedup**: 0.939x (regression)


## Key Lessons

- The vectorized loop does fewer iterations but the same total fabs+max operations per element, plus the #pragma unroll 4 and struct-field access pattern may introduce register pressure or instruction overhead that slightly exceeds the marginal bandwidth savings.
