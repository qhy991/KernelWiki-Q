---
id: exp-a-kd-20260518-1162
title: sort_64ep
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 2.416x
---

## Challenge

The before code's sorting loop computes rank_post_pad by reading tokens_cnts[threadIdx.x][expert_id] from the shared-memory 2D prefix-sum matrix and adding cumsum[expert_id]. This requires two shared-memory reads and one shared-memory write per token (the increment of tokens_cnts)


## Solution

The after code replaces this with a single atomicAdd(&local_offsets[expert_id], 1) per token, which atomically reads-increments-returns the offset in one shared-memory operation. The after approach also eliminates the per-expert prefix-sum pass (the loop where each of num_experts threads sums tok...

## Results

- **speedup**: 2.416x (improved)


## Key Lessons

- The after approach also eliminates the per-expert prefix-sum pass (the loop where each of num_experts threads sums tokens_cnts across blockDim.x rows), removing one __syncthreads barrier and O(blockDim.x * num_experts) shared-memory read-modify-write operations.
