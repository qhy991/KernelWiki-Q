---
id: exp-a-kd-20260518-0662
title: fp16_topk2_r2048
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- pipeline-stages
- vectorized-loads
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- vectorized-loads
impl_family: custom_cuda
speedup: 1.645x
---

## Challenge

The after kernel replaces per-element scalar half loads (__half2float on individual addresses) with half2 vectorized loads via reinterpret_cast<const half2*>(...), cutting the number of global memory load transactions from 8 individual 2-byte reads to 4 coalesced 4-byte reads per topk iteration. The


## Solution

alf22float2 intrinsic converts two half values to two float values in a single hardware instruction, halving the type-conversion instruction count versus the before kernel's two separate __half2float calls per element. The #pragma unroll on the inner k2 loop (4 iterations for vec_size=8) eliminates 

## Results

- **speedup**: 1.645x (improved)


## Key Lessons

- The #pragma unroll on the inner k2 loop (4 iterations for vec_size=8) eliminates per-iteration branch overhead and gives the compiler freedom to schedule the 4 FMA operations and 4 __half22float2 conversions in an optimal pipeline order.
