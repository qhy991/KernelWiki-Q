---
id: exp-a-kd-20260518-1176
title: coalesced_offset_4M
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- elementwise
- register-reuse
- vectorized-loads
kernel_types:
- elementwise
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- vectorized-loads
impl_family: cutlass
---

coalesced_offset_4M

## Solution

block_base + tid * 8 + v. The nvcc compiler optimizes both expressions to the same register allocation and instruction sequence, so the measured 0.66% delta is within noise. The real Machete benefit of coalescing the CuTe layout's (athrid, val) dimension into (athrid_val) is that i

## Key Lessons

- The before kernel computes per-element offsets using a hierarchical two-stride decomposition (block_base + tid * thread_stride + v * value_stride), while the after kernel precomputes a thread base and adds a flat index (thread_base + v).
