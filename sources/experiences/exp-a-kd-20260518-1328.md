---
id: exp-a-kd-20260518-1328
title: schedule_pingpong
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- shared-memory-optimization
- pipeline-stages
kernel_types:
- gemm
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- pipeline-stages
impl_family: cutlass
speedup: 0.999x
---

## Challenge

The before kernel allocates double-buffered shared memory (4 buffers of 64x64 half = 32KB) to simulate the Pingpong pattern's overlap of two GEMM tiles, while the after kernel uses only single-buffered shared memory (2 buffers = 16KB) for the WarpSpecialized pattern. Des


## Solution

pite the 2x shared memory difference, both kernels produce nearly identical latency because the shared memory allocation difference only affects occupancy when shared memory is the limiting resource, and the simulated compute workload is too simple to expose the mainloop

## Results

- **speedup**: 0.999x (neutral)


## Key Lessons

- Despite the 2x shared memory difference, both kernels produce nearly identical latency because the shared memory allocation difference only affects occupancy when shared memory is the limiting resource, and the simulated compute workload is too simple to expose the mainloop/epilogue overlap that Pin
- The before kernel allocates double-buffered shared memory (4 buffers of 64x64 half = 32KB) to simulate the Pingpong pattern's overlap of two GEMM tiles, while the after kernel uses only single-buffered shared memory (2 buffers = 16KB) for the WarpSpecialized pattern.
