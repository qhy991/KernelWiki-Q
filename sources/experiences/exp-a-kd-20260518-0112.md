---
id: exp-a-kd-20260518-0112
title: small_4warp
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- pipeline-stages
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
speedup: 1.206x
---

## Challenge

The after code replaces atomicAdd on shared s_prime[] with per-warp slots in addition_storage[] followed by a deterministic serial reduction by one lane per row. With 4 warps contending on 16 s_prime entries, atomicAdd requires


## Solution

each lane-0 thread to acquire exclusive access to the same shared-memory location, causing serialization. The after code writes each warp partial sum to a disjoint slot (avoiding contention), then one lane reads all 4 warp contr

## Results

- **speedup**: 1.206x (improved)


## Key Lessons

- This trades N atomic operations per row for N sequential shared-memory reads by a single lane, which is faster because shared-memory reads are pipelined and do not require lock/retry overhead.
