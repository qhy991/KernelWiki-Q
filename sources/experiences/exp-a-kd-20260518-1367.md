---
id: exp-a-kd-20260518-1367
title: block_reduce_max_float
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

block_reduce_max_float

## Solution

The after source (block_reduce_max_float_after.cu) uses blockReduce<float,256>(val, detail::_max<float>) which calls warpReduce<T>(val, fn) at both stages with fn=_max wrapping `max(a,b)`. The shared-memory staging, __syncthreads barrier, and inactive-thread initialization are identical. Since NV...

## Key Lessons

- The before source (block_reduce_max_float_before.cu) has a hand-written blockReduceMax<float,256> that duplicates the entire blockReduceSum structure but uses warpReduceMax<T> instead of warpReduceSum<T> at both reduction stages: `val = warpReduceMax<T>(val)` for the first warp-level pass, and `val 
