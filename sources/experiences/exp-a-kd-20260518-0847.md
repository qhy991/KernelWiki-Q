---
id: exp-a-kd-20260518-0847
title: repack_4bit
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
- register-reuse
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- register-reuse
impl_family: custom_cuda
speedup: 0.876x
---

## Challenge

The 4-bit regression stems from the different shared-memory tile layouts, not the indexing strategy alone. The GPTQ before kernel uses sh_stride=64 with a 1024-int tile (16 rows x 64 columns), where each thread reads a contiguous column of tile_ints=2 values at stride 64 — these reads hit consecutiv


## Solution

Q after kernel uses sh_stride=8 with a 128-int tile (16 rows x 8 columns), where reads at sh_stride=8 stride cause more frequent bank conflicts on the 32-bank shared memory (stride 8 means every 4th bank is hit, causing 4-way conflicts). Additionally, the AWQ path reads 2 global-memory values per it

## Results

- **speedup**: 0.876x (regression)


## Key Lessons

- Additionally, the AWQ path reads 2 global-memory values per iteration (packed_src_0, packed_src_1) vs the GPTQ path's 2*2=4 reads in the column-prefill loop plus 8 register extractions — but since the AWQ tile is 8x smaller in memory footprint, the reduced cache locality dominates and the latency pe
- The two-step column-then-extract pattern in GPTQ happens to have better memory access patterns for the 4-bit case with its wide tile.
