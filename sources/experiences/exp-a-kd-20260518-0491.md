---
id: exp-a-kd-20260518-0491
title: fp16_4096_1000tok
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

fp16_4096_1000tok

## Solution

re with the next iteration's computation, which is insufficient to meaningfully hide memory latency given the much larger compute workload. The conditional null-pointer branch in the before version is a single predictable branch that costs virtually nothing on a GPU. The two implementations p

## Key Lessons

- The two implementations produce nearly identical timing because the structural pattern difference (deferred vs immediate write) has negligible impact when the per-thread deferred output is only 16 bytes — the latency hiding benefit scales with the amount of deferred data, not with the total computat
- While the per-iteration workload is larger (16 warps doing cross-warp shared-memory reduction for a 4096-element RMS norm), the deferred-write pattern still only overlaps a 16-byte global store with the next iteration's computation, which is insufficient to meaningfully hide memory latency given the
