---
id: exp-a-kd-20260518-0023
title: split4_H128_B1
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
- pipeline-stages
- epilogue-fusion
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- epilogue-fusion
impl_family: custom_cuda
speedup: 0.047x
---

## Challenge

The before code uses two simple kernels with no inter-CTA synchronization: write_partial_lse writes all partial LSE values to a large global workspace, and host_reduce_lse reads them back in a single-pass reduction. The after code replaces this with a single fused kernel using atomicMax/atomicAdd...


## Solution

in the after kernel must busy-wait for all split_kv CTAs to reach the barrier before proceeding, which adds significant overhead on A800 (sm_80) hardware. The before kernels have no synchronization overhead because they are sequential at the host level. The real benefit of the fused reduction onl...

## Results

- **speedup**: 0.047x (regression)


## Key Lessons

- The before kernels have no synchronization overhead because they are sequential at the host level.
