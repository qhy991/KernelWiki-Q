---
id: exp-a-kd-20260518-1246
title: zp_scalar_vs_vec
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
---

zp_scalar_vs_vec

## Solution

-iteration (128 total loads). This 2x reduction in memory instruction count allows the GPU to coalesce the accesses into fewer memory transactions and reduces the instruction overhead in the inner loop. The measured 7.8x speedup is amplified b

## Key Lessons

- This 2x reduction in memory instruction count allows the GPU to coalesce the accesses into fewer memory transactions and reduces the instruction overhead in the inner loop.
- The measured 7.8x speedup is amplified by the single-thread launch configuration where kernel launch overhead and loop instruction count dominate; in the real Marlin kernel with full thread blocks, the relative improvement would be smaller but the same vectorization principle applies.
