---
id: exp-a-kd-20260518-0178
title: naive_warp_idx
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- register-reuse
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 0.064x
---

## Challenge

The after code replaces a simple threadIdx.x / 32 integer division (compiled to a single shift instruction) with __shfl_sync(0xffffffff, threadIdx.x / 32, 0), which adds a full-warp shuffle barrier per invocation. In a tight loop calling this function 1M times per thread, the


## Solution

shuffle instruction overhead dominates: each __shfl_sync requires warp convergence, a register broadcast from lane 0, and synchronization across all 32 lanes. The before code's plain division has no such barrier cost. The canonical_warp_idx() pattern is designed for correctn

## Results

- **speedup**: 0.064x (regression)


## Key Lessons

- The canonical_warp_idx() pattern is designed for correctness guarantees (ensuring warp-wide agreement on the index when threads may have diverged), not as a performance optimization.
