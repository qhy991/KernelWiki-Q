---
id: exp-a-kd-20260518-0052
title: conv3d_large_analytic
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- activation
kernel_types:
- activation
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.548x
---

## Challenge

In the before source, every access recalculates the activation tensor address via three sequential plain-integer div/mod operations to decompose the NZPQ row index into (n,z,p,q), then recomputes the spatial coordinate (dd,hh,ww) with stride/padding/dilation arithmetic, and finally calls ndhwc_of...


## Solution

The after source replaces the div/mod chain with FastDivmod objects that use __umulhi+shift (a single multiply-high instruction and shift per division, avoiding the ~20-cycle integer divide path on SM80), and replaces the per-access ndhwc_offset() call with a base_offset computed once plus three ...

## Results

- **speedup**: 1.548x (improved)


## Key Lessons

- The after source replaces the div/mod chain with FastDivmod objects that use __umulhi+shift (a single multiply-high instruction and shift per division, avoiding the ~20-cycle integer divide path on SM80), and replaces the per-access ndhwc_offset() call with a base_offset computed once plus three pre
