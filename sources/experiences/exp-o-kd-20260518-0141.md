---
id: exp-o-kd-20260518-0141
title: sgemm_8x8_m512
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- transpose
- shared-memory-optimization
- pipeline-stages
kernel_types:
- transpose
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- pipeline-stages
impl_family: custom_cuda
speedup: 1.087x
---

## Challenge

The after kernel uses cp.async.ca to asynchronously copy B-tile data from global to shared memory in the prologue. In the before code, the 8x8 variant loads B synchronously via FLOAT4 stores (lines 28-30 in before source), which blocks until each global read completes. The


## Solution

after code issues 2 CP_ASYNC_CA calls (16 bytes each) via __cvta_generic_to_shared pointer, commits the group, then proceeds with A loading and transpose while the B transfers are in flight. With BM=128/BN=128 tiling, each thread loads 8 values for A and the online transpose writes 8 scalar value...

## Results

- **speedup**: 1.087x (improved)


## Key Lessons

- The after kernel uses cp.async.ca to asynchronously copy B-tile data from global to shared memory in the prologue.
