---
id: exp-a-kd-20260518-0450
title: lru_256buf_32miss
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.990x
---

## Challenge

The before code uses a two-branch if/else pattern: if (i < total_evictable) routes all non-hit slots (including misses) to the LRU front, while hits go to the MRU back. The after code introduces a three-branch pattern: misses (i < TOTAL_MISSES=32) are written to req_lru_slots[total_evictable - TO...


## Solution

The optimization's real value is algorithmic correctness: preventing recently-loaded misses from being placed at the LRU front, which reduces redundant host-to-device cache transfers over successive inference steps.

## Results

- **speedup**: 0.990x (neutral)


## Key Lessons

- At this microbenchmark's tiny kernel size (1 block, 2 iterations per thread), the measured latencies are dominated by kernel launch overhead (~2.5us), making the ~1% difference noise rather than a meaningful signal.
- The before code uses a two-branch if/else pattern: if (i < total_evictable) routes all non-hit slots (including misses) to the LRU front, while hits go to the MRU back.
