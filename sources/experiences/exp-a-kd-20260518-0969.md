---
id: exp-a-kd-20260518-0969
title: softmax_vs_sigmoid_256experts
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- softmax
kernel_types:
- softmax
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

softmax_vs_sigmoid_256experts

## Solution

The after sigmoid code instead does 8 independent __expf(-x) calls plus 8 divisions and 8 additions per element. The sigmoid expression 1/(1+exp(-x)) requires both an expf and a division per element, whereas softmax amortizes the division to one reciprocal per row. With VPT=8 and 8 threads per ro...

## Key Lessons

- In the 256-expert before code, the softmax path performs 8 expf calls (one per VPT=8 element) on values already shifted by thread_max, plus 3 SHFL_XOR iterations per reduction (log2(8)=3 for both max and sum), then a single reciprocal multiply across all 8 elements.
