---
id: exp-o-20260323-212535-b4ad4cd8
title: fp32_rms_norm_llama3_8b_hs4096
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-03-23'
confidence: inferred
reproducibility: concept
---

## Challenge

Further optimize RMS norm kernel beyond Round 1's 2.175x speedup


## Solution

256-thread kernel with rsqrtf, #pragma unroll 4, 8-warp smem reduction, __launch_bounds__(256,4)


### Key Changes

- Reduced thread count from 512 to 256 for better occupancy (min_blocks_per_sm=4)
- Used rsqrtf() instead of fdividef(1.0f, sqrtf(...)) - faster hardware instruction
- Reduced smem from 16 to 8 floats (8 warps instead of 16)
- #pragma unroll 4 for the main loops (4 float4 vectors per thread)
- __launch_bounds__(256, 4) hints compiler for 4 blocks per SM


## Key Lessons

- rsqrtf() is significantly faster than 1.0f/sqrtf() for RMS norm inverse computation
- 256 threads with min_blocks_per_sm=4 can outperform 512 threads for memory-bound kernels
- Reducing shared memory footprint (8 vs 16 floats) improves L1 cache efficiency
- #pragma unroll 4 with 4 iterations per thread is optimal for hidden_size=4096 with 256 threads
- For RMS norm at hs=4096: 256 threads, unroll-4, rsqrtf gives ~2.7x over PyTorch baseline
