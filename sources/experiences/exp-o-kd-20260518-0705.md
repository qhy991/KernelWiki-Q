---
id: exp-o-kd-20260518-0705
title: unknown
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

unknown

## Solution

irs via a flat global_idx that increments token_idx first, the LSE access pattern `head_idx * num_tokens + token_idx` strided across memory, causing poor coalescing because adjacent threads access different rows of the LSE matrix.
Applied change: The v2 kernel uses row-major LSE layout [num_tokens, 

## Key Lessons

- When threads map to (token_idx, head_idx) pairs via a flat global_idx that increments token_idx first, the LSE access pattern `head_idx * num_tokens + token_idx` strided across memory, causing poor coalescing because adjacent threads access different rows of the LSE matrix.
