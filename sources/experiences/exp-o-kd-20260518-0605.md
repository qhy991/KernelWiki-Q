---
id: exp-o-kd-20260518-0605
title: adaptive_splitk
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- shared-memory-optimization
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 2.063x
---

## Challenge

The before code (fixed_grid_gemm_kernel) launches a 2D grid dim3(2,2,1) producing only 4 blocks for the M=32,N=32 output (2*2 tiles of 16x16). Each block iterates over the full K=2880 in a single loop (for k=0; k<K; k+=16), then writes its complete result via store_matrix_sync directly to global C[]


## Solution

es only K/2=1440 K-elements per shard, producing a partial 16x16 tile stored to shared memory via store_matrix_sync(partial, frag_c, WMMA_N, mem_row_major). Threads then iterate over the 16*16=256 shared-memory elements and atomicAdd each into global C[out_row*N+out_col]. The cudaMemset(d_C,0) befor

## Results

- **speedup**: 2.063x (improved)


## Key Lessons

- The before code (fixed_grid_gemm_kernel) launches a 2D grid dim3(2,2,1) producing only 4 blocks for the M=32,N=32 output (2*2 tiles of 16x16).
