---
id: exp-a-kd-20260518-0470
title: topk8_rows128
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
- epilogue-fusion
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.441x
---

## Challenge

The before code launches two separate kernels: rescale_normalize_kernel divides each weight by the row sum, then rescale_scale_kernel multiplies by routed_scaling_factor. Each kernel launch incurs dispatch overhead (grid setup, parameter passing, stream queueing) and forces an intermediate global...


## Solution

The after code merges both operations into one rescale_fused_kernel launch where each element is read once, divided by sum, and conditionally multiplied by the scaling factor in the same loop iteration, writing the final result once. Eliminating the second kernel launch and the redundant load-sto...

## Results

- **speedup**: 1.441x (improved)


## Key Lessons

- The before code launches two separate kernels: rescale_normalize_kernel divides each weight by the row sum, then rescale_scale_kernel multiplies by routed_scaling_factor.
