---
id: exp-a-kd-20260518-1372
title: scale_host_sync
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- elementwise
kernel_types:
- elementwise
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 260.608x
---

## Challenge

The after path eliminates the cudaMemcpy(DeviceToHost) call that was required in the before path to extract the scale value from GPU memory to the host before passing it as a kernel launch scalar argument. Each D2H memcpy is a synchronous ope


## Solution

ration that stalls the CUDA stream and forces the CPU to wait for the GPU to complete prior work before copying the 4-byte scale value. The after kernel instead receives a device pointer and dereferences it with *scale_ptr inside the kernel,

## Results

- **speedup**: 260.608x (improved)


## Key Lessons

- The after path eliminates the cudaMemcpy(DeviceToHost) call that was required in the before path to extract the scale value from GPU memory to the host before passing it as a kernel launch scalar argument.
