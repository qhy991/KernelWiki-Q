---
id: exp-a-kd-20260518-0680
title: small_m8_k512
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
impl_family: custom_cuda
speedup: 1.000x
---

## Challenge

Reading small_m8_k512_before.cu and small_m8_k512_after.cu: the source pair has the same structural differences as the k128 case (before uses division/modulo + stride variables + per-iteration numCols_SFout computation; after uses bitwise shift/AND + shift-O


## Solution

R expression + kernel-scope numKTiles precomputation + __forceinline__). With numCols=512, numKTiles=8, and 8*64=512 total iterations, the integer arithmetic savings per iteration are even more proportionally negligible relative to the memory traffic (readin

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- Reading small_m8_k512_before.cu and small_m8_k512_after.cu: the source pair has the same structural differences as the k128 case (before uses division/modulo + stride variables + per-iteration numCols_SFout computation; after uses bitwise shift/AND + shift-OR expression + kernel-scope numKTiles prec
