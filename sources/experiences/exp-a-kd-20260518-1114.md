---
id: exp-a-kd-20260518-1114
title: attention_workload
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
- pipeline-stages
- register-reuse
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- register-reuse
impl_family: custom_cuda
speedup: 0.976x
---

## Challenge

The before source (attention_workload_before.cu) implements addx4 as a scalar loop: for each of 4 half elements, it extracts uint16 from a Uint4x2 struct, reinterpret-casts to __half, performs scalar __half addition, then repacks the result as uint16 — 4 separate scalar add operations per call. T...


## Solution

in the after code may introduce slight register pressure overhead that offsets the instruction count reduction. The 2.44% slower result on NVIDIA suggests the optimization's benefit is specific to AMD hardware where the __half2 native addition has a clearer throughput advantage over scalar __half...

## Results

- **speedup**: 0.976x (regression)


## Key Lessons

- The 2.44% slower result on NVIDIA suggests the optimization's benefit is specific to AMD hardware where the __half2 native addition has a clearer throughput advantage over scalar __half paths, and where the union-based type punning avoids expensive library function calls for __hip_bfloat16 operation
- However, the before code's scalar extraction and addition pattern is already efficiently compiled by nvcc into 4 FADD instructions on the FP16 pipeline, and the union-based __half2 reinterpretation in the after code may introduce slight register pressure overhead that offsets the instruction count r
