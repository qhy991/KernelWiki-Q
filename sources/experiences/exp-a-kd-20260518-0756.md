---
id: exp-a-kd-20260518-0756
title: fp8_e4m3_to_bf16
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.879x
---

## Challenge

The after kernel reads uint8_t FP8 E4M3 data (1 byte per element, 262144 bytes total) from global memory and dequantizes each element via software bit-manipulation-based fp8_e4m3_to_float, __float2bfloat16 conversion, and a scale multiply. The before kernel reads bf16 data (2 bytes per element, 5242


## Solution

tal) and copies directly. In this microbenchmark, the workload (512KB bf16) fits within the A800 L2 cache (40MB), so global memory bandwidth is not the bottleneck. The per-element dequantization compute overhead (~6-8 extra instructions: bit extraction, int-as-float reconstruction, float multiply, f

## Results

- **speedup**: 0.879x (regression)


## Key Lessons

- The after kernel reads uint8_t FP8 E4M3 data (1 byte per element, 262144 bytes total) from global memory and dequantizes each element via software bit-manipulation-based fp8_e4m3_to_float, __float2bfloat16 conversion, and a scale multiply.
