---
id: exp-a-kd-20260518-1054
title: float_absmax_aligned_1024
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- register-reuse
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- vectorized-loads
impl_family: custom_cuda
speedup: 0.938x
---

## Challenge

The before code uses a straightforward scalar loop where each thread iterates with stride=blockDim.x, accessing in[i] with naturally coalesced memory access across the 256-thread block. The after code wraps the same scalar lambda into vectorize_read_with_alignment<16>, which first performs a can_vec


## Solution

floats per vector iteration, and immediately decomposes them back into 16 individual scalar_op calls inside DefaultReadVecOp's unrolled loop. This layered approach adds overhead without benefit: the 64-byte vector loads are heavier instructions than 4-byte scalar loads already coalesced by the hardw

## Results

- **speedup**: 0.938x (regression)


## Key Lessons

- For a read-only reduction like absmax, the vector load + decompose pattern cannot outperform a naturally coalesced scalar loop because there is no output write to amortize the vector overhead.
- This layered approach adds overhead without benefit: the 64-byte vector loads are heavier instructions than 4-byte scalar loads already coalesced by the hardware, the reinterpret_cast and vec_n_t wrapper add register pressure and prevent the compiler from generating the simplest strided-access patte
