---
id: exp-o-kd-20260518-0110
title: f32_large
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.185x
---

## Challenge

The after code uses float4 vectorized load/store (FLOAT4 macro) to read/write 4 adjacent floats per thread in a single 128-bit coalesced memory


## Solution

transaction, whereas the before code issues individual 32-bit loads/stores per thread. On a memory-bound elementwise operation like GELU, the

## Results

- **speedup**: 1.185x (improved)


## Key Lessons

- The after code uses float4 vectorized load/store (FLOAT4 macro) to read/write 4 adjacent floats per thread in a single 128-bit coalesced memory transaction, whereas the before code issues individual 32-bit loads/stores per thread.
