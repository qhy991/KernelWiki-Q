---
id: exp-o-kd-20260518-0798
title: . Since rms_norm is memory-latency bound, having fewer concurrent blocks reduced
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Introduce adaptive max_block_size based on num_tokens to increase SM concurrency for large batches.
Problem before: The block size was fixed at `std::min(hidden_size, 1024)`, regardless of the number of tokens being processed. For large batch sizes (many tokens), this meant each 


## Solution

. Since rms_norm is memory-latency bound, having fewer concurrent blocks reduced the GPU's ability to hide memory latency through block-level interleaving.
Applied change: An adaptive max_block_size is introduced: 1024 when num_tokens < 256, and 256 otherwise. The actual block size is `std::min(hidd

## Key Lessons

- Since rms_norm is memory-latency bound, having fewer concurrent blocks reduced the GPU's ability to hide memory latency through block-level interleaving.
