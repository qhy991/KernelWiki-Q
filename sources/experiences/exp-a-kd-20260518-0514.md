---
id: exp-a-kd-20260518-0514
title: small_16rows_topk6
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- shared-memory-optimization
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 1.133x
---

## Challenge

The after kernel eliminates the per-warp inner scanning loop that iterated through ~32 experts per warp with stride WARP_SIZE reads from shared memory. Instead, each thread directly reads its own expert value f


## Solution

rom shared_scores[tid] and participates in warp-level shuffle reduction. This removes the loop overhead (branch, iteration counter, shared memory read stride) and reduces shared memory reads from ~32 per warp p

## Results

- **speedup**: 1.133x (improved)


## Key Lessons

- The after kernel eliminates the per-warp inner scanning loop that iterated through ~32 experts per warp with stride WARP_SIZE reads from shared memory.
