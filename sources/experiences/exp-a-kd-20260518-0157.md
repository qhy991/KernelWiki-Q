---
id: exp-a-kd-20260518-0157
title: gemm_m256_n256_k4096_split8
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.717x
---

## Challenge

The before code uses a single gemm_serial kernel with an 8x8=64-block grid iterating over 128 K-tiles (K=4096/TILE=32). The 64-block grid already occupies roughly 59% of the A800's 108 SMs. The after code splits K into 8 slices of K_len=512 (16 K-tiles each) and launches 8 gemm_partial kernels concu


## Solution

treams, each still using the 64-block grid. Collectively the 8 partials need 512 blocks but only 108 SMs are available, causing SM contention that prevents full 8x parallelism. The GPU scheduler interleaves blocks from different streams rather than running all 8 kernels in parallel, limiting achieva

## Results

- **speedup**: 1.717x (improved)


## Key Lessons

- Collectively the 8 partials need 512 blocks but only 108 SMs are available, causing SM contention that prevents full 8x parallelism.
