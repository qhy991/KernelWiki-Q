---
id: exp-a-kd-20260518-0768
title: scale_load_every_iter
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
- register-reuse
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- register-reuse
impl_family: custom_cuda
speedup: 0.141x
---

## Challenge

The after kernel adds a conditional branch (if k % b_sh_wr_iters == 0) inside the hot loop to load the scale from shared memory only at group boundaries, copying from the register buffer on other iterations. In this microbenchmark, the NVCC compiler at -O3 fully optimizes the before version: since s


## Solution

e same value every iteration, the compiler hoists the shared memory read into a register and reuses it across all 200 iterations, eliminating the actual shared memory access. The after version's conditional branch adds per-iteration instruction overhead (modulo, comparison, branch) that exceeds the 

## Results

- **speedup**: 0.141x (regression)


## Key Lessons

- In this microbenchmark, the NVCC compiler at -O3 fully optimizes the before version: since sh_scales[threadIdx.x % num_groups] reads the same value every iteration, the compiler hoists the shared memory read into a register and reuses it across all 200 iterations, eliminating the actual shared memor
- In the real Marlin kernel, the scale/ZP loads are 128-bit int4 ldsm operations that are significantly heavier than a simple float read, and the register pressure from tensor-core fragments prevents the compiler from hoisting, making the conditional-load optimization beneficial in that context but no
