---
id: exp-a-kd-20260518-0510
title: small_experts_8
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 0.764x
---

## Challenge

The before code distributes 8192 tokens across 1024 threads, giving each thread only 8 tokens to count (tokens_per_thread = CEILDIV(8192, 1024) = 8). The after code uses 256 threads, forcing each thread to process 32 tokens (CEILDIV(8192, 256) = 32). Although the after code saves shared memory (3...


## Solution

the optimization targets GPU resource utilization (allowing concurrent kernels) rather than single-invocation latency.

## Results

- **speedup**: 0.764x (regression)


## Key Lessons

- Although the after code saves shared memory (32 bytes vs 4096 bytes) and eliminates idle warps during initialization, the dominant cost in this kernel is the token-counting loop where each thread iterates over its assigned tokens and performs atomicAdd to shared memory.
