---
id: exp-o-kd-20260518-0161
title: tokens_512
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- rotary-embedding
- register-reuse
- vectorized-loads
kernel_types:
- rotary-embedding
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- vectorized-loads
impl_family: custom_cuda
speedup: 2.100x
---

## Challenge

The after kernel uses reinterpret_cast<int2*> and reinterpret_cast<int*> to perform vectorized memory loads/stores, where each lane executes a single 8-byte (int2) or 4-byte (int) transaction per head iteration. The before kernel uses scalar nv_bfloat16 reads/writes: each lane reads 4 individual bf1


## Solution

arate 2-byte global loads), plus matching scalar stores. The scalar pattern prevents the CUDA memory controller from coalescing these narrow transactions efficiently: 32 lanes each issuing 2-byte reads at addresses spaced by lane_id*4+j creates scattered 2-byte sectors rather than a single contiguou

## Results

- **speedup**: 2.100x (improved)


## Key Lessons

- The scalar pattern prevents the CUDA memory controller from coalescing these narrow transactions efficiently: 32 lanes each issuing 2-byte reads at addresses spaced by lane_id*4+j creates scattered 2-byte sectors rather than a single contiguous 256-byte coalesced transaction.
