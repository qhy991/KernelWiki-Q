---
id: exp-o-kd-20260518-0861
title: Slot for a value that is entirely determined by the scalar_out_t template type
  a
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Remove runtime min_scaling_factor kernel parameter, replaced by compile-time type trait min_scaling_factor<scalar_out_t>::val() accessed inside compute_dynamic_per_token_scales.
Problem before: The min_scaling_factor value was computed on the host side in rms_norm_dynamic_per_tok


## Solution

slot for a value that is entirely determined by the scalar_out_t template type at compile time.
Applied change: The min_scaling_factor runtime parameter is removed from all kernel/device function signatures (3 signatures) and all call sites (4 sites) in this file. The host-side computation block is 

## Key Lessons

- Similarly, qmax inside compute_dynamic_per_token_scales changed from std::numeric_limits<scalar_out_t>::max() to quant_type_max_v<scalar_out_t>, which provides a ROCm-specific specialization for Float8_e4m3fnuz (0x7E=224.0 instead of 0x7F=240.0).
- The performance effect on CUDA is minimal since a single float parameter contributes negligible launch overhead, but the pattern is significant as a best-practice refactoring: type-dependent constants should be compile-time constexprs rather than runtime kernel arguments.
