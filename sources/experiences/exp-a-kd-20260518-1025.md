---
id: exp-a-kd-20260518-1025
title: bf16_gs128_ng4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 0.961x
---

## Challenge

The before kernel reads bf16 input from global memory twice (one pass for absmax via warp-shuffle, another for quantization), while the after kernel copies input to shared memory in one global read and quantizes from shared. F


## Solution

or this workload (4096 groups * 128 elements * 2 bytes = ~1MB total), the A800's 40MB L2 cache holds the entire input between passes, making the second global read an L2 hit rather than a DRAM fetch. The shared-memory approach

## Results

- **speedup**: 0.961x (regression)


## Key Lessons

- For this workload (4096 groups * 128 elements * 2 bytes = ~1MB total), the A800's 40MB L2 cache holds the entire input between passes, making the second global read an L2 hit rather than a DRAM fetch.
