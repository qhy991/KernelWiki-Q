---
id: exp-a-kd-20260518-1403
title: scale_bandwidth
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.989x
---

## Challenge

The before kernel loads each scale as a 2-byte half value via direct global load (scales_fp16[tid]), while the after kernel loads a 1-byte ScaleFP8 struct and converts it to half through a constant-memory LUT lookup (fp8_scale_lut[scales_fp8[tid].bits]). The 1-byte load halves the scale tensor bandw


## Solution

nstant-memory LUT access introduces an extra memory instruction per scale. In this microbenchmark the scale tensor (256*512=131072 elements, 256KB for fp16 vs 128KB for fp8) is small relative to the packed weight tensor (131072*8=1MB) and the dequantization compute (16 half multiplies per block). Th

## Results

- **speedup**: 0.989x (neutral)


## Key Lessons

- The scale bandwidth savings are therefore a small fraction of total kernel traffic and are offset by the LUT access cost, yielding a neutral result.
