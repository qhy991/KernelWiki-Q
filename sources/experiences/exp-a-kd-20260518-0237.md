---
id: exp-a-kd-20260518-0237
title: f32_large
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.121x
---

## Challenge

The after kernel uses float4 (128-bit) vectorized loads/stores via the FLOAT4 macro to process 4 float elements per thread in a single memory transact


## Solution

ion, instead of the before kernel which processes one element per thread with separate 32-bit load/store instructions. This reduces the total number o

## Results

- **speedup**: 1.121x (improved)


## Key Lessons

- The after kernel uses float4 (128-bit) vectorized loads/stores via the FLOAT4 macro to process 4 float elements per thread in a single memory transaction, instead of the before kernel which processes one element per thread with separate 32-bit load/store instructions.
