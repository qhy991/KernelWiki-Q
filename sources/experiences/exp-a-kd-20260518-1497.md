---
id: exp-a-kd-20260518-1497
title: shuffle_required_8elts
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.996x
---

## Challenge

Both before and after code execute the exact same __shfl_xor_sync instruction because THREADS_PER_SF==2 means two threads share each SF vector and must exchange their local maxi


## Solution

ma via warp shuffle. The if constexpr guard evaluates to true at compile time, so the shuffle instruction is generated in both versions. The 0.37% difference (slight regression)

## Results

- **speedup**: 0.996x (neutral)


## Key Lessons

- Both before and after code execute the exact same __shfl_xor_sync instruction because THREADS_PER_SF==2 means two threads share each SF vector and must exchange their local maxima via warp shuffle.
