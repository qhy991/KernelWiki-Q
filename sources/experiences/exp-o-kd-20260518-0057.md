---
id: exp-o-kd-20260518-0057
title: gelu_float
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- epilogue-fusion
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.058x
---

## Challenge

The before source uses erff(scalar / ROOT_TWO_F), invoking a single-precision division instruction. On Ampere GPUs, FP32 division is implemented via the MUFU.DIV SFU instruction with limited throughput (roughly 1/16th of FFMA throughput), while FP32 multiplication maps to a full-throughput FFMA i...


## Solution

The after source replaces the division with erff(scalar * HALF_ROOT_TWO_F), converting the SFU-bound division into a regular FFMA multiply that the compiler can fuse with the surrounding 0.5f * scalar * ... FMA chain. Since the erf call itself still dominates kernel latency, removing the division...

## Results

- **speedup**: 1.058x (improved)


## Key Lessons

- The after source replaces the division with erff(scalar * HALF_ROOT_TWO_F), converting the SFU-bound division into a regular FFMA multiply that the compiler can fuse with the surrounding 0.5f * scalar * ...
