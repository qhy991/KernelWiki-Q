---
id: exp-a-kd-20260518-0231
title: f32_1M
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- elementwise
- vectorized-loads
kernel_types:
- elementwise
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.033x
---

## Challenge

The after code uses float4 128-bit vector loads/stores to process 4 floats per thread, reducing thread count and instruction overhead. However, the scalar before kernel already benefits from GPU coalescing—consecutive threads naturally coalesce their 32


## Solution

-bit accesses into wide memory transactions on A800 (sm_80). For a simple memory-bound elementwise add at 1M elements, the scalar kernel's coalescing is already effective, so the additional vectorization yields only a marginal ~3% throughput gain that f

## Results

- **speedup**: 1.033x (improved)


## Key Lessons

- The kernel is fully memory-bound on both sides, and the A800's L2 cache and memory subsystem handle the coalesced scalar pattern well enough that float4 vectorization does not unlock substantially more bandwidth.
