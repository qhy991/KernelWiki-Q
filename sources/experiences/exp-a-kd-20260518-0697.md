---
id: exp-a-kd-20260518-0697
title: float_block_1024
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 1.001x
---

## Challenge

The after code replaces integer bit-shift (>>5) with float division (/32.f) for warp count calculation. For aligned block sizes that are exact multiples of 32, both approaches compute the same warp count. The float division ad


## Solution

ds one FP instruction, but this is negligible in the context of warp shuffle and shared memory operations. The real value of /32.f is correctness for non-aligned block sizes where >>5 truncates the warp count (e.g., blockDim.x

## Results

- **speedup**: 1.001x (neutral)


## Key Lessons

- The real value of /32.f is correctness for non-aligned block sizes where >>5 truncates the warp count (e.g., blockDim.x=100 gives 100>>5=3 but needs 4 warp sums), while /32.f preserves partial warps via the float comparison (100/32.f=3.125, threadIdx.x < 3.125 allows 4 readers).
- The float division adds one FP instruction, but this is negligible in the context of warp shuffle and shared memory operations.
