---
id: exp-o-kd-20260518-0327
title: float_4layer_8pairs
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 28.087x
---

## Challenge

With 4 layers, the before code issues 64 individual cudaMemcpyAsync calls (4 layers × 8 pairs × 2 for key+value), quadrupling the CPU-side dispatch overhead compared to the 1-layer case. The cumulative per-call overhead now heavily dominates the total time, pushing before_avg to 0.162ms


## Solution

The after code still uses a single kernel launch with a 2D grid of (4, 8) = 32 thread blocks, processing all layer-pair combinations concurrently. The kernel launch overhead is constant regardless of grid size, so the after approach scales efficiently: more layers and pairs increase GPU paralleli...

## Results

- **speedup**: 28.087x (improved)


## Key Lessons

- With 4 layers, the before code issues 64 individual cudaMemcpyAsync calls (4 layers × 8 pairs × 2 for key+value), quadrupling the CPU-side dispatch overhead compared to the 1-layer case.
