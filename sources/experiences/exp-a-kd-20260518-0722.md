---
id: exp-a-kd-20260518-0722
title: scoring_none_8epg
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.006x
---

## Challenge

The before code uses a runtime if(scoring_func == SCORING_SIGMOID) branch that evaluates SCORING_NONE=0, so the branch always falls through to the else path — the sigmoid call is never executed. The after code uses if constexpr to eliminate this branch entirely at compile time, producing a kern


## Solution

el where apply_scoring<SCORING_NONE> is a plain identity pass-through. However, in this isolated microbenchmark the kernel body is too simple (a single-loop scoring+bias over 8 elements per thread) and the branch overhead of one uniform conditional per iteration is negligible compared to the me

## Results

- **speedup**: 1.006x (neutral)


## Key Lessons

- The compiler may also predict the branch correctly since it is warp-uniform.
