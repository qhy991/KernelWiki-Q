---
id: exp-a-kd-20260518-0946
title: fp16_medium_64k
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 0.963x
---

## Challenge

With 64K half-elements (128 KB input + 256 KB float output), both kernels are memory-bound at this size. The scalar before kernel naturally achieves warp-level coalescing on A800 because each warp of 32 threads accesses a contiguous 64-byte segment of h


## Solution

alf values. The vectorized after kernel's vec4_t<__half> load (8 bytes per thread) and float4_t store (16 bytes per thread) do not reduce the number of HBM transactions at warp level — the memory controller already packs the same address range into the

## Results

- **speedup**: 0.963x (regression)


## Key Lessons

- The scalar before kernel naturally achieves warp-level coalescing on A800 because each warp of 32 threads accesses a contiguous 64-byte segment of half values.
