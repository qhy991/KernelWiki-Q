---
id: exp-o-kd-20260518-0281
title: float_topk8_8192rows
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- epilogue-fusion
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.477x
---

## Challenge

Same fusion pattern as the smaller case but with 8192 rows and topk=8, producing a larger output array (65536 float elements). The before code again requires two separate kernel launches and two full global-memory traversals: first rescale_kernel reads/writes all 65536 elements for division, then...


## Solution

The after code's fused kernel traverses the array once, performing both division and multiplication per element in a single read-modify-write cycle. The 1.477x speedup is slightly lower than the 1.579x of the smaller case because at larger scale the kernel launch overhead becomes a smaller fracti...

## Results

- **speedup**: 1.477x (improved)


## Key Lessons

- The 1.477x speedup is slightly lower than the 1.579x of the smaller case because at larger scale the kernel launch overhead becomes a smaller fraction of total execution time, so the dominant savings come from halving the global memory traffic rather than eliminating launch overhead.
- Same fusion pattern as the smaller case but with 8192 rows and topk=8, producing a larger output array (65536 float elements).
