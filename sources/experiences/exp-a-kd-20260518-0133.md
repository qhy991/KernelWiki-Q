---
id: exp-a-kd-20260518-0133
title: warp_exp_allthreads
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

warp_exp_allthreads

## Solution

in the after code likely comes from the extra integer arithmetic (wid * kLinesPerWarp + lane) to compute the row index, versus the direct tid indexing in the before code. In the real fMHA kernel, the benefit of the warp-partitioned pattern is not from reducing exp calls but from eliminating the e...

## Key Lessons

- In the real fMHA kernel, the benefit of the warp-partitioned pattern is not from reducing exp calls but from eliminating the extra __syncthreads() and the m_prime-overwrite pattern that required it, which this microbenchmark does not capture.
