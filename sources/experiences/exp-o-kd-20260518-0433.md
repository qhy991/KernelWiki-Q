---
id: exp-o-kd-20260518-0433
title: mn128_k7168_gs128
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- epilogue-fusion
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.207x
---

## Challenge

The before path includes a separate cudaMemsetAsync call to zero-initialize the output_s_packed buffer before each kernel launch, followed by the quantization kernel. The cudaMemsetAsync fires a device-side memset kernel that writes the entire scales buffer to zero, then the quantization kernel o...


## Solution

The after path eliminates the separate memset entirely: the quantization kernel is launched with an expanded grid covering all TMA-aligned padding groups, and each padding group thread writes a zero byte directly into the output_s_packed buffer at the correct byte offset. This fuses zero-initiali...

## Results

- **speedup**: 1.207x (improved)


## Key Lessons

- These are two separate GPU operations that cannot overlap because the memset must complete before atomicOr can accumulate into the same buffer.
