---
id: exp-a-kd-20260518-1494
title: xor_smem_layout
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
- register-reuse
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- register-reuse
impl_family: custom_cuda
speedup: 0.818x
---

## Challenge

The XOR remapping adds precomputation overhead: the kernel must compute a_sh_wr_trans[a_sh_wr_iters] and a_sh_rd_trans[b_sh_wr_iters][thread_m_blocks] arrays at startup, consuming extra registers and ALU instructions. In this simplified microbenchmark with a


## Solution

single write-barrier-read pattern, the bank conflict savings from XOR are negligible because there are few shared memory accesses per kernel invocation, and the __syncthreads() barrier already hides any conflict stalls. In the full Marlin kernel, the precompu

## Results

- **speedup**: 0.818x (regression)


## Key Lessons

- In this simplified microbenchmark with a single write-barrier-read pattern, the bank conflict savings from XOR are negligible because there are few shared memory accesses per kernel invocation, and the __syncthreads() barrier already hides any conflict stalls.
