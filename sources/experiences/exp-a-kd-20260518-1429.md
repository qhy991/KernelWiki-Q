---
id: exp-a-kd-20260518-1429
title: int8_h1024
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- register-reuse
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 0.993x
---

## Challenge

With hidden_size=1024 and blockDim=1024, each thread processes exactly one element in the RMS, absmax, and quantization loops. The before kernel passes min_scaling_factor as a runtime float parameter (computed on host as epsilon), while


## Solution

the after kernel uses MinScalingFactor<int8_t>::val() as a constexpr trait. The single additional float parameter occupies one kernel launch slot and one register per thread, but at this scale the computation per thread (one load + one multiply for RMS, one load + two multiplies for absmax, one l...

## Results

- **speedup**: 0.993x (neutral)


## Key Lessons

- With hidden_size=1024 and blockDim=1024, each thread processes exactly one element in the RMS, absmax, and quantization loops.
