---
id: exp-o-kd-20260518-0638
title: Een num_keys and num_queries, the subtraction produces an unnecessarily small
  (o
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Fix CausalFromBottomRight getSmallestQueryForKey formula: change key_start - p.num_keys - p.num_queries to key_start - p.num_keys + p.num_queries.
Problem before: The CausalFromBottomRight attention masking formula computed first_query = max(0, key_start - num_keys - num_queries)


## Solution

een num_keys and num_queries, the subtraction produces an unnecessarily small (often negative, clamped to zero) value for most valid configurations, causing incorrect causal masking where the bottom-right diagonal boundary is computed wrongly.
Applied change: Changed the formula to first_query = max

## Key Lessons

- Optimization focus: Fix CausalFromBottomRight getSmallestQueryForKey formula: change key_start - p.num_keys - p.num_queries to key_start - p.num_keys + p.num_queries.
