---
id: exp-a-kd-20260518-1420
title: small_batch_m1
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.858x
---

## Challenge

The after code regresses because the simplified microbenchmark's partial_compute_kernel uses a naive per-thread dot-product loop, not tensor core MMA instructions. With the before config {thread_k=64, thread_n=256, num_threads=256}, each thread processes exactly 1 N-column per K-slice (tid < 256 cov


## Solution

n. With the after config {thread_k=128, thread_n=128, num_threads=256}, only threads 0-127 handle N-columns in the inner loop while threads 128-255 are idle, wasting half the block's compute capacity. The reduced global reduction steps (64 to 32 K-blocks) cannot compensate for this thread underutili

## Results

- **speedup**: 0.858x (regression)


## Key Lessons

- The after code regresses because the simplified microbenchmark's partial_compute_kernel uses a naive per-thread dot-product loop, not tensor core MMA instructions.
