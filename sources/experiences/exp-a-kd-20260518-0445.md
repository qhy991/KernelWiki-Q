---
id: exp-a-kd-20260518-0445
title: prediv_sorted_ids
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- shared-memory-optimization
- pipeline-stages
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- pipeline-stages
impl_family: custom_cuda
---

prediv_sorted_ids

## Solution

nt A matrix row lookups use a simple shared memory read instead of performing integer division on every access. GPU integer division requires multiple instruction cycles (typically ~20 cycles on NVI

## Key Lessons

- The after version pre-computes integer division (sorted_id / top_k) once during the sorted_ids loading phase, storing the results in a separate shared memory array (sh_rd_block_sorted_ids).
- GPU integer division requires multiple instruction cycles (typically ~20 cycles on NVIDIA GPUs), while shared memory reads are ~5 cycles with bank-conflict-free access patterns.
