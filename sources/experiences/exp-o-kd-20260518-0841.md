---
id: exp-o-kd-20260518-0841
title: unknown
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

unknown

## Solution

l performed a full warp reduction plus shared-memory write/read/sync, which is wasteful.
Applied change: Introduced a compile-time maxBlockSize template parameter. When maxBlockSize > WARP_SIZE, shared memory is sized to maxActiveLanes = ceil(maxBlockSize/WARP_SIZE) instead of WARP_SIZE, and the fin

## Key Lessons

- When maxBlockSize > WARP_SIZE, shared memory is sized to maxActiveLanes = ceil(maxBlockSize/WARP_SIZE) instead of WARP_SIZE, and the final warp reduce uses _nextPow2(maxActiveLanes) lanes.
