---
id: exp-a-kd-20260518-0850
title: fp8_fusion_large
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- epilogue-fusion
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.511x
---

## Challenge

In the before code, two separate kernels are launched sequentially: merge_attn_states_kernel writes BF16 output (128-bit uint4 stores) to an intermediate buffer, then quantize_fp8_kernel reads that entire BF16 buffer back and writes FP8 output. This incurs two kernel launches and a full round-tri...


## Solution

The after code fuses FP8 conversion into the merge kernel: each thread computes the merged float32 result and immediately converts it to FP8 using float_to_fp8e4 with the pre-inverted scale, then stores via a narrower uint2 pack (16 bytes of BF16 input per pack, but only 8 bytes of FP8 output per...

## Results

- **speedup**: 1.511x (improved)


## Key Lessons

- This eliminates the intermediate BF16 buffer write/read and the second kernel launch, which is especially significant for the large case (4.2M elements) where the memory traffic saving dominates.
