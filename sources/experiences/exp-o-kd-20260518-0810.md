---
id: exp-o-kd-20260518-0810
title: For extremely long sequences with hundreds of blocks, this caused many redundant
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Replaced nested block-level loop over paged KV-cache blocks with direct per-token address computation using token_to_seq mapping.
Problem before: The original kernel iterated over paged KV-cache blocks within each batch split, calling copy_entry for each token position inside eac


## Solution

For extremely long sequences with hundreds of blocks, this caused many redundant block_table reads and divergent paths across splits.
Applied change: The rewrite uses a token_to_seq mapping array that directly maps each token_id to its batch_id. Each CTA computes the target address for one token by:

## Key Lessons

- This replaces the entire nested block-then-token iteration with a single direct address computation per token.
- Measured impact should be confirmed via point-level validation.
