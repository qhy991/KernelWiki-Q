---
id: exp-a-kd-20260518-1287
title: dyn_h16384
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- vectorized-loads
impl_family: custom_cuda
speedup: 0.765x
---

## Challenge

The after code uses fabsf/fmaxf intrinsics instead of branch-based absmax, BlockReduce<256> instead of <1024>, and vectorize_with_alignment<16> for the quantization pass. While fabsf/fmaxf eliminate warp divergence in the absmax scan, and vectorized loads reduce memory transactions, the 4x reducti


## Solution

on in block size (1024->256) reduces the warp count per block from 32 to 8. The dynamic kernel has two phases (absmax + quantize) separated by __syncthreads(), and the 256-thread block has insufficient latency-hiding capacity in both phases. The BlockReduce<256> uses less shared memory (~1KB vs ~4

## Results

- **speedup**: 0.765x (regression)


## Key Lessons

- The after code uses fabsf/fmaxf intrinsics instead of branch-based absmax, BlockReduce<256> instead of <1024>, and vectorize_with_alignment<16> for the quantization pass.
