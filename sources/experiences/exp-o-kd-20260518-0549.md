---
id: exp-o-kd-20260518-0549
title: float_to_int8_unaligned
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- elementwise
- vectorized-loads
kernel_types:
- elementwise
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.172x
---

## Challenge

The before code processes hidden_size=16384 elements per row with a scalar stride loop. The after code uses DefaultVecOp<4> to process 4 floats per iteration via reinterpret_cast to vec_n_t<float,4>*, enabling 128-bit wide load/store instru


## Solution

ctions. With a larger hidden size, each thread processes more vector elements (16384/4=4096 vectors per row vs 256 threads = 16 vectors per thread), giving the GPU more opportunity to overlap wide memory transactions and hide latency. The w

## Results

- **speedup**: 1.172x (improved)


## Key Lessons

- The wider vectorized accesses also improve L2 cache efficiency since each 128-byte cache line is fully consumed rather than being loaded for just 4 bytes per transaction, producing a stronger 17.2% speedup compared to the smaller hidden_size case.
