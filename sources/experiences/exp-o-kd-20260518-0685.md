---
id: exp-o-kd-20260518-0685
title: BK=16 iteration loop (which performs 8x16=128 FMAs per thread per BK step)
  and t
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Replace synchronous FLOAT4 global-to-shared loads for B with cp.async.ca asynchronous copies in the main loop body (8x16 tile, BM=128, BN=256, 256 threads, 16 floats per thread for B).
Problem before: In the 8x16 SGEMM main loop (BM=128, BN=256, 256 threads), each thread synchron


## Solution

BK=16 iteration loop (which performs 8x16=128 FMAs per thread per BK step) and the A-tile online transpose.
Applied change: Issue 4 cp.async.ca instructions for the next B tile at the start of the loop body, commit the group, then proceed with A loading, the BK=16 compute loop (8 registers for A, 16

## Key Lessons

- The synchronous load prevents overlap with the compute-heavy BK=16 iteration loop (which performs 8x16=128 FMAs per thread per BK step) and the A-tile online transpose.
