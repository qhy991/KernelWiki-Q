---
id: exp-a-kd-20260518-1051
title: float_aligned_4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- register-reuse
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- vectorized-loads
impl_family: custom_cuda
speedup: 0.879x
---

## Challenge

The after kernel replaces the before kernel's 16-iteration scalar loop (256 threads, stride=256, 4096/16=16 iterations each) with vectorize_read_with_alignment<16> that takes the fast-path for aligned data, loading one 128-byte vec_n_t<float,16> vector per thread iteration and unrolling it via Defau


## Solution

alar MinMax operator+= calls through a __device__ lambda functor. This indirect calling pattern through the DefaultReadVecOp struct and lambda increases register pressure: each thread must hold the 16-element vector struct, the MinMax accumulator, and lambda capture state simultaneously. NVCC may no

## Results

- **speedup**: 0.879x (regression)


## Key Lessons

- The after kernel replaces the before kernel's 16-iteration scalar loop (256 threads, stride=256, 4096/16=16 iterations each) with vectorize_read_with_alignment<16> that takes the fast-path for aligned data, loading one 128-byte vec_n_t<float,16> vector per thread iteration and unrolling it via Defau
- This indirect calling pattern through the DefaultReadVecOp struct and lambda increases register pressure: each thread must hold the 16-element vector struct, the MinMax accumulator, and lambda capture state simultaneously.
