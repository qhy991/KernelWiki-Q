---
id: exp-a-kd-20260518-1483
title: contiguous_gemm_1024x512x128
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: cutlass
speedup: 0.859x
---

## Challenge

For M=1024, N=512, K=128, both programs compute identical stride values (lda=128, ldb=512, ldc=512). The before code assigns lda=K_VAL, ldb=N_VAL, ldc=N_VAL directly from tensor stride constants, while the after code derives them via packed_stride_from_shape(M,K)=K, packed_stride_from_shape(K,N)=N, 


## Solution

th identical stride parameters in both versions. Since stride values are identical and the kernel code is identical, the execution paths are identical at the hardware level. The measured 16% regression is attributable to sub-millisecond timing noise and GPU scheduling variability (runs execute seque

## Results

- **speedup**: 0.859x (regression)


## Key Lessons

- Since stride values are identical and the kernel code is identical, the execution paths are identical at the hardware level.
