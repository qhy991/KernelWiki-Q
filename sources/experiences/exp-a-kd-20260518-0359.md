---
id: exp-a-kd-20260518-0359
title: f32_N1M
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.103x
---

## Challenge

The after code uses float4 vectorized loads/stores, processing 4 floats per thread instead of 1. This reduces the total thread co


## Solution

unt by 4x and replaces 4 separate 32-bit memory transactions with a single 128-bit coalesced transaction. For a memory-bound elem

## Results

- **speedup**: 1.103x (improved)


## Key Lessons

- The after code uses float4 vectorized loads/stores, processing 4 floats per thread instead of 1.
- For a memory-bound elementwise kernel like swish at N=1M, the wider memory access pattern better saturates the memory bus, reducing per-element latency.
