---
id: exp-o-kd-20260518-0683
title: Preventing overlap with the BK=16 compute iterations and A-tile transpose of
  the
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Replace synchronous FLOAT4 global-to-shared loads for B with cp.async.ca asynchronous copies in the main loop body (8x8 tile, BM=128, BN=128, 256 threads).
Problem before: In the 8x8 SGEMM main loop (BM=128, BN=128, 256 threads), B-tile loading for the next iteration is synchrono


## Solution

preventing overlap with the BK=16 compute iterations and A-tile transpose of the current iteration.
Applied change: Issue cp.async.ca instructions for the next B tile at the start of the loop body, commit the group, then proceed with A loading, BK compute, and A transpose while the B async copies ar

## Key Lessons

- Optimization focus: Replace synchronous FLOAT4 global-to-shared loads for B with cp.async.ca asynchronous copies in the main loop body (8x8 tile, BM=128, BN=128, 256 threads).
