---
id: exp-a-kd-20260518-1448
title: offset_zero
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
- pipeline-stages
- register-reuse
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- register-reuse
impl_family: custom_cuda
---

offset_zero

## Solution

in the after kernel (sorted_token_ids[sorted_token_ids_offset + it], cumsum[cumsum_offset + expert_id], expert_ids[expert_ids_offset + i], total_tokens_post_pad[model_offset]) performs an extra integer addition before the address calculation, and these additions propagate into the address generat...

## Key Lessons

- Even though model_offset=0 makes all offsets zero and the resulting addresses are identical to the before kernel's direct indexing, the compiler cannot fully eliminate the offset additions at compile time because model_offset is a runtime kernel parameter.
