---
id: exp-a-kd-20260518-1182
title: diff_scale_8h128d
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- pipeline-stages
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
speedup: 1.001x
---

## Challenge

In the diff_scale_8h128d case, the after kernel uses k_scale=4.0/127 (optimal for key range [-4,4]) and v_scale=0.5/127 (optimal for value range [-0.5,0.5]), while the before kernel uses a shared kv_scale=4.0/127 that must accommodate the wider key range. The execution pattern is id


## Solution

entical between before and after: both perform roundf, fmaxf/fminf clamp, and multiply — the same instruction count and memory bandwidth. The only difference is the scale constant passed to the arithmetic, which does not affect pipeline throughput or latency. The kernel runtime is t

## Results

- **speedup**: 1.001x (neutral)


## Key Lessons

- The kernel runtime is therefore expected to be neutral.
- In the diff_scale_8h128d case, the after kernel uses k_scale=4.0/127 (optimal for key range [-4,4]) and v_scale=0.5/127 (optimal for value range [-0.5,0.5]), while the before kernel uses a shared kv_scale=4.0/127 that must accommodate the wider key range.
