---
id: exp-a-kd-20260518-1289
title: dynazp_h16384
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- vectorized-loads
impl_family: custom_cuda
speedup: 0.892x
---

## Challenge

The after code merges two sequential BlockReduce<float,1024> calls into a single BlockReduce<MinMax,256> and adds vectorize_with_alignment<16> for the quantization pass. For hidden_size=16384 with 128 tokens, each block processes 16384 elements. The before version uses 1024 threads with two scalar r


## Solution

__syncthreads barrier between), while the after uses 256 threads with a single MinMax reduction. Although the single-pass MinMax reduction eliminates one __syncthreads() and one full BlockReduce traversal, the 4x reduction in threads per block (32 warps to 8 warps) reduces the SM's ability to hide m

## Results

- **speedup**: 0.892x (regression)


## Key Lessons

- Although the single-pass MinMax reduction eliminates one __syncthreads() and one full BlockReduce traversal, the 4x reduction in threads per block (32 warps to 8 warps) reduces the SM's ability to hide memory latency during the two-phase scan+quantize workflow.
