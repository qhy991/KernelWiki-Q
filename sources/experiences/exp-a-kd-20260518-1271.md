---
id: exp-a-kd-20260518-1271
title: fp16_hidden4096_tokens64_block1024
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.995x
---

## Challenge

As expected for the control case: the before code uses block_size=min(4096,1024)=1024 (line 106 of before source), and the after code computes max_block_size=(64<256)?1024:256=1024 (line 112 of after source), yielding the identical block_size=1024. Both kernels launch 64 blocks of 1024


## Solution

threads, each thread looping 4 times. The only code difference is the after kernel's blockReduceSum dispatch (lines 62-66): since num_tokens=64 < 256, it takes the blockReduceSum<float,1024> branch, which is the same specialization the before code's default template parameter produces.

## Results

- **speedup**: 0.995x (neutral)


## Key Lessons

- The only code difference is the after kernel's blockReduceSum dispatch (lines 62-66): since num_tokens=64 < 256, it takes the blockReduceSum<float,1024> branch, which is the same specialization the before code's default template parameter produces.
