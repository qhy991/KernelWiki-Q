---
id: exp-a-kd-20260518-0043
title: pipeline_gemm_2048
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- shared-memory-optimization
- pipeline-stages
kernel_types:
- gemm
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- pipeline-stages
impl_family: cutlass
---

pipeline_gemm_2048

## Solution

ed loops over BK with scalar FMAs) is not heavy enough to hide the global memory load latency behind it, so the pipeline overlap provides no measurable benefit. Additionally, the double-buffered after kernel uses 2x shared memory (two stages of sA[BM][BK] and sB[BK][BN]), which can reduc

## Key Lessons

- The before code uses a single-stage shared memory buffer with serialized load-then-compute within each K iteration, while the after code uses double-buffered shared memory (2 stages) to overlap global loads with computation.
