---
id: exp-a-kd-20260518-0731
title: uint16_cast_to_float
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: inline_asm
speedup: 0.997x
---

## Challenge

The before source file uses __half2float(__ushort_as_half(half_values[i])) for half-to-float conversion, which is CUDA's built-in FP16-to-FP32 conversion routine. The after source file uses cast_to_float(uint16_t) which delegates to the custom half_to_float(uint16_t) that uses inline PTAS asm volati


## Solution

compile to the identical hardware instruction: the GPU's cvt.f32.f16 FP16-to-FP32 conversion, which is a single-cycle operation on modern NVIDIA architectures (sm_80+). The before path's __half2float internally uses the same PTAS cvt.f32.f16 instruction. Since nvcc fully inlines __half2float and hal

## Results

- **speedup**: 0.997x (neutral)


## Key Lessons

- Since nvcc fully inlines __half2float and half_to_float, the generated SASS is identical.
