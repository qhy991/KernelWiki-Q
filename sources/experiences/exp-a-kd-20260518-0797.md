---
id: exp-a-kd-20260518-0797
title: fp8_per_token_quant_h2048
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.987x
---

## Challenge

The after code replaces per-element multiplication (token_input[i] * inverted_scale) with division (token_input[i] / token_scale) in the quantization loop. With hidden_size=2048 and 1024 threads, each thread processes only 2 elements per loop, yielding a lo


## Solution

w compute-to-memory ratio. The kernel is primarily memory-bound since each element requires one float read and one float write alongside the arithmetic operation, so the latency difference between FP32 multiplication and division is largely hidden by memory

## Results

- **speedup**: 0.987x (neutral)


## Key Lessons

- The kernel is primarily memory-bound since each element requires one float read and one float write alongside the arithmetic operation, so the latency difference between FP32 multiplication and division is largely hidden by memory access stalls.
