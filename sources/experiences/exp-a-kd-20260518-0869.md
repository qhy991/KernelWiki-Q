---
id: exp-a-kd-20260518-0869
title: large_64experts
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- shared-memory-optimization
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 1.000x
---

## Challenge

The before version reads expert_map_ptr directly from global memory (one int32 load per thread), while the after version loads expert_map into shared memory cooperatively and then reads from smem. With only 64 int32 elements (256 bytes),


## Solution

the expert_map fits entirely in L1 cache on sm_80 (128KB L1 per SM), so the global memory load hits L1 with near-zero latency. The shared-memory path adds a cooperative load loop and __syncthreads() barrier, which offset any lookup-late

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- The before version reads expert_map_ptr directly from global memory (one int32 load per thread), while the after version loads expert_map into shared memory cooperatively and then reads from smem.
