---
id: exp-a-kd-20260518-0825
title: large_m_tile128k
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- shared-memory-optimization
kernel_types:
- gemm
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: cutlass
speedup: 0.762x
---

## Challenge

The after kernel uses TILE_K=128 versus TILE_K=64, with the same structural analysis as medium_m_tile64. Doubling the K-tile increases shared memory per iteration and cooperative loading per thread, but halves the number of K itera


## Solution

tions. In the simplified kernel, the net effect is slightly slower because the cooperative loading overhead scales linearly with TILE_K while the sync barrier reduction only scales by the iteration count ratio. The actual CUTLASS o

## Results

- **speedup**: 0.762x (regression)


## Key Lessons

- In the simplified kernel, the net effect is slightly slower because the cooperative loading overhead scales linearly with TILE_K while the sync barrier reduction only scales by the iteration count ratio.
