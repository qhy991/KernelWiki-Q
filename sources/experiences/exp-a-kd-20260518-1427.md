---
id: exp-a-kd-20260518-1427
title: fp8_scale_hidden256
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.002x
---

## Challenge

The before source defines a local float const min_scaling_factor = 1.0f / (fp8_e4m3_adjusted_max_v<fp8_type> * 512.f) at kernel entry, while the after source replaces this with a template struct call min_scaling_factor<fp8_type>::val() annotated with C10_ALWAYS_INLINE (__forceinline__). Both comp...


## Solution

after code could be faster.

## Results

- **speedup**: 1.002x (neutral)


## Key Lessons

- Since the arithmetic values and dataflow are semantically identical, and the __forceinline__ annotation on a constexpr-valued static method does not change what the compiler generates versus a local const float, there is no mechanism by which the after code could be faster.
