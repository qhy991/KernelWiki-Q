---
id: exp-a-kd-20260518-1121
title: half_d512
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

half_d512

## Solution

t instead of 4, giving 2x effective bandwidth advantage over float, which reduces the relative impact of any cache-path routing. On Ampere, the compiler again auto-optimizes const __restrict__ loads

## Key Lessons

- Half-precision loads already transfer 2 bytes per element instead of 4, giving 2x effective bandwidth advantage over float, which reduces the relative impact of any cache-path routing.
- The half-precision source pair follows the same pattern: before uses plain half-element loads (input[...]) and after uses __ldg(&input[...]).
