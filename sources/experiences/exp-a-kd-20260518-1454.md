---
id: exp-a-kd-20260518-1454
title: float_8h_128hs
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
---

float_8h_128hs

## Solution

r head-block (head_size/x = 16). Each thread uses vectorize_with_alignment<4> for key writes (4-float vector = 128-bit transactions) and an unrolled 8-iteration loop for value writes. However, with only 64 threads per block and 128 blocks, the total active thread count per SM drops dramatically — 8x

## Key Lessons

- The after kernel launches only 64 threads (min(8*16, 512)) per block because it assigns one thread per head-block (head_size/x = 16).
- Each thread uses vectorize_with_alignment<4> for key writes (4-float vector = 128-bit transactions) and an unrolled 8-iteration loop for value writes.
