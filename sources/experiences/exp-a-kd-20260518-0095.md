---
id: exp-a-kd-20260518-0095
title: epilogue_inline_vs_smem
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- shared-memory-optimization
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
---

epilogue_inline_vs_smem

## Solution

The after code applies scale/bias once in a centralized epilogue pass (M*K1=4096 operations) and writes the transformed values to __shared__ memory. GEMM1 then reads from shared memory, which has ~10x lower latency than global memory, and the inner loop has fewer FLOPs per iteration (no scale/bia...

## Key Lessons

- The before code reads the accumulator from global memory inside the GEMM1 K1 loop and applies scale/bias inline for every (r, c, kk) combination, causing M*N*K1=262144 global memory accesses plus scale/bias FLOPs.
