---
id: exp-o-kd-20260518-0168
title: fuse_rescale_topk6_bs2048
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
- register-reuse
- epilogue-fusion
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.438x
---

## Challenge

The before code uses two separate CUDA kernels (normalize_kernel and scale_kernel) that each perform a full global-memory read+write pass over the output array. The first kernel divides each element by its row sum; the second kernel multiplies by the scaling factor. This requires two kernel launc...


## Solution

The after code consolidates both operations into one fused_rescale_kernel where each thread performs output[idx] / row_sums[row] followed immediately by output[idx] *= routed_scaling_factor on the same register-held value. This eliminates one entire global memory round-trip per element and one ke...

## Results

- **speedup**: 1.438x (improved)


## Key Lessons

- This eliminates one entire global memory round-trip per element and one kernel launch, reducing both memory traffic and dispatch overhead.
