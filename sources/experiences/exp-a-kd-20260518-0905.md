---
id: exp-a-kd-20260518-0905
title: contiguous_float
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.981x
---

## Challenge

When input_stride equals hidden_size (contiguous layout), both the before and after scalar rms_norm kernels compute identical element addresses: blockIdx.x * hidden_size + idx versus blockIdx.x * input_stride + idx yield the same offset


## Solution

The after kernel adds an input_stride parameter to the signature but this does not change memory access patterns, computational work, or instruction count within the kernel body. The measured ~1.9% difference is within timing noise, confirming no optimization opportunity when the tensor is alread...

## Results

- **speedup**: 0.981x (neutral)


## Key Lessons

- The after kernel adds an input_stride parameter to the signature but this does not change memory access patterns, computational work, or instruction count within the kernel body.
