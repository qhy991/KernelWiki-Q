---
id: exp-a-kd-20260518-0817
title: smem_overlap_balanced
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
- register-reuse
- epilogue-fusion
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- register-reuse
- epilogue-fusion
- vectorized-loads
impl_family: custom_cuda
speedup: 0.808x
---

## Challenge

The after kernel accumulates in fp32 (__half2float conversion per multiply-add, float registers) and fuses a bias vector loaded from shared memory, while the before kernel accumulates entirely in fp16 (__hadd/__hmul on half registers) with no bias work. The fp16 before path has fewer conversion inst


## Solution

yielding lower per-kernel latency. The observed regression is expected: the after kernel does more arithmetic work (float multiply-add instead of half multiply-add, plus bias addition) for a mathematically richer result (C = A*B + bias with fp32 precision). The overlap technique saves 256 bytes of s

## Results

- **speedup**: 0.808x (regression)


## Key Lessons

- The observed regression is expected: the after kernel does more arithmetic work (float multiply-add instead of half multiply-add, plus bias addition) for a mathematically richer result (C = A*B + bias with fp32 precision).
