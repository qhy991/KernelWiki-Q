---
id: exp-o-kd-20260518-0382
title: fp8_fusion_small
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- pipeline-stages
- epilogue-fusion
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- epilogue-fusion
impl_family: custom_cuda
---

fp8_fusion_small

## Solution

the after code fuses FP8 quantization into the merge kernel. The speedup is smaller (1.28x vs 1.51x) because the small case has fewer total elements (262K), so kernel launch overhead and memory traffic constitute a smaller fraction of total execution time, and the two-kernel pipeline is already c...

## Key Lessons

- The speedup is smaller (1.28x vs 1.51x) because the small case has fewer total elements (262K), so kernel launch overhead and memory traffic constitute a smaller fraction of total execution time, and the two-kernel pipeline is already completing quickly due to the smaller working set fitting better 
- The same fusion mechanism applies as the large case: the before code launches two kernels (merge + quantize) with an intermediate BF16 buffer, while the after code fuses FP8 quantization into the merge kernel.
