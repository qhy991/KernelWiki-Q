---
id: exp-a-kd-20260518-0446
title: shmem_overlap_tiled
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- shared-memory-optimization
- pipeline-stages
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- pipeline-stages
impl_family: custom_cuda
speedup: 1.081x
---

## Challenge

The before and after kernels use identical computation logic (the same tiled_gemm_kernel function with 16x16 tile tiling over K). The sole difference is the dynamic shared memory allocation size: before requests 41984 bytes (non-overlapped budget where block_meta gets tb_m*4*2 and sh_b/sh_red are...


## Solution

the after budget allows at least 4 blocks (4*32896=131584 < 167912). The higher occupancy means more thread blocks execute concurrently per SM, improving pipeline utilization and reducing per-invocation latency by 7.49%.

## Results

- **speedup**: 1.081x (improved)


## Key Lessons

- With 167KB shared memory per SM, the before budget allows at most 3 concurrent blocks per SM (3*41984=125952 < 167912), while the after budget allows at least 4 blocks (4*32896=131584 < 167912).
