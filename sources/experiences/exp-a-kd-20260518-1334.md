---
id: exp-a-kd-20260518-1334
title: experts_8
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 2.051x
---

## Challenge

The after code replaces the cooperative shared-memory load + __syncthreads barrier + serial lane-0 accumulation loop (iterating up to 8 experts) + __shfl_sync broadcast with per-thread __ldg reads from pre-computed alig


## Solution

ned offsets. With only 8 local experts, the serial loop is short and the shared memory footprint is small (9*8=72 bytes), so the improvement is modest. The main gains come from removing the __syncthreads barrier (which

## Results

- **speedup**: 2.051x (improved)


## Key Lessons

- The after code replaces the cooperative shared-memory load + __syncthreads barrier + serial lane-0 accumulation loop (iterating up to 8 experts) + __shfl_sync broadcast with per-thread __ldg reads from pre-computed aligned offsets.
