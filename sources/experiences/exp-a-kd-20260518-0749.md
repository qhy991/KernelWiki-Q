---
id: exp-a-kd-20260518-0749
title: shm_8bit_m16_k128
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.966x
---

## Challenge

Reading the before/shm_8bit_m16_k128_before.cu source, the compute_shm_before function uses sh_block_meta_size = tb_m * 4 (only 4 bytes per row for a single float scale) and sh_a_size = PIPE_STAGES * (tb_m * tb_k) * 2 (always 2 bytes per element regardless of activation type). Reading after/shm_8bit


## Solution

6 (4 floats per row for activation scale, sub-scale, and quantization descriptors) and sh_a_size = PIPE_STAGES * (tb_m * tb_k) * (is_a_8bit ? 1 : 2), correctly using 1 byte per element for 8-bit activations. For the tested W4A8 configuration (is_a_8bit=true), the A-tile savings from 2 to 1 byte per 

## Results

- **speedup**: 0.966x (regression)


## Key Lessons

- This allows 7 blocks/SM (vs 6 before), but the proxy kernel's grid-stride loop distributes only 128 row-tiles across 648 or 756 total blocks, leaving most blocks idle.
