---
id: exp-a-kd-20260518-0925
title: small_m_4
experience_type: analysis
source_category: agent-experiment
architectures:
- sm120
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: cublas
speedup: 1.024x
---

## Challenge

The before code performs a direct (4,4096)x(4096,4096) GEMM with CUBLAS_OP_N, where M=4 is far below any tile dimension. The after code swaps operands via CUBLAS_OP_T to compute (4096,4096)x(4096,4), transforming M=4 into the N dimension. On SM80, cuBLAS internally handles b


## Solution

oth small-M and the transposed form with similar optimized paths, resulting in nearly identical latency dominated by kernel launch overhead and the minimal arithmetic for such a tiny output (4x4096=16K elements). The ~2.4% measured difference is within benchmark noise and do

## Results

- **speedup**: 1.024x (improved)


## Key Lessons

- On SM120 with CUTLASS, the compile-time tile shape selection would make this swap significantly beneficial because TILE_M=128 would cover the full swapped dimension instead of wasting most of the 64-wide tile on only 4 active rows.
