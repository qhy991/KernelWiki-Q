---
id: exp-a-kd-20260518-0122
title: float_scalar
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: inline_asm
speedup: 1.000x
---

## Challenge

The before code manually puns float to int32_t via memcpy, calls the int32_t warp_uniform, then puns back. The after code uses a templated warp_uniform<T> with an anonymous union { T value; uint3


## Solution

2_t asInt; } to achieve the same type punning inline. Both paths compile down to a single SHFL.U32 PTX instruction — the union and memcpy approaches are equivalent at the PTX level because nvcc o

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- Both paths compile down to a single SHFL.U32 PTX instruction — the union and memcpy approaches are equivalent at the PTX level because nvcc optimizes away the intermediate copy.
