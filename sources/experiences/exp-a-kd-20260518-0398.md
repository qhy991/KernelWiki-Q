---
id: exp-a-kd-20260518-0398
title: nonpow2_7ep_2topk
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
- register-reuse
- epilogue-fusion
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.174x
---

## Challenge

The after kernel fuses renormalization directly into the moeTopK loop by accumulating row_sum_for_renormalize during the top-K selection iterations and then dividing each weight by this sum in a post-loop block. This eliminates a separate renormalize kernel lau


## Solution

nch and its associated global memory round-trip for reading back the top-K weights. The fused version keeps the renormalize computation in registers (threadIdx.x==0 holds the accumulated sum) and re-reads only the k output slots from global memory already writt

## Results

- **speedup**: 1.174x (improved)


## Key Lessons

- This eliminates a separate renormalize kernel launch and its associated global memory round-trip for reading back the top-K weights.
