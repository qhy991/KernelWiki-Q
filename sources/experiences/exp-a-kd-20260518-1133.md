---
id: exp-a-kd-20260518-1133
title: topk2_epg8
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.027x
---

## Challenge

For num_experts_per_group=8, each warp handles one group with 8 elements across 8 lanes. The before code's __shfl_xor_sync loop iterates 5 times (offsets 16,8,4,2,1) and the after code's cg::reduce does the same number of warp shuffle steps internally. Both find max1 in O(log 32) shuffles and max...


## Solution

in the after code is within measurement noise for single-warp microbenchmarks at this scale. The core insight — using warp-level reduction with ballot_sync uniqueness checking instead of a full sort — is present in both implementations, making the API difference (manual shfl_xor_sync vs cg::reduc...

## Results

- **speedup**: 1.027x (improved)


## Key Lessons

- The core insight — using warp-level reduction with ballot_sync uniqueness checking instead of a full sort — is present in both implementations, making the API difference (manual shfl_xor_sync vs cg::reduce) inconsequential for performance.
