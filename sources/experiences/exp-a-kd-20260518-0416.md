---
id: exp-a-kd-20260518-0416
title: push_signal_rw
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- elementwise
kernel_types:
- elementwise
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.152x
---

## Challenge

The volatile-read-write benchmark removes __syncthreads() per cycle to isolate memory throughput. The before code's volatile TestSemaphore struct places m_counter at offset 4 within each 128-byte-aligned slot, giving every block a unique pair of cache lines. Volatile loads and stores by thread 0 ...


## Solution

The after code's volatile uint32_t array packs 16 consecutive blocks' signals into one 64-byte cache line. Each volatile write by thread 0 (v_sig[blockIdx.x] = ...) broadcasts an invalidation to every other SM that has cached that line, forcing the next volatile read (v_sig[blockIdx.x]) on those ...

## Results

- **speedup**: 0.152x (regression)


## Key Lessons

- The volatile-read-write benchmark removes __syncthreads() per cycle to isolate memory throughput.
