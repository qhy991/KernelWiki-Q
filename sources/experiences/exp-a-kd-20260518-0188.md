---
id: exp-a-kd-20260518-0188
title: gemm_like_warp_select
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.032x
---

## Challenge

In this GEMM-like kernel, the inner loop body is dominated by global-memory loads of A and B matrices (lines 33-41 in both before and after). The __shfl_sync in the before version adds one SHFL instruction per tile iterat


## Solution

ion, but that overhead is negligible compared to the 32 global-memory loads per tile (K=128, stride=4). The arithmetic intensity and memory access patterns are identical between before and after; the only difference is th

## Results

- **speedup**: 1.032x (improved)


## Key Lessons

- Because the shuffle cost is amortized over heavy memory operations, the ~3% observed delta falls within typical measurement noise, making this case neutral.
- The arithmetic intensity and memory access patterns are identical between before and after; the only difference is the removal of one warp-shuffle barrier per 10,000-tile iteration.
