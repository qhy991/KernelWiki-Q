---
id: exp-a-kd-20260518-0999
title: clone_always_small
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.237x
---

## Challenge

In the before source (clone_always_small_before.cu), each iteration copies 2048 ints (8 KB) from d_topk_ids to d_cloned_topk_ids via cudaMemcpy before the sort. For this small batch, the CUB radix sort on 2048 key-value pairs is very fast, so the fixed overhead of the cudaMemcpy call (API dispatc...


## Solution

the after source (clone_always_small_after.cu), the clone is skipped entirely because sortAndScanExpert's const int* parameter accepts d_topk_ids directly. The 19% speedup reflects how removing the memcpy overhead is proportionally more significant when the computational kernel is lightweight, as...

## Results

- **speedup**: 1.237x (improved)


## Key Lessons

- In the after source (clone_always_small_after.cu), the clone is skipped entirely because sortAndScanExpert's const int* parameter accepts d_topk_ids directly.
