---
id: exp-a-kd-20260518-0838
title: bf16_reduce_direct
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- pipeline-stages
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
speedup: 2.932x
---

## Challenge

The before kernel (direct_reduce_kernel_bf16) processes bf16 data one scalar element at a time, loading a single 2-byte nv_bfloat16 per memory transaction and accumulating via __hadd in bf16 precision. Each iteration requires a scalar load from d_src_ptrs_bf16[g][i], a half-precision add, and a s...


## Solution

The after kernel (upcast_reduce_kernel_bf16) uses the packed array_t<nv_bfloat16,8> type with __align__(16), loading 8 bf16 values (16 bytes) per ld.128 instruction, upcasting all 8 to float32 via __bfloat162float in an unrolled loop, accumulating in array_t<float,8> using native fp32 add, and do...

## Results

- **speedup**: 2.932x (improved)


## Key Lessons

- The fp32 accumulation also provides numerically superior results (closer to the CPU fp32 reference) compared to the direct bf16 __hadd path, though the measured speedup is dominated by the memory throughput improvement rather than the arithmetic path.
