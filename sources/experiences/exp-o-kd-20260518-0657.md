---
id: exp-o-kd-20260518-0657
title: H` that makes `kBlockSizeI = 128`, which yields `kNumWarpsPerBlock = 8` even
  for
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Added a max-head-dimension dispatch layer that specializes `AttentionBackwardKernel` with `kMaxK=64`, `128`, or unbounded, instead of always using the generic template.
Problem before: The old launch path instantiated `AttentionBackwardKernel` without any bound on the maximum hea


## Solution

h` that makes `kBlockSizeI = 128`, which yields `kNumWarpsPerBlock = 8` even for small `K/V` dimensions that only need a 64x64 tile.
Applied change: The change computes `maxK = max(query.size(2), value.size(2))` at runtime, dispatches it into compile-time buckets `{64, 128, inf}`, and threads that b

## Key Lessons

- Optimization focus: Added a max-head-dimension dispatch layer that specializes `AttentionBackwardKernel` with `kMaxK=64`, `128`, or unbounded, instead of always using the generic template.
