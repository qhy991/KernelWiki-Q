---
id: exp-o-kd-20260518-0270
title: float_h128_std
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
- register-reuse
- epilogue-fusion
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- epilogue-fusion
impl_family: custom_cuda
---

float_h128_std

## Solution

the after code, the fused kernel keeps normalized values in the thread-local register array e[EPT] after the RMSNorm phase (line 70: e[i] *= nf * w[lane*EPT+i]), then immediately applies standard RoPE on the same register-held values (lines 78-80: e[i] = x*c - y*s, e[i+1] = y*c + x*s) before writ...

## Key Lessons

- In the after code, the fused kernel keeps normalized values in the thread-local register array e[EPT] after the RMSNorm phase (line 70: e[i] *= nf * w[lane*EPT+i]), then immediately applies standard RoPE on the same register-held values (lines 78-80: e[i] = x*c - y*s, e[i+1] = y*c + x*s) before writ
