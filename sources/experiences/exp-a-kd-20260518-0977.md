---
id: exp-a-kd-20260518-0977
title: lop3_dequant
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- pipeline-stages
- register-reuse
- epilogue-fusion
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- register-reuse
- epilogue-fusion
impl_family: inline_asm
speedup: 0.964x
---

## Challenge

The naive dequantization in lop3_dequant_before.cu extracts 4-bit nibbles via shift/mask, sign-extends, subtracts the zero point (8.0f), and converts each value to fp16 via __floats2half2_rn. The LOP3-based dequantization in lop3_dequant_after.cu replaces shift/mask/OR with two lop3.b32 instructions


## Solution

raction and fp16 exponent injection, then applies __hsub2 and __hfma2 with magic constants. In this isolated microbenchmark, both approaches achieve nearly identical throughput (0.964x, delta=3.75%) because the nvcc compiler heavily optimizes the naive path: it recognizes the shift/mask/subtract pat

## Results

- **speedup**: 0.964x (regression)


## Key Lessons

- In this isolated microbenchmark, both approaches achieve nearly identical throughput (0.964x, delta=3.75%) because the nvcc compiler heavily optimizes the naive path: it recognizes the shift/mask/subtract pattern and maps it to efficient PTX.
- The LOP3-based dequantization in lop3_dequant_after.cu replaces shift/mask/OR with two lop3.b32 instructions that fuse nibble extraction and fp16 exponent injection, then applies __hsub2 and __hfma2 with magic constants.
