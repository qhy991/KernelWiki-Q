---
id: exp-a-kd-20260518-0117
title: split_keys
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- pipeline-stages
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
speedup: 4.533x
---

## Challenge

In the before code, the kernel launches with grid(1, 8) — a single block per (batch,head) pair that serially iterates over all 32 key blocks (2048/64) via `for (kb = 0; kb < num_key_blocks; ++kb)`. In the after code, the kernel launches with grid(4, 8) — 4 blocks per (batch,head), where each block s


## Solution

* kBlockSizeJ` and strides by `num_splits_key * kBlockSizeJ`, processing only 8 key blocks instead of 32. The 4x increase in concurrent blocks directly maps to 4x more SM utilization. The ~4.53x measured speedup slightly exceeds the 4x theoretical split because the before version's single block also

## Results

- **speedup**: 4.533x (improved)


## Key Lessons

- In the after code, the kernel launches with grid(4, 8) — 4 blocks per (batch,head), where each block starts at `split_key_idx * kBlockSizeJ` and strides by `num_splits_key * kBlockSizeJ`, processing only 8 key blocks instead of 32.
- In the before code, the kernel launches with grid(1, 8) — a single block per (batch,head) pair that serially iterates over all 32 key blocks (2048/64) via `for (kb = 0; kb < num_key_blocks; ++kb)`.
