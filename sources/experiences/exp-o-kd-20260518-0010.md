---
id: exp-o-kd-20260518-0010
title: double_reduction
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- epilogue-fusion
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.151x
---

## Challenge

The before code unconditionally launches the reduction kernel whenever split_kv > 1, even when fused reduction (atomic accumulation) is already active inside the main kernel. This causes a double reduction: the main kernel atomically adds each split's partial result into the output buffer, and th...


## Solution

The after code adds !is_fused_reduction to the guard, skipping the separate reduction kernel when fused reduction handles accumulation inside the main kernel. This both fixes correctness (after_correct=1) and saves one full kernel launch plus its computation, yielding a 15% latency improvement. T...

## Results

- **speedup**: 1.151x (improved)


## Key Lessons

- The before code unconditionally launches the reduction kernel whenever split_kv > 1, even when fused reduction (atomic accumulation) is already active inside the main kernel.
