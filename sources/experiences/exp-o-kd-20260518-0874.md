---
id: exp-o-kd-20260518-0874
title: Arge expert counts (e.g., 256 experts), forcing fallback to global memory or
  uin
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Replaced per-thread sequential expert counting with warp-based atomicAdd counting in moe_align_block_size_kernel.
Problem before: The original moe_align_block_size_kernel used a per-thread counting approach where each thread maintained its own row of expert counts in a 2D shared-


## Solution

arge expert counts (e.g., 256 experts), forcing fallback to global memory or uint16_t compression. The sgl variant hardcoded __shared__ int32_t shared_counts[32][8] which only worked for exactly 256 experts with 8 experts per warp.
Applied change: The new moe_align_block_size_kernel uses warp-based 

## Key Lessons

- This eliminates the prefix-sum phase and its two __syncthreads() barriers, and the cumsum is computed directly from the atomic counts.
- Measured impact should be confirmed by point-level validation.
