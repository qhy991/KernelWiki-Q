---
id: exp-a-kd-20260518-1307
title: uniform_chunk
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.677x
---

## Challenge

The before code uses kChunkSize=512 (kNThreads*kNItems), producing 8 chunks for seqlen=4096, while the after code reads chunk sizes from cu_chunk_seqlen with block_size=2048, producing only 2 chunks. Each chunk iteration inv


## Solution

olves a fixed per-chunk overhead (loop bookkeeping, boundary checks). The after code reduces this overhead by a factor of 4x in chunk count, and the larger per-chunk iteration body (processing 2048 items per chunk vs 512) ha

## Results

- **speedup**: 1.677x (improved)


## Key Lessons

- The before code uses kChunkSize=512 (kNThreads*kNItems), producing 8 chunks for seqlen=4096, while the after code reads chunk sizes from cu_chunk_seqlen with block_size=2048, producing only 2 chunks.
