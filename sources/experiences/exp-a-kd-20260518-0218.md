---
id: exp-a-kd-20260518-0218
title: seq128_dim64
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
- epilogue-fusion
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
---

seq128_dim64

## Solution

The after source keeps each query row's running max, normalization term, and output vector inside one kernel, so it avoids the extra global-memory round trip for the attention matrix and removes one kernel launch. That reduction in workspace traffic and launch overhead is why the fused version is...

## Key Lessons

- The after source keeps each query row's running max, normalization term, and output vector inside one kernel, so it avoids the extra global-memory round trip for the attention matrix and removes one kernel launch.
- The before source writes every query-key score into a global `scores` workspace and then launches a second kernel that rereads those scores to compute softmax-weighted value sums.
