---
id: exp-o-kd-20260518-0719
title: Ents at capture time, making the pointers stale if buffer assignments change
  acr
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Introduced RankData struct to pack per-rank pointer arrays into a single indirection level, replacing the flat void* peer_comm_buffer_ptrs[MAX_RANKS_PER_NODE] array with a RankData* pointer.
Problem before: The original AllReduceParams stored peer communication buffer pointers as


## Solution

ents at capture time, making the pointers stale if buffer assignments change across allreduce calls within the same graph.
Applied change: Introduced a RankData struct that bundles the MAX_RANKS_PER_NODE pointer array into a single GPU-resident object. AllReduceParams now holds a RankData* (single p

## Key Lessons

- During CUDA graph capture, the host cannot re-fill this array between replay iterations because the graph records the struct contents at capture time, making the pointers stale if buffer assignments change across allreduce calls within the same graph.
- Expected impact: This indirection pattern enables CUDA graph capture for multi-iteration allreduce operations where different calls may use different input/output buffer addresses.
