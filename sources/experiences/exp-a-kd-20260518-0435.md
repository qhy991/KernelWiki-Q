---
id: exp-a-kd-20260518-0435
title: half_128h_8hd_256t
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
impl_family: custom_cuda
speedup: 1.189x
---

## Challenge

The after code uses uint4 (128-bit) packed load/store to process 8 half values per thread in a single memory transaction, replacing the before code's scalar element-wise access pattern that issued one 2-byte load/


## Solution

store per element. On A800 (sm_80) with 128-byte memory transaction granularity, the before code's individual 2-byte loads result in ~63x bandwidth waste per transaction, while the after code's 16-byte packed load

## Results

- **speedup**: 1.189x (improved)


## Key Lessons

- The after code uses uint4 (128-bit) packed load/store to process 8 half values per thread in a single memory transaction, replacing the before code's scalar element-wise access pattern that issued one 2-byte load/store per element.
