---
id: exp-o-kd-20260518-0827
title: E uint16 fallback, can exceed device shared memory limits, forcing a fallback
  to
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Replace per-thread 2D token-count matrix with warp-partitioned shared_counts using atomicAdd.
Problem before: The existing moe_align_block_size_kernel uses a 2D per-thread token-count matrix tokens_cnts[blockDim.x+1][num_experts] stored in dynamic shared memory. For large expert 


## Solution

e uint16 fallback, can exceed device shared memory limits, forcing a fallback to global memory which is significantly slower.
Applied change: The sgl_moe_align_block_size_kernel partitions experts across warps using a compact shared_counts[32][8] array where each warp owns 8 expert slots. Token coun

## Key Lessons

- Token counting uses atomicAdd on shared_counts[warp_idx][expert_offset] instead of conflict-free per-thread increments.
