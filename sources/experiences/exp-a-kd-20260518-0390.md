---
id: exp-a-kd-20260518-0390
title: fp16_256grp_64gs_vec
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 0.995x
---

## Challenge

With group_size=64 and vec_size=8, num_vec_elems=8, only threads 0-7 (lane_id < 8) execute the vector loop in the after version (each doing 1 vector load covering 8 elements), while threads 8-15 are idle in the loop body. This creates warp divergence within each warp (32 threads spanning 2 groups


## Solution

of 16). The before version has all 16 threads per group doing 4 scalar iterations with no divergence. The near-neutral result (0.5% difference) reflects that for group_size=64, the workload per group is so small (4 scalar reads per thread or 1 vector read per active thread) that memory access la

## Results

- **speedup**: 0.995x (neutral)


## Key Lessons

- With group_size=64 and vec_size=8, num_vec_elems=8, only threads 0-7 (lane_id < 8) execute the vector loop in the after version (each doing 1 vector load covering 8 elements), while threads 8-15 are idle in the loop body.
