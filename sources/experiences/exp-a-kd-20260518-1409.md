---
id: exp-a-kd-20260518-1409
title: dequant_skip_flop
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.003x
---

## Challenge

The before code has dequant_skip_flop = is_a_8bit || b_type == kFE4M3fn (blanket skip), while the after code uses (b_type == kFE4M3fn && !(s_type == kFE8M0fnu)). For the tested case (kFE4M3fn weights with kBFloat16 scales), both evaluate dequant_skip_flop to true, ca


## Solution

using both to skip the per-element multiplication FLOP. The after code also adds an s_type-based dispatch that calls dequant_fp8_scales when s_type is kFE4M3fn or kFE8M0fnu, but for kBFloat16 scales this branch is not taken. Both kernels execute the same dequant_data

## Results

- **speedup**: 1.003x (neutral)


## Key Lessons

- The after code's key improvement is correctness: for MXFP8, dequant_skip_flop becomes false, ensuring the e8m0 scale dequantization runs.
