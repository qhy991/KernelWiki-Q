---
id: exp-o-kd-20260518-0230
title: fp16_128_1000tok
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
- register-reuse
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- register-reuse
impl_family: custom_cuda
speedup: 1.260x
---

## Challenge

The before kernel uses 4 warps (128 threads) per CTA block with s_partial[4] shared memory and two __syncthreads() barriers. The after kernel uses 1 warp (32 threads) where each thread handles 4 half values (kElemsPerThread=4), performing the entire reduction within the warp via __shfl_down_sync and


## Solution

_rms via __shfl_sync. The larger speedup at 128 compared to 64 comes from two factors: (1) the CTA before kernel at hidden_size=128 uses 4 warps with 2 full __syncthreads() barriers per iteration, which is proportionally more expensive than the 2-warp setup at hidden_size=64; (2) the warp after kern

## Results

- **speedup**: 1.260x (improved)


## Key Lessons

- The increased work per thread in the warp version (4 values instead of 2) is still well within register capacity and does not significantly degrade the per-thread throughput.
