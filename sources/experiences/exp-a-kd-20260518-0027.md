---
id: exp-a-kd-20260518-0027
title: large_kv_uncapped
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.998x
---

## Challenge

The before code launches 16 sequential split kernels, each doing atomicAdd into the same 2048-element output buffer. The after code caps split_kv to 8 (sm_count/2=8), reducing kernel launches by half. Despite fewer launches, the measu


## Solution

red latency is neutral because this microbenchmark's atomicAdd workload is too light to create meaningful contention on the 2048 output slots — the GPU serializes the 16 or 8 launches quickly and the total work (same number of input e

## Results

- **speedup**: 0.998x (neutral)


## Key Lessons

- Despite fewer launches, the measured latency is neutral because this microbenchmark's atomicAdd workload is too light to create meaningful contention on the 2048 output slots — the GPU serializes the 16 or 8 launches quickly and the total work (same number of input elements processed) dominates.
