---
id: exp-a-kd-20260518-1496
title: shuffle_elision_16elts
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: inline_asm
speedup: 1.009x
---

## Challenge

The after code removes the __shfl_xor_sync warp shuffle via if constexpr when THREADS_PER_SF==1, since each thread already covers all 16 values in the SF vector and no cross-thread reduction is needed. The before code still executes the shuffle (computing neighborMax and combinedMax) even though the


## Solution

e A800 (sm_80) with simulated quantization, the __shfl_xor_sync overhead per iteration (~1-2 clock cycles) is small relative to the total per-iteration cost of loading 8 half2 values, computing 8 __habs2+__hmax2 operations, float conversions, clamping, rounding, and packing into two uint32_t outputs

## Results

- **speedup**: 1.009x (neutral)


## Key Lessons

- The after code removes the __shfl_xor_sync warp shuffle via if constexpr when THREADS_PER_SF==1, since each thread already covers all 16 values in the SF vector and no cross-thread reduction is needed.
