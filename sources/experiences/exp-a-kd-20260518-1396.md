---
id: exp-a-kd-20260518-1396
title: bf16_u4b8_dequant
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- epilogue-fusion
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: inline_asm
---

bf16_u4b8_dequant

## Solution

neg_bias) which is semantically equivalent to lo - bias since the multiply is by 1.0. The NVPTX backend lowers both __hfma2(x,1.0,bias) and __hsub2(x,bias) to the same HFMA2 instruction with a negated operand, so there is no instruction-level difference. The measure

## Key Lessons

- The before code uses a single __hfma2(lo, 1.0, neg_bias) which is semantically equivalent to lo - bias since the multiply is by 1.0.
- The after code splits the BF16 INT4 dequant into two steps: skip_flop=true stores the raw lop3 result into frag_b via reinterpret_cast (no FP op), then skip_flop=false calls that and applies __hsub2 for bias subtraction.
