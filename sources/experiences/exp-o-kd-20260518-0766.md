---
id: exp-o-kd-20260518-0766
title: Division is executed for every row in every pipeline stage of every slice,
  creat
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Pre-compute sorted_token_ids / top_k division into sh_rd_block_sorted_ids to eliminate repeated integer division in the hot A-fetch loop.
Problem before: In the MoE Marlin kernel, every A-matrix fetch iteration computes `sh_block_sorted_ids[row] / top_k` to map a token row to its


## Solution

division is executed for every row in every pipeline stage of every slice, creating significant redundant computation in the hot loop.
Applied change: Pre-compute `sh_rd_block_sorted_ids[tid4 * 4 + i] = sh_block_sorted_ids[tid4 * 4 + i] / top_k` once during the shared-memory initialization phase, st

## Key Lessons

- Optimization focus: Pre-compute sorted_token_ids / top_k division into sh_rd_block_sorted_ids to eliminate repeated integer division in the hot A-fetch loop.
