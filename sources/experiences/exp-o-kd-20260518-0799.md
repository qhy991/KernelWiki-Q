---
id: exp-o-kd-20260518-0799
title: Concurrent block occupancy and reducing the scheduler's ability to hide global
  m
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.477x
---

## Challenge

Optimization focus: Added adaptive block sizing based on num_tokens: for large batch sizes use smaller blocks (256 threads) to increase SM concurrency, and compute block_size from hidden_size/VEC_SIZE.
Problem before: The kernel always launched with dim3 block(std::min(hidden_size, 1024)) regardless


## Solution

concurrent block occupancy and reducing the scheduler's ability to hide global memory latency via warp-level context switching.
Applied change: An adaptive block sizing scheme was added: when num_tokens >= 256, max_block_size is capped at 256 threads instead of 1024. Additionally, block_size is comp

## Results

- **speedup**: 1.477x


## Key Lessons

- Applied change: An adaptive block sizing scheme was added: when num_tokens >= 256, max_block_size is capped at 256 threads instead of 1024.
- With many tokens (large batch), each SM could only run a few large blocks, limiting concurrent block occupancy and reducing the scheduler's ability to hide global memory latency via warp-level context switching.
