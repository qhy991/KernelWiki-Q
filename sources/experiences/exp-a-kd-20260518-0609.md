---
id: exp-a-kd-20260518-0609
title: half_xl
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
- pipeline-stages
- fine-grained-quantization
- vectorized-loads
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- fine-grained-quantization
- vectorized-loads
impl_family: custom_cuda
speedup: 1.003x
---

## Challenge

At 2097152 elements (64x32768), both versions have sufficient blocks to saturate the A800 GPU (1024 before, 512 after). The after version's flashinfer::vec_t with 128-bit vector loads/stores should provide higher per-thread memory throughput compared to the before version's 64-bit vec4_t loads,


## Solution

but the measured improvement is negligible (~0.3%). This kernel has extremely low arithmetic intensity (one conditional division/multiplication per element), making it memory-bandwidth-bound, but the total data volume (~4MB read + ~4MB write) is small enough that the A800's 2TB/s bandwidth is

## Results

- **speedup**: 1.003x (neutral)


## Key Lessons

- The wider vectors help reduce the number of memory transactions, but the kernel's ~5.6us runtime means the GPU spends most of its time on pipeline startup and wrap-up rather than sustained memory transfers, so the throughput gain from wider vectors is absorbed by overhead.
