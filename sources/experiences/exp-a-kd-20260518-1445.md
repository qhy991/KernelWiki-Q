---
id: exp-a-kd-20260518-1445
title: single_token_flash
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 2.389x
---

## Challenge

The before kernel passes k_scale and v_scale as scalar float parameters embedded in the kernel launch parameter buffer. The after kernel receives const float* pointers and dereferences them with *k_scale and *v_scale inside the FP8 conversion calls. The 2.4x measured improvem


## Solution

ent is a microbenchmark noise artifact for a 1-token (1 thread block, 512 threads) kernel where launch overhead and timing granularity dominate. The contradictory batch case showing 2.2x regression for the same change confirms that neither result reflects a genuine kernel-lev

## Results

- **speedup**: 2.389x (improved)


## Key Lessons

- The before kernel passes k_scale and v_scale as scalar float parameters embedded in the kernel launch parameter buffer.
