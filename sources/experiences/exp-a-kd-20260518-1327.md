---
id: exp-a-kd-20260518-1327
title: stride_permute
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: cutlass
speedup: 1.007x
---

## Challenge

The before kernel computes output indices using hardcoded M*K row-major stride, while the after kernel uses explicit input_stride and output_stride parameters. Both generate identical addressing at the SASS level for the standard row-major cas


## Solution

e because the compiler constant-folds the stride values. The stride construction method (layout permutation vs direct Stride construction) only affects CUTLASS's internal template dispatch and stride interpretation for the transposed sparse GE

## Results

- **speedup**: 1.007x (neutral)


## Key Lessons

- Both generate identical addressing at the SASS level for the standard row-major case because the compiler constant-folds the stride values.
- The real impact is on CUTLASS's ability to correctly interpret the physical memory layout of the transposed output tensor, not on the memory access pattern of equivalent operations.
