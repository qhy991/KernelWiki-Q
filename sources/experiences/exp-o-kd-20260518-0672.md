---
id: exp-o-kd-20260518-0672
title: Nt scheduling overhead and limited instruction-level parallelism within each
  thr
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: FP16x2 (2 elements/thread) → FP16x8 (8 elements/thread): each thread processes 4 half2 values (8 half elements) at once, quadrupling per-thread workload and reducing total thread count.
Problem before: The hardswish_f16x2_kernel processes only 2 half elements per thread. While al


## Solution

nt scheduling overhead and limited instruction-level parallelism within each thread, as each thread has only one half2 load-compute-store sequence.
Applied change: The hardswish_f16x8_kernel processes 8 half elements per thread using 4 separate half2 register pairs (reg_x_0 through reg_x_3). Each th

## Key Lessons

- While already vectorized at the half2 level, the low per-thread work count still results in high total thread count (N/2 threads), causing significant scheduling overhead and limited instruction-level parallelism within each thread, as each thread has only one half2 load-compute-store sequence.
