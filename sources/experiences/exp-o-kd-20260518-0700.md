---
id: exp-o-kd-20260518-0700
title: Und-trip for the top-K weights.
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Fuse renormalization into topkGatingSoftmax warp-optimized kernel for power-of-2 expert counts.
Problem before: The warp-optimized topkGatingSoftmax kernel (used for power-of-2 expert counts, which covers most production MoE models) had the same issue: renormalization required a 


## Solution

und-trip for the top-K weights.
Applied change: The topkGatingSoftmax kernel accumulates row_sum_for_renormalize during the top-K selection loop (thread_group_idx==0 threads add max_val to the sum) and, after the loop, divides each output weight by this sum when renormalize is true. Only thread_grou

## Key Lessons

- Eliminates one kernel launch and one global memory round-trip.
