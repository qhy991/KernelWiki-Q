---
id: exp-a-kd-20260518-1446
title: mask_active
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.831x
---

## Challenge

The after kernel adds two per-iteration overheads not present in the before kernel: (1) a null-pointer comparison `token_mask == nullptr` and (2) a global-memory read `token_mask[i / topk_num]` with an integer division to compute the token index. Even though the mask filters approximately half the t


## Solution

s, reducing atomicAdd call volume, the extra global-memory load and division arithmetic per iteration dominate the cost savings from fewer atomic operations. The before kernel's unconditional `atomicAdd(..., 1)` avoids both the pointer check and the mask memory read entirely, resulting in fewer inst

## Results

- **speedup**: 0.831x (regression)


## Key Lessons

- The before kernel's unconditional `atomicAdd(..., 1)` avoids both the pointer check and the mask memory read entirely, resulting in fewer instructions per loop iteration despite executing more atomicAdd operations.
