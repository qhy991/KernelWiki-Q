---
id: exp-a-kd-20260518-0719
title: align_256ep_64bs
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 2.097x
---

## Challenge

The before code runs the exclusive prefix sum as a serial loop on thread 0, iterating over all 256 expert entries one-by-one with per-iteration shared-memory loads, CEILDIV rounding, and cumulative addition. All 1023 other threads sit idle at the __syncthreads() barrier waiting for this loop to c...


## Solution

The after code replaces this with CUB BlockScan<int32_t, 1024>, which distributes the scan across all threads: each thread reads its own expert count from shared_counts, applies the CEILDIV rounding, and then BlockScan computes the exclusive prefix sum via a tree-based reduction with O(log2(1024)...

## Results

- **speedup**: 2.097x (improved)


## Key Lessons

- This eliminates the serial bottleneck entirely for 256 experts, where the serial loop took ~256 iterations, yielding a ~2x measured speedup.
- The after code replaces this with CUB BlockScan<int32_t, 1024>, which distributes the scan across all threads: each thread reads its own expert count from shared_counts, applies the CEILDIV rounding, and then BlockScan computes the exclusive prefix sum via a tree-based reduction with O(log2(1024))=1
