---
id: exp-a-kd-20260519-1375-1
title: 'Make TOPK a compile-time template parameter and add #pragma unroll so the
  compil'
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: experimental
reproducibility: snippet
impl_family: custom_cuda
---

## Challenge

MoE sum kernel passes topk as a runtime integer parameter, preventing the compiler from unrolling the inner accumulation loop and forcing per-iteration branch and loop-counter overhead for a known-small iteration count


## Solution

Make TOPK a compile-time template parameter and add #pragma unroll so the compiler fully expands the 2-iteration inner loop into two independent load-add sequences with no loop control or branch overhead

## Key Lessons

- Pass small fixed iteration counts (e.g., top-K) as compile-time template parameters instead of runtime integers, enabling #pragma unroll to eliminate loop overhead and unroll address arithmetic — applies whenever the loop bound is a known constant at kernel launch time
