---
id: exp-a-kd-20260518-0333
title: sgemm_512x512x512
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- transpose
- shared-memory-optimization
kernel_types:
- transpose
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 0.416x
---

## Challenge

The after kernel uses s_a[BM][BK] (128x8) row-major layout for shared memory. In the compute phase, threads within a warp read s_a[ty*TM+m][k] where consecutive threads (different ty values) access different rows but the same c


## Solution

olumn k, causing severe bank conflicts because BK=8 is much smaller than 32 banks. Each warp of 32 threads spans only 2 rows (16 threads per row for TM=8), and with only BK=8 columns the access pattern hits the same banks repea

## Results

- **speedup**: 0.416x (regression)


## Key Lessons

- In the compute phase, threads within a warp read s_a[ty*TM+m][k] where consecutive threads (different ty values) access different rows but the same column k, causing severe bank conflicts because BK=8 is much smaller than 32 banks.
- Each warp of 32 threads spans only 2 rows (16 threads per row for TM=8), and with only BK=8 columns the access pattern hits the same banks repeatedly.
