---
id: exp-o-kd-20260518-0203
title: bf16_m2048_topk4_k7168
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- register-reuse
- epilogue-fusion
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- epilogue-fusion
impl_family: custom_cuda
speedup: 5.671x
---

## Challenge

The before code launches three separate kernels over a large tensor (m=2048, topk=4, row_stride=7168, total_rows=8192). The shuffle kernel writes 8192*7168 floats to d_shuffled (~224MB), the mul kernel reads d_shuffled and writes 8192*7168 floats to d_multiplied (~224MB), and the sum kernel reads d_


## Solution

ion between each step, and two large intermediate buffers must be allocated and fully written/read from global memory. The fused after kernel eliminates both intermediate buffers entirely — each thread accumulates factor*val into a register-local sum_val across the topk loop, reads input_tensor dire

## Results

- **speedup**: 5.671x (improved)


## Key Lessons

- The fused after kernel eliminates both intermediate buffers entirely — each thread accumulates factor*val into a register-local sum_val across the topk loop, reads input_tensor directly by the permutation index, and writes the final output only once.
- These three kernel launches serialize via device synchronization between each step, and two large intermediate buffers must be allocated and fully written/read from global memory.
