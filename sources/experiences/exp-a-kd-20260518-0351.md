---
id: exp-a-kd-20260518-0351
title: M512_K128
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.000x
---

## Challenge

The after kernel uses float4 vectorized loads via the FLOAT4 macro (reinterpret_cast<float4*>), allowing each thread to load 4 floats from both A and x in a single coalesced memory transaction instead of 4 separate scalar l


## Solution

oads. This reduces the total number of global memory transactions by 4x and improves memory bandwidth utilization. The inner loop also iterates 4x fewer times (NUM_WARPS is divided by 4), reducing loop overhead and instruct

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- The after kernel uses float4 vectorized loads via the FLOAT4 macro (reinterpret_cast<float4*>), allowing each thread to load 4 floats from both A and x in a single coalesced memory transaction instead of 4 separate scalar loads.
