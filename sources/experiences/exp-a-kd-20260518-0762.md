---
id: exp-a-kd-20260518-0762
title: half2_kU4B8
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- register-reuse
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 1.001x
---

## Challenge

In the before source, dequant_kU4B8 creates a local FragB struct (half2 data[2]), populates frag_b[0] and frag_b[1] via __hsub2 and __hfma2, and returns the struct by value. The caller assigns it to a local FragB fb. In the after source, dequant_kU4B8 takes half2* frag_b and writes frag_b[0]/frag...


## Solution

The optimization would only show a measurable benefit in the full Marlin GEMM kernel context where heavy template nesting, multiple dequant calls per iteration, and high overall register pressure may prevent the compiler from consistently applying NRVO.

## Results

- **speedup**: 1.001x (neutral)


## Key Lessons

- Since both paths end up with identical register allocations and instruction sequences after NRVO, the measured latencies are indistinguishable within measurement noise.
- The NVCC compiler (CUDA 12.8, sm_80) applies Named Return Value Optimization (NRVO) for this simple 2-element struct in an isolated microbenchmark: the local FragB inside dequant is constructed directly at the caller's fb destination, eliminating the copy that return-by-value would otherwise require
