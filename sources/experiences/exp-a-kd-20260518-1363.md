---
id: exp-a-kd-20260518-1363
title: warp_reduce_sum_float
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

warp_reduce_sum_float

## Solution

The after source (warp_reduce_sum_float_after.cu) replaces += with `val = fn(val, VLLM_SHFL_XOR_SYNC(val, mask))` where fn is detail::_sum<float>, a trivial __inline__ __device__ wrapper returning `a + b`. Since _sum is semantically identical to += and NVCC resolves the function pointer to a comp...

## Key Lessons

- Since _sum is semantically identical to += and NVCC resolves the function pointer to a compile-time constant, the generated SASS should be identical.
