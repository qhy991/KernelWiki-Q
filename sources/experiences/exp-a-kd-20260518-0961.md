---
id: exp-a-kd-20260518-0961
title: gelu_erf_vs_tanh_fp16
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- activation
kernel_types:
- activation
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.006x
---

## Challenge

The before code calls ::erf() inside gelu_kernel while the after code calls ::tanhf() with additional multiply-add operations (f*f*f, BETA*(f + KAPPA*x_cube)). On sm_80 (A800), both erf and tanhf are implemented as SFU (Special Func


## Solution

tion Unit) microcode with comparable instruction latency. The tanh path adds extra arithmetic (x_cube computation, inner product) that roughly offsets any SFU advantage. Furthermore, for fp16 the kernel is memory-bandwidth-bound: ea

## Results

- **speedup**: 1.006x (neutral)


## Key Lessons

- The before code calls ::erf() inside gelu_kernel while the after code calls ::tanhf() with additional multiply-add operations (f*f*f, BETA*(f + KAPPA*x_cube)).
