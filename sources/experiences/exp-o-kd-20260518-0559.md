---
id: exp-o-kd-20260518-0559
title: varlen_chunk
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.293x
---

## Challenge

The before code uses kChunkSize=512 chunks, requiring 6 chunks for seqlen=3000, while the after code uses 3 variable-length chunks (488+2048+464) from cu_chunk_seqlen. The after code halves the chunk-


## Solution

loop iteration count, reducing per-chunk overhead (boundary computation, syncthreads implied by chunk boundaries). The variable chunk sizes also mean the after code processes most tokens in a single l

## Results

- **speedup**: 1.293x (improved)


## Key Lessons

- The before code uses kChunkSize=512 chunks, requiring 6 chunks for seqlen=3000, while the after code uses 3 variable-length chunks (488+2048+464) from cu_chunk_seqlen.
