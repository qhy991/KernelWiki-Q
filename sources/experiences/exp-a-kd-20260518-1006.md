---
id: exp-a-kd-20260518-1006
title: pack_factor_4bit_4bit
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

pack_factor_4bit_4bit

## Solution

identical constants, so all downstream indexing (stage_k_threads, src_k_packed, first_k_packed, src_k_pos) produces identical code. The generalized tile_ints loop loads the same 2 values and th

## Key Lessons

- No measurable performance difference exists because the generated PTX/SASS instructions are functionally equivalent.
- The generalized tile_ints loop loads the same 2 values and the computed cur_int/cur_pos yields the same indices as the split-loop approach.
