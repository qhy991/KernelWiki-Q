---
id: exp-a-kd-20260518-1428
title: fp8_scale_hidden4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- pipeline-stages
- register-reuse
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- register-reuse
- vectorized-loads
impl_family: custom_cuda
speedup: 0.893x
---

## Challenge

The before source uses a simple local const float min_scaling_factor computed from fp8_e4m3_adjusted_max_v<fp8_type> * 512.f, with a single-struct fp8_e4m3_adjusted_max<T> that has a plain constexpr val() method. The after source introduces quant_type_max<T> with enable_if_t SFINAE checking three ty


## Solution

lus __forceinline__. Although both paths compute identical float values, the additional template complexity (nested enable_if_t disjunctions across three type candidates) could cause nvcc's PTX/SASS generation pipeline to make different register allocation or instruction scheduling decisions for the

## Results

- **speedup**: 0.893x (regression)


## Key Lessons

- The before source uses a simple local const float min_scaling_factor computed from fp8_e4m3_adjusted_max_v<fp8_type> * 512.f, with a single-struct fp8_e4m3_adjusted_max<T> that has a plain constexpr val() method.
