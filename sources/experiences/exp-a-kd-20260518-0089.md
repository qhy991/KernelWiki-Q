---
id: exp-a-kd-20260518-0089
title: tile_offset_row_small
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 1.000x
---

## Challenge

Both the before and after kernels compute a byte offset into shared memory via integer arithmetic on uint8_t pointers, then perform a single-threaded load of kColumn=4 floats. The only difference is the multiplicative factor in the row offset: before uses offset_row * stride_bytes (one row per ti...


## Solution

the after code, which is negligible at the kernel launch granularity (microsecond-scale kernel with 128 threads). The dominant cost is the shared-memory copy and synchronization barrier, not the address computation, so the single additional multiply has no measurable effect on latency.

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- Both the before and after kernels compute a byte offset into shared memory via integer arithmetic on uint8_t pointers, then perform a single-threaded load of kColumn=4 floats.
