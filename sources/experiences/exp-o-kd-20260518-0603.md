---
id: exp-o-kd-20260518-0603
title: mfma_wider_instr
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 3.518x
---

## Challenge

The before code (naive_gemm_kernel) processes one K-element per scalar multiply-add iteration in a per-thread loop over K=2880, requiring 2880 __half2float conversions and individual FMA operations per output element with no hardware matrix acceleration. The after code (wmma_gemm_kernel) uses WMMA 1


## Solution

ile simultaneously through the hardware matrix multiply unit, reducing the total instruction count from 737280 scalar operations to 180 mma_sync calls per tile. The fill_fragment(frag_c, 0.0f) accumulator initialization parallels the AMD sum4 = {0} zero-init that eliminates the conditional (k1 == k_

## Results

- **speedup**: 3.518x (improved)


## Key Lessons

- The fill_fragment(frag_c, 0.0f) accumulator initialization parallels the AMD sum4 = {0} zero-init that eliminates the conditional (k1 == k_str) ternary.
