---
id: exp-a-kd-20260518-1039
title: fp16_gs128_4kgrp
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.048x
---

## Challenge

The before kernel uses a scalar element-by-element loop (lines 48-53) that issues individual 2-byte fp16 loads via __half2float(group_input[i]) for each of the 8 elements per thread (group_size=128 / threads_per_group=16). The after kernel replaces this with __half2 packed loads (lines 54-61), readi


## Solution

ast<const __half2*>(group_input) and decomposing with __low2float/__high2float, reducing input memory iterations from 8 to 4 per thread. The output path similarly uses float4 packed writes (16 bytes per transaction) instead of individual 4-byte float writes. The 4.8% speedup is near the 5% threshold

## Results

- **speedup**: 1.048x (improved)


## Key Lessons

- The output path similarly uses float4 packed writes (16 bytes per transaction) instead of individual 4-byte float writes.
