---
id: exp-a-kd-20260518-1129
title: topk_k16_N256
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- shared-memory-optimization
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 1.352x
---

## Challenge

In the before code, naive_topk_kernel performs 16 selection iterations (K=16), each scanning all 256 elements with a warp-level max-reduction via shfl_down_sync, plus __syncthreads() per iteration and a used-flag array of N=256 ints in shared memory (total ~3KB smem)


## Solution

The after WarpSelect code reads input elements in rounds of WARP_SIZE, uses __ballot_sync to early-exit when no candidate exceeds the k-th value (skipping entire rounds), and only triggers a warp-level bitonic sort/merge when the staging buffer fills to 32 elements. For N=256 and k=16, the buffer...

## Results

- **speedup**: 1.352x (improved)


## Key Lessons

- In the before code, naive_topk_kernel performs 16 selection iterations (K=16), each scanning all 256 elements with a warp-level max-reduction via shfl_down_sync, plus __syncthreads() per iteration and a used-flag array of N=256 ints in shared memory (total ~3KB smem).
