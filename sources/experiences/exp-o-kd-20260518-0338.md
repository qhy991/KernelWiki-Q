---
id: exp-o-kd-20260518-0338
title: unfused_vs_fused_mla_cache_write
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
- epilogue-fusion
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.902x
---

## Challenge

The fused kernel reads source data directly using cp_local_token_select_indices[blockIdx.x] and writes to the KV cache in a single pass. The unfused approach requires two index_select kernel launches to first copy selected rows into intermediate buffers (temp_kv_c and temp_k_pe), followed by a third


## Solution

The fusion eliminates two kernel launch overheads and the global memory round-trip for the intermediate tensors: selected token data is written to an intermediate buffer in global memory by the index_select kernels, then read back from global memory by the cache_write kernel. In the fused version, e

## Results

- **speedup**: 1.902x (improved)


## Key Lessons

- The fusion eliminates two kernel launch overheads and the global memory round-trip for the intermediate tensors: selected token data is written to an intermediate buffer in global memory by the index_select kernels, then read back from global memory by the cache_write kernel.
