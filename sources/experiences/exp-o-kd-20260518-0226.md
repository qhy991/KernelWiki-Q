---
id: exp-o-kd-20260518-0226
title: reduce_8ranks_fp16
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- register-reuse
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 1.154x
---

## Challenge

The after code eliminates two overheads present in the before kernel: (1) the local buffers[RANKS] array, which consumes 8 registers per thread to store per-rank pointer copies read from the kernel parameter block, and (2) the round-robin modulo computation ((local_rank + ii) % RANKS) used to remap 


## Solution

er buffer pointers at kernel start. The after pattern instead dereferences a single RankData* pointer into a GPU-resident struct and directly indexes ptrs[ii] without remapping. This frees registers, reduces per-thread stack footprint, and removes 8 integer modulo operations. The single extra global

## Results

- **speedup**: 1.154x (improved)


## Key Lessons

- The after code eliminates two overheads present in the before kernel: (1) the local buffers[RANKS] array, which consumes 8 registers per thread to store per-rank pointer copies read from the kernel parameter block, and (2) the round-robin modulo computation ((local_rank + ii) % RANKS) used to remap 
- The after pattern instead dereferences a single RankData* pointer into a GPU-resident struct and directly indexes ptrs[ii] without remapping.
