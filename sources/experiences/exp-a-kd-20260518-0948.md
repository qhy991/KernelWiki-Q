---
id: exp-a-kd-20260518-0948
title: mn128_k7168_gs128
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 0.980x
---

## Challenge

The before code uses atomicOr to pack 4 UE8M0 exponents into each uint32 word, serializing writes from up to 4 groups sharing the same output index. The after code replaces this with a direct byte-level reinterpret_cast write where each thread targets a unique byte offset within the uin


## Solution

t32. However, atomicOr is only executed by lane_id==0 threads (1 per group of 16), so at most 4 atomic operations target any given uint32 word. With only 4 contending threads, the L2 atomic throughput on A800 resolves these without significant serialization delay. The dominant kernel ti

## Results

- **speedup**: 0.980x (regression)


## Key Lessons

- The before code uses atomicOr to pack 4 UE8M0 exponents into each uint32 word, serializing writes from up to 4 groups sharing the same output index.
