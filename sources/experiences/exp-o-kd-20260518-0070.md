---
id: exp-o-kd-20260518-0070
title: warp_idx_shfl
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- register-reuse
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 11.535x
---

## Challenge

The before code calls __shfl_sync(0xffffffff, threadIdx.x / 32, 0) inside an unrolled loop of 10000 iterations. __shfl_sync is a warp-level barrier that forces all 32 lanes to converge before the shuffle completes, and the compiler cannot reorder instructions across it. This prevents the compiler...


## Solution

The after code replaces this with canonical_warp_idx(), which is simply threadIdx.x / 32 — a pure arithmetic expression with no synchronization semantics. Since threadIdx.x is a read-only special register constant across the kernel lifetime, the compiler can compute the division once, hoist it ou...

## Results

- **speedup**: 11.535x (improved)


## Key Lessons

- This prevents the compiler from hoisting the warp-index computation out of the loop or folding it into a register — each iteration emits a full SHFL instruction plus the implicit barrier, serializing the loop body.
