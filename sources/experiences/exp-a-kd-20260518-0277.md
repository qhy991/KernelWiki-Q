---
id: exp-a-kd-20260518-0277
title: f16x2_to_x8_large
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- register-reuse
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 0.966x
---

## Challenge

The after code (f16x8) processes 8 half values per thread using 4 separate half2 register loads, quadrupling per-thread work relative to the before code (f16x2) which processes 2 halves per thread. However, on the A800 with 1M elements, the total work is already small enough that both kernels sat...


## Solution

in the after kernel. The before kernel's simpler structure with fewer instructions per thread and fewer branches gives it a slight edge in this workload size.

## Results

- **speedup**: 0.966x (regression)


## Key Lessons

- The after code (f16x8) processes 8 half values per thread using 4 separate half2 register loads, quadrupling per-thread work relative to the before code (f16x2) which processes 2 halves per thread.
