---
id: exp-a-kd-20260518-1131
title: topk_k8_N128
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- shared-memory-optimization
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 0.928x
---

## Challenge

In the before code, naive_topk_kernel with K=8 performs 8 selection iterations scanning 128 elements each, using a warp-level shfl_down_sync reduction (5 shuffles per iteration). Total: 8 iterations times (128/32=4 strided loads plus 5 shuffles) plus 8 syncthreads calls


## Solution

The after WarpSelect code processes 4 rounds (128/32), each with __ballot_sync, __popc compact, shared memory staging, and __syncwarp. With k=8 and N=128, the buffer fills roughly once before the k-th threshold becomes restrictive enough to trigger ballot_sync early-exit, so most rounds still nee...

## Results

- **speedup**: 0.928x (regression)


## Key Lessons

- In the before code, naive_topk_kernel with K=8 performs 8 selection iterations scanning 128 elements each, using a warp-level shfl_down_sync reduction (5 shuffles per iteration).
