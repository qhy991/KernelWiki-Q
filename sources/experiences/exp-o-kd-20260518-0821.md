---
id: exp-o-kd-20260518-0821
title: unknown
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

unknown

## Solution

on the critical path.
Applied change: A compile-time fill_threads parameter (256) reserves a subgroup of threads for initialization while the remaining threads (threadIdx.x >= fill_threads) handle counting/alignment. The fill threads write sorted_token_ids using fill_threads as the stride, then issu

## Key Lessons

- The working threads use tid = threadIdx.x - fill_threads and stride = blockDim.x - fill_threads, so the tokens_cnts array indexing uses the working-thread count rather than total blockDim.x.
- The sequential init-then-count pattern meant the initialization overhead was always on the critical path.
