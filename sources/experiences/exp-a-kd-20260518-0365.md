---
id: exp-a-kd-20260518-0365
title: tokens_2048
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- rotary-embedding
- register-reuse
kernel_types:
- rotary-embedding
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 2.066x
---

## Challenge

At 2048 tokens the same optimization mechanisms apply with even more amortization of launch overhead. The naive before kernel iterates over 2048*128*192 = 50.3M elements using a grid-stride loop with per-element conditional reads, generating high instruction overhead (integer division + modulo + ...


## Solution

The after kernel processes the same data with 2048*8=16384 warps, each performing 16 unrolled int2 loads, 16 unrolled int2 stores, and 16 unrolled int stores via register-buffered k_rope_buf, resulting in 512 fully coalesced 256-byte NOPE transactions and 128 fully coalesced 128-byte ROPE transac...

## Results

- **speedup**: 2.066x (improved)


## Key Lessons

- The ~2x speedup is consistent across token counts because the per-element overhead ratio is similar regardless of problem size.
