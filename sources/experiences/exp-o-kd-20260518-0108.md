---
id: exp-o-kd-20260518-0108
title: d64_s512
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- shared-memory-optimization
- pipeline-stages
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- pipeline-stages
impl_family: custom_cuda
speedup: 1.158x
---

## Challenge

The after variant (kStage=2) uses double-stage async copy pipelining where the next Q/K/V tiles are loaded into an alternate shared memory stage (smem_sel_next) via CP_ASYNC_CG while MMA computation proceeds on the current stage (smem_


## Solution

sel). The deferred CP_ASYNC_WAIT_GROUP(kStage-2) after MMA compute overlaps global memory transfers with tensor core work, hiding gmem latency. The before variant (kStage=1) loads current Q/K tiles synchronously each iteration, issues

## Results

- **speedup**: 1.158x (improved)


## Key Lessons

- The after variant (kStage=2) uses double-stage async copy pipelining where the next Q/K/V tiles are loaded into an alternate shared memory stage (smem_sel_next) via CP_ASYNC_CG while MMA computation proceeds on the current stage (smem_sel).
