---
id: exp-a-kd-20260518-0041
title: cache_op_hint
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- pipeline-stages
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
speedup: 1.007x
---

## Challenge

The after code uses cp.async.cg (cache-global, bypass L1) for B operand loads while keeping cp.async.ca (cache-all, L1+L2) for A loads. The before code uses cp.async.ca for both operands. In both source files, the shared-memory tile sizes are TILE_M=64 x TILE


## Solution

_K=32 for A and TILE_K=32 x TILE_N=64 for B, giving 8KB and 8KB per stage. With 2 stages, the total working set is 32KB which fits comfortably in L1 (128KB on A800). Since the L1 cache has sufficient capacity, bypassing it for B does not reduce meaningful cac

## Results

- **speedup**: 1.007x (neutral)


## Key Lessons

- Since the L1 cache has sufficient capacity, bypassing it for B does not reduce meaningful cache pollution - the B data that would have occupied L1 lines is simply not cached, meaning subsequent accesses to those addresses (if any) must go to L2.
