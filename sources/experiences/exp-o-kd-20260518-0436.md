---
id: exp-o-kd-20260518-0436
title: bf16_m64_n128_k256
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- epilogue-fusion
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.171x
---

## Challenge

The before source launches two separate kernels: scaled_gemm_kernel computes C = scaleA * (scaleB * A*B), then bias_add_kernel reads C back from global memory and adds the row-broadcast bias vector. The after source replaces both with a single fused_gemm_bias_kernel that computes C = scaleA * (scale


## Solution

e the final __float2half store. For this small matrix (M=64, N=128, K=256), total latency is only ~0.017ms across two kernel launches. At this scale, the fixed overhead of each kernel launch (parameter setup, driver submission, and implicit inter-kernel dependency since the bias-add reads from C wri

## Results

- **speedup**: 1.171x (improved)


## Key Lessons

- At this scale, the fixed overhead of each kernel launch (parameter setup, driver submission, and implicit inter-kernel dependency since the bias-add reads from C written by the GEMM) becomes proportionally significant.
