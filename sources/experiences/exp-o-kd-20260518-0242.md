---
id: exp-o-kd-20260518-0242
title: large_1024rows_topk6
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.116x
---

## Challenge

The after code replaces scalar stride loads (for loop iterating expert with stride WARP_SIZE, reading one 32-bit float per iteration) with float4 vectorized loads using reinterpret_cast<float4*>. Each lane now loads 3 float4 vectors (VEC_PER_LANE=3, covering 12 elements per lane = VP


## Solution

T), each carrying 4 contiguous floats in a single 128-bit memory transaction. This reduces global memory transaction count from 12 separate 32-bit loads per lane to 3 coalesced 128-bit loads, improving memory bandwidth utilization. Adjacent lanes load adjacent float4 chunks, ensuring

## Results

- **speedup**: 1.116x (improved)


## Key Lessons

- The #pragma unroll on both the outer vec-per-lane loop and the inner float4 processing loop eliminates loop overhead and allows the compiler to schedule the LDG.128 instructions efficiently.
- Adjacent lanes load adjacent float4 chunks, ensuring coalesced access patterns where consecutive warp threads access consecutive 128-byte segments.
