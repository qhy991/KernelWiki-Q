---
id: exp-o-kd-20260518-0438
title: experts_256_topk8
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- pipeline-stages
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
speedup: 124.850x
---

## Challenge

In the before source (experts_256_topk8_before.cu lines 158-169), each benchmark iteration launches compute_expert_offsets<<<1,1>>>, calls cudaDeviceSynchronize, then performs cudaMemcpy D2H to read problem_sizes1 back to host, runs a CPU-side prefix-sum loop with 128-aligned ceiling rounding ove...


## Solution

the after source (experts_256_topk8_after.cu lines 136-142), a single compute_expert_blockscale_offsets<<<1,1,0,0>>> kernel reads problem_sizes1 directly from GPU memory, computes both expert_offsets and the 128-aligned blockscale_offsets prefix-sums in one serial loop (lines 20-26 of the kernel)...

## Results

- **speedup**: 124.850x (improved)


## Key Lessons

- The before path's ~2.4ms is dominated by the sync+transfer barrier rather than actual computation; the after path's ~0.02ms is just the lightweight GPU kernel execution.
