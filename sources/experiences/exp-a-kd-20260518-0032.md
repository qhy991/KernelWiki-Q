---
id: exp-a-kd-20260518-0032
title: relu_float_constref
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- activation
- register-reuse
kernel_types:
- activation
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 1.001x
---

## Challenge

The before code uses ReLuBefore::operator()(const T&, const T&) which takes value by const reference and copies it into a local result variable before comparison. The after code uses ReLuAfter::operator()(T value) which takes value by copy directly. At -O3 optimization, the NV


## Solution

CC compiler inlines both functors and emits identical PTX: the const-ref parameter is dereferenced into a register at the call site, producing the same register-resident value and the same comparison/conditional-move sequence. Since the ReLu body is trivially small (one compar

## Results

- **speedup**: 1.001x (neutral)


## Key Lessons

- Since the ReLu body is trivially small (one comparison, one conditional move), the parameter passing convention has zero measurable effect on the generated SASS.
