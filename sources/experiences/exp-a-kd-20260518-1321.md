---
id: exp-a-kd-20260518-1321
title: int8_aligned_gemm
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.002x
---

## Challenge

The before code uses a hardcoded alignment value of 16 (static constexpr int HardcodedAlignment = 16), while the after code computes it as 128 / sizeof_bits<ElementAB>::value. For int8_t, sizeof_bits is 8, so the computed alignment equals 128/8 = 16, which is numerically identical to the hardcode...


## Solution

in the after source.

## Results

- **speedup**: 1.002x (neutral)


## Key Lessons

- Both kernels therefore use vec_size = 16, produce the same memory access pattern (element-by-element loads within a vector-width loop), and generate identical PTX.
