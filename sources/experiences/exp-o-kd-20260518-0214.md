---
id: exp-o-kd-20260518-0214
title: topk8_rows4096
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
- epilogue-fusion
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.655x
---

## Challenge

With 4096 rows the two-kernel before path scales poorly because each kernel launch must dispatch 4096 blocks. The normalize kernel writes all 32768 elements to global memory, then the scale kern


## Solution

el re-reads all of them for a single multiply per element. The fused after kernel reads each element once, applies division and conditional scaling in the same loop body, and writes the final re

## Results

- **speedup**: 1.655x (improved)


## Key Lessons

- With 4096 rows the two-kernel before path scales poorly because each kernel launch must dispatch 4096 blocks.
- The fused after kernel reads each element once, applies division and conditional scaling in the same loop body, and writes the final result once.
