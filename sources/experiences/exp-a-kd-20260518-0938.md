---
id: exp-a-kd-20260518-0938
title: float_topk2_d2048
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

float_topk2_d2048

## Solution

he 2-iteration loop into two independent load-add sequences with no loop overhead. More importantly, __ldg(&input[...]) routes each load through the GPU read-only texture cache (L1 read-only path on sm_80), which has a dedicated cache separate from the L1 data cache. Since each thread reads 2 distin

## Key Lessons

- Since each thread reads 2 distinct input locations separated by HIDDEN_SIZE=2048 floats (8KB stride), the __ldg loads hit the read-only cache path while the before kernel's regular loads compete for the same L1 data cache lines as other memory operations.
- More importantly, __ldg(&input[...]) routes each load through the GPU read-only texture cache (L1 read-only path on sm_80), which has a dedicated cache separate from the L1 data cache.
