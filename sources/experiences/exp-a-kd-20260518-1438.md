---
id: exp-a-kd-20260518-1438
title: half_h128_t16
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
impl_family: custom_cuda
---

half_h128_t16

## Solution

8 half elements) and processes it in an unrolled loop. The regression comes from the after kernel's more complex index mapping: each thread must compute global_idx, then derive token_head_idx, pack_idx, token_idx, head_idx, and pack_offset through division/modulo operations, adding integer arithmeti

## Key Lessons

- The before kernel for h128 launches 512 blocks with 128 threads each; each block processes one (token, head) pair where all 128 threads iterate over the 128 half elements with stride=128, meaning each thread handles exactly one element.
