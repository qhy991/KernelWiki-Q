---
id: exp-o-kd-20260518-0430
title: float_topk4_d2048
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.741x
---

## Challenge

With TOPK=4, the before kernel runs a 4-iteration runtime loop using plain global-memory loads, incurring loop-carried dependencies on the accumulator variable x and preventing the compiler from scheduling independent loads early. The after kernel uses TOPK=4 as a compile-time template parameter ...


## Solution

the optimization.

## Results

- **speedup**: 1.741x (improved)


## Key Lessons

- This eliminates the branch-and-comparison overhead per iteration and allows the compiler to overlap the 4 independent __ldg reads more aggressively since they can be issued to the read-only cache path simultaneously rather than sequentially through the data cache.
