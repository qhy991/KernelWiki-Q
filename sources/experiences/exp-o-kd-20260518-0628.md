---
id: exp-o-kd-20260518-0628
title: Separate reduction kernel, producing incorrect output. It also wasted GPU time
  o
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Skip separate reduction kernel launch when fused reduction is active.
Problem before: When is_fused_reduction was true and split_kv > 1, the code unconditionally launched the separate reduction kernel after the main attention kernel. This caused double reduction: once inside the 


## Solution

separate reduction kernel, producing incorrect output. It also wasted GPU time on an unnecessary kernel launch.
Applied change: Added !params.fmha_params.is_fused_reduction to the guard condition for launching the reduction kernel, so the separate reduction kernel is only launched when fused reducti

## Key Lessons

- Skipping it eliminates both the correctness bug and the wasted kernel launch overhead.
- The separate reduction kernel is only needed for the non-fused path where each split writes partial results that must be reduced in a separate pass.
