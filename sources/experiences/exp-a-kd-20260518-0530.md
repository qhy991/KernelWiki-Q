---
id: exp-a-kd-20260518-0530
title: fp32_64K
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- elementwise
- register-reuse
kernel_types:
- elementwise
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 1.081x
---

## Challenge

In the before code, every thread dereferences a global memory pointer (scale_ptr) to compute scale_val = 1.0f / (*scale_ptr), causing a per-thread global memory load and division. In


## Solution

the after code, scale_val is passed as a scalar kernel argument, which the GPU hardware stores in the kernel parameter buffer and broadcasts to all threads via constant cache or register allocation. For a small 64K-element tensor, the kernel runtime is short and the overhead of the global memory ...

## Results

- **speedup**: 1.081x (improved)


## Key Lessons

- In the before code, every thread dereferences a global memory pointer (scale_ptr) to compute scale_val = 1.0f / (*scale_ptr), causing a per-thread global memory load and division.
