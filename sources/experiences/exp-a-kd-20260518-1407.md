---
id: exp-a-kd-20260518-1407
title: frag_s_load
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
impl_family: inline_asm
speedup: 1.000x
---

## Challenge

The before and after code use identical load-width logic for the tested configuration (16-bit scales). The before version checks b_type_id != kFE2M1f.id() to decide int4 vs int2 load width, while


## Solution

the after version uses !is_8bit_scale. Both evaluate to the same branch for the same scale element size. Since both paths compile to the same PTX instructions for the same data widths and stride calculations, the measured latencies are identical. The optimization is a semantic generalization that...

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- Since both paths compile to the same PTX instructions for the same data widths and stride calculations, the measured latencies are identical.
- The optimization is a semantic generalization that enables MXFP8 (kFE4M3fn weights with kFE8M0fnu scales) to correctly select the int2 load path, which the before code would have missed because b_type_id for kFE4M3fn != kFE2M1f.id() would incorrectly choose int4.
