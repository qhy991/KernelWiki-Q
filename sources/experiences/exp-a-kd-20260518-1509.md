---
id: exp-a-kd-20260518-1509
title: small_8ep_512tok
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- shared-memory-optimization
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 0.758x
---

## Challenge

The before kernel uses per-thread sharding where each thread processes CEILDIV(512,32) = 16 contiguous tokens and writes to its own row of the (blockDim.x+1, 8) tokens_cnts matrix. This spreads writes across distinct shared memory rows, reducing bank conflicts


## Solution

The after kernel uses stride-based iteration (tid, stride = 32) where all threads cycle through the entire input simultaneously. During counting, all 32 threads concurrently increment tokens_cnts[(threadIdx.x+1)*8 + topk_ids[tid]] for different experts, causing more bank conflicts because multipl...

## Results

- **speedup**: 0.758x (regression)


## Key Lessons

- During counting, all 32 threads concurrently increment tokens_cnts[(threadIdx.x+1)*8 + topk_ids[tid]] for different experts, causing more bank conflicts because multiple threads access the same bank offsets within the count matrix rows.
