---
id: exp-a-kd-20260518-0508
title: small_half_hdim1024
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.689x
---

## Challenge

The before kernel uses FP8Wrapper(val) which calls the inline fp8e4m3fn_from_fp32_value function. This function converts float to fp8 via compact integer bit-manipulation: extracting the sign bit, handling denormalized values through a float-addition trick, and rounding via mant_odd extraction. N...


## Solution

The after kernel uses static_cast<__nv_fp8_e4m3>(val), which delegates to NVCC's internal __nv_fp8_e4m3 conversion path. On sm_80 (Ampere), lacking hardware FP8 cvt instructions (available only on sm_89+), NVCC must emulate the conversion through a heavier internal software path involving more in...

## Results

- **speedup**: 0.689x (regression)


## Key Lessons

- The before kernel uses FP8Wrapper(val) which calls the inline fp8e4m3fn_from_fp32_value function.
