---
id: exp-o-kd-20260518-0660
title: '`tmp_sum_i` and then loading that tensor back in the second kernel. That made
  a '
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Replaced scalar `tmp_sum_i` accumulation with an alignment-aware, vectorized `computeDi` reduction that multiplies `grad_output` and `output` directly from registers and reduces inside a warp before storing to shared memory.
Problem before: The old implementation formed the per-r


## Solution

`tmp_sum_i` and then loading that tensor back in the second kernel. That made a bandwidth-heavy scalar reduction part of the critical path and introduced atomic contention on the row accumulator.
Applied change: The new implementation computes `Di = sum(do_i * o_i)` directly from `grad_output` and `

## Key Lessons

- Even on misaligned shapes it still avoids the extra global tensor, so the mechanism is a plausible source of the backward speedups called out in the commit history.
- That made a bandwidth-heavy scalar reduction part of the critical path and introduced atomic contention on the row accumulator.
