---
id: exp-a-kd-20260518-0740
title: fp16_128_vs_256
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.016x
---

## Challenge

Same structural pattern as the bf16 case but using __half2 packed types. The before code loads int4 via __ldg and computes silu-and-mul element-by-element using scalar __half conversions to float. The after code uses ld256/st256 (falling back to two uint4 loads on sm_80)


## Solution

and processes __half2 pairs via __half22float2, float2 dual-lane silu, __float22half2_rn, and __hmul2 intrinsics. The __hmul2 intrinsic processes two half-precision values per instruction, reducing the per-element instruction count. On sm_80, the memory access pattern h

## Results

- **speedup**: 1.016x (neutral)


## Key Lessons

- On sm_80, the memory access pattern has equivalent bandwidth to the before version since ld256 decomposes into two 128-bit loads.
- Same structural pattern as the bf16 case but using __half2 packed types.
