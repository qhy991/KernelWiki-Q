---
id: exp-a-kd-20260518-0154
title: asymmetric_128x256_k4
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.901x
---

## Challenge

In the before kernel, blockIdx.x maps to original matrix rows and threadIdx.x maps to columns. With column-major storage (LDC=M=128), a warp of 32 threads accesses workspace at stride 128: offsets row+0*128, row+1*128, ..., row+31*128 per partition iteration. Each stride-128 access causes the war...


## Solution

The after kernel swaps the grid: blockIdx.x maps to original columns (N=256) and threadIdx.x maps to original rows (M=128). Now the same column-major data is accessed at stride 1: offsets 0+col_idx*128, 1+col_idx*128, ..., 31+col_idx*128. These contiguous addresses coalesce into 1-2 cache line re...

## Results

- **speedup**: 1.901x (improved)


## Key Lessons

- In the before kernel, blockIdx.x maps to original matrix rows and threadIdx.x maps to columns.
