---
id: exp-a-kd-20260518-0401
title: varied_8tok_1024d
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
- register-reuse
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- register-reuse
impl_family: custom_cuda
speedup: 1.003x
---

## Challenge

In the before source, __shared__ float scale creates a single shared-memory location that all 8 warps within one CTA race to write without __syncthreads(). The after source uses const float scale, placing scale in per-thread registers so each warp holds its own value. The measured speedup is only 1.


## Solution

ory-bound loops over hidden_dim=1024 elements (pass-1: fabsf+max scan at stride kWarpSize; pass-2: quantize+clamp+write at stride kWarpSize). A single 4-byte shared-memory write/read vs register access adds negligible overhead relative to these 1024-iteration data-processing loops. The correctness c

## Results

- **speedup**: 1.003x (neutral)


## Key Lessons

- The measured speedup is only 1.003x because the kernel's dominant cost is the two memory-bound loops over hidden_dim=1024 elements (pass-1: fabsf+max scan at stride kWarpSize; pass-2: quantize+clamp+write at stride kWarpSize).
