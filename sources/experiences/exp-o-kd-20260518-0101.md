---
id: exp-o-kd-20260518-0101
title: d64_s512
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- register-reuse
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 1.420x
---

## Challenge

The before code stores the output accumulator R_D as FP16 (half), requiring __half2float conversion before each FMA during online rescaling and __float2half_rn rounding after each FMA — 8 conversion instructions per kWarpTileHeadDimV element per tile iteration


## Solution

The after code stores R_D as FP32, performing the rescaling FMA directly on float values without any format conversion. This eliminates the conversion overhead and reduces instruction count. At seqlen=512 with d=64, the kernel iterates over 8 K-tiles, meaning the per-iteration conversion savings ...

## Results

- **speedup**: 1.420x (improved)


## Key Lessons

- This eliminates the conversion overhead and reduces instruction count.
