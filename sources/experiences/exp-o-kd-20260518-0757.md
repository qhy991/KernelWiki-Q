---
id: exp-o-kd-20260518-0757
title: Only one useful element while the adjacent element in the same cache line is
  als
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Added 128-bit int4 vectorized fast path for interleaved data in swigluoai_and_mul_kernel with int4 input loads and int2 output stores.
Problem before: The swigluoai_and_mul_kernel processes interleaved gate/up data using scalar loads with stride-2 indexing (input[2*idx] for gate,


## Solution

only one useful element while the adjacent element in the same cache line is also relevant but loaded separately.
Applied change: The change vectorizes the interleaved data access using int4 loads that fetch VEC_SIZE consecutive elements (containing PAIRS gate/up pairs) in a single 128-bit transacti

## Key Lessons

- The interleaved access pattern (even indices for gate, odd for up) is particularly inefficient because each scalar load fetches only one useful element while the adjacent element in the same cache line is also relevant but loaded separately.
