---
id: exp-a-kd-20260518-1365
title: warp_reduce_sum_half
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

warp_reduce_sum_half

## Solution

The after source (warp_reduce_sum_half_after.cu) replaces += with `val = fn(val, VLLM_SHFL_XOR_SYNC(val, mask))` where fn is detail::_sum<half>, returning `a + b` for __half type. NVCC inlines the __inline__ __device__ wrapper for half-precision addition just as it does for float, producing equiv...

## Key Lessons

- The before source (warp_reduce_sum_half_before.cu) uses warpReduceSum<half> with `val += VLLM_SHFL_XOR_SYNC(val, mask)` for half-precision accumulation inside the unrolled shuffle-xor loop.
