---
id: exp-o-kd-20260518-0294
title: batched_output
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- shared-memory-optimization
- register-reuse
- epilogue-fusion
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- register-reuse
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.190x
---

## Challenge

The after code replaces three inefficient global-memory patterns in the before code with shared-memory staging and warp-cooperative writes. In the before path, each topk iteration writes one result directly to output[out_idx] (line 78), and a single thread (thread_group_idx == 0) reads back from glo


## Solution

[idx] / output_sum (line 92). This creates 3 global-memory round-trips per result: one write (topk loop), one read (accumulation), and one read-modify-write (rescale). The after path eliminates all of these: results are staged in smem_score/smem_idx shared arrays (line 87-88), output_sum accumulates

## Results

- **speedup**: 1.190x (improved)


## Key Lessons

- The after path eliminates all of these: results are staged in smem_score/smem_idx shared arrays (line 87-88), output_sum accumulates in registers (line 89), and is broadcast via __shfl_sync (line 103).
- The after code replaces three inefficient global-memory patterns in the before code with shared-memory staging and warp-cooperative writes.
