---
id: exp-o-kd-20260518-0854
title: At value, then the kernel launch re-sends that scalar to the device via kernel
  a
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Changed static_scaled_int8_quant signature from float scale (host scalar) to torch::Tensor const& scale (device tensor pointer).
Problem before: The static_scaled_int8_quant function accepted scale as a float host scalar, which was passed as a kernel launch argument to static_sca


## Solution

at value, then the kernel launch re-sends that scalar to the device via kernel argument registers. This unnecessary round-trip through the CPU adds latency and breaks the asynchronous execution pipeline.
Applied change: The signature was changed to accept scale as a torch::Tensor const& (a device te

## Key Lessons

- The kernel now receives a device pointer (scale_type* scale_ptr) instead of a scalar value, and reads the scale with *scale_ptr inside the kernel.
