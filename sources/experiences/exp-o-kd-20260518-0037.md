---
id: exp-o-kd-20260518-0037
title: float_1x224x224_3to4
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- vectorized-loads
impl_family: custom_cuda
speedup: 1.300x
---

## Challenge

The before kernel processes each output element individually: it computes the linear output index, decomposes it into (c, w, h, n) via modular arithmetic (idx%c_out, then cascade integer divisions), conditionally inserts zeros, and writes one scalar float per thread iteration. This results in unc...


## Solution

The after kernel replaces this with a shared-memory tiling approach: 192 threads cooperatively load 192 float4 values (768 bytes each) into shared memory in one coalesced 128-bit read per thread, then each thread reads 3 float elements from shared memory, inserts a zero at every 4th position, and...

## Results

- **speedup**: 1.300x (improved)


## Key Lessons

- This results in uncoalesced 4-byte stores and redundant index math for every element including padding zeros.
