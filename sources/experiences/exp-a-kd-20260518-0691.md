---
id: exp-a-kd-20260518-0691
title: float_64epg_cand
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- activation
- register-reuse
- epilogue-fusion
kernel_types:
- activation
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.410x
---

## Challenge

The before approach requires two kernel launches: compute_swb_kernel writes sigmoid_accurate(scores)+bias to an intermediate scores_with_bias buffer via a full global-memory write pass, then candidates_before_kernel reads from that buffer using the ternary pattern (i < epg) && is_finite(swb[i]) ?...


## Solution

The after approach uses a single fused kernel where each thread reads raw scores[i] and group_bias[i], applies sigmoid_accurate inline (one SFU tanhf instruction), adds bias inline, checks is_finite on the raw input, and uses the result directly in the max-comparison loop. This eliminates (1) one...

## Results

- **speedup**: 1.410x (improved)


## Key Lessons

- This eliminates (1) one kernel launch, (2) the scores_with_bias buffer allocation and its 2xTOTAL_ELEMENTS global-memory write+read cycle, and (3) improves data locality since the sigmoid+bias result stays in registers.
- The before approach requires two kernel launches: compute_swb_kernel writes sigmoid_accurate(scores)+bias to an intermediate scores_with_bias buffer via a full global-memory write pass, then candidates_before_kernel reads from that buffer using the ternary pattern (i < epg) && is_finite(swb[i]) ? sw
