---
id: exp-o-kd-20260518-0855
title: unknown
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

unknown

## Solution

ge: Replace the compile-time pipe_stages=4 constant with a runtime stages parameter. On Ampere+ GPUs (SM80+), stages remains 4. On Turing (SM75), stages is set to 2, roughly halving shared memory consumption for all pipeline-stage-dependent buffers (A-matrix, B-matrix, scales, g_idx). The stages par

## Key Lessons

- Expected impact: On Turing GPUs, using 2 stages instead of 4 reduces shared memory usage enough for thread configs like {128,128,256} and {64,128,128} to fit within the 64KB shared memory limit, enabling the Marlin kernel to actually run.
