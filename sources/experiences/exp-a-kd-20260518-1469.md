---
id: exp-a-kd-20260518-1469
title: vec_vs_scalar_float_512
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
---

vec_vs_scalar_float_512

## Solution

efault, which loads/stores each of the 4 items per thread individually and must handle the valid_items boundary check. The vectorized approach achieves higher memory bandwidth utilization by issuing wider coalesced loads, while the scalar approach incurs overhead from per-element valid_items handlin

## Key Lessons

- The vectorized path cannot safely handle partial sequences in varlen mode because it assumes contiguous aligned full-length data via reinterpret_cast to vec_t, so the scalar fallback is necessary for correctness even though it is slightly slower per element on aligned data.
