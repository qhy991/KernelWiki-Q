---
id: exp-a-kd-20260518-0703
title: swigluoai_scalar
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- activation
- register-reuse
kernel_types:
- activation
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 0.953x
---

## Challenge

The before code uses three explicit ternary comparisons for clamping and four named intermediate variables (gate_f, clamped_gate, clamped_up, sigmoid_val, glu), while the after code uses fminf/fmaxf intrinsics and a single compact expression. However, when both are called inside a simple scalar


## Solution

loop kernel (one element per thread iteration), the nvcc compiler at -O3 already optimizes ternary comparisons into efficient select instructions equivalent to fminf/fmaxf, and the named intermediate variables are dead-stored into registers without additional memory traffic. The dominant bottlen

## Results

- **speedup**: 0.953x (regression)


## Key Lessons

- The ~5% measured delta falls within normal run-to-run variance for a kernel whose runtime is dominated by memory access rather than compute.
