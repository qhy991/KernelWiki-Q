---
id: exp-a-kd-20260518-0765
title: msplit_small_n
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- shared-memory-optimization
- pipeline-stages
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- pipeline-stages
impl_family: custom_cuda
speedup: 1.000x
---

## Challenge

The before code uses a for-loop over tot_m_blocks with max_m_blocks=4 and blocks=sms, computing par from an externally-provided max_par=16. The after code uses a while(rest_m) loop with max_par=128 (for N<=4096) and blocks=sms*blocks_per_sm. At M=64 with thread_m_blocks=4, both patterns produce a si


## Solution

in one go (par_count=1 for tot_m=64 with max_thread_m_blocks*16=64). The dispatch logic difference only manifests when M is large enough to require multiple splits or when the fallback mechanism kicks in for constrained shared memory. Since this microbenchmark uses a simplified naive GEMM kernel (no

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- Since this microbenchmark uses a simplified naive GEMM kernel (not the real Marlin pipeline kernel), the blocks_per_sm and max_par tuning have no effect on the identical grid configuration for M=64.
- At M=64 with thread_m_blocks=4, both patterns produce a single kernel launch covering the entire M dimension in one go (par_count=1 for tot_m=64 with max_thread_m_blocks*16=64).
