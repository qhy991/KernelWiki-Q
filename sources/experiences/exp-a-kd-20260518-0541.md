---
id: exp-a-kd-20260518-0541
title: fill_256exp
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
- shared-memory-optimization
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 0.848x
---

## Challenge

With 256 experts, the direct thread-per-expert approach still outperforms binary search in this isolated test. The before code uses 256 threads that each read two values from global cumsum and write 1-4 block entries, which is very cheap


## Solution

The after code's binary search requires O(log2(256))=8 comparison iterations per block, each reading prefix[mid] from global memory. Even though binary search distributes across all 1024 threads, the per-block computational overhead (8 iterations with global memory reads) exceeds the savings from...

## Results

- **speedup**: 0.848x (regression)


## Key Lessons

- The regression is larger at 256 experts than at 64 because the binary search depth grows to 8 iterations.
