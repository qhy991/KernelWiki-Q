---
id: exp-o-kd-20260518-0085
title: float_medium
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- activation
- register-reuse
kernel_types:
- activation
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 2.306x
---

## Challenge

The after source folds SiLU recomputation, `dx2`, `dx1`, and `x4` production into one kernel, so each element loads `x1`


## Solution

, `x2`, and `dx4` once and reuses the computed sigmoid and `x3` in registers. The before source launches five separate k

## Results

- **speedup**: 2.306x (improved)


## Key Lessons

- The after source folds SiLU recomputation, `dx2`, `dx1`, and `x4` production into one kernel, so each element loads `x1`, `x2`, and `dx4` once and reuses the computed sigmoid and `x3` in registers.
