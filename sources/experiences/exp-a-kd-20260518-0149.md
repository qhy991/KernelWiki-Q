---
id: exp-a-kd-20260518-0149
title: f16_m16n8k16
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- register-reuse
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: inline_asm
speedup: 1.001x
---

## Challenge

The before code uses uint32_t* reinterpret_cast and "r" (integer register) constraints for the f32 accumulator (C) and result (D) in the mma.sync.aligned.m16n8k16 F16 inline asm. The after code changes these to float* and "f" (float register) constraints. With NVCC 12.8


## Solution

-O3, the ptxas backend correctly infers that the HMMA instruction's accumulator operands must reside in float registers regardless of the C-level type or constraint annotation, so no cross-bank MOV instructions are generated in either version. The 0.07% measured differen

## Results

- **speedup**: 1.001x (neutral)


## Key Lessons

- This optimization pattern (using type-matching asm constraints) was a genuine performance fix for CUDA 11-era compilers that would generate unnecessary integer-to-float register moves, but has been rendered moot by compiler improvements.
