---
id: exp-o-kd-20260518-0012
title: b2b_gemm_f16_sm80_medium
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- pipeline-stages
- register-reuse
- epilogue-fusion
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- register-reuse
- epilogue-fusion
impl_family: custom_cuda
speedup: 2.151x
---

## Challenge

The after code achieves a 2.15x speedup primarily because it eliminates the intermediate tensor_D0 round-trip through global memory. In the before code, Gemm0 writes its output to tensor_D0 in DRAM, then Gemm1 reads that same tensor_D0 back from DRAM as its A-ma


## Solution

trix input — costing 2*M*N0*sizeof(half_t) = 2*512*64*2 = 128KB of DRAM traffic. The fused after code keeps the intermediate in the register file, so GEMM0's output never leaves the SM. Additionally, the before code requires two separate kernel launches (with dr

## Results

- **speedup**: 2.151x (improved)


## Key Lessons

- The after code achieves a 2.15x speedup primarily because it eliminates the intermediate tensor_D0 round-trip through global memory.
