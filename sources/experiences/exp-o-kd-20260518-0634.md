---
id: exp-o-kd-20260518-0634
title: Red-memory pipeline stages. The first GEMM (Q @ K^T) always used the maximum
  num
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Replace bool kSingleValueIteration template parameter with int kMaxK in run_grouped() and run_attention() function templates, and update call sites to pass concrete kMaxK values instead of boolean flags.
Problem before: The fMHA kernel dispatch used a boolean kSingleValueIteratio


## Solution

red-memory pipeline stages. The first GEMM (Q @ K^T) always used the maximum number of shared-memory stages regardless of head dimension, wasting SMEM and limiting occupancy for small-head-dimension workloads.
Applied change: Replace the bool kSingleValueIteration with int kMaxK. Inside DefaultFMHAG

## Key Lessons

- Optimization focus: Replace bool kSingleValueIteration template parameter with int kMaxK in run_grouped() and run_attention() function templates, and update call sites to pass concrete kMaxK values instead of boolean flags.
