---
id: exp-a-kd-20260518-0392
title: fp16_17grp_128gs_dyn
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.998x
---

## Challenge

The before version launches 2 blocks of 256 threads. Block 0 has 16 valid groups (no divergence); block 1 has only 1 valid group with 240 out-of-bounds threads skipping the absmax and quantization loops via the guard, causing severe branch divergence. The after version launches 17 blocks of 16 threa


## Solution

nce (slightly slower after) reflects a trade-off: the before version's 256-thread blocks have higher occupancy potential but 240/256 threads in block 1 are wasted, while the after version's 16-thread blocks have very low occupancy (only 16 threads per SM on A800 which supports up to 2048) but every 

## Results

- **speedup**: 0.998x (neutral)


## Key Lessons

- The primary value of the after version is correctness: it eliminates the __syncthreads-inside-divergent-conditional bug from the original code (addressed by KP_000's warp shuffle) and prevents any out-of-bounds pointer computation for non-divisible num_groups.
