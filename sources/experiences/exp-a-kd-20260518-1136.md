---
id: exp-a-kd-20260518-1136
title: bf16_key_load
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.714x
---

## Challenge

The after code uses __nv_cvt_fp8_to_halfraw which is a Hopper (sm_89+) hardware intrinsic; on this sm_80 A800 GPU it must be emulated by the CUDA runtime, producing a much slower software fallback


## Solution

path. The before code reads bf16 directly with a single 32-bit load, while the after code must perform two fp8->half conversions via the emulated intrinsic plus an additional half->float->bf16 conv

## Results

- **speedup**: 0.714x (regression)


## Key Lessons

- The after code uses __nv_cvt_fp8_to_halfraw which is a Hopper (sm_89+) hardware intrinsic; on this sm_80 A800 GPU it must be emulated by the CUDA runtime, producing a much slower software fallback path.
