---
id: exp-a-kd-20260518-0854
title: large_36mb
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- pipeline-stages
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- vectorized-loads
impl_family: custom_cuda
---

large_36mb

## Solution

The after code keeps all data on-device: it uploads a 256-byte LUT to constant memory once, then launches a single GPU kernel that processes the data in-place using uint4 (16-byte) vector loads and constant-cache-backed LUT lookups. The dominant cost in the before path is the two ~144 MB PCIe tra...

## Key Lessons

- The dominant cost in the before path is the two ~144 MB PCIe transfers (~12 GB/s effective bandwidth yields ~12 ms per direction, plus CPU processing), while the after kernel completes in ~1 ms since it only touches device memory at high internal bandwidth (~900 GB/s HBM) and the nibble remap is a s
