---
id: exp-a-kd-20260518-0648
title: fused_loop
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- shared-memory-optimization
- epilogue-fusion
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- epilogue-fusion
impl_family: custom_cuda
speedup: 0.992x
---

## Challenge

The after code fuses sigmoid, shared-memory bias backup, bias addition, and index assignment into a single Vlen-width loop, but in this isolated microbenchmark it shows no measurable improvement over the before code's two separate unrolled loops. The compiler with #pragma unroll already schedules bo


## Solution

body does not change the effective instruction order. Additionally, the after code introduces overhead absent in the before code: it writes bias_chunk[v] to shared memory (smem_bias[tidx + v]) for tidy==0 threads, and assigns idx_chunk[v] = tidx + v. These extra shared-memory writes and integer oper

## Results

- **speedup**: 0.992x (neutral)


## Key Lessons

- The real advantage of the fused pattern in the original kernel is downstream: smem_bias enables score restoration via (thread_max_val - smem_bias[thread_max_idx]) during the topk loop, eliminating the need to carry row_chunk through the entire topk iteration.
