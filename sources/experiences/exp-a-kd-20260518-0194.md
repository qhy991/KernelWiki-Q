---
id: exp-a-kd-20260518-0194
title: warp_idx_compute
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

warp_idx_compute

## Solution

global memory reads and writes (1M elements), the warp_idx value is computed only once and is loop-invariant. The compiler likely hoists the division out of the loop regardless of whether __shfl_sync or plain arithmetic is used, and the __shfl_sync overhead (~20-30 cycles) is amortized a

## Key Lessons

- The before code uses __shfl_sync(0xffffffff, threadIdx.x / 32, 0) to compute warp_idx once at kernel entry, and the after code uses canonical_warp_idx() which is simply threadIdx.x / 32 without the shuffle barrier.
