---
id: exp-a-kd-20260518-0044
title: b2b_gemm_unfused
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
- shared-memory-optimization
- pipeline-stages
- register-reuse
- epilogue-fusion
kernel_types:
- gemm
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- pipeline-stages
- register-reuse
- epilogue-fusion
impl_family: cutlass
speedup: 0.875x
---

## Challenge

The before code launches two separate GEMM kernels with a global memory intermediate buffer (C_int), while the after code fuses both GEMMs into a single kernel that keeps the intermediate result in registers. The regression occurs because the simplified fused kernel does not correctly replicate the 


## Solution

pping by reusing accum0 values across K slices, which introduces redundant computation in the second GEMM's inner loop. In the real CUTLASS B2bMmaPipelined, FragmentIteratorA1 properly iterates over the accumulator tile to extract the correct A1 fragments for each warp-level MMA iteration, and the i

## Results

- **speedup**: 0.875x (regression)


## Key Lessons

- The regression occurs because the simplified fused kernel does not correctly replicate the CUTLASS FragmentIteratorA1 pattern: it approximates the accumulator-to-A1 mapping by reusing accum0 values across K slices, which introduces redundant computation in the second GEMM's inner loop.
