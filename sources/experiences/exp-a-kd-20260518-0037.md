---
id: exp-a-kd-20260518-0037
title: gelu_erff_half
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- pipeline-stages
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: inline_asm
---

gelu_erff_half

## Solution

uction tanh.approx.f32, and converts back. For __half data (2 bytes per element), the memory bandwidth demand is half that of float32, so the kernel is compute-bound rather than memory-bound. In this regime, replacing the expensive erff SFU call (which requires many clock cycles and cannot be pipeli

## Key Lessons

- For __half data (2 bytes per element), the memory bandwidth demand is half that of float32, so the kernel is compute-bound rather than memory-bound.
