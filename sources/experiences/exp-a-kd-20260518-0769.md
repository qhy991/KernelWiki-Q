---
id: exp-a-kd-20260518-0769
title: xor_full_row_128r
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 0.971x
---

## Challenge

The before kernel computes shared memory indices using col ^ row (XOR with full row), while the after kernel uses col ^ (row % 8) (XOR with only the lower 3 bits of row). Both patterns provide equivalent bank-conflict avoidance for the 8-stride shared memory layout: XOR with row % 8 permutes the 8 c


## Solution

ffective way as XOR with full row, because only the column position (0-7) affects bank selection for stride-8 accesses. The after version adds one extra modulo operation (row % 8) per access, which costs ~1 ALU cycle, but this is negligible within the loop body that already includes a shared memory 

## Results

- **speedup**: 0.971x (regression)


## Key Lessons

- Both patterns provide equivalent bank-conflict avoidance for the 8-stride shared memory layout: XOR with row % 8 permutes the 8 columns across the 32 4-byte banks in the same effective way as XOR with full row, because only the column position (0-7) affects bank selection for stride-8 accesses.
