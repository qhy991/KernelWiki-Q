---
id: exp-o-kd-20260518-0380
title: repack_8bit
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- register-reuse
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- fine-grained-quantization
impl_family: custom_cuda
speedup: 1.104x
---

## Challenge

The 8-bit improvement comes from eliminating the b1_vals/b2_vals local arrays of size tile_ints=4 each (8 uint32 registers total). The GPTQ before kernel first reads 4+4=8 shared-memory values into these arrays, then extracts 8 values from them in a second loop. The AWQ after kernel skips the interm


## Solution

ur_n_pos, reads exactly 2 values per iteration (packed_src_0 and packed_src_1), and extracts directly. This reduces register pressure (8 fewer uint32 local variables), reduces total shared-memory reads from 8 to 8 per warp-tile (but without the intermediate storage), and simplifies the address compu

## Results

- **speedup**: 1.104x (improved)


## Key Lessons

- The 8-bit case benefits more than 4-bit because tile_ints=4 (vs 2 for 4-bit), meaning the eliminated local array is larger and the saved register pressure and instruction count are more significant.
