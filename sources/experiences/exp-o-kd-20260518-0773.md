---
id: exp-o-kd-20260518-0773
title: unknown
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: cutlass
---

unknown

## Solution

ransfer can start, and the CPU must wait until the transfer finishes. Additionally, when scales initially reside on the GPU, moving them to the CPU causes torch.compile graph breaks.
Applied change: The refactored code always passes the GPU pointer (a_scales.data_ptr<float>() / b_scales.data_ptr<flo

## Key Lessons

- This creates a pipeline stall because all pending GPU work must complete before the transfer can start, and the CPU must wait until the transfer finishes.
- Applied change: The refactored code always passes the GPU pointer (a_scales.data_ptr<float>() / b_scales.data_ptr<float>()) directly to the CUTLASS epilogue visitors, along with a boolean flag (a_scales.numel() != 1 / b_scales.numel() != 1) that indicates whether the scale is a vector requiring broa
