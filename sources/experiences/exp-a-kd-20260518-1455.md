---
id: exp-a-kd-20260518-1455
title: half_8h_128hs
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 0.721x
---

## Challenge

The before kernel launches 512 threads per block (min(8*128, 512)) with a strided scalar loop writing 2-byte half elements. The after kernel launches 64 threads per block (min(8*16, 512)) with vectorize_with_alignment<8> for key writes (8-half vector = 128-bit transactions) and an unrolled 8-iterati


## Solution

0%) because the after kernel's vectorized 128-bit key-store transactions only cover 8 x 2-byte = 16 bytes of key data per thread — the same byte width as the float case's 4 x 4-byte = 16 bytes. With half elements, the before kernel's 512 threads can write 1024 x 2-byte = 2048 bytes per token with hi

## Results

- **speedup**: 0.721x (regression)


## Key Lessons

- The regression is more pronounced for half than float (38.78% vs 25.70%) because the after kernel's vectorized 128-bit key-store transactions only cover 8 x 2-byte = 16 bytes of key data per thread — the same byte width as the float case's 4 x 4-byte = 16 bytes.
- The after kernel launches 64 threads per block (min(8*16, 512)) with vectorize_with_alignment<8> for key writes (8-half vector = 128-bit transactions) and an unrolled 8-iteration value loop.
