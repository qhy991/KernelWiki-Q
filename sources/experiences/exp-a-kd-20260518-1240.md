---
id: exp-a-kd-20260518-1240
title: seq16k
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- shared-memory-optimization
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
---

seq16k

## Solution

oarse 256-bin FP16 histogram over all elements in shared memory, then only refines the ambiguous threshold-bin candidates across up to 4 byte-level passes. The key difference is that the after code never sorts elements above the threshold — it directly writes them to output — and dis

## Key Lessons

- With seq_len=16384 and TopK=2048, most elements are either clearly above or below the threshold after the coarse pass, so the refinement work is proportional to the small threshold-bin subset rather than the full sequence.
- The key difference is that the after code never sorts elements above the threshold — it directly writes them to output — and discards elements below the threshold without further processing.
