---
id: exp-a-kd-20260518-1154
title: large_64experts
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- pipeline-stages
- register-reuse
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- register-reuse
impl_family: custom_cuda
---

large_64experts

## Solution

after code accumulates the count in the register variable 'occurrences' — a pure ALU increment with no memory ordering overhead — and issues just one atomicAdd per thread after the loop completes. This reduces the number of atomic operations from O(topk_length) to at most THREADS_PER_EXPERT=512 p...

## Key Lessons

- Since all 512 threads in a block target the same atomic_buffer[expert_id] address, these atomics serialize on the same memory location, causing severe contention and pipeline stalls.
