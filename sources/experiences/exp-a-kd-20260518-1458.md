---
id: exp-a-kd-20260518-1458
title: coalesced_atomic
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- transpose
kernel_types:
- transpose
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.996x
---

## Challenge

The before kernel (scattered_atomic_kernel) uses adr = mindx + M * nindx where mindx = m_group*16 + (tid%16) and nindx = (j + (tid/16)*4) + nt*NTILE + (N/GrpsShrB)*grp. CPU simulation confirms that for tid=0 j=0..3, addresses are 0, 128, 256, 384 -- each M=128 floats (512 bytes) apart, meaning each 


## Solution

2,3 -- contiguous within the same 128-byte cache line segment. The g_mindx = m*4 + (tid%64) formula provides a wider contiguous base (64 elements wide) compared to scattered's mindx (16 elements wide per m_group), and g_nindx groups 4 adjacent N-columns with stride M*4 instead of M, so that the 4 at

## Results

- **speedup**: 0.996x (neutral)


## Key Lessons

- The g_mindx = m*4 + (tid%64) formula provides a wider contiguous base (64 elements wide) compared to scattered's mindx (16 elements wide per m_group), and g_nindx groups 4 adjacent N-columns with stride M*4 instead of M, so that the 4 atomicAdds per j-loop target addresses that are 512 elements apar
