---
id: exp-o-kd-20260518-0337
title: large_64k
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.456x
---

## Challenge

The 45.6% speedup on the large_64k case stems from the progressive histogram's early-exit scan and finer binning. With 65536 elements and topK=100, the 512-bin before code must scan all bins in one BlockScan, then do a full second pass for collection


## Solution

The after code's 2048-bin step-0 histogram places the threshold bin earlier in the sorted order; the prefix-sum rounds with __syncthreads_or can break after finding it, often in just 1-2 rounds instead of 4. Additionally, the progressive approach's narrowing isPartialMatch filtering in later step...

## Results

- **speedup**: 1.456x (improved)


## Key Lessons

- The after code's 2048-bin step-0 histogram places the threshold bin earlier in the sorted order; the prefix-sum rounds with __syncthreads_or can break after finding it, often in just 1-2 rounds instead of 4.
