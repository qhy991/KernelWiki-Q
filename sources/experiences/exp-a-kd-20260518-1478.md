---
id: exp-a-kd-20260518-1478
title: nibble_lut_1mb
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.298x
---

## Challenge

Same mechanism as the 16MB case: the __constant__ kNibbleLUT suffers from warp-divergent constant-cache serialization. Each thread in the warp reads a different LUT index (kNibbleLUT[b[i]] for 16 local bytes b[0]..b[15] plus different ind


## Solution

ices across threads), forcing the constant cache to service each unique address request sequentially. The __device__ lut_global reads are handled by the L2 cache, which services divergent addresses from different threads in parallel via i

## Results

- **speedup**: 0.298x (regression)


## Key Lessons

- The smaller 1MB dataset shows a less extreme but still clear regression (~3.3x slower), because the constant-cache serialization overhead dominates even at smaller data volumes where the computation itself is fast.
