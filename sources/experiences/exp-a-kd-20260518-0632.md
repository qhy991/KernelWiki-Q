---
id: exp-a-kd-20260518-0632
title: nibble_extract_16K
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- pipeline-stages
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: inline_asm
speedup: 1.002x
---

## Challenge

The before code uses 3 explicit shift instructions (>>4, >>8, >>12) plus 4 lop3 and 4 sub.f16x2 operations per thread to extract and convert nibbles using a single BOTTOM_MASK. The after code reduces shifts to 1 (>>8) by applying BOTTOM_MASK and TOP_MASK on both the original


## Solution

and once-shifted word, and uses 2 sub.f16x2 plus 2 fma.rn.f16x2 for conversion. On NVIDIA GPUs, SHL/SHR instructions are single-cycle ALU operations and the lop3.b32 count remains 4 in both variants, so saving 2 shifts per thread (0.24% of the ~11-instruction per-thread mix)

## Results

- **speedup**: 1.002x (neutral)


## Key Lessons

- The before code uses 3 explicit shift instructions (>>4, >>8, >>12) plus 4 lop3 and 4 sub.f16x2 operations per thread to extract and convert nibbles using a single BOTTOM_MASK.
