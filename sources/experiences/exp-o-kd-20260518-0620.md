---
id: exp-o-kd-20260518-0620
title: unknown
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

unknown

## Solution

the TMEM buffer, so the pipeline never transitions to a state where the consumer can proceed.
Applied change: Added `is_accumulator_needed` guard to the first-iteration accumulator wait: `if (is_first_iteration && is_accumulator_needed)`. When K=0, the consumer_wait i

## Key Lessons

- Problem before: When K=0, `acc_pipeline.consumer_wait()` on the first iteration would deadlock because the MMA warp never writes accumulator data into the TMEM buffer, so the pipeline never transitions to a state where the consumer can proceed.
