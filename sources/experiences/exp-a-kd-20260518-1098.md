---
id: exp-a-kd-20260518-1098
title: arch_guard_sm75
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- pipeline-stages
- vectorized-loads
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- vectorized-loads
impl_family: inline_asm
speedup: 7.142x
---

## Challenge

The before code uses a scalar loop that extracts each 4-bit value and converts it to fp16 one at a time, requiring 8 iterations per element with per-element float conversion


## Solution

The after code uses lop3.b32 to extract two pairs of 4-bit values in a single instruction and fma.rn.f16x2/sub.f16x2 for packed half2 arithmetic, processing all 8 values with only 4 lop3 + 4 arithmetic instructions instead of 8 scalar iterations. This vectorized PTX path reduces instruction count...

## Results

- **speedup**: 7.142x (improved)


## Key Lessons

- The after code uses lop3.b32 to extract two pairs of 4-bit values in a single instruction and fma.rn.f16x2/sub.f16x2 for packed half2 arithmetic, processing all 8 values with only 4 lop3 + 4 arithmetic instructions instead of 8 scalar iterations.
