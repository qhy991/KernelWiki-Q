---
id: exp-o-kd-20260518-0755
title: unknown
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

unknown

## Solution

also mixed the bounds check with the value read in a single expression, which could limit compiler optimization of the load path.
Applied change: The candidates loop now reads raw scores and bias separately. It first initializes candidates to neg_inf, then conditionally reads the raw score, checks i

## Key Lessons

- This replaces the single scores_with_bias buffer read with an inline fused computation, eliminating the intermediate buffer while also restructuring the code into a cleaner if-block that separates the bounds check from the transformation.
- It first initializes candidates to neg_inf, then conditionally reads the raw score, checks is_finite on the raw input, applies sigmoid inline if scoring_func==SCORING_SIGMOID, and adds the per-element bias.
