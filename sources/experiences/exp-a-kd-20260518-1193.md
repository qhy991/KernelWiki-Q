---
id: exp-a-kd-20260518-1193
title: vcache_inline_fp8_head128
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- register-reuse
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- fine-grained-quantization
impl_family: custom_cuda
speedup: 1.000x
---

## Challenge

The after code uses inline V-cache fp8 dequantization where each fp8 chunk is loaded into a local uint8_t fp8_temp[8] temporary and immediately converted to Half8 via inline_dequant_fp8_chunk, storing directly into Vlocal[h][d]. The before code uses deferred dequantization where all raw fp8 V data i


## Solution

t Vlocalb8[VHELOOP][VTLOOP*VBLOCKS][8] buffer (2*32*8 = 512 bytes), then batch-converted to Half8 Vlocal[VHELOOP][VTLOOP*VBLOCKS] (2*32*16 = 1024 bytes) in a separate loop. The inline approach avoids the large Vlocalb8[][][] allocation, reducing peak register pressure since both buffers would coexis

## Results

- **speedup**: 1.000x (neutral)


## Key Lessons

- The inline approach avoids the large Vlocalb8[][][] allocation, reducing peak register pressure since both buffers would coexist during Phase 2 in the deferred approach.
- For V, which has a 2D buffer structure [VHELOOP][VTLOOP*VBLOCKS], the deferred pattern requires significantly more register space than K's compact 1D Klocalb8[KHELOOP] buffer.
