---
id: exp-a-kd-20260518-0780
title: float_aligned_hs4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.983x
---

## Challenge

This case benchmarks the full rms_norm kernel with float type across 1000 tokens. In the before source, both variance and output loops read 4-byte float values with scalar iterations. In the after source, variance uses vec_n_t<float, 8> (256-bit loads) but the output loop (lines 109-112) is unchange


## Solution

loat elements per token with scalar 4-byte loads. For float data, the output normalization loop reads 16KB per token (4096 × 4 bytes) twice (once for variance, once for output), and the unchanged output loop dominates total kernel time because float data is 2x larger than __half. The variance vector

## Results

- **speedup**: 0.983x (neutral)


## Key Lessons

- For float data, the output normalization loop reads 16KB per token (4096 × 4 bytes) twice (once for variance, once for output), and the unchanged output loop dominates total kernel time because float data is 2x larger than __half.
