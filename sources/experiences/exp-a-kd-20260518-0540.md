---
id: exp-a-kd-20260518-0540
title: fill_64exp
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
- shared-memory-optimization
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
---

fill_64exp

## Solution

The after code uses binary search on the prefix array to determine which expert owns each block, requiring O(log2(64))=6 comparison iterations per block where each iteration reads from global memory (prefix[mid]). In the isolated test, both approaches read their index arrays from global memory, a...

## Key Lessons

- In the full kernel context, the binary search reads from shared memory (prefix[] computed by Blelloch scan resides in smem), while the direct approach would need a global memory round-trip for cumsum, making the shared-memory advantage of binary search context-dependent.
