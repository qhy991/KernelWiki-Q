---
id: exp-a-kd-20260518-1433
title: fp8_sf_param
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- pipeline-stages
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
speedup: 1.001x
---

## Challenge

The after code removes the min_scaling_factor kernel parameter and computes 1/(224*512) inside the kernel via min_scaling_factor<SimFP8E4M3FNuz>::val(). The __device__ __forceinline__ trait call evaluates to the same constant value a


## Solution

s the before code passes via the runtime parameter. NVCC folds both into an immediate operand in the fmax instruction. Removing one float from the kernel parameter buffer saves 4 bytes at kernel launch, but the A800 launch pipeline a

## Results

- **speedup**: 1.001x (neutral)


## Key Lessons

- The change is a code-structure improvement (centralizing per-type constants in traits) rather than a performance optimization.
