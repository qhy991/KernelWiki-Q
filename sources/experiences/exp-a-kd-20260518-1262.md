---
id: exp-a-kd-20260518-1262
title: pertoken_16384
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- fine-grained-quantization
- epilogue-fusion
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
- epilogue-fusion
impl_family: custom_cuda
speedup: 7.932x
---

## Challenge

The after kernel fuses absmax reduction and quantization into a single kernel launch per token, replacing the before's two-kernel approach. In the before code, segmented_max_reduction uses a serial shared-memory reduction tree with multiple __syncthreads barriers per level, plus


## Solution

an atomicMax on global memory across blocks, while scaled_quant_kernel re-reads the entire input in a second pass. The after kernel reads each token's input only once for absmax (via thread_max_vec with vec4_t loads) and once for quantization (via scaled_fp8_conversion_vec with v

## Results

- **speedup**: 7.932x (improved)


## Key Lessons

- The after kernel reads each token's input only once for absmax (via thread_max_vec with vec4_t loads) and once for quantization (via scaled_fp8_conversion_vec with vec4_t loads), with blockReduceMax using warp shuffles instead of a shared-memory tree, eliminating most __syncthreads and global atomic
