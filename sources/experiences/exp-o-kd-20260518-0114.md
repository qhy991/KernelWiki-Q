---
id: exp-o-kd-20260518-0114
title: hgemm_tn_4096
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.030x
---

## Challenge

The after code launches the kernel with BLOCK_SWIZZLE=true using a 3D grid (16,32,2) where N_SWIZZLE=2 groups the 32 column-tile blocks into two z-dimension slices. Inside the kernel, bx=blockIdx.z*16+blockIdx.x remaps adjacent x-dimension blocks to dis


## Solution

tant column tiles separated by 16*128=2048 columns. In the before code, all 32 column blocks map sequentially (bx=blockIdx.x from 0..31), causing adjacent blocks to access overlapping B-matrix rows and compete for the same L2 cache sets. The swizzle rem

## Results

- **speedup**: 1.030x (improved)


## Key Lessons

- The after code launches the kernel with BLOCK_SWIZZLE=true using a 3D grid (16,32,2) where N_SWIZZLE=2 groups the 32 column-tile blocks into two z-dimension slices.
