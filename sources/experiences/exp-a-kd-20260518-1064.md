---
id: exp-a-kd-20260518-1064
title: rms_block1024
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 0.993x
---

## Challenge

The before code uses a two-phase warp-shuffle reduction: first warp-level via __shfl_xor_sync across all lanes, then cross-warp combination via shared memory and a second warp shuffle across the first-lane values


## Solution

The after code uses cub::BlockReduce<float, 1024> with blockDim.x as numValid. CUB internally employs a similar warp-shuffle + shared-memory strategy for full-block reductions, so the two approaches produce nearly equivalent PTX for this configuration. The small difference falls within measuremen...

## Results

- **speedup**: 0.993x (neutral)


## Key Lessons

- The before code uses a two-phase warp-shuffle reduction: first warp-level via __shfl_xor_sync across all lanes, then cross-warp combination via shared memory and a second warp shuffle across the first-lane values.
