---
id: exp-a-kd-20260518-1346
title: small_m_decode
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- transpose
kernel_types:
- transpose
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.377x
---

## Challenge

The before kernel launches with grid.y=M=8, creating only 8 thread block rows, while the after kernel transposes the problem so grid.y=N=8192, creating many more blocks. However, with M=8 the before kernel already has sufficient blocks for SM occupancy on an A800 (16 blocks), and the transposed m...


## Solution

in the after kernel reads A_swap (B^T) in row-major and B_swap (A^T) in column-major, which is less cache-friendly than the before kernel's A row-major and B column-major access pattern. The resulting L2 cache thrashing from transposed B accesses dominates, causing the regression.

## Results

- **speedup**: 0.377x (regression)


## Key Lessons

- However, with M=8 the before kernel already has sufficient blocks for SM occupancy on an A800 (16 blocks), and the transposed memory access pattern in the after kernel reads A_swap (B^T) in row-major and B_swap (A^T) in column-major, which is less cache-friendly than the before kernel's A row-major 
