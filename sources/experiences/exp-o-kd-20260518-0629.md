---
id: exp-o-kd-20260518-0629
title: For both the source-load and the no-source branches, retaining unnecessary
  globa
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Added ScaleType::Kind Scale template parameter and compile-time branching in is_source_needed() to eliminate source-loading at compile time when OnlyAlphaScaling is selected.
Problem before: The is_source_needed() method in LinearCombinationClamp (and its partial specialization a


## Solution

for both the source-load and the no-source branches, retaining unnecessary global memory reads and multiply-add operations that would never execute when beta was known to be zero at the call site.
Applied change: A new ScaleType::Kind template parameter (with default ScaleType::Default for backward 

## Key Lessons

- Because Scale is a compile-time constant, the compiler can eliminate unreachable branches entirely via dead code elimination when OnlyAlphaScaling or NoBetaScaling is selected, producing tighter epilogue code that skips source tensor loads and beta-multiply operations.
- Problem before: The is_source_needed() method in LinearCombinationClamp (and its partial specialization and FastLinearCombinationClamp) relied solely on a runtime comparison beta_ != ElementCompute(0) to decide whether the source tensor (C matrix) must be loaded during the GEMM epilogue.
