---
id: exp-a-kd-20260518-1168
title: gqa_8q_2kv
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.013x
---

## Challenge

The before code uses an 8-head MHA K-cache layout (block_stride=8192), while the after code uses a 2-head GQA layout (block_stride=2048) with head_mapping={0,0,0,0,1,1,1,1}. The 4x KV head compression reduces total K-cache from ~


## Solution

8MB to ~2MB, offering moderate spatial locality improvement. However, each query head still reads HEAD_SIZE=64 half-values per block regardless of stride, so the per-call dot-product workload dominates equally in both versions. T

## Results

- **speedup**: 1.013x (neutral)


## Key Lessons

- The before code uses an 8-head MHA K-cache layout (block_stride=8192), while the after code uses a 2-head GQA layout (block_stride=2048) with head_mapping={0,0,0,0,1,1,1,1}.
