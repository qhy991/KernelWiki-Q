---
id: exp-o-kd-20260518-0759
title: Overhead (branch, counter update) and prevents the compiler from interleaving
  it
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Add static NGroup template parameter with #pragma unroll loop dispatch.
Problem before: The group iteration loop `for (int i_group = 0; i_group < n_group; i_group++)` in group_idx_and_topk_idx_kernel uses a runtime variable n_group as the loop bound. The compiler cannot fully unr


## Solution

overhead (branch, counter update) and prevents the compiler from interleaving iterations for better instruction scheduling and register usage.
Applied change: Added an `int NGroup = -1` template parameter to group_idx_and_topk_idx_kernel. When NGroup > 0, the loop is refactored into a lambda `proces

## Key Lessons

- A non-unrolled loop incurs loop overhead (branch, counter update) and prevents the compiler from interleaving iterations for better instruction scheduling and register usage.
