---
id: exp-a-kd-20260518-0701
title: per_channel_g128_h2048
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

per_channel_g128_h2048

## Solution

s more efficient: all 256 threads do 8 iterations with coalesced access, and the 4-byte float scalar loads are already wide enough that float4 vectorization provides modest benefit. The regression is smaller than group_n=32 (1.4x vs 7.6x) because larger groups improve thread utiliz

## Key Lessons

- The regression is smaller than group_n=32 (1.4x vs 7.6x) because larger groups improve thread utilization per iteration and reduce outer-loop overhead.
- For FP8 quantization (1-byte output, VEC_SIZE=16), the 16x vectorization benefit for memory throughput would make this pattern potentially beneficial when group_n is large enough to provide reasonable per-group thread utilization.
