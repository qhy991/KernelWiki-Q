---
id: exp-o-kd-20260518-0319
title: fp16_d4096
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 2.508x
---

## Challenge

The before kernel processes each element individually: one __ldg load for x, one for y, one scalar store for output, repeated d times per thread stride. This results in 3*d separate 4-byte memory transactions per token. The after kernel replaces these with int4 (128-bit) vectorized loads and stores:


## Solution

values in one 16-byte transaction, and each int4 store writes 2 floats in one transaction. The total memory transactions drop from 3*d scalar accesses to approximately 3*d/2 vector accesses (2 for x_vec, 2 for y_vec, 2 for out_vec iterations), plus a small scalar cleanup for remainder elements. For 

## Results

- **speedup**: 2.508x (improved)


## Key Lessons

- This results in 3*d separate 4-byte memory transactions per token.
