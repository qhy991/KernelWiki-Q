---
id: exp-o-kd-20260518-0815
title: As unnecessary because the caller only needs the indices, not sorted logits,
  and
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Eliminate the final TopKSort and its associated logit/indices loading, replacing with direct smemIndices-to-global store.
Problem before: After collecting the top-K indices and logits into shared memory, the kernel performed a full TopKSort (cub::BlockRadixSort) to sort the top-K


## Solution

as unnecessary because the caller only needs the indices, not sorted logits, and the indices do not need to be in logit-sorted order.
Applied change: Remove the entire TopKSort stage: the register arrays topKLogits/topKIndices, the smemLogits loads into those arrays, the cub::BlockRadixSort call, an

## Key Lessons

- This sort was unnecessary because the caller only needs the indices, not sorted logits, and the indices do not need to be in logit-sorted order.
