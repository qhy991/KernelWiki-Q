---
id: exp-a-kd-20260518-0739
title: bf16_128_vs_256
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: inline_asm
speedup: 1.022x
---

## Challenge

On sm_80 (A800, cc 8.0), ld256 decomposes into two separate uint4 __ldg loads and st256 into two uint4 stores, so total memory bandwidth per transaction matches the before code's single int4 __ldg load. The after code's packed inner loop uses __nv_bfloat162 types with __bfloat1622float2 conver


## Solution

sion, float2 dual-lane silu computation, __float22bfloat162_rn reconversion, and __hmul2 for the gating multiplication. This reduces instruction count per element by processing two bf16 values per inner-loop iteration instead of one. However, the 2.1% measured improvement is within typical mic

## Results

- **speedup**: 1.022x (improved)


## Key Lessons

- This reduces instruction count per element by processing two bf16 values per inner-loop iteration instead of one.
