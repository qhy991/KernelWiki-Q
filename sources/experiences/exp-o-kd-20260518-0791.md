---
id: exp-o-kd-20260518-0791
title: E beyond the allocated shared memory region. The expert accumulation and expert_
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Warp-aligned thread launch and shared memory layout fix for moe_align_block_size_kernel.
Problem before: When num_experts < WARP_SIZE (32), launching only num_experts threads wastes GPU compute because a partially-filled warp cannot execute efficiently. Additionally, the shared m


## Solution

e beyond the allocated shared memory region. The expert accumulation and expert_ids assignment loops also ran unconditionally on all threads, producing incorrect results when threads beyond num_experts participated.
Applied change: Launch max(num_experts, WARP_SIZE) threads to guarantee warp-level e

## Key Lessons

- Problem before: When num_experts < WARP_SIZE (32), launching only num_experts threads wastes GPU compute because a partially-filled warp cannot execute efficiently.
- Measured impact should be confirmed by point-level validation.
