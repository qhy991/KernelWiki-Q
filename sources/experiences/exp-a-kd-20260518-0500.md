---
id: exp-a-kd-20260518-0500
title: reduce_8k_256ep
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.746x
---

## Challenge

At the smaller 8k workload, the atomicAdd approach's overhead is proportionally larger because the fixed-cost per-block overhead (atomic contention from 512 threads writing the same address, cudaMemset of t


## Solution

he global buffer, and the extra global memory read) dominates over the variable-cost counting loop. CUB BlockReduce uses shared-memory cooperative reduction that avoids atomic contention entirely, and does

## Results

- **speedup**: 1.746x (improved)


## Key Lessons

- At the smaller 8k workload, the atomicAdd approach's overhead is proportionally larger because the fixed-cost per-block overhead (atomic contention from 512 threads writing the same address, cudaMemset of the global buffer, and the extra global memory read) dominates over the variable-cost counting 
