---
id: exp-o-kd-20260518-0738
title: per_layer_kv
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Added kIsMLA template parameter to hicache_transfer_per_layer and wrapped V-cache load/store in if constexpr (!kIsMLA) to conditionally skip V transfers.
Problem before: The hicache_transfer_per_layer kernel always performed both K-cache and V-cache load/store operations per work


## Solution

aches, the V-cache transfers are completely unnecessary but still execute, wasting memory bandwidth and compute throughput.
Applied change: Added a bool kIsMLA template parameter (default false) to hicache_transfer_per_layer and wrapped all V-cache operations in if constexpr (!kIsMLA). When kIsMLA=t

## Key Lessons

- For MLA (Multi-head Latent Attention) models, which use a combined latent representation rather than separate K and V caches, the V-cache transfers are completely unnecessary but still execute, wasting memory bandwidth and compute throughput.
