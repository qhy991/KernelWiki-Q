---
id: exp-o-kd-20260518-0832
title: GPU would incur an additional kernel launch overhead (~5-10 microseconds per
  lau
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Fused paged_kv_last_page_len and block_table_bound computation into the advance_step kernel body, extending the per-query loop to compute FlashInfer-specific outputs alongside the base token/seq_len/position/slot_mapping updates.
Problem before: The original advance_step_kernel o


## Solution

GPU would incur an additional kernel launch overhead (~5-10 microseconds per launch) and each thread would need to re-read seq_lens and block_tables data that the first kernel already loaded.
Applied change: The advance_step_flashinfer_kernel fuses the two additional output computations (paged_kv_la

## Key Lessons

- Expected impact: Fusion eliminates one kernel launch overhead and improves register/data locality: the thread already holds seq_len, block_index, and block_offset in registers from the base computation, so paged_kv_last_page_len and block_table_bound are derived from existing register values with on
- The measured impact should be validated at the point level.
