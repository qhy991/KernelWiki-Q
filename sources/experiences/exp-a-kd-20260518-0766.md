---
id: exp-a-kd-20260518-0766
title: topk3_512rows
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- shared-memory-optimization
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 2.238x
---

## Challenge

The after kernel pre-computes sh_ids[i] / top_k into a second shared memory array (sh_precomputed) during the init phase, using one __syncthreads() barrier. In the hot loop, it reads sh_precomputed[i] instead of performing the division. Integer division by 3 on GPU requires multiple ALU cycles (n...


## Solution

in the after version add ~100-200 cycles total but are negligible against the ~800×4-cycle savings from eliminating repeated divisions.

## Results

- **speedup**: 2.238x (improved)


## Key Lessons

- In the hot loop, it reads sh_precomputed[i] instead of performing the division.
