---
id: exp-a-kd-20260518-0428
title: moe_block_32_valid_scan
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

moe_block_32_valid_scan

## Solution

chain. The after code still only needs moe_block_size/4=8 threads for the cooperative shared-memory load (which happens concurrently, not serially), and the warp-level counting still takes 32 threads each checking div_ceil(32,32)=1 element and reducing in 5 shuffle stages. The key difference is that

## Key Lessons

- The speedup is larger for moe_block_size=32 than for moe_block_size=16 because the serial scan cost scales linearly with moe_block_size while the after code's cost is nearly constant.
- The key difference is that the before code's serial scan latency grows proportionally with block size (8 sequential global loads vs 4), while the after code's latency stays near-constant because the shared-memory load is parallelized and the warp reduction is always 5 stages regardless of block size
