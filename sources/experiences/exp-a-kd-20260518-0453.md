---
id: exp-a-kd-20260518-0453
title: concat_mla_256tokens
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- rotary-embedding
- pipeline-stages
- register-reuse
- vectorized-loads
kernel_types:
- rotary-embedding
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
- register-reuse
- vectorized-loads
impl_family: custom_cuda
speedup: 1.017x
---

## Challenge

At 256 tokens the kernel processes only 1024 warps, yielding low occupancy where kernel launch overhead and suboptimal SM utilization dominate the runtime. The before kernel (two-phase register-buffered loads/stores with default cache policy) and after kernel


## Solution

(software-pipelined streaming with ld.global.nc.L1::no_allocate loads, st.global.L1::no_allocate stores, and prefetch.global.L2) perform the same total data movement: load 16*128 bf16 nope rows + 1*64 bf16 rope row per token, store 16*192 bf16 output rows per

## Results

- **speedup**: 1.017x (neutral)


## Key Lessons

- At 256 tokens the kernel processes only 1024 warps, yielding low occupancy where kernel launch overhead and suboptimal SM utilization dominate the runtime.
