---
id: exp-o-kd-20260518-0693
title: At least 32 bits, wasting half the bus width. Thread count equals N, causing
  hig
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- activation
kernel_types:
- activation
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: FP16 scalar element processing to half2 vectorized processing (f16 -> f16x2).
Problem before: The scalar f16 kernel processes one half element per thread. Each thread issues a single 16-bit load and a single 16-bit store, severely underutilizing the GPU's 32-bit minimum memory tr


## Solution

at least 32 bits, wasting half the bus width. Thread count equals N, causing high scheduling overhead.
Applied change: The f16x2 kernel processes 2 half elements per thread using the half2 vector type. A single 64-bit load (HALF2, which is a 32-bit hardware transaction for 2 half values) fetches 2 c

## Key Lessons

- Memory access is more efficient because 2 half values are packed into a single 32-bit transaction, matching the GPU's minimum transaction width and halving the number of memory instructions compared to scalar half access.
