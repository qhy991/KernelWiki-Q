---
id: exp-a-kd-20260518-0497
title: vec_32k_256ep
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- fine-grained-quantization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
- vectorized-loads
impl_family: custom_cuda
speedup: 1.055x
---

## Challenge

The after kernel replaces scalar int32 loads (one 32-bit transaction per topk_ids element) with int4 vector loads that fetch four int32 values in a single 128-bit transaction. Fo


## Solution

r topk_length=32768 with 256 expert blocks, each thread iterates over ~64 int4 vectors instead of ~64 scalar int32 elements. The reduced global memory transaction count and impro

## Results

- **speedup**: 1.055x (improved)


## Key Lessons

- For topk_length=32768 with 256 expert blocks, each thread iterates over ~64 int4 vectors instead of ~64 scalar int32 elements.
