---
id: exp-a-kd-20260518-0614
title: bfloat16_topk6_4096rows
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- epilogue-fusion
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- epilogue-fusion
impl_family: custom_cuda
speedup: 1.579x
---

## Challenge

The before code launches two separate CUDA kernels sequentially: rescale_kernel divides each output element by its row sum, then scale_kernel multiplies by routed_scaling_factor. Each kernel launch incurs its own launch overhead (~5us per launch on CUDA) and requires its own global-memory read-mo...


## Solution

The after code consolidates both operations into a single fused_rescale_scale_kernel that performs division and conditional multiplication in the same per-element loop iteration. This eliminates one kernel launch overhead and avoids the second global-memory traversal, where each output element wo...

## Results

- **speedup**: 1.579x (improved)


## Key Lessons

- This eliminates one kernel launch overhead and avoids the second global-memory traversal, where each output element would otherwise be read, modified, and written back a second time.
