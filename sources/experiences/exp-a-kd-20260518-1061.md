---
id: exp-a-kd-20260518-1061
title: block_sz_1024
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 0.964x
---

## Challenge

At blockDim.x=1024, both before and after approaches use a full block size reduction. The before code uses vllm's custom two-phase warp shuffle + shared memory approach with function-pointer dispatch for max, while the after code uses CUB BlockReduce<float,1024> with functor dispatch. With 1024 t...


## Solution

in the after path likely comes from CUB's more general-purpose reduction infrastructure having slightly more instruction overhead for the max-specific case compared to the hand-rolled vllm reduction, which is tightly optimized for the single max operation. The function-pointer in the before code ...

## Results

- **speedup**: 0.964x (regression)


## Key Lessons

- The slight 3.75% overhead in the after path likely comes from CUB's more general-purpose reduction infrastructure having slightly more instruction overhead for the max-specific case compared to the hand-rolled vllm reduction, which is tightly optimized for the single max operation.
