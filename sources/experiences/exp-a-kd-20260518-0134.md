---
id: exp-a-kd-20260518-0134
title: out_rescale_mprime
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.999x
---

## Challenge

The before code repurposes m_prime as a rescale factor and requires an extra __syncthreads() between the rescale and the subsequent accumulation. The after code uses a dedicated out_rescale array, eliminating the extra __syncthreads(). However, in this microbenchmark the entire k


## Solution

ernel runs in a single block with very small data sizes (32 query rows, 128 keys), so the __syncthreads() overhead is negligible — a single-block syncthreads on SM80 completes in ~20-30 cycles, which is dwarfed by the 8 key-block iterations of floating-point work. The 0.07% diffe

## Results

- **speedup**: 0.999x (neutral)


## Key Lessons

- In the real fMHA kernel with larger dimensions and more key-block iterations, removing a syncthreads from the hot loop can compound into measurable savings because each syncthreads stalls the entire CTA and prevents overlap with memory operations.
