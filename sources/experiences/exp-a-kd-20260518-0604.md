---
id: exp-a-kd-20260518-0604
title: fp16_hidden8192
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- register-reuse
- epilogue-fusion
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- epilogue-fusion
impl_family: custom_cuda
speedup: 0.838x
---

## Challenge

The after kernel regresses because the Q/K split doubles CTA count from 256 to 512 (dim3(256,2)) with 1024 threads per CTA, while the fused version uses 256 CTAs (1D grid) each processing 2 rows. On A800 (2048 threads/SM), each SM can run at most 2 CTAs of 1024 threads. The fused version's 256 CTAs 


## Solution

a per CTA. The split version's 512 CTAs each perform one reduction with 2 __syncthreads() for only 1 row — total barrier/reduction count is identical (256×2=512 vs 512×1=512), but each call amortizes over half the data. The after kernel's `is_q = blockIdx.y == 0` conditional and dynamic pointer disp

## Results

- **speedup**: 0.838x (regression)


## Key Lessons

- The after kernel regresses because the Q/K split doubles CTA count from 256 to 512 (dim3(256,2)) with 1024 threads per CTA, while the fused version uses 256 CTAs (1D grid) each processing 2 rows.
- The broadcast read pattern (`buffer[0]`) where all 1024 threads read from one shared-memory slot after __syncthreads() also introduces a serialization bottleneck compared to the fused version's per-warp slot indexing (`buffer_q[threadIdx.x/32]`) where only 32 threads in the final read phase access t
