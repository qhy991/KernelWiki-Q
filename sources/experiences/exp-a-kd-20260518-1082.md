---
id: exp-a-kd-20260518-1082
title: valid_only_8experts
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- register-reuse
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
speedup: 0.999x
---

## Challenge

The before kernel compares topk_ids[i] directly against blockIdx.x in a single conditional, while the after kernel first loads topk_ids[i] into a local const register (expert_id) then checks two branches: expert_id == -1 (always false for valid-only data) and expert_id == blk_expert_id. The NVCC com


## Solution

er at -O3 already materializes topk_ids[i] into a register in the before version before the comparison, so the explicit local variable provides no additional register reuse benefit. The extra -1 branch predicate is always predicted false and adds negligible instruction overhead (~1 extra ISEL per it

## Results

- **speedup**: 0.999x (neutral)


## Key Lessons

- The before kernel compares topk_ids[i] directly against blockIdx.x in a single conditional, while the after kernel first loads topk_ids[i] into a local const register (expert_id) then checks two branches: expert_id == -1 (always false for valid-only data) and expert_id == blk_expert_id.
