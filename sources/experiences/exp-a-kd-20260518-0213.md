---
id: exp-a-kd-20260518-0213
title: float_medium
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- activation
- register-reuse
kernel_types:
- activation
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
---

float_medium

## Solution

, `x2`, and `dx4` once and reuses the computed sigmoid and `x3` in registers. The before source launches five separate k

## Key Lessons

- The after source folds SiLU recomputation, `dx2`, `dx1`, and `x4` production into one kernel, so each element loads `x1`, `x2`, and `dx4` once and reuses the computed sigmoid and `x3` in registers.
