---
id: exp-a-kd-20260518-1418
title: q4_0_need_sum_true
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
- shared-memory-optimization
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 0.437x
---

## Challenge

The after code uses a threadIdx.x guard (`threadIdx.x < n_per_r / QK8_1`, which is 8/32 = 25% of threads) that causes severe warp divergence in the ds loading path. Only 8 of 32 threads per warp execute the global-to-shared memory load, while the remaining 24 a


## Solution

re idle. The before code's permutation loop with mmq_x=4 iterates only once (ids0=0, and nwarps*QI8_1=32 > mmq_x=4, so the loop body runs once) but keeps all threads active with no warp divergence. In isolation, the direct-indexing pattern is slower because war

## Results

- **speedup**: 0.437x (regression)


## Key Lessons

- In isolation, the direct-indexing pattern is slower because warp divergence dominates over the reduced arithmetic.
