---
id: exp-o-kd-20260518-0851
title: 'Rnel launch overhead. Without fused scaling, the intermediate half values
  could '
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- register-reuse
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
---

## Challenge

Optimization focus: Replaced unscaled vec_conversion with scaled_vec_conversion that applies per-element scale factor during FP8 dequantization (fp8->half path).
Problem before: The old fp8->half dequantization used unscaled vec_conversion that only reinterpreted fp8 bits as half. Scaling had to be 


## Solution

rnel launch overhead. Without fused scaling, the intermediate half values could also lose precision when a global scale was applied separately.
Applied change: The new scaled_vec_conversion fuses the scale multiplication into the fp8->half path: it first converts fp8 to half, promotes to float for t

## Key Lessons

- This eliminates the separate dequantization pass and reduces precision loss by scaling in float before downcasting.
- Optimization focus: Replaced unscaled vec_conversion with scaled_vec_conversion that applies per-element scale factor during FP8 dequantization (fp8->half path).
