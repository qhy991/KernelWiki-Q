---
id: exp-o-kd-20260518-0804
title: And an extra kernel launch overhead.
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- topk
kernel_types:
- topk
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Fused vectorized BF16/FP16-to-float conversion into topkGatingSoftmax kernel loading path.
Problem before: The topkGatingSoftmax kernel required float input, meaning BF16/FP16 gating output had to be converted to float by the host before the kernel was called. This required a sep


## Solution

and an extra kernel launch overhead.
Applied change: The kernel now accepts InputType (float, __nv_bfloat16, or __half) as a template parameter. During the data loading stage, BF16/FP16 elements are converted to float using hardware-accelerated vectorized conversion: __bfloat16222float2 converts two

## Key Lessons

- Expected impact: Eliminates one full global memory read+write pass over the gating output data and one kernel launch.
- Performance impact should be measurable when the gating output is BF16/FP16, particularly for large token counts.
