---
id: exp-a-kd-20260518-0441
title: scan_8experts
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
- shared-memory-optimization
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 1.064x
---

## Challenge

For scan_size=8, the Blelloch scan requires log2(8)=3 iterations in both up-sweep and down-sweep phases, totaling ~6 __syncthreads() barriers (plus initial fill barrier). The warp-level scan uses 6 __syncthreads() barriers as well. Since scan_size=8 fits entirely within one warp (8 < 32), the wa


## Solution

rp_exclusive_scan handles all elements without needing inter-warp aggregation, but the two-level structure still requires the same number of cross-warp barriers. The net __syncthreads() count is similar for both algorithms at this small size, so the performance difference is primarily driven by

## Results

- **speedup**: 1.064x (improved)


## Key Lessons

- Since scan_size=8 fits entirely within one warp (8 < 32), the warp_exclusive_scan handles all elements without needing inter-warp aggregation, but the two-level structure still requires the same number of cross-warp barriers.
