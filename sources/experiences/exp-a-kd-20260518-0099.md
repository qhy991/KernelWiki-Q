---
id: exp-a-kd-20260518-0099
title: float_4x112x112_3to4
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- elementwise
- shared-memory-optimization
- vectorized-loads
kernel_types:
- elementwise
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- vectorized-loads
impl_family: custom_cuda
speedup: 1.292x
---

## Challenge

The same optimization mechanism applies as in the 1x224x224 case: the before code uses scalar element-wise processing with per-element modular index decomposition (idx%c_out followed by cascade divisions for w/h/n coordinates), resulting in uncoalesced 4-byte global memory stores and expensive in...


## Solution

The after code uses shared-memory tiling where 192 threads cooperatively load float4 vectors into shared memory, then each thread assembles one float4 output containing 3 data floats and 1 zero padding float, writing it as a single 128-bit coalesced store. The similar ~29% speedup confirms the im...

## Results

- **speedup**: 1.292x (improved)


## Key Lessons

- The same optimization mechanism applies as in the 1x224x224 case: the before code uses scalar element-wise processing with per-element modular index decomposition (idx%c_out followed by cascade divisions for w/h/n coordinates), resulting in uncoalesced 4-byte global memory stores and expensive integ
