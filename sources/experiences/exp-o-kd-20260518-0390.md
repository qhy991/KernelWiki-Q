---
id: exp-o-kd-20260518-0390
title: expert_offset_seqscan_64ep
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 5143.103x
---

## Challenge

In the before source, sequentialCountLessThan iterates linearly through sorted_indices, counting elements less than the target and breaking early when sorted_indices[i] >= target. For 64 experts with 262144 total sorted entries, threads handling higher expert IDs scan through many entries before ...


## Solution

The after source's findTotalEltsLessThanTarget uses binary search with O(log2(262144))=~18 iterations regardless of expert ID, avoiding the linear scan entirely. The massive speedup comes from eliminating O(N) sequential memory reads per thread in favor of O(log N) random reads, plus the thread-l...

## Results

- **speedup**: 5143.103x (improved)


## Key Lessons

- In the before source, sequentialCountLessThan iterates linearly through sorted_indices, counting elements less than the target and breaking early when sorted_indices[i] >= target.
