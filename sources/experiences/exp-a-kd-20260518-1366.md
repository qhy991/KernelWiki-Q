---
id: exp-a-kd-20260518-1366
title: block_reduce_sum_float
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 1.008x
---

## Challenge

The before source (block_reduce_sum_float_before.cu) uses blockReduceSum<float,256> calling warpReduceSum<T> directly at both stages: first warp-level reduction via `val = warpReduceSum<T>(val)`, then cross-warp via shared-memory staging (__shared__ T shared[], lane/wid computation, __syncthreads...


## Solution

The after source (block_reduce_sum_float_after.cu) uses blockReduce<float,256>(val, detail::_sum<float>) calling warpReduce<T>(val, fn) at both stages. The shared-memory staging and zero-initialization for inactive threads are identical between both versions. Since _sum wraps `a + b` and NVCC inl...

## Results

- **speedup**: 1.008x (neutral)


## Key Lessons

- Since _sum wraps `a + b` and NVCC inlines the function pointer, the generated SASS is equivalent.
