---
id: exp-a-kd-20260518-0160
title: pdl_chained_gemm
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: inline_asm
---

pdl_chained_gemm

## Solution

ntrol.wait PTX instruction is unavailable, so the test uses cudaStreamWaitEvent as a proxy. Since CUDA streams already serialize kernels on the same stream, adding explicit cudaStreamWaitEvent between them does not change the execution order - kernels were already serialized. The real PDL benefit co

## Key Lessons

- Since CUDA streams already serialize kernels on the same stream, adding explicit cudaStreamWaitEvent between them does not change the execution order - kernels were already serialized.
- The after code adds cudaStreamWaitEvent between consecutive kernels to simulate the PDL griddepcontrol.wait pattern.
