---
id: exp-a-kd-20260518-0705
title: write_cache_h8_hs64_bs16
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- attention
kernel_types:
- attention
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.768x
---

## Challenge

In the before layout [num_blocks, num_heads, block_size, head_size], the reshape_and_cache kernel writes value_cache with stride-1 between consecutive head_offset values (same block_offset), giving excellent write coalescing: a warp of 32 threads writing adjacent head_offset values packs into a s...


## Solution

the after layout [num_blocks, num_heads, head_size, block_size], the same consecutive head_offset values map to stride=block_size=16 addresses, spreading each warp's writes across 16*4=64-byte scattered segments that cannot coalesce into a single transaction. This 30% regression on the write side...

## Results

- **speedup**: 0.768x (regression)


## Key Lessons

- In the before layout [num_blocks, num_heads, block_size, head_size], the reshape_and_cache kernel writes value_cache with stride-1 between consecutive head_offset values (same block_offset), giving excellent write coalescing: a warp of 32 threads writing adjacent head_offset values packs into a sing
