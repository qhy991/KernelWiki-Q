---
id: exp-a-kd-20260518-1014
title: i16_only_fits_256ep
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- scan
- shared-memory-optimization
kernel_types:
- scan
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 2.379x
---

## Challenge

The before code uses moe_align_block_size_global_mem_kernel which stores the tokens_cnts and cumsum arrays in global memory (passed as int32_t* device pointers), because the int32_t shared memory requirement of 264196 bytes exceeds the A800's 164KB per-block limit. Every access to tokens_cnts[my_...


## Solution

The after code reorganizes shared memory layout (cumsum at the base, tokens_cnts as uint16_t at offset blockDim.x+1) and instantiates the kernel with token_cnts_t=uint16_t, reducing shared memory to 132612 bytes which fits within the 164KB limit. All tokens_cnts and cumsum reads/writes now hit sh...

## Results

- **speedup**: 2.379x (improved)


## Key Lessons

- The before code uses moe_align_block_size_global_mem_kernel which stores the tokens_cnts and cumsum arrays in global memory (passed as int32_t* device pointers), because the int32_t shared memory requirement of 264196 bytes exceeds the A800's 164KB per-block limit.
