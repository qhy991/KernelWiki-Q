---
id: exp-a-kd-20260518-0612
title: bf16_hidden_4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.001x
---

## Challenge

The before source calls cudaGetDeviceProperties() each iteration to simulate runtime cc_major lookup, while the after source uses the compile-time constexpr kMaxVecBytes=16 constant. However, the GPU kernel (qknorm_scalar_kernel) is identical in both versions — same block/grid configuration, sa


## Solution

me thread stride, same warp+block reduction, same RMS normalization logic. CUDA event timing only measures GPU-side kernel execution, which is identical, so no measurable GPU performance delta exists. The host-side cudaGetDeviceProperties overhead (~microseconds, further cached by the CUDA runt

## Results

- **speedup**: 1.001x (neutral)


## Key Lessons

- The before source calls cudaGetDeviceProperties() each iteration to simulate runtime cc_major lookup, while the after source uses the compile-time constexpr kMaxVecBytes=16 constant.
