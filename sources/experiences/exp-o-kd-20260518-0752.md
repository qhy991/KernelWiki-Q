---
id: exp-o-kd-20260518-0752
title: Array of the vec_n_t pack, NVCC may scalarize the global load into multiple
  narr
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- register-reuse
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: custom_cuda
---

## Challenge

Optimization focus: Materialize vector pack into a local variable before passing to vec_op in vectorize_read_with_alignment, encouraging single vector LD.
Problem before: In vectorize_read_with_alignment, the fast-path aligned loop passes v_in[i] directly to vec_op without materializing it first. Si


## Solution

array of the vec_n_t pack, NVCC may scalarize the global load into multiple narrow memory transactions instead of a single wide vector load, the same issue seen in vectorize_with_alignment.
Applied change: The change adds vin_t tmp = v_in[i]; to explicitly load the entire vector pack from global mem

## Key Lessons

- Since vec_op is a templated callable that iterates over the internal scalar array of the vec_n_t pack, NVCC may scalarize the global load into multiple narrow memory transactions instead of a single wide vector load, the same issue seen in vectorize_with_alignment.
- Impact should be confirmed by point-level validation.
