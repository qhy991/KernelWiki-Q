---
id: exp-a-kd-20260518-0571
title: mask_0pct
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- activation
kernel_types:
- activation
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 0.970x
---

## Challenge

In the before source, the kernel has 4 fields in Params and each thread performs only the bounds check before proceeding to load/compute/store. In the after source, Params has 6 fields (adding expert_ids and expert_step) and each thread performs an additional global


## Solution

memory read of expert_ids[token_id] plus an integer comparison against -1 before reaching the same load/compute/store path. When no tokens are masked, every thread passes the filter check, so there is no warp divergence — all threads follow the same path. The ~3% s

## Results

- **speedup**: 0.970x (regression)


## Key Lessons

- The ~3% slowdown reflects the added instruction overhead (one extra global load and one branch per thread) which is small relative to the activation compute but nonzero because the expert_ids read is from a separate memory region that does not coalesce with the main data path.
