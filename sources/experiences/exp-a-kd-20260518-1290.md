---
id: exp-a-kd-20260518-1290
title: dynazp_h4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
speedup: 1.058x
---

## Challenge

The after code improves over the before version for hidden_size=4096 with 512 tokens. The key difference from the h16384 regression is the higher grid size (512 blocks vs 128), which provides more grid-level parallelism to compensate for the reduced per-block occupancy from 256 threads. The merged M


## Solution

BlockReduce pass and one __syncthreads() barrier, which is particularly valuable because the AZP kernel's before version had two serial reductions with a barrier between them. The vectorize_with_alignment<16> in the quantization pass coalesces 16 float loads and 16 int8 stores into single vector tra

## Results

- **speedup**: 1.058x (improved)


## Key Lessons

- The merged MinMax reduction eliminates one full BlockReduce pass and one __syncthreads() barrier, which is particularly valuable because the AZP kernel's before version had two serial reductions with a barrier between them.
- The key difference from the h16384 regression is the higher grid size (512 blocks vs 128), which provides more grid-level parallelism to compensate for the reduced per-block occupancy from 256 threads.
