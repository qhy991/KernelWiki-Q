---
id: exp-o-kd-20260518-0584
title: smem_small_batch
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
---

smem_small_batch

## Solution

= stages * (tb_k * tb_n / pack_factor) * 4, halving the multiplier from 4 to 2 directly halves the A and B buffer footprints. The scales cache (sh_s_size = tb_scales * stages) and group-index cache (sh_g_idx_size = stages * tb_k / 4) also shrink proportionally.

## Key Lessons

- Since sh_a_size = stages * (tb_m * tb_k) * element_size and sh_b_size = stages * (tb_k * tb_n / pack_factor) * 4, halving the multiplier from 4 to 2 directly halves the A and B buffer footprints.
