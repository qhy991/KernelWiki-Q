---
id: exp-o-kd-20260518-0186
title: small_batch_4
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 5.276x
---

## Challenge

The after code uses a multi-backend kernel that reads each source element once into a local variable (int32_t val) and writes it to 3 destination buffers. In the before code, 3 separate kern


## Solution

el launches each read the source data independently, resulting in 3x redundant source reads. For small batch (bs=4), the data volume is tiny, so eliminating 2 of 3 kernel launches (each ~5us

## Results

- **speedup**: 5.276x (improved)


## Key Lessons

- The single-read-multi-write pattern also reduces L2 cache pressure since source data only needs to be fetched once.
