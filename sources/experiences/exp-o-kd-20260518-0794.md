---
id: exp-o-kd-20260518-0794
title: Memory allocation overhead, increases computation volume on dummy rows, and
  requ
experience_type: optimization
source_category: agent-experiment
architectures:
- sm100
tags:
- gemm
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Swap A/B matrices in GEMM dispatch when M < 16 or M % 4 != 0, eliminating Python-side zero-padding overhead.
Problem before: When the M dimension of the GEMM input (batch of activation rows) is smaller than 16 or not a multiple of 4, SM100 (Blackwell) tensor cores cannot efficien


## Solution

memory allocation overhead, increases computation volume on dummy rows, and requires a separate slicing step, all of which hurt throughput especially for small batch sizes common in LLM inference.
Applied change: The swap_ab mechanism transposes the GEMM problem when M < 16 or M % 4 != 0. Instead of

## Key Lessons

- Instead of computing A * B with a problematic small M, it computes B^T * A^T (equivalent result by transpose property), where the weight matrix dimension N becomes the effective 'M' in the kernel's view.
