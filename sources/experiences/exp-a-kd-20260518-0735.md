---
id: exp-a-kd-20260518-0735
title: fp8_quant_4k
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
speedup: 1.824x
---

## Challenge

In the before code, segmented_max_reduction allocates 4KB of shared memory per block (cache[1024]), reads every input element to compute per-thread max-abs, then performs a 1024-thread halving reduction tree with __syncthreads barriers, and finally uses atomicMaxFloat to merge block-level results...


## Solution

The after code eliminates all of this: it only launches scaled_fp8_quant_kernel with a pre-uploaded scale, reading the 4096 float elements once. At this size, both the kernel launch overhead and the shared-memory reduction cost are significant relative to the quant kernel alone, producing a 1.82x...

## Results

- **speedup**: 1.824x (improved)


## Key Lessons

- The after code eliminates all of this: it only launches scaled_fp8_quant_kernel with a pre-uploaded scale, reading the 4096 float elements once.
