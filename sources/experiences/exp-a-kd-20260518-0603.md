---
id: exp-a-kd-20260518-0603
title: fp16_hidden4096
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- layer-norm
- shared-memory-optimization
- register-reuse
- epilogue-fusion
- vectorized-loads
kernel_types:
- layer-norm
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- register-reuse
- epilogue-fusion
- vectorized-loads
impl_family: custom_cuda
speedup: 0.991x
---

## Challenge

The after kernel splits Q and K into separate CTAs via blockIdx.y (grid dim3(512,2) vs 1D grid(512)), reducing per-CTA register arrays from 6 vec_t (v_q,v_k,v_q_weight,v_k_weight,v_q_out,v_k_out) to 2 (v_data,v_weight) and shared memory from 64 to 32 floats. However, at hidden_size=4096 with 512 thr


## Solution

0 (108 SMs, 65536 regs/SM) already achieves sufficient occupancy for the fused version — the 6 vec_t arrays (~96 bytes registers) fit within per-SM limits. The total warp+CTA reduction call count is identical (512 fused CTAs doing 2 reductions each = 1024, vs 1024 split CTAs doing 1 each = 1024). Th

## Results

- **speedup**: 0.991x (neutral)


## Key Lessons

- The per-CTA reduction amortization over 2 rows in the fused version does not save measurable time because the reduction overhead is small relative to the vectorized load, cast, and normalization computation.
