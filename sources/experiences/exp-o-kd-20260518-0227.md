---
id: exp-o-kd-20260518-0227
title: twoshot_8ranks_fp16
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- pipeline-stages
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- pipeline-stages
impl_family: custom_cuda
---

twoshot_8ranks_fp16

## Solution

e into a single kernel launch, the after path eliminates one full kernel launch overhead and allows the copy and reduce phases to pipeline within the same execution context. The before pa

## Key Lessons

- The after code replaces the explicit cudaMemcpyAsync (separate D2D copy kernel on the same stream) with an in-kernel copy distributed across all GPU threads.
- By merging the copy and reduce into a single kernel launch, the after path eliminates one full kernel launch overhead and allows the copy and reduce phases to pipeline within the same execution context.
