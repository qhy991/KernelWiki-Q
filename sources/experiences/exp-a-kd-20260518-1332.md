---
id: exp-a-kd-20260518-1332
title: align_offset
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
impl_family: custom_cuda
---

align_offset

## Solution

memory allocation per block that consumes on-chip memory and forces a memory path through shared instead of read-only cache, (2) the __syncthreads barrier that stalls all threads until the cooperative load completes, and (3) the serial loop in lane 0 that iterates over all preceding experts to a

## Key Lessons

- This eliminates three bottlenecks: (1) the shared memory allocation per block that consumes on-chip memory and forces a memory path through shared instead of read-only cache, (2) the __syncthreads barrier that stalls all threads until the cooperative load completes, and (3) the serial loop in lane 0
