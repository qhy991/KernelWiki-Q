---
id: exp-a-kd-20260518-0389
title: fp16_128grp_128gs_vec
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- register-reuse
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
- vectorized-loads
impl_family: custom_cuda
speedup: 0.909x
---

## Challenge

The after code introduces a half8_vec_t struct that loads 8 __half values at once via reinterpret_cast<uint4>, then iterates over those 8 elements in an inner loop (#pragma unroll). This replaces the before code's simple stride-16 scalar loop where each thread reads one __half per iteration (8 itera


## Solution

d forces the compiler through a different code generation path than the well-optimized __half2float scalar access, potentially losing hardware-level fp16-to-float conversion optimizations that nvcc applies to the scalar path; (2) the inner loop over 8 elements adds register pressure and instruction 

## Results

- **speedup**: 0.909x (regression)


## Key Lessons

- This replaces the before code's simple stride-16 scalar loop where each thread reads one __half per iteration (8 iterations total).
- The 10% regression stems from two factors: (1) the reinterpret_cast<uint4> load forces the compiler through a different code generation path than the well-optimized __half2float scalar access, potentially losing hardware-level fp16-to-float conversion optimizations that nvcc applies to the scalar pa
