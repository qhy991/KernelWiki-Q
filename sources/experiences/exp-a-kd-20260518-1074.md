---
id: exp-a-kd-20260518-1074
title: float_ctx2048
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
- register-reuse
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- register-reuse
impl_family: custom_cuda
---

float_ctx2048

## Solution

the after source, cooperative loading into `__shared__ float q_vecs[2][64]` eliminates the redundancy but introduces three overheads: (1) __syncthreads() barrier, (2) shared memory read latency (~5 cycles per access) for every `q_vecs[tg_off][j]` in the inner loop across 32 key tokens, and (3) 2-...

## Key Lessons

- The 64x redundant global memory reads (128 threads x 64 floats = 8192 reads for only 128 unique values) are largely absorbed by the A800's L2 cache, since the 512-byte query footprint fits comfortably in cache.
- In the after source, cooperative loading into `__shared__ float q_vecs[2][64]` eliminates the redundancy but introduces three overheads: (1) __syncthreads() barrier, (2) shared memory read latency (~5 cycles per access) for every `q_vecs[tg_off][j]` in the inner loop across 32 key tokens, and (3) 2-
