---
id: exp-a-kd-20260518-0935
title: stream_128bit
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- fine-grained-quantization
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- fine-grained-quantization
impl_family: inline_asm
speedup: 0.998x
---

## Challenge

The before code loads 128-bit vectors via __ldg (read-only cache path into L1, with L2 allocation) and writes via plain int4 stores (which allocate L2 write-back lines). The after code replaces these with ld.global.cs.v4.u32 and st.global.cs.v4.u32 PTX inline assembly, which stream 4x32-bit values t


## Solution

ing persistent L2 lines. However, the 128KB working set (8192 vectors x 16 bytes for both src and dst = 256KB total) fits entirely within the A800's 5MB L2 cache, so there is no L2 eviction pressure from regular stores. Without competing work sharing the L2, bypassing L2 allocation via .cs provides 

## Results

- **speedup**: 0.998x (neutral)


## Key Lessons

- The after code replaces these with ld.global.cs.v4.u32 and st.global.cs.v4.u32 PTX inline assembly, which stream 4x32-bit values through the cache without allocating persistent L2 lines.
- Without competing work sharing the L2, bypassing L2 allocation via .cs provides no measurable latency advantage — both paths have identical DRAM access patterns and the memory bandwidth bottleneck dominates.
