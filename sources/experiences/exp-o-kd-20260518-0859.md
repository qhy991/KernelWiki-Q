---
id: exp-o-kd-20260518-0859
title: Dimension has padding (e.g., [batch, seq, num_heads, head_size + 64]). The
  kerne
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- rotary-embedding
kernel_types:
- rotary-embedding
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
---

## Challenge

Optimization focus: Replace hardcoded head_size stride with configurable head_stride in kernel address computation, enabling support for non-contiguous tensor layouts (sliced/padded heads).
Problem before: The rotary embedding kernel computed the per-head offset as head_idx * head_size, assuming all


## Solution

dimension has padding (e.g., [batch, seq, num_heads, head_size + 64]). The kernel could not correctly address non-contiguous head layouts at all.
Applied change: A new head_stride parameter (int64_t) is added to apply_rotary_embedding, rotary_embedding_kernel, and batched_rotary_embedding_kernel. Th

## Key Lessons

- This enables the Python wrapper to remove .contiguous() and .copy_() entirely.
- This forced the Python wrapper to call .contiguous() on every query/key tensor before invoking the kernel and then .copy_() results back—a potentially expensive allocation and memory copy, especially for sliced tensors where the head dimension has padding (e.g., [batch, seq, num_heads, head_size + 6
