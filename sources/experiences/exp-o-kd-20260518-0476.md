---
id: exp-o-kd-20260518-0476
title: multiple_lora_8grp_64tok
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- rotary-embedding
kernel_types:
- rotary-embedding
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 32.242x
---

## Challenge

The before code issues 8 separate kernel launches (rotary_embedding_kernel with grid=8 blocks each), incurring 8x kernel launch overhead. Each launch with only 8 thread blocks severely underutilizes the 108 SMs on the A800, leaving most SMs idle during execution


## Solution

The after code consolidates all 64 tokens into a single batched_rotary_embedding_kernel launch with 64 thread blocks, amortizing the launch overhead to once instead of 8 times. The per-token cos_sin_cache_offsets lookup adds one int64 global load per block, which is negligible compared to elimina...

## Results

- **speedup**: 32.242x (improved)


## Key Lessons

- The after code consolidates all 64 tokens into a single batched_rotary_embedding_kernel launch with 64 thread blocks, amortizing the launch overhead to once instead of 8 times.
