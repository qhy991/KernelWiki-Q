---
id: exp-a-kd-20260518-0955
title: sm_overlap_128threads
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- shared-memory-optimization
- pipeline-stages
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- pipeline-stages
impl_family: custom_cuda
speedup: 0.996x
---

## Challenge

The before code places sh_red at an offset (STAGES*PIPE_STAGE_INT4/2) that overlaps with pipeline data in the same shared memory allocation, while the after code carves sh_red beyond the full pipeline footprint (STAGES*PIPE_STAGE_INT4). In this simplified single-block microbenchmark, both versions u


## Solution

_syncthreads() barriers between pipeline loads and reduction write phases, so the overlap never manifests as actual data corruption. The measured latency difference is within noise (~0.4%) because the total shared memory access pattern and instruction count are nearly identical — the only change is 

## Results

- **speedup**: 0.996x (neutral)


## Key Lessons

- The measured latency difference is within noise (~0.4%) because the total shared memory access pattern and instruction count are nearly identical — the only change is the offset used for reduction writes.
