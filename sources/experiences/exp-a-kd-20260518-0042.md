---
id: exp-a-kd-20260518-0042
title: double_buffer_warp
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
- shared-memory-optimization
- pipeline-stages
kernel_types:
- reduction
languages:
- cuda-cpp
- cute-dsl
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- shared-memory-optimization
- pipeline-stages
impl_family: cutlass
speedup: 0.963x
---

## Challenge

The after code uses double-buffered shared memory (2x TILE_M*TILE_K + 2x TILE_K*TILE_N = 2x the shared memory of the before code) to overlap loading the next k-tile with computing on the current k-tile. However, the per-thread inne


## Solution

r loop over TILE_K multiplies dominates execution time and is compute-bound, not memory-bound. The double-buffering cannot hide latency that isn't exposed, and the 2x shared memory cost (32KB vs 16KB) may slightly reduce SM occupan

## Results

- **speedup**: 0.963x (regression)


## Key Lessons

- The after code uses double-buffered shared memory (2x TILE_M*TILE_K + 2x TILE_K*TILE_N = 2x the shared memory of the before code) to overlap loading the next k-tile with computing on the current k-tile.
