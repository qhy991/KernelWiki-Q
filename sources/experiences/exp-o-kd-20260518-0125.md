---
id: exp-o-kd-20260518-0125
title: f32_K256
experience_type: optimization
source_category: agent-experiment
architectures:
- sm90
tags:
- reduction
kernel_types:
- reduction
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
impl_family: custom_cuda
speedup: 1.477x
---

## Challenge

The after kernel uses float4 reinterpret_cast to load and store 4 contiguous floats in a single 128-bit memory transaction per thread, instead of issuing 4 separate 32-bit scalar loads and stores. Thread coun


## Solution

t drops from 256 to 64 per row, reducing warp scheduling overhead and barrier synchronization cost in block_reduce_sum_f32. Fewer threads also means fewer partial variance values (64 instead of 256) enter the

## Results

- **speedup**: 1.477x (improved)


## Key Lessons

- The after kernel uses float4 reinterpret_cast to load and store 4 contiguous floats in a single 128-bit memory transaction per thread, instead of issuing 4 separate 32-bit scalar loads and stores.
