---
id: exp-a-kd-20260518-0746
title: convert_half2_64k
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- register-reuse
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- register-reuse
impl_family: inline_asm
speedup: 1.009x
---

## Challenge

Same conversion pattern as the 256k case but with fewer pairs (65536). The before kernel uses 4 scalar __half2float calls per iteration, the after uses 2 vector __half22float2 calls. On sm_80, __half22float2 produces the same PTX instruction sequence a


## Solution

s two __half2float calls since the Ampere architecture converts half-to-float element-by-element without a paired vector conversion unit. The 0.85% measured difference is within benchmark noise and likely reflects minor register allocation or schedulin

## Results

- **speedup**: 1.009x (neutral)


## Key Lessons

- On sm_80, __half22float2 produces the same PTX instruction sequence as two __half2float calls since the Ampere architecture converts half-to-float element-by-element without a paired vector conversion unit.
- Same conversion pattern as the 256k case but with fewer pairs (65536).
