---
id: exp-a-kd-20260518-1480
title: nibble_vec_1mb
experience_type: analysis
source_category: agent-experiment
architectures:
- sm90
tags:
- dequantization
- vectorized-loads
kernel_types:
- dequantization
languages:
- cuda-cpp
captured_at: '2026-01-01'
confidence: inferred
reproducibility: concept
techniques:
- vectorized-loads
impl_family: custom_cuda
---

nibble_vec_1mb

## Solution

; (2) the vectorized kernel has 16x fewer total work units (65536 vectors vs 1048576 bytes), so fewer thread-blocks are launched (only 256 blocks vs 4096 blocks), reducing GPU occupancy and parallelism. At this small data size, the kernel runtime is dominated by launch overhead and L2 cache h

## Key Lessons

- The regression at 1 MB stems from two factors: (1) the 1 MB dataset fits entirely in the A800's 40 MB L2 cache per GPC, so both before and after kernels are L2-cache-resident and the memory coalescing advantage of uint4 is less impactful since L2 can service the byte-by-byte reads efficiently; (2) t
